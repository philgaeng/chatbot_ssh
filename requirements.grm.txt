# GRM Ticketing System — additional Python dependencies
# Install inside chatbot-rest conda env (host pytest + local tooling):
#   make dev-grm-deps
#   # or: pip install -r requirements.grm.txt
# Do NOT add to requirements.txt (reserved for existing chatbot stack).
# ⚠ One deliberate exception, DPG-17 (2026-08-18): `pydantic-settings` moved OUT of this file
# and into requirements.txt, because backend/config/llm_config.py — the single LLM registry
# both surfaces read — is on the chatbot side. The rule stands; a dependency that the base
# surface genuinely imports belongs in the base manifest, or the rule is merely false.

alembic>=1.13                 # ticketing/migrations/
openpyxl>=3.1                 # quarterly XLSX reports (no pandas)
python-jose[cryptography]>=3.3 # Keycloak JWT verification (JWKS)
python-keycloak>=3.9.0        # Keycloak Admin API (realm setup + officer invite)
httpx>=0.27                   # async-capable HTTP client for outbound API calls
pytest>=8.0                   # ticketing engine integration tests
reportlab>=4.0                # complainant closure PDF download
apscheduler>=3.10             # ops/ monitor scheduler (broker-independent)
pip-audit>=2.7                # ops security dependency/CVE scan (report-only)
pip-licenses>=5.0             # ops licence scan — DPG indicator 2 (report-only, see ops/security.py)
