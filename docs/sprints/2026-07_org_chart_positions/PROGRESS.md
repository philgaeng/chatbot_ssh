# Org Chart & Positions Sprint — Progress

> **Standing rule: update this file at every commit** — ticket status, checklist ticks, deviations, and the migration-head table. A commit that changes sprint state without a matching update here is **incomplete**. (Restated in [README.md](README.md), [agents/README.md](agents/README.md), [agents/BUILD-HANDOVER.md](agents/BUILD-HANDOVER.md), and every spec.) Status values: `todo` · `in_progress` · `blocked` · `review` · `done`.
> Sprint definition: [README.md](README.md) · Feature source: [`docs/ticketing_system/16_org_chart_and_positions.md`](../../ticketing_system/16_org_chart_and_positions.md)

## Ticket status

| ID | Title | Workstream | Status | Branch | Commits | Notes |
|---|---|---|---|---|---|---|
| OC-06 | Admin-setup UX/UI evaluation | ux-evaluation | done | dev/hardening (docs only) | — | Published `ui/03_admin_setup_flow_evaluation.md`; 2 blockers + 15 majors triaged below |
| SH-1..6 | Backend hardening (authz/validation/data-integrity) | hardening | done | dev/organisation | `8e55e65a` `27b4cf63` `96e77868` `313afc2a` `92054b36` `a9d76e3e` `290711d9` | 6/6 landed. **SH-4 dedup finder now built** on `feature/sh4-org-dedup` (pure matcher `ticketing/services/org_dedup.py` + preview endpoint `POST /organizations/duplicate-candidates` + email/address wired onto Create/Update/Response; **no migration** — `k1m3o5q7` columns reused). **Merged + DB-validated** (21 SH-4 tests + 76/76 org regression green in Docker). Per-SHA→ticket map in [BUILD-HANDOVER](agents/BUILD-HANDOVER.md) §2 Status column |
| OC-01 | Org tree on `organizations` + CSV import | backend-org-tree | done | dev/organisation | `1ba1831d` | **DB-validated in Docker:** migration up→down→up clean, **48/48 tests green** (OC-01 27 + org regression 21). Sole head `m3o5q7s9`. Gates SH-7/OC-02+/RB-4 |
| OC-02 | `position_types` + matrix + `/position-types` | backend-org-tree | done | dev/organisation | `64c00790` | **DB-validated in Docker:** migration `o5q7s9u1` up/down/up clean, **12/12 tests green**, 5 DoR position types seeded. `owner_organization_id` = first org-scoped-catalog column (SH-7 extends + filters) |
| OC-03 | `officer_positions` + invite pre-fill + supervisor resolver | backend-positions | todo | — | — | Needs OC-01/02 tables; routes through SH-3 validated helpers |
| OC-04 | Chart behaviors + SEAH leak-proofing | backend-positions | todo | — | — | Rebase on HR-04; needs OC-03 |
| SH-7 | Hierarchical admin scope (4 role keys + `org_category` gating + attenuated delegation + org-scoped catalog) | hardening | in_progress | dev/organisation | S1 landed | **S1 foundation DONE + DB-validated** (migration `q7s9u1w3`; `country_admin`→`org_admin` retired across code/seeds/catalog/tests; `officer_admin` tier added; `owner_organization_id` on roles/workflow_definitions; 113/113 rename-touched tests green, up/down/up clean). **Remaining: S2** subtree scope-reach · **S3** `org_category` root gating · **S4** attenuated delegation · **S5** catalog availability filter · **S6** authz-matrix/SEAH-leak tests. No further migration (S2–S5 are logic on the S1 columns) |
| OC-05 | Portal org-chart UI | portal | superseded | — | — | **Folded into the Settings rebuild** ([BUILD-HANDOVER](agents/BUILD-HANDOVER.md) · RB-4) after OC-06 pivot; original spec kept as historical |
| RB-1..4 | Settings frontend rebuild (incl. former OC-05) | portal | todo | — | — | **Design gate met** (design phase done at 84/74) — see [BUILD-HANDOVER](agents/BUILD-HANDOVER.md) §2 Phase 2; RB-1 i18n scaffolding can start early |

## Migration head coordination (critical)

Record the actual head each migration chains onto, to keep the ticketing Alembic chain linear across both sprints.

| Revision | Ticket | Chained on (`down_revision`) | Notes |
|---|---|---|---|
| `k1m3o5q7` | SH-4 org email/address | `h2j4l6n8` | SH-4 dedup-finder groundwork |
| `m3o5q7s9` | OC-01 org columns | `k1m3o5q7` | **Applied + verified 2026-07-10** — up→down→up clean on the Docker `db`; sole head; DOR/ADB backfilled then categorized (gov/donor) |
| `o5q7s9u1` | OC-02 position_types | `m3o5q7s9` | **Applied + verified 2026-07-10** — up→down→up clean; single head; 5 DoR position types seeded |
| `q7s9u1w3` | SH-7 admin ladder + org-scoped catalog | `o5q7s9u1` | **Applied + verified 2026-07-11** — up→down→up clean; `country_admin`→`org_admin` data rename; `owner_organization_id` on roles/workflow_definitions; officer_admin role added |
| — | OC-03 officer_positions | **`q7s9u1w3`** | Chain on SH-7's head above |

## Acceptance checklists

### OC-06 — UX evaluation
- [x] Cold-start walk as all four admin personas — done via **live API persona probing** (`GET /users/me/admin-context` on :5003) + code-grounding; no screenshots (bypass API :5002 down by misconfig, no browser tooling) — runtime API/DB probing substituted per ticket's "note each step"
- [x] Journey map (4-in-1 flow, inter-tab jumps marked) — report §1 (seams S1–S6)
- [x] Scored findings table with file:line evidence + owner per finding — report §3 (26 findings; `[live]`-marked cells were runtime-verified)
- [x] Top-5 low-IT-literacy friction points ranked with fixes — report §4
- [x] Orphaned-state catalogue (which OC-01..05 validations close them) — report §5 (O1–O10)
- [x] Nepali/i18n readiness note for the admin surface — report §6
- [x] "Does org-chart actually help?" verdict recorded **before OC-05 starts** — report §7
- [x] Report published at `docs/ticketing_system/ui/03_admin_setup_flow_evaluation.md`; blockers/majors triaged below

### OC-01 — Org tree
- [x] Migration `m3o5q7s9`: `parent_organization_id` self-FK (SET NULL), **`org_category`** (NOT NULL + CHECK, backfilled), `unit_type` (CHECK), `territory_location_code` (FK→locations), `territory_includes_children`, `display_name_ne`; backfill + real `downgrade()` + safety header. *(authored; DB apply pending Docker)*
- [x] Model columns + self-relationship; `descendant_org_ids`/`ancestor_org_ids` CTE helper (path-array cycle guard) + `would_create_cycle` — `ticketing/services/org_tree.py`
- [x] Router: new fields on GET/POST/PATCH; `tree=true` + `root_id` filter; cycle/unit_type/territory validation + reparent category-cascade + child-delete 409 guard
- [x] unit_type validation permits **independent roots** (`company`/`development_partner`) and **`company`→`company` nesting**; reporting-line nesting unchanged; off-pattern category/unit **warns, not blocks** (design §2.4, doc 16 §3.1)
- [x] `POST /organizations/import` CSV: whole-file validate → single-transaction parents-first insert → idempotent upsert (`ticketing/seed/org_import_core.py`, pure planner)
- [x] Gated at country tier (reuse `admin_access` `MANAGE_ORG_STRUCTURE`); **authz matrix** green (pure gate test)
- [x] `tests/ticketing/test_org_tree.py` **green in Docker (27/27: 18 pure + 9 integration)** — create/patch/reparent/subtree/child-delete/CSV all-or-nothing + idempotency + authz; org regression (authz/lifecycle/charset) also green → **48/48 total**
- [x] **Manual:** `alembic upgrade→downgrade→upgrade` clean on Docker `db`; existing orgs (DOR/ADB) still resolve; integration test rows self-clean

### OC-02 — Position types + matrix
- [x] Migration `o5q7s9u1` + `PositionType` model (all cols incl. `display_name_ne` + `owner_organization_id`); UUID PK is Python-side `String(36)` (house style, not `gen_random_uuid()`)
- [x] `/position-types` CRUD; `position_key` immutable (omitted from update schema); delete guard on in-use (self-ref `reports_to_position_key`; **holder guard is an OC-03 `# INTEGRATION POINT`** since `officer_positions` doesn't exist yet)
- [x] Matrix validation: `default_role_key` exists + track-compatible (reuses SH-2 `role_scope_matches_track`), `allowed_unit_types` ⊆ UNIT_TYPES, `reports_to_position_key` exists + no self-ref, `owner_organization_id` exists
- [x] `default_role_key` edit does not re-sync holders (no side effect; commented — OC-03 adds "review holders")
- [x] **Authz matrix** green (MANAGE_ORG_STRUCTURE); **SEAH-track authoring open-question logged** (doc 16 §7 "org_admin seah may propose — TBD")
- [x] `tests/ticketing/test_position_types.py` green — **12/12 in Docker** (6 pure + 6 integration)
- [x] Seed ~5 real DoR position types committed (`ticketing/seed/position_types.py`, wired into `seed_standard`; 2 titles share 1 role — the matrix demonstrated)

### OC-03 — Officer positions + invite pre-fill
- [ ] Migration + model for `officer_positions`
- [ ] Invite pre-fill routes through existing `upsert_user_role_row` + `create_scope_row` (no direct enforcement-row writes)
- [ ] Override (explicit role/scope beats matrix); dual-hat; transfer ends old row without silent re-scope
- [ ] `/users/{id}/positions` GET/POST/DELETE; **supervisor resolver = per-`(project, step)`** (explicit → next-step Handler pool → top-step explicit → null); **no `parent_organization_id` walk** ([DECISION §5](DECISION-project-participants-and-supervision.md))
- [ ] `validate_jurisdiction` still enforced; **authz matrix** (incl. project_admin own-scope) green
- [ ] `tests/ticketing/test_officer_positions.py` green
- [ ] Manual: invite-by-position produces correct role/scope; resolver returns override then derived

### OC-04 — Chart behaviors + SEAH
- [ ] §5.1 display (position title on roster/chips/timeline/reports)
- [ ] §5.3 supervisor visibility in Watching, `visibility_mode`-bounded, **SEAH-filtered inline**
- [ ] §5.4 prefer-own-office ranking in `auto_assign_officer` (preference, not filter)
- [ ] §5.5 escalation-notify via resolved supervisor (**+ reassignment on assignment-failure**), **suppressed on SEAH unless supervisor holds SEAH role**
- [ ] §5.6 **donor informed at last step** (auto-fill "Kept informed" with donor roles; go-live blocks if absent); **SEAH-suppressed** on the SEAH track (leak test) ([DECISION §3](DECISION-project-participants-and-supervision.md))
- [ ] Rebased on HR-04; notify attached to post-HR-04 seam (base commit recorded)
- [ ] `tests/ticketing/test_chart_behaviors.py` green **incl. both SEAH-leak tests**
- [ ] Manual: standard supervisor sees no trace of subordinate SEAH case

### OC-05 — Portal UI
- [ ] `OrgTreeTab.tsx` (tree editor + CSV import) — new component
- [ ] `PositionTypesTab.tsx` (matrix editor + review-holders prompt) — new component
- [ ] Invite pre-fill flow (tree picker → position → pre-filled editable fields + override badge)
- [ ] Display surfaces show position title with role-label fallback
- [ ] `lib/api.ts` typed wrappers for all OC-0x endpoints; `apiFetch` signature unchanged
- [ ] **God-file untouched beyond ~5-line tab wiring**; landed after HR-06
- [ ] Vitest for extracted logic; tsc/eslint(0 hooks)/build green
- [ ] Manual flows recorded; existing tabs no regression

## Deviations / findings log

| Date | Ticket | Deviation / adjacent finding | Action |
|---|---|---|---|
| 2026-07-11 | SH-7 | **S1 foundation landed (partial ticket — enforcement S2–S5 to follow).** Migration `q7s9u1w3` (on `o5q7s9u1`): renames `country_admin`→`org_admin` on admin_scopes + the catalog role row (role_id preserved → user_roles FKs intact), inserts the `officer_admin` admin role, adds `owner_organization_id` (FK→organizations SET NULL) to `roles` + `workflow_definitions`. Code: `admin_access.py` `is_country_admin`→`is_org_admin` + new `is_officer_admin` + `ADMIN_ROLE_KEYS` = {super/org/project/officer}; `officer_admin` passes `INVITE_OFFICERS`; blanket rename across dependencies/routers/schemas/constants/seeds/8 test files (85 refs); `owner_organization_id` mapped on `Role`/`WorkflowDefinition`; catalog gains `officer_admin`. **Decisions (as planned):** gates stay pure, subtree enforced surgically (S2); legacy `organization_id IS NULL` org_admin = country-wide bridge (no invented anchor). **Verification:** up→down→up clean; **113/113 rename-touched admin/authz/org/roles tests green**. **Finding — pre-existing suite failures:** the full ticketing suite shows **34 failures unrelated to SH-7** (officer-staffing go-live gate: "Add L1 for packages 02–05"); confirmed by rebuilding at clean `d01c045c` where the same operational tests fail (8/14 in the two spot-checked files). +1 keycloak-provisioning test fails only because the `keycloak` container isn't in this stack. SH-7 introduced **zero** new failures. | Continue SH-7 S2–S5 (no new migration); OC-03 now chains on `q7s9u1w3`; **flag: the 34 pre-existing staffing failures + keycloak infra dep are a separate seed/CI gap worth a ticket** |
| 2026-07-10 | OC-02 | **Position types built + DB-validated in Docker** (migration `o5q7s9u1` on `m3o5q7s9`; up→down→up clean; 12/12 tests; 5 DoR positions seeded). **Decisions:** (a) UUID PK is **Python-side `String(36)` `default=_uuid`**, not `gen_random_uuid()` — recon found that's the house style (the CLAUDE.md "gen_random_uuid" note is aspirational; only seed SQL uses it); (b) `owner_organization_id` is the **first org-scoped-catalog column** — roles/workflow_definitions don't have one yet, so OC-02 only *stores/validates* it (FK→organizations SET NULL); **SH-7 owns the availability filter** (available at node + descendants) and extends the column to roles/workflows; (c) delete guard currently blocks only on `reports_to_position_key` self-refs — **holder count is an `# INTEGRATION POINT (OC-03)`** because `officer_positions` doesn't exist yet (409 shape pre-shaped with a `holders_count` slot); (d) `default_role_key` track-compat reuses SH-2 `role_scope_matches_track`, with a `both`-track position requiring a role usable on both (`Both`/unscoped). **Open question logged:** SEAH-track position-type authoring (doc 16 §7 "org_admin seah may propose — TBD") — currently standard-track/super only. | OC-03 chains on `o5q7s9u1`, wires holder guard + invite pre-fill; SH-7 adds the org-scoped-catalog availability filter |
| 2026-07-10 | OC-01 | **DB-validated in Docker.** Rebuilt `ticketing_api` with the working tree; `alembic upgrade head` applied `k1m3o5q7`+`m3o5q7s9`, `downgrade -1` dropped all 6 tree cols, re-`upgrade` restored them (up→down→up clean). Columns/CHECKs/FKs confirmed on `ticketing.organizations`; DOR/ADB backfilled to `government`. **48/48 tests green** (OC-01 27 + org regression 21). **Two fixes during validation:** (1) recursive-CTE array type mismatch — cast `organization_id::text` in `subtree`/`ancestors` (Postgres unifies `varchar(64)[]` base vs `varchar[]` recursive); (2) corrected the KL Road seed + dev DB so **ADB = `donor`/`development_partner`, DOR = `government`/`department`** (was blanket-backfilled `government`). | Ready to commit; OC-02 chains on `m3o5q7s9` |
| 2026-07-10 | OC-01 | **Org tree built — code complete + pure tests green (uncommitted; migration not yet DB-applied — host lacks docker).** Migration `m3o5q7s9` (sole head on `k1m3o5q7`) adds the 6 tree columns. Router: `tree`/`root_id` list, create/patch tree placement + reparent cycle-guard + subtree category cascade, child-delete 409 guard, `POST /organizations/import`. Services: `org_tree.py` (path-guarded CTEs) + `org_import_core.py` (pure planner). **Decisions:** (a) `org_category` **denormalized NOT NULL on every node** (= root's category) for cheap subtree reads — reparent cascades it; existing seeded ADB is backfilled `'government'` (mislabelled) → correct via `PATCH .../ADB {org_category:'donor'}`; (b) create **defaults an omitted root's category to `government`** so the SH-6 charset test (root, no category) stays green; (c) **incidentally mapped SH-4's `email`/`address` onto the `Organization` model** to remove the model↔DB autogenerate-drift landmine before OC-02 (hand-written migration, so no drop emitted). **Pending Docker:** `alembic up/down/up` + 9 integration tests (`test_org_tree.py -m integration`). **SH-7 handoff:** per-`org_category` root-creation gating left as `# INTEGRATION POINT (SH-7)` in `create_organization`. | Chain OC-02 on `m3o5q7s9`; run DB validation under Docker before commit; SH-7 adds attenuated root-creation gating |
| 2026-07-07 | OC-06 | **F1 (blocker):** Devanagari org name un-creatable from UI (ASCII-only client id lock); server accepts it and mints `NP_सव` **[live]** | New-ticket: org id charset policy + manual-id entry; relevant to OC-01/05 org UI |
| 2026-07-07 | OC-06 | **F2 (blocker):** step→role binding has zero server-side referential/track validation; publish only checks empty; wrong-track defaults persist silently | New-ticket: validate `assigned_role_key`+tier roles on write/publish — the enforcement truth positions feed |
| 2026-07-07 | OC-06 | **F3 (major, security):** blanket `require_admin`; `MANAGE_PROJECT`/`INVITE_OFFICERS`/`can_manage_structure` defined-but-unwired; UI gating cosmetic. **[live]** project_admin created a global org (201), 403'd only on project create | New-ticket: wire scoped gates. OC-01/OC-03 must not inherit the blanket gate |
| 2026-07-07 | OC-06 | **F4 (major):** `can_manage_structure` is track-blind → SEAH admin gets standard-track structure rights **[live]**; doc 16 §7 wants standard-only | **OC-01 acceptance:** add standard-track check for org-tree/CSV |
| 2026-07-07 | OC-06 | **F6/O4 (major):** invite path never validates org existence or org↔project jurisdiction; no FK on `officer_scope.organization_id`; no rollback on multi-scope invite | **OC-03 acceptance:** position-fed invite must add these checks (reuses the unvalidated helpers) |
| 2026-07-07 | OC-06 | **F5/F8/F9/F11/F12/F13/F14/F21/F23/F24 (major/minor):** invite = 7 decisions; read-only org→project authoring; stale go-live; raw JSON errors; project_admin sees actions that 403; god-file 4,723 lines; zero i18n; role auto-default; empty-staffing foot-gun; duplicated `ORG_ROLE_COLORS` w/ banned hues | **OC-05 acceptance items** (see report §8) — incl. invite pre-fill as *result-not-form*, tree replaces flat list, position-types not a top tab |
| 2026-07-07 | OC-06 | **F7/F10/F15/F16/F17/F18 + i18n infra:** org delete cascade-orphans project link; "run migrations" empty states; role delete guard ignores tier fields; `GET /organizations` unauthenticated; activated-but-tickets-blocked; no country/dup-name validation | New backlog/security tickets (report §8) |
| 2026-07-07 | OC-06 | Verdict: org-chart **does** target the #1 friction (officer setup) — build it — but payoff is contingent on OC-05 invite UX (result-not-form) + OC-01/03 closing validation/authz gaps, not inheriting them | Resolve OC-05 design forks before OC-05 implementation (report §7–§8) |
| 2026-07-07 | OC-06 | Doc bug: OC-05 spec's "Grounded by the OC-06 UX evaluation" links the OC-06 *ticket*, not its findings | Repoint OC-05 §header to `ui/03_admin_setup_flow_evaluation.md` when OC-05 starts |
| 2026-07-07 | Handover A | **Org model decided (design §2.4):** the org tree is a **forest** — GoN reporting line is a rooted subtree; contractors and ADB (`development_partner`) are **independent roots**, never nested under a ministry; **multi-ministry-ready**. Wireframe corrected (was nesting a contractor under the ministry). | Design doc §2.4 + `settings-wireframes.html` Frame 2 updated |
| 2026-07-07 | Handover A | **Contractor create + dedup decided:** `project_admin` may create a participant org **via the project-scoped actor path** (SH-1b), not the country-gated global `/organizations` route; dedup is a **fuzzy soft-flag** (distinctive name tokens + corporate email + address), never a hard block. | **SH-1/SH-1b** authz split + **SH-4** upgraded to a fuzzy candidate finder (was exact-name warn); design §2.4 |
| 2026-07-07 | Handover A | **Admin model decided (design §2.5):** super_admin / **org_admin** (subtree-scoped, attenuated sub-delegation) / project_admin; `country_admin` retired → org_admin at a ministry root. Driven by scale (DoR ≈5000 staff). | New **SH-7** (hierarchical admin scope) on OC-01's `descendant_org_ids`; supersedes SH-1 flat gate + absorbs SH-1b; doc 11 §1 flagged "revision in progress" |
| 2026-07-08 | Handover A | **Org model refined (design §2.4):** independent roots aren't flat — a company/donor owns its **own multi-level subtree** (main contractor → subcontractors; donor → regional offices). `company` may parent `company`; subtree/territory/admin-scope apply uniformly. | Design §2.4 + wireframe Frame 2 (subcontractor child + affordances) + atlas empty-state copy; **OC-01** unit_type validation must allow company roots + `company`→`company` nesting |
| 2026-07-08 | Handover A | **Workflow↔role clarified + simplified (design §4.3/§4.4):** a step is a **cast** of 4 role slots — Handles it / Oversees / Kept informed / Can view (= `assigned_role_key`/`supervisor_role`/`informed_roles`/`observer_roles`), answering "different roles per level"; **templates pre-fill the cast**; **"archetype" renamed "acts as"** (one vocabulary for role + step slots); "Add / reassign officers" (`reassign`) capability added to Cases. | Frame 04 (cast) + Frame 08 (acts-as, permission) + design §4.3/§4.4; **UI-labels only, no schema change** |
| 2026-07-08 | Handover A | **Notifications clarified (design §4.5):** notifications are **derived from the cast** (slot = notification tier), not per-step; one **per-track** grid (event × slot × channel) in `settings.notification_rules`, pre-seeded, observers silent, complainant channel separate. | Frame 04 note + new **Frame 09** (matrix) + design §4.5; surfaces the existing model, **no schema change** |
| 2026-07-08 | Handover A | **Devil's-advocate review** (2 independent agents, cold read): **Completeness 57% · Ease-of-use 60%** (vs old 48%). Findings A (day-one landing), B (creation/authoring incl. SLA), C (on-screen jargon), D (English chrome), E (self-inflicted inconsistencies), F (lifecycle/scale/SEAH). | Published [`DESIGN-REVIEW.md`](DESIGN-REVIEW.md) |
| 2026-07-08 | Handover A | **Remediation pass A/B/C/E/F** (D deferred): first-run spine (Frame 01 + §7.A); step SLA + escalation target (Frame 04 + §7.B); plain-language pass (Frames 02/05/06/07 + §7.C); atlas now 9 surfaces + vocab unify + notif proto-phasing + `org_admin` naming (§7.E); lifecycle/scale/SEAH **spec'd** (§7.F). | Design **§7** + wireframes (pins→41) + atlas (10 surfaces); B1/B3/B4 + F wireframes + full C label-map = next increment |
| 2026-07-08 | Handover A | **B/F wireframes rendered:** deck 9→**13 frames** (pins→52) — **Frame 10** create-a-project + workflow→project link (B1/B3); **Frame 04** clone-a-template variant (B4); **Frame 11** officer directory at scale + manage/transfer/deactivate (F1/F3); **Frame 12** org "⋯" lifecycle + SH-4 guard + tree at scale (F2/F3); **Frame 13** SEAH setup + invisibility (F4); **Frame 07** revoke/bootstrap/orphan-rehome variant (F5). | §7.B/§7.F flipped to `[wireframed]`; [`DESIGN-REVIEW.md`](DESIGN-REVIEW.md) status updated; only full-C-sweep + D remain open |
| 2026-07-08 | Handover A | **Re-score (round 2, independent):** Completeness **57→70%** (+13), Ease-of-use **60→66%** (+6). B/F confirmed closed; but remediation **introduced**: a self-contradiction (§2.5 authz vs §2.1/doc-11/pin-35), Frame 07/atlas `org_admin` slug leak (C incomplete), missing workflow step add/remove/reorder, no atlas states for Frames 10–13, undefined merge/in-flight-ticket, over-stated SEAH invisibility. | Logged in [`DESIGN-REVIEW.md`](DESIGN-REVIEW.md) round-2 findings; a **round-3 fix pass** would clear most (self-inflicted) |
| 2026-07-08 | Handover A | **Re-score (round 3, independent):** Completeness **70→77%** (+7), Ease-of-use **66→70%** (+4). 4/6 round-2 findings closed; but **§2.5 root/sub split was relabeled not operationalized** (undefined "root" in the forest; no appoint-form selector; **conflicts with doc 11 §2 LOCKED**), and the de-slug claim was overstated (2 residuals + cross-deck "whitelist/share" mismatch — **fixed post-re-score**). Real gaps left: admin audit-log, dual-hat invite, workflow level-delete in-flight, CSV schema, project country-validation. | Logged in [`DESIGN-REVIEW.md`](DESIGN-REVIEW.md); **admin-tier model needs a real decision + doc-11 reconciliation (not another wireframe patch)** |
| 2026-07-08 | Handover C | **Re-score round 5 (confirmation): Completeness 80→84% (+4), Ease-of-use 73→74% (+1).** Both round-4 blockers CLOSED (doc 11 consistent; `org_category` has a schema home). Round-5 follow-up fixed the org-scoped-catalog propagation gaps: doc 16 §3.2 `owner_organization_id`; **Frame 04 "owned by Jhapa — not here" exclusion** (kills the new silent-failure) + Frame 08 "System · everywhere / Custom · X & below"; doc 11 §11 3d + SH-7 catalog-filter acceptance; §7 officer_admin note; org_category/unit_type warn-not-block guard. **Ease-of-use plateaued** — remaining ceiling is deferred (English chrome = RB-1) or intrinsic (Frame 04/07 density). | **Design phase declared done at 84/74** (from 48 baseline). Next work = the build (SH-7, OC-01..04, RB-1..4). [`DESIGN-REVIEW.md`](DESIGN-REVIEW.md) round-5 |
| 2026-07-08 | Handover C | **HANDOVER-C executed (§1–§3):** doc 11 §4–§11 propagated to the 4-tier ladder + `officer_admin` (access matrix, scope table `country_code`→subtree, §6/§8/§9/§11, status line — grep-clean of "3 keys"/`country_code`/"country admin"); **`org_category`** added to doc 16 §3 schema + `unit_type` reconciliation + §7 creation-gating; **org-scoped catalog** spec'd (`owner_organization_id` in doc 11 §3.3/§3.5/§5/§3.6 + DESIGN §4.4 + Frame 08 owning-level chips + SH-7 filter); residual slugs/symbols fixed; 5 build items filed in Handover B. | Only open item = confirmation re-score (HANDOVER-C DoD); decks structurally sound |
| 2026-07-08 | Handover A | **Re-score round 4 + design phase declared done (80% / 73%, from 48% baseline).** Admin model coheres across doc 11 §2 / DESIGN §2.5 / Frame 07 / atlas. **Decision — org-scoped catalog:** a role/workflow/position-type is available at its owning org **level and below** (kills proliferation from any-depth authoring). Remaining = cleanup (doc 11 §4–§11 not propagated; `org_category` no data home; 2 residual slugs) + 5 build-time items. | Execution handed off → **[`HANDOVER-C-admin-model-finalize-and-cleanup.md`](archive/HANDOVER-C-admin-model-finalize-and-cleanup.md)**; scores in [`DESIGN-REVIEW.md`](DESIGN-REVIEW.md) round-4 |
| 2026-07-08 | Handover A | **Admin model finalized + doc 11 updated (resolves the round-2/3 blocker):** 4-tier strict-subset ladder — **super_admin / org_admin (subtree, any depth; authors catalog) / project_admin (contractors + staffing) / officer_admin (invite only)**; `country_admin` retired → `org_admin`. **`org_category` actor types** (government / local_government / donor / third_party) give "root" a concrete definition; new institutional root = super-only, third_party delegable. **doc 11 §2 rewritten** (was the stale LOCKED spec that "conflicted") + §3–§8 bridging note. | doc 11 §2; DESIGN §2.5/§2.4/§4.4 + decision log; Frame 07 + atlas s6; Handover B **SH-7** (4 keys + org_category + attenuation); [`DESIGN-REVIEW.md`](DESIGN-REVIEW.md) round-4 |
| 2026-07-08 | Handover A | **Round-3 fix pass — all round-2 findings closed:** §2.5 split into root-tier (catalog owner) vs sub-tier (blocker fixed); workflow **levels add/remove/reorder** (Frame 04 pin 53); **de-slugged** Frame 07 + atlas mock UI (org_admin/project_admin/super_admin → labels, "matrix"/"attenuated"/"whitelist" gone); atlas +4 surfaces (**14 total**) for Frames 10–13; **merge + in-flight-ticket** semantics (pins 54–55, §7.F); **SEAH invisibility corrected** (case existence only; people appear — doc 16 §6); minors (locked-perms cue, SEAH-filter gate, dual-hat, "Next:" strip). | Wireframes **13 frames / pins 1–56**; atlas 14 surfaces; [`DESIGN-REVIEW.md`](DESIGN-REVIEW.md) round-3 table; re-score = next optional checkpoint |
| 2026-07-10 | Design (project model) | **Project participants & supervision reworked ([DECISION](DECISION-project-participants-and-supervision.md)).** Org project-**role** catalog removed (`project_actor_roles` / `org_role` / `routing_org_role`); a project now = one defaulted **`implementing_agency`** + optional **`project_donors`** (0..n). Donor `donor-*` roles auto-fill the last step's "Kept informed"; **go-live blocks** if a donor isn't informed there (**SEAH-suppressed**). **Supervisor** = per-`(project, step)` reassignment authority (default next-step Handler pool; **org-tree `parent_organization_id` derivation deleted**). Go-live gates on **staffing** (every level has an officer). | Propagated: doc 13 (banner + go-live/API), doc 16 §5.5/§6, 02-spec §3, 03-spec §5.5/§5.6, DESIGN §2.4/§3.1, BUILD-HANDOVER §1; **affects OC-03/OC-04 + doc-13 build** |
| 2026-07-10 | SH-4 | **Dedup finder built (extends SH-4 from exact-name warn → fuzzy candidate finder; design §2.4).** New pure module `ticketing/services/org_dedup.py` (no DB/HTTP — mirrors `org_import_core.py`/`org_tree.py` split) scores an existing org against a proposed `{name, email, address}` on three signals: **distinctive name-token Jaccard** (generic-word stoplist — corporation/company/contractor/construction/pvt/ltd/jv/office/department/roads/… stripped so "…Construction JV" ≈ "…Contractor JV", and two all-generic names never match), **corporate email-domain** equality (free providers gmail/yahoo/hotmail/outlook/… ignored), and **fuzzy address** (`difflib`). A candidate qualifies if **any** single signal clears its own threshold (name Jaccard ≥ 0.5, corp email-domain match, address ratio ≥ 0.82); ranked by a weighted aggregate (name .5 / email .35 / address .15), ties break on id. **email/address wired** into `OrganizationCreate/Update/Response` + persisted on create/update (`""`→NULL). **Decisions:** (a) **soft-flag surface = dedicated preview endpoint** `POST /organizations/duplicate-candidates` (auth-only read), **not** a create-time block and **not** the old silent `ADB_2` rename — keeps `POST /organizations` response shape + OC-01 tests unchanged, matches the "Use existing / Create anyway" two-phase UX, and gives the client `lib/orgDedup.ts` (RB-4) a stable contract; (b) **no migration** — reuses `k1m3o5q7` email/address columns (authoring one would fork the chain); (c) endpoint pre-filters by same-country + country-less orgs to bound the scan, matcher stays country-agnostic. Tests: `tests/ticketing/test_org_dedup.py` — **17 pure (green on host)** + 4 integration (`@pytest.mark.integration`, need Docker DB). Did **not** touch position types (OC-02). | Review + merge `feature/sh4-org-dedup` → `dev/organisation`; run the 4 integration tests under Docker |
| 2026-07-10 | Build launch | **Status reconciled to git before handoff.** Verified **SH-1..SH-6 committed** on `dev/organisation` (`8e55e65a`..`290711d9`); **SH-4 dedup finder still outstanding** (only guards + `k1m3o5q7` email/address migration landed); OC-01/SH-7/OC-02..04/RB-1..4 not started (`officer_admin` code hits = existing onboarding module, not the role key). Live Alembic head = **`k1m3o5q7`** (sole head, 17 revs) → OC-01 chains there. Added a Status column to BUILD-HANDOVER §2, a "start here → OC-01" pointer, and **default-Opus + parallel-subagents** run guidance; base branch synced `integration/seah-claude` → `dev/organisation`. | Ticket table + migration-head table above updated; [BUILD-HANDOVER](agents/BUILD-HANDOVER.md) §2/§4 synced |
| 2026-07-10 | Handover A/B/C | **Design phase closed → build launched.** Consolidated the three design-phase handovers into a single build entry point and archived the originals: created **[`BUILD-HANDOVER.md`](agents/BUILD-HANDOVER.md)** (read order, locked decisions, two-phase plan SH-1..7 + OC-01..04 + RB-1..4, sequencing, DoD); moved HANDOVER-A/B/C into [`archive/`](archive/) with ARCHIVED banners → BUILD-HANDOVER; reconciled every inbound/outbound link (README §"Current state", DESIGN/DESIGN-REVIEW/PROGRESS repointed; grep-verified no dead links repo-wide). | Design at **84/74** (from 48 baseline); next work is the build off `integration/seah-claude` per BUILD-HANDOVER §2 |

## Sprint close checklist
- [ ] All 6 tickets `done`, CI green on integration branch
- [ ] `docs/ticketing_system/16_org_chart_and_positions.md` status → as-built (cite migrations/routers)
- [ ] Doc 16 §10 open questions resolved and recorded
- [ ] OC-06 evaluation published; findings triaged
- [ ] Summary doc written; this folder moved to `docs/sprints/archive/`; sprints index updated
