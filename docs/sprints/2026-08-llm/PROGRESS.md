# Sprint tracker — DPG compliance & LLM independence

> **Update this file at every commit.** Status, checklist ticks, deviations.
> Sprint index: [`README.md`](README.md) · Test ledger: [`TESTS.md`](TESTS.md) · Questions: [`QUESTIONS.md`](QUESTIONS.md)
> Status legend: ⬜ not started · 🟡 in progress · ✅ done · ⏸ blocked · ❌ dropped (log why)

**Sprint status: 🟡 IN PROGRESS — Sprints 0, 1 and 2 closed; Sprint 3 starting** · Created 2026-08-17 · Baseline `integration/stage` @ `f0d4552d`

> ## ✅ Sprint 2 closed 2026-08-27 — three tickets closed with their blockers named
>
> **DPG-20 ✅ · DPG-21 ✅ · DPG-25 ✅.** **DPG-22, DPG-23 and DPG-24 are ⏸, not 🟡** — the
> distinction matters, and it is the same one Sprint 0 drew around DPG-03. Nothing is in progress on
> them: every remaining step is **external to this repository**, and leaving them 🟡 would read as work
> someone is doing.
>
> | Ticket | What is left | Who can do it | Raised |
> |---|---|---|---|
> | **DPG-22** | Nepali WER. Blocked **twice**: the benchmark set has no audio, and the open ASR endpoint 404s (**D-53**). Fixing one does not unblock it | Someone with a phone and a quiet room (~6–20 recordings + transcripts); separately, an ASR provider that serves `/v1/audio/*` | 2026-08-20 |
> | **DPG-23** | The open column — 2 of 105 classification items. **D-50**: the account is rate-limited, and our own ~20,700-char prompt trips the token limit | A pre-paid balance on the HF account (the free tier 402s at ~$0.10/month), plus the prompt work | 2026-08-20 |
> | **DPG-24** | The job has **never executed**. It is written and pinned by 14 tests | Whoever administers the GitHub repo: `HF_TOKEN` as a secret + `DPG_MODEL_*` as repo variables. **Nobody working in this repository can do this** | 2026-08-20 |
>
> ⛔ **What closing does NOT mean.** Sprint 2's stated purpose was to **choose an open model**, and it
> did not. The mechanism, the data, the harness, the scoring rules and the CI gate are built, tested and
> reproducible; the choice is absent because **SEAH recall is not measurable from this repository** and
> selecting on what *is* measurable would pick the detector that flags nothing. That is recorded in
> §"What a reader should not conclude", in `03-open-models-spec.md`, and in the indicator-4
> clause-by-clause check — **four of seven clauses do not hold**, and a version that is true today is
> drafted beside them. Closing the sprint does not close that gap; it stops pretending someone is
> working on it.
>
> **Sprint 3 (`dpg/sprint3-pii`) is unblocked and starts at DPG-30**, which is doc-only and depends on
> none of the above. It branches from `dpg/sprint2-open-models` rather than `integration/stage`, because
> it needs two things that exist only here: the amended spec (§31.4, the three-consumer table) and the
> benchmark set's **labelled PII spans**, which DPG-35 consumes. ⚠ **`integration/stage` is 47 commits
> behind and that is now the largest carried risk on this programme** — D-55 (every Nepali session
> storing an off-taxonomy category) and D-58 (the seeded taxonomy) are both fixed here and on no
> deployed branch.

> **Sprint 2 (`dpg/sprint2-open-models`) — all six tickets landed, 2026-08-20.** Six commits.
> **DPG-20 ✅ · DPG-21 ✅ · DPG-25 ✅ · DPG-22 🟡 · DPG-23 🟡 · DPG-24 🟡.**
>
> ⭐ **The one that matters: the LLM suite is green against the open configuration.** Four passed,
> one xfailed, exit 0 — the product's own call paths, both surfaces, against `openai/gpt-oss-20b` on
> Hugging Face Inference Providers. That is indicator 4 **executing**, not asserted.
>
> **What the three 🟡 are waiting on, and none of it is code:**
> - **DPG-22** — WER needs audio nobody has recorded, and the open ASR endpoint **404s** (D-53).
> - **DPG-23** — 103 of 105 classification calls hit the provider's token limit, because **our**
>   prompt is ~20,700 characters (the catalogue is injected twice). Detection ran fully.
> - **DPG-24** — the job is written, pinned by 14 tests, and has never executed in CI.
>
> **Nine deviations, eight of them defects in code that was already live:**
> **D-49** four categories silently lost their high-priority flag, including dust and the SEAH
> category · **D-50** ⚠ *my own diagnosis was wrong* — a rate limit, not depleted credit ·
> **D-51** the classifier invents categories on 17% of grievances and they are stored *(closed 08-25 — taxonomy first, then a repair-or-drop guard)* ·
> **D-52** the SEAH detector flags 5 of 8 gendered non-harassment complaints into a channel most
> officers cannot see · **D-53** the open ASR endpoint does not exist · **D-54** a candidate model
> refuses a grievance about children falling ill · **D-55** ⚠ *found while closing D-51* — **every
> Nepali session stored a category outside the taxonomy**, from our own leaf-vs-key lookup, and the
> test suite pinned the wrong value as expected · **D-56** the SEAH detector's confidence is
> computed on every call and **discarded three times**, so the queue that deliberately holds false
> positives has no ordering signal · **D-57** the officer's correction **overwrites the
> classifier's answer unlogged**, so the AI baseline's real-world score cannot be computed —
> even though the change-tracking machinery exists and is used elsewhere · **D-58** ⚠ *found
> while diagnosing four days of red CI* — **the taxonomy is authored twice**, and the copy
> that seeds every database is not the one D-49 and D-51 repaired.
>
> ⚠ **The single most important missing number in this sprint is SEAH recall.** `gpt-5-nano` flags
> 5 of 8 confusables; `gpt-oss-20b` flags **0 of 105 items, of anything**. Those are consistent with
> "better calibrated" *and* with "says no to everything", and the committed set has no positives to
> tell them apart. **Neither model may be selected on this evidence.**
>
> ⭐ **Two findings the sprint was not looking for.** Both models invent the *same* `Road Hazard -
> Dust` — two vendors, two architectures, one fabricated label — which makes D-51 a **taxonomy**
> question more than a model one. And DPG-25's costing found the T1/T2 crossover sits at
> 40,000–780,000 grievances/month against a national ceiling near 7,700: **T2 is a data-sovereignty
> decision with a price, not a cost decision at all.**


> **Sprint 1, first wave, closed 2026-08-18.** All eight tickets ✅ on `dpg/sprint1-llm-agnostic`,
> eight commits, DPG-10's net green at every one of them.
>
> ✅ **Second wave closed 2026-08-19** — DPG-19, DPG-19b, DPG-18 (layer + consolidation) and DPG-15b,
> five more commits. All three of its questions (Q-20, Q-21, Q-22) were answered by the owner and
> each was confirmed against the code before it was implemented.
>
> ⚠ **Reopened on 2026-08-18 with that second wave — DPG-18, DPG-19, DPG-15b — after the owner
> reviewed the result.** Two of the three start from corrections the review surfaced: `gpt-3.5-turbo` is still
> live on three call sites (the Sept-2025 nano migration moved one of five), and the contact-extraction
> path that would send a phone number to a model **has no caller at all** — which also means this
> tracker overstated D-33's severity (**D-37**) and the privacy assessment overstated its egress
> inventory by two of six call sites. Three questions gate the work: **Q-20**, **Q-21**, **Q-22**. See §Sprint 1 — definition of done below for what is
> genuinely finished and the three things that are not. **Sprint 2 (`dpg/sprint2-open-models`) is
> unblocked and starts at DPG-20**, which is free; DPG-22/23/24 spend inference and Q-19 (there is no
> LLM budget) still gates them.
>
> **Sprint 0 closed 2026-08-18.** DPG-01, 02, 04, 05 and 06 are ✅ on `dpg/sprint0-licensing`.
> **DPG-03 cannot be closed here** — it is a written determination from ADB's Office of the General
> Counsel and nobody working on this repository can produce it. See its row and §DPG-03 below for the
> clock. Sprint 1 (`dpg/sprint1-llm-agnostic`) is unblocked and starts at **DPG-10**.

---

## Status board

| Ticket | Title | Status | Branch | Commit(s) | Tests green | Notes |
|---|---|---|---|---|---|---|
| **DPG-01** | LICENSE + NOTICE + SPDX | ✅ | `dpg/sprint0-licensing` | | ✅ T-01 (9) | Apache-2.0 landed **provisionally** (owner: proceed, revisit if the consultant advises — Q-02). 583 files stamped; `NOTICE` holder is an explicit `⚠ PENDING` per DPG-03 |
| **DPG-02** | Dependency licence audit | ✅ | `dpg/sprint0-licensing` | | ✅ T-02 (16) | 153 pkgs, 4 sets, 0 unknown/non-OSI. Rasa closed. Scan **scheduled** (Q-06). Found 2 transitive LGPL + our own npm manifest declaring `UNLICENSED` |
| **DPG-03** | IP ownership (ADB OGC) | ⏸ | non-code | | n/a | **BLOCKED — external, and it is the only Sprint 0 item nobody here can move.** Owner is writing to the **DPG consultant** (Q-01, in flight). ⚠ That is **not** the ADB OGC channel an IP determination requires. **Date raised with consultant: ______** · **Date sent to ADB OGC: ______** — fill both in; this is a clock and it should be visible |
| **DPG-04** | Privacy assessment + data-flow | ✅ | `dpg/sprint0-licensing` | | n/a | `docs/dpg/privacy-assessment.md` — 13 legs verified against code, Individual Privacy Act 2018 assessment, **17 findings** incl. 3 new high/medium ones nobody had found. Honesty marker per Q-07. Q-03 marked **moot while T2 parked** |
| **DPG-05** | Project hygiene (indicator 8) | ✅ | `dpg/sprint0-licensing` | | n/a | `SECURITY.md` (private channel, SEAH-aware in/out-of-scope, response targets), `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` (Covenant 2.1 + grievance-confidentiality clause), `.github/ISSUE_TEMPLATE/` (`blank_issues_enabled: false`, security **redirect**) + PR template. Governance/versioning **deferred** per the spec's scope gate — logged |
| **DPG-06** | Root `README.md` vs reality | ✅ | `dpg/sprint0-licensing` | | n/a | Rewritten from the compose files. Rasa service + Action Server rows gone; **13 services, verified against `docker compose config --services`**; folder tree matched to disk; branch corrected; `rasa_chatbot/` removed from CLAUDE.md too. **Found a 5th fiction the spec missed** — see D-10 |
| **DPG-10** | **Test net (must land first)** | ✅ | `dpg/sprint1-llm-agnostic` | | ✅ T-10 (65) | `tests/backend/test_llm_services.py` (44) + `tests/ticketing/test_llm_client.py` (21), green on **unmodified** code. 6 mutations checked. Found **two** latent `UnboundLocalError`s, not one — D-28, D-29 |
| **DPG-11** | `backend/services/llm_client.py` | ✅ | `dpg/sprint1-llm-agnostic` | | ✅ T-11-a, T-11-b, T-11-d (backend), T-14-b | Six call sites resolve model + endpoint + timeout from DPG-17. Import-time client, shadow client, dead guard and the EC2 `load_dotenv` all gone. `OpenAI(` appears once in `backend/`. Callers re-grepped: 5 Celery task imports, all function-level, none touching module internals |
| **DPG-12** | Ticketing client via settings | ✅ | `dpg/sprint1-llm-agnostic` | | ✅ T-12-a, T-12-b, T-17-c, T-17-d | All **five** sites moved, including the two the first inventory missed and the two `grep "gpt-"` could not see. `TicketingSettings.openai_api_key` removed. OpenAPI description no longer advertises a model. ⚠ Found a sixth disagreement: `11_llm_pipeline_policy.md` said note translation used `gpt-4o-mini`; the code called `gpt-4` |
| **DPG-13** | `json_object` → `json_schema` | ✅ | `dpg/sprint1-llm-agnostic` | | ✅ T-13-a…e | 7 sites, 7 Pydantic models, ladder + per-call logging. ⚠ **The spec's premise was wrong and it mattered**: capability is per **(endpoint, model)**, not per endpoint — `gpt-3.5-turbo` 400s on `json_schema` and `gpt-4` 400s on **both**. Measured, one request per cell, before the defaults were written |
| **DPG-14** | Three latent defects | ✅ | `dpg/sprint1-llm-agnostic` | | ✅ T-14-a, T-14-c | **14.1** classification is live on `gpt-5-nano`, **no truncation** (`finish_reason: stop` ×3) — but latency 14–20.5 s against a 20 s poll deadline (**D-30**). **14.2** shadow client + dead guard → lands in DPG-11, where the factory removes them. **14.3** ⚠ **confirmed and fixed**: the SDK takes `language`, not `language_code`; every transcription raised `TypeError`. **D-28** fixed too |
| **DPG-15** | Degraded mode audit + probe | ✅ | `dpg/sprint1-llm-agnostic` | | ✅ T-15-a…c (+3) | Nine call sites audited **by driving them against a dead port with the real DB**, not by reading. **Two active faults found and fixed** — a failed classification stored as `LLM_generated` (**D-32**, live path), and a stored phone number overwritten with `""` (**D-33** — ⚠ real, reproduced, but on a path with **no production caller**: see **D-37**). `/health/llm` added, host-only, out of every container healthcheck. Three absences logged (**D-34**, **D-35**, **D-36**) |
| **DPG-16** | Env plumbing | ✅ | `dpg/sprint1-llm-agnostic` | | ✅ T-16-a (+3) | `.env.example` block generated from `declared_env_vars()` (23 vars) and pinned **both ways**; `.env.open` / `.env.openai` committed, secret-free, verified not gitignored; **all five LLM-touching containers rebuilt and exec-checked**, plus an override run proving the plumbing rather than the defaults; `docs/dpg/open-model-configuration.md` written |
| **DPG-17** | **One config file** (`backend/config/llm_config.py`) | ✅ | `dpg/sprint1-llm-agnostic` | | ✅ T-17-a (30), T-17-b (2) | 8 task keys, 18 env vars, 2 endpoints, aliases warned once. Defaults reproduce today's models **and today's timeouts** (SDK default / 120 s classify / 30 s ticketing). `pydantic-settings` moved to `requirements.txt` and **verified in a rebuilt `celery_llm`**. T-17-c/d land with DPG-11/12 — they need the factories |
| **DPG-18** | **One entry point + one model** (owner, 2nd wave) | ✅ | `dpg/sprint1-llm-agnostic` | | ✅ T-18-a…f | `call_llm()` per surface, shaping shared, `ModelProfile` deciding temperature / cap name / reasoning budget per model. **Two models: whisper + `gpt-5-nano`.** SEAH detection moved off `gpt-3.5-turbo` and **verified against the live provider** — 4/4 cases correct |
| **DPG-19** | **Meaningful input + bounded error text** (owner, 2nd wave) | ✅ | `dpg/sprint1-llm-agnostic` | | ✅ T-19-a…e (+2) | `MIN_CLASSIFY_CHARS=25` on stripped length; below it **no call is made**; an empty answer above it stays **not a failure** and is logged with the length. **D-29 closed** — the message is `grievance_id` + 3 words. ⚠ **D-42**: one of my mutations proved a test decorative |
| **DPG-19b** | **Declare the parked paths + the reachability pin** (owner, 2nd wave) | ✅ | `dpg/sprint1-llm-agnostic` | | ✅ T-19b-a…b (+1) | `PARKED_TASKS` with a reason per path; docstring labels where a reader meets them; **the pin: enqueued in production OR declared parked, nothing else** (`test_tasks.py` does not count). Four documents corrected. Nothing deleted, no test deleted |
| **DPG-15b** | **Wait 30 s, fail terminally, say something** (owner, 2nd wave) | ✅ | `dpg/sprint1-llm-agnostic` | | ✅ T-15b-a…d (+2) | Wait **lowered** 20→30 s from the registry; first attempt 30 s / retries 120 s; `LLM_failed` reachable — **verified against the DB, poll now returns in 0.19 s instead of running the budget**; three messages in the review step. **D-30 and D-34 closed**; **D-45** found |
| **DPG-20** | Benchmark set | ✅ | `dpg/sprint2-open-models` | | ✅ T-20 (16) | **105 items, 23 of 24 categories, 4 scripts, CC0-1.0.** General slice committed with a provenance README; SEAH slice deliberately absent (§0.2) and the *confusable negatives* committed in its place, which is the false-alarm half of the SEAH measurement. 8 gate fixtures pin `MIN_CLASSIFY_CHARS` in both directions. ⚠ Found **D-49** (four categories' priority flag silently cleared) and corrected two documentation claims about the length gate. ⏸ No audio — WER is deferred with a followup |
| **DPG-21** | Provider setup + smoke | ✅ | `dpg/sprint2-open-models` | | ✅ T-21 (16) | **Built and committed; the matrix is partial and says so.** `scripts/ops/llm_smoke.py` probes six capabilities per model, verifies each licence from the model card at probe time, and prints a `ModelProfile` to paste. Shortlist of 7 + 6 excluded-with-reasons lives in `llm_candidates.json` as **data**, so a loosened Q-04-02 is an edit not a redesign. **Full matrix measured, second pass**: 6 of 7 candidates complete, every licence verified from its model card. ⭐ Three findings the probe paid for: `json_schema` is *honoured* by gpt-oss (proved, not assumed) while **`Qwen3.5-9B` accepts it and silently ignores it**; gpt-oss and Qwen both **reason** while `DEFAULT_PROFILE` assumed an overhead of 0 — the D-40 trap sitting under the repo's own open config; and latency spans **30×** (0.7 s to 23.8 s on one sentence), which against a 30 s budget is a selection input, not a footnote. ⚠ Found **D-54** (Apertus refuses a grievance) and corrected **D-50**'s own diagnosis (a rate limit, not depletion) |
| **DPG-22** | ASR evaluation | ⏸ | `dpg/sprint2-open-models` | | n/a | **Everything measurable without audio is done; WER is not reachable and says so.** Licences **verified from the model cards at probe time**: whisper-large-v3 apache-2.0 ✅, **whisper-large-v3-turbo MIT** (⚠ the spec's table says Apache-2.0 — it is wrong, though both pass), `indicwav2vec_v1_nepali` MIT ✅, `mms-1b-all` **cc-by-nc-4.0 ❌ cannot ship** as predicted, and Omnilingual ASR **could not be resolved publicly** — the spec's Apache-2.0 claim is unverified and must not be repeated. ⚠ Found **D-53**: `.env.open`'s ASR endpoint **404s**. ⏸ WER blocked twice — no audio, no endpoint — both logged. SLR54 fine-tune opportunity logged as the acceptance requires |
| **DPG-23** | Text-model evaluation | ⏸ | `dpg/sprint2-open-models` | | ✅ T-23 (22) | **Harness built, priced, and the CLOSED baseline measured in full; the open column is blocked by D-50.** `scripts/ops/llm_benchmark.py` runs the product's own call path — not a reimplementation — proves the credential before scoring anything (§6.1), scores **set-level**, scores detection **recall-first with a confusion matrix**, meters real token usage, and **refuses** a `--seah-set` inside the repo. Baseline on `gpt-5-nano`, 105 items: **F1 0.771** (P 0.740 / R 0.805), exact-set 0.676, SEAH false-alarm **6.7%**, p95 **24.6 s** (passes 30 s) / p99 **40.6 s** (does not), **93.8% of completion tokens are reasoning**. ⚠ Found **D-51** and **D-52**, both live defects. **Open column started**: `gpt-oss-20b` completed **105/105 detection** (false alarm **0.000** vs nano's 0.067, detection p50 **0.35 s** vs 3.73 s) and **2/105 classification** — the ~20,700-char prompt trips the provider's token limit, which is §3.4's finding about **our prompt**, not the model. ⚠⚠ **The 0.000 must not be read as a win**: a detector that flags nothing has a perfect false-alarm rate and catches nothing, and the committed set has no positives to tell those apart. ⭐ **Both** models invent the *same* `Road Hazard - Dust` — two vendors, two architectures, one fabricated label — which makes D-51 largely a **taxonomy** finding. Published at `docs/dpg/model-benchmarks.md` with every unmeasured cell marked |
| **DPG-24** | CI platform-independence job | ⏸ | `dpg/sprint2-open-models` | | ✅ T-24 (13) | **Fifth job added and pinned; it has never run, because D-50.** `live_llm` registered, deselected by `backend-tests`, selected here, **and a test asserts it is not deselected in both places** — the composite property, because that is how T3-08's quarantine came back. Live suites exercise **both** surfaces. Skips (never fails) without secrets. Q-17's never-required decision, the synthetic-data-only routing split **and its invariant**, the unset token cap and the unresolved post-demo payer are all in the job header, and each is pinned by a test. Badge in `README.md`. ⚠ `MEASURED SPEND` and `WHO PAYS` are `⚠ not yet measured` / `⚠ UNRESOLVED` — stated, not blank |
| **DPG-25** | T2 vLLM — documented + costed | ✅ | `dpg/sprint2-open-models` | | n/a | `docs/dpg/vllm-deployment.md` — flags, networking, snapshot, sizing **from measured tokens** rather than the source narrative's generic chat estimate. ⭐ **The costing changed the ticket's conclusion:** the T1/T2 crossover is **40,000–780,000 grievances/month** across every price assumption, against **tens/month** for two districts and **7,700/month** for all 77. T1 wins by 3–4 orders of magnitude and **volume growth does not close it** — one request per grievance, not per turn, so a dedicated GPU is idle almost always. **So T2 is a data-sovereignty decision with a price, not a cost decision**, and presenting it as a break-even would have misled. `13_security.md` §8.1 carries the posture, all `⚠ not deployed`; Q-03/Q-05 parked with their analysis; the e2e test deferred and logged |
| **DPG-30** | **Egress inventory (first)** | ✅ | `dpg/sprint3-pii` | | n/a | `docs/dpg/pii-egress-inventory.md` — **12 egress paths, ranked by likelihood rather than by alarm**, every citation opened. **Four discrepancies against DPG-04's diagram (R1–R4)**, recorded in both documents; the pattern in all four is *right about topology, optimistic about content*. ⭐ Two findings the ticket paid for: **D-62** — the OTP is logged at **INFO** beside the phone it authenticates, and one of the three sites has a truncation that can never fire; **D-63** — *"Redis has no persistence volume"* is an **inference stated as fact in four documents**, including a containment property in the incident runbook. ⚠ **One acceptance item unmet**: the in-container check could not run (no Docker), which is exactly the item D-63 needed |
| **DPG-31** | Deterministic PII layer | ⬜ | `dpg/sprint3-pii` | | | Ships without DPG-32 |
| **DPG-32** | NER layer | ⏸ | — | | | **MOVED OUT of Sprint 3** (Q-12c) → standalone anonymiser-service initiative. ⚠ **This row used to say "leaves person names unredacted" — that is D-08's error and it is false.** §31.2b redacts names deterministically (title triggers, thar gazetteer, self-identification); DPG-32 **raises recall**, it is not the whole control. Disclose the *measured residual*, not an absence |
| **DPG-33** | Model-call boundary | ⬜ | `dpg/sprint3-pii` | | | |
| **DPG-34** | Log / Celery / backup boundary | ⬜ | `dpg/sprint3-pii` | | | The leak that actually happens |
| **DPG-35** | Recall measurement | ◐ | `dpg/sprint3-pii` | | | **Split** (Q-12c): deterministic recall stays; PERSON/third-party travel with DPG-32 |
| **DPG-36** | Reconcile the live spec | ⬜ | `dpg/sprint3-pii` | | | |

> **Sprint 4 — queued, added 2026-08-25.** Six tickets. ⚠ It starts from a correction: the SEAH detector
> **already answers with a confidence** (`level: high|medium|low`, `LLM_services.py:633`) and the product
> discards it three times — never persisted (`public.grievances` has no such column, and
> `sensitive.py:103` reads it anyway), never on the ticket (`is_seah` is a boolean), so the SEAH queue has
> **no ordering signal** in a queue that by design holds deliberate false positives. `grievance_categories_alternative`
> is likewise already the top-k list. ⛔ **DPG-44 is written and blocked** — SEAH recall is not measurable
> here, so a detection-prompt change can only be scored on the error the design accepts on purpose (Q-25).
| **DPG-40** | Keep the confidence we already ask for | 🔵 queued | — | | n/a | Persist `level` (public stream), carry it to the ticket, order the queue by it. Ungated — no prompt change. ⚠ `is_seah` stays boolean and stays the access control; a grade that gates access is the trap this sprint is written to avoid. ✅ **Q-26 decided 2026-08-25: order only** — the grade sorts the queue and never appears on the officer's screen |
| **DPG-41** | Calibration before consumption | 🔵 queued | — | | n/a | Does the self-reported confidence separate right from wrong? ⚠ Detection calibration has **n=7 false alarms and zero positives** — it can show that low-confidence flags are more often false alarms and **nothing at all** about recall. *"Not usable for ranking"* is an acceptable verdict |
| **DPG-42** | Classification prompt: confidence per category | 🔵 queued | — | | n/a | A/B against the 08-21 baseline (P 0.773 / R 0.752 / exact 0.686). Report **precision@1, recall@3 and exact-set**, plus token and latency cost — the 30 s interactive budget is pass/fail. One variable per run |
| **DPG-43** | The promotion rule + complainant copy | 🔵 queued | — | | n/a | When an alternate becomes primary. ⚠ Must not fork D-51's narrower promotion (empty-result repair) into a second path. ⚠ `high_priority` is computed from the stored categories, so promotion can change priority — measure how often. Complainant-facing copy is **Q-27**, rewritten 2026-08-25 after the first version proved unreadable; **option A (no change to the complainant's screen) is recommended**, on the principle Q-26 settled |
| **DPG-44** | ⛔ SEAH detection prompt | ⛔ **blocked** | — | | n/a | **Do not start.** Blocked on **Q-25** — a held-out set with positives; 🔶 **requested 2026-08-25, not delivered**. Written so nobody starts it by accident: it is the most tempting ticket in the sprint and the benchmark will reward the wrong change. Paired recall + false-alarm reporting may be *built*; the prompt may not be *touched* |
| **DPG-45** | Publish; keep the instrument honest | 🔵 queued | — | | n/a | `model-benchmarks.md` §2/§7 + the live specs. ⚠ Re-check `InventionMeter` reads: a prompt edit can move the log wording it depends on, and 0/105 would mean the meter broke, not the model improved. **`00_compliance_status.md` untouched by design** — model quality is Q-04, not a compliance gate |
| **DPG-46** | The officer correction rate — the classifier's real-world score | 🔵 queued | — | | n/a | Added 2026-08-25 out of **Q-27**. Route the officer classification patch through `update_grievance_with_tracking` so old→new lands in `grievance_status_history.field_changes` (D-57), then report **how often officers change the AI's categories**, split *changed / added to / confirmed*. ⭐ Real Nepali grievances, no benchmark authoring, no data leaves anywhere. ⚠ Aggregate rate only — a corpus built from real grievances is a separate privacy decision |

---

## Question status

Answers verbatim + reasoning: [`DECISIONS.md`](DECISIONS.md). Still live: [`QUESTIONS.md`](QUESTIONS.md).
**All 20 answered 2026-08-17**; Q-19 was raised by the answers and was **answered 2026-08-20**.

| Q | Subject | Owns | Answer | Recorded in |
|---|---|---|---|---|
| Q-18 | Shared LLM config module | DPG-17 | ✅ `backend/config/llm_config.py`, pydantic-settings, dep → `requirements.txt` | [`02` DPG-17](02-llm-agnostic-spec.md#dpg-17) |
| Q-03 | T2 jurisdiction | DPG-25 | ✅ **Moot — T2 parked** | [`03` DPG-25](03-open-models-spec.md#dpg-25), [`00` §2](00-dpg-context-and-decisions.md#2-deployment-ladder--the-code-is-identical-at-every-tier), [`01` DPG-04](01-licensing-and-governance-spec.md#dpg-04) |
| Q-05 | T2 operator + payer | DPG-25 | ✅ **T2 parked** — no run-cost owner | [`03` DPG-25](03-open-models-spec.md#dpg-25), [`00` §5](00-dpg-context-and-decisions.md) |
| Q-04 | Which config production runs | DPG-23 | ✅ **The open one**, on cost; closed as fallback | [`03` DPG-23](03-open-models-spec.md#dpg-23), [`00` §4](00-dpg-context-and-decisions.md#4-dpg-evidence-pack) |
| Q-10 | Open config as repo default | DPG-17 | ✅ Yes, flipped in Sprint 2 | [`02` DPG-17](02-llm-agnostic-spec.md#dpg-17) §Correction |
| Q-11 | One model or per-task | DPG-17 | ✅ One text model first, then downsize; HF pricing is the driver | [`02` DPG-14](02-llm-agnostic-spec.md#dpg-14) (truncation check), [`03` DPG-25](03-open-models-spec.md#dpg-25) |
| Q-12b | Redact when | DPG-31, DPG-36 | ✅ **At transmission** — and the restore mapping is PII | [`04` DPG-31](04-pii-redaction-spec.md#dpg-31), [`04` DPG-36](04-pii-redaction-spec.md#dpg-36) |
| Q-12c | Where the ML dependency lives | DPG-32 | ✅ Dedicated service, **own initiative** — DPG-32/35 leave Sprint 3 | [`04` DPG-32](04-pii-redaction-spec.md#dpg-32), [`04` DPG-35](04-pii-redaction-spec.md#dpg-35) |
| Q-12 | Nepali NER licence | DPG-32 | ✅ Email the author; fine-tune as fallback | [`04` DPG-32](04-pii-redaction-spec.md#dpg-32) |
| Q-13 | `gpt-5-nano` · voice live? | DPG-14 | ✅ Deliberate cost choice · **voice not live** | [`02` DPG-14](02-llm-agnostic-spec.md#dpg-14), [`03` DPG-22](03-open-models-spec.md#dpg-22) |
| Q-14 | SEAH fail open/closed | DPG-15 | ✅ **Fail-open stays** — deterministic pre-filter verified in code | [`02` DPG-15](02-llm-agnostic-spec.md#dpg-15) |
| Q-15 | Benchmark provenance | DPG-20 | ✅ Synthetic phase 1; hybrid later | [`03` DPG-20](03-open-models-spec.md#dpg-20) |
| Q-16 | Benchmark size + labellers | DPG-20 | ✅ **No labeller budget** — target is aspirational | [`03` DPG-20](03-open-models-spec.md#dpg-20) |
| Q-06 | Licence scan scheduled? | DPG-02 | ✅ Scheduled in `ops/security.py` | [`01` DPG-02](01-licensing-and-governance-spec.md#dpg-02) |
| Q-07 | Privacy-assessment author | DPG-04 | ✅ Agent drafts, consultant advises — **no legal review, must be disclosed** | [`01` DPG-04](01-licensing-and-governance-spec.md#dpg-04) |
| Q-08 | Assess against what | DPG-04 | ✅ Nepal Individual Privacy Act 2018; no existing agreement | [`01` DPG-04](01-licensing-and-governance-spec.md#dpg-04) |
| Q-09 | Translation: specialist? | DPG-23 | ✅ LLM first; dedicated service only if better | [`03` DPG-23](03-open-models-spec.md#dpg-23) |
| Q-17 | Provider outage blocks merges? | DPG-24 | ✅ Every commit, never a required check | [`03` DPG-24](03-open-models-spec.md#dpg-24) |
| Q-01 | Who opens the OGC request | DPG-03 | 🔶 **In flight** — writing to the consultant. ⚠ Not the same channel as ADB OGC. **Sprint 0 closed 2026-08-18 with this still open** — `NOTICE` names no holder and says so | [`01` DPG-03](01-licensing-and-governance-spec.md#dpg-03) · **date raised with consultant: ______** · **date sent to ADB OGC: ______** |
| Q-02 | Apache-2.0 over MIT | DPG-01 | 🔴 **OPEN** — delegated to the consultant. **DPG-01 gated** | [`01` DPG-01](01-licensing-and-governance-spec.md#dpg-01) |
| Q-19 | **LLM budget** (new) | DPG-22/23/24 | ✅ **ANSWERED 2026-08-20** — a few hundred USD, owner-funded, covering benchmarking **and** the pilot's own inference in 2 districts; then a costed proposal to the Nepal Government. Sprint 2 unblocked; the constraint is now a ceiling shared with production, not an absence | [`QUESTIONS.md`](QUESTIONS.md#q-19) |

## ⏸ Sprint 0's two external blockers — named, with their clocks

> Required by [DPG-01](01-licensing-and-governance-spec.md#dpg-01)'s acceptance criteria: *"If either
> external answer is still outstanding when the sprint closes, **the blocker is named in
> `PROGRESS.md` with the date it was raised** — not left as an unticked box."* Sprint 0 closed
> 2026-08-18 with both still outstanding.

| Blocker | Question | Who can answer it | Status | Date raised | What is blocked |
|---|---|---|---|---|---|
| **Copyright holder** | Q-01 / **DPG-03** | **ADB's Office of the General Counsel** — in writing. ⚠ **The DPG consultant cannot issue an IP determination**; if the consultant is the route *to* OGC, record it that way | 🔶 Owner is writing to the consultant | **______** *(consultant)* · **______** *(ADB OGC)* | `NOTICE`'s copyright line — currently an explicit `⚠ PENDING`, deliberately not guessed. `docs/dpg/ip-ownership.md` cannot be written. Evidence-pack row 3 has no file. Also blocks naming a **data controller** (privacy assessment F-11) |
| **Which licence** | Q-02 / **DPG-01** | The **DPG consultant** | 🔴 Open — Apache-2.0 landed **provisionally** on the owner's decision to proceed | 2026-08-17 | Nothing, operationally. If the answer changes the licence, `LICENSE`, `NOTICE` and **585 SPDX headers** change together via `scripts/ops/add_spdx_headers.py` |

**Fill in the dates.** They are the only thing that makes an external dependency visible as a clock
rather than an excuse — and DPG-03 is the longest lead time on the whole programme.

**What Sprint 0 did instead of waiting:** landed everything that does not depend on the answers.
Indicator 2 fails outright with *no* licence, so a provisional Apache-2.0 with an honest
pending-holder disclosure in `NOTICE` is strictly better than an empty repository root — and it is
reversible before publication. That reasoning is recorded in `NOTICE` itself, not just here.

---

## Sprint 2 acceptance — checked against reality, 2026-08-20

Each line is the spec's own wording, with the verdict beside it. Two are qualified and say why.

| Acceptance criterion | Verdict |
|---|---|
| Labelled benchmark set built and committed (test data, **not** production PII), with provenance | ✅ 105 items, CC0-1.0, provenance README. "No production PII" is **verified by a test against the live DB**, not assumed |
| Benchmark table completed and published in `docs/dpg/model-benchmarks.md` | ✅ Published. ⚠ **Complete is the wrong word and the document says so**: the closed column is full, the open column has detection only, and every unmeasured cell names its blocker |
| **The full test suite green against the open configuration** | ✅ **4 passed, 1 xfailed, exit 0** — both surfaces, real calls, `openai/gpt-oss-20b`. The xfail is D-53 (the open ASR endpoint 404s) and is `strict=True`, so it reddens the day someone fixes it |
| CI platform-independence job passing; badge in the repo `README.md` | 🟡 **Job written and pinned by 14 tests; it has never run.** Needs `HF_TOKEN` + `DPG_MODEL_*` set as repo secrets/variables — the one remaining step nobody in this repository can take. Badge is in place |
| `docs/dpg/open-model-configuration.md` written — the reviewer-facing document | ✅ With the measured capability matrix, the excluded-on-licence list, and what is **not** yet true |
| T2 vLLM **documented and costed** — `⚠ not deployed`, and the deferred e2e test logged | ✅ And the costing changed the conclusion — see the crossover |
| The indicator-4 answer now true sentence by sentence | ⚠ **No — and that is the finding.** Checked clause by clause in `00-dpg-context-and-decisions.md`: **four of seven do not hold**, chiefly that the repository default is still proprietary because no open model has been *chosen*. A version that **is** true today is drafted beside it |
| Every deferral logged in `followups/` + `TODO.md`, same commit | ✅ Nine follow-ups, nine `TODO.md` rows |

### ⚠ What a reader should not conclude from the ticks above

**Sprint 2 did not choose a model, and choosing one was its point.** The mechanism, the data, the
harness, the scoring rules and the CI gate are all built, tested and reproducible. What is missing is
**SEAH recall** — and until that exists for both candidates, selecting on the numbers that *do* exist
would mean picking a detector that flags nothing because it scored a perfect false-alarm rate.

---

## Deviations

> Every departure from the specs goes here, in the commit that creates it — including adjacent bugs found
> and **not** fixed (per the sprint conventions: log, don't fix). Per
> [`docs/sprints/README.md`](../README.md), a deferral also needs a `followups/<slug>.md` doc and a
> `TODO.md` pointer row **in the same commit**. A deferral logged only here is half-logged.

**Status legend:** ✅ Closed — nothing further to do · 🔵 Open — real work, tracked in `followups/` +
`TODO.md` · ⏸ Blocked — waiting on an external answer.

> **Merged 2026-08-18 (D-23).** Deviations used to live in *two* tables sharing one ID namespace —
> this one, and the pre-sprint findings table below, which had quietly accumulated D-01…D-11. That is
> how the D-08…D-11 collision happened, and reusing numbers that belonged to **corrections** is how
> D-08's error came back as D-22. One table now. The table below holds **only** `P-##` planning
> findings; a `D-##` never goes there again.

| # | Status | Ticket | What changed vs the spec | Why / where | Followup / TODO row |
|---|---|---|---|---|---|
| D-15 | ⏸ **Blocked** — Q-08-01 | DPG-05 | **Governance model and release/versioning policy not written** — steps 1–4 of the ticket shipped, step 5's "public roadmap" is a link to this sprint plan rather than a second document | Taken on the spec's own scope gate. The four shipped files describe **what is already true**; a governance model and a supported-version window would describe **commitments nobody has agreed to**, on a project whose IP ownership is formally open with ADB OGC. Whether the DPGA requires either is Q-08-01, still unanswered. Indicator 8 already reads 🟢 without them | ✅ [`followups/governance-and-versioning-policy.md`](followups/governance-and-versioning-policy.md) + `TODO.md` 🔵 TECH DEBT row |
| D-16 | ✅ Closed | DPG-02 | **D-01's deferral was half-logged.** The CI-never-runs-these-tests finding was recorded here on 2026-08-18 but had **no `followups/` doc and no `TODO.md` row** — and `followups/` did not exist. Per [`sprints/README.md`](../README.md) that makes it a defect, not a deferral. Closed now, and the measured count corrected: **27 files, not 21** — 21 directly in `tests/` plus **6 in `tests/shared/`**, which the original note missed | The rule exists so debt stays visible; a deferral logged in one place decays | ✅ [`followups/ci-untested-root-test-files.md`](followups/ci-untested-root-test-files.md) + `TODO.md` row |
| D-17 | ✅ Closed | DPG-06 | ⚠ **The spec's own service count was wrong, and it missed a fifth fiction.** Spec step 1 says "Eleven services; name them as they are" — `docker compose config --services` returns **13**, plus 2 profile-gated (`db_init` under `init`, `keycloak` under `auth`) = **15 defined**. And the spec's four-row table of README falsehoods missed the **Environments table**: all three URLs (`chatbot.facets-ai.com`, `grm.facets-ai.com`, `grm.stage.facets-ai.com`) appear **nowhere else in the repository** and are not a `server_name` in any nginx config. The real hosts are `nepal-gms-chatbot.facets-ai.com` (AWS staging) and `grm-chatbot.dor.gov.np` (DOR production). **A DPG reviewer clicking a dead production URL on the front page is the same failure mode as the Rasa row**, one click earlier | Wrote the README from `docker compose config --services` and `deployment/nginx/*.conf`, per the spec's own instruction to generate from the compose files rather than from memory — which is exactly what caught it | — (fixed in place; no deferral) |
| D-18 | ✅ Closed | DPG-04 | **Corrected a false comment in the privacy-critical path rather than only logging it.** `backend/api/routers/grievance.py` asserted that `GET /api/grievance/{id}` **never decrypts** and called it "the T3-04 defect". T3-04 landed: `grievance_manager.py:190` calls `_decrypt_sensitive_data` on the JOIN result. The comment described the pre-fix state and was never updated | Sprint convention is *log, don't fix* for adjacent findings — **overridden deliberately here.** It is a comment, not behaviour (zero risk), and it sits in the file a privacy reviewer opens first, about the control §3.3 of the new assessment calls the platform's strongest. Writing a document that cites that boundary while leaving a comment asserting the opposite would be self-defeating. The correction is dated in place and states the caveat that survives (encryption is conditional on the key) | — (fixed in place) |
| D-19 | ✅ **Fixed 2026-08-19** | DPG-04 | ⚠ **Three privacy findings no spec had, found by reading the code — logged, not fixed.** **(F-2)** `_encrypt_field` returns the **plaintext value unchanged** when `DB_ENCRYPTION_KEY` is unset *and* when the pgcrypto call raises — the error is logged and the write proceeds, so a degraded deployment silently stores complainant PII in the clear (`base_manager.py:266-297`). **(F-3)** `*_hash` search tokens are **unsalted SHA-256** of phone/email/name/address; Nepal's mobile space is enumerable, so the phone hash is reversible and these columns are personal data, not pseudonyms (`base_manager.py:559-573`). **(F-4)** `backup_db.sh` writes an **unencrypted** `pg_dump` + uploads tar by default — encryption only if `BACKUP_GPG_RECIPIENT`/`BACKUP_PASSPHRASE` is set; the narrative, officer notes, voice notes and photos are in the clear (`backup_db.sh:45-60`) | Each is a code change with a blast radius (a key-presence assertion could refuse to boot; salting the hashes is a migration + a search-path rewrite; backup encryption needs key custody decided). None belongs in a documentation ticket | ✅ **Now tracked** (2026-08-18): [`followups/storage-layer-privacy-defects.md`](followups/storage-layer-privacy-defects.md) — one doc, because all three share a root cause (the storage layer was never privacy-reviewed; every existing privacy spec describes the architecture, none describes what happens when a write fails) — plus a 🔴 `TODO.md` row. **Deadline is go-live, not submission** (assessment §0.5) · ✅ **All three fixed in one pass, before any genuine grievance was processed** — which is the whole point: on 2026-08-19 they were ordinary engineering; a week after go-live each would have been a breach assessment. **F-2** fails **closed** (`EncryptionUnavailableError` abandons the write; the keyless dev mode survives but warns once per process instead of never). **F-3** is `HMAC-SHA256(pepper, value)` with `SEARCH_TOKEN_PEPPER` falling back to the encryption key, plus `scripts/database/rehash_search_tokens.py` to re-derive existing tokens — ⚠ **that backfill must run**, or phone lookup breaks silently. **F-4** discards an unencryptable dump *and* the uploads tar unless `BACKUP_ALLOW_UNENCRYPTED=1`, and encrypts the uploads tar at all, which it never did. 15 tests in `tests/backend/test_storage_privacy.py`; the six F-4 tests **run the real script** against a stubbed `docker` rather than grepping it, and **8 of 8 mutations went red** — including a narrowed prune glob, which would have broken retention while every other test stayed green. The blast radius the deferral feared (§D-19: "a key-presence assertion could refuse to boot") was avoided by *not* making the keyless path fatal — see the followup's "deliberately not done" |
| D-20 | ✅ Closed | DPG-04 | **`NOTICE` corrected** — it claimed the generated dependency inventory "will be published at docs/dpg/dependency-licenses.md" and that the hand-written one was authoritative until then. DPG-02 published it the day before | A licence file making a false statement about the licence evidence is the worst possible place for one | — (fixed in place) |
| D-21 | ✅ Closed 2026-08-18 | DPG-04 | ⚠ **`docs/deployment/09_privacy.md` §Implementation boundaries still forbids cross-schema reads from `ticketing.*` into `public.*`.** That rule was **deliberately retired** by T3-07 and replaced with an enumerated, test-pinned table in `CLAUDE.md`. The live privacy spec and the locked architecture now disagree | Logged, not fixed — it is a live domain spec owned by the privacy/ticketing docs, and amending it correctly means moving T3-07's *reason* with the rule (engineering rule 7), which is more than a one-line edit | ✅ **Fixed 2026-08-18** — `09_privacy.md` §Implementation boundaries now states the as-built contract (enumerated closed set, no FKs, no PII columns, HTTP-only state changes) **with T3-07's reason carried alongside it**, per engineering rule 7. That is why this was not a one-line deletion. F-15 in the assessment updated |
| D-22 | ✅ Closed | DPG-04 | ⚠ **I reintroduced an error this sprint had already found and corrected.** The first draft of `privacy-assessment.md` said person names go unredacted until the ML layer lands, and that deterministic redaction covers only numeric identifiers and email. **That is exactly D-08**, which was raised, corrected across `04-pii-redaction-spec.md` and both DPG docs, and owner-flagged — before I wrote the assessment. §31.2b ships **three** person-name recognisers with no ML dependency: honorific/role-title triggers, a Nepali thar gazetteer, and self-identification patterns; in a road-works GRM those catch the *named official*, the sharpest exposure. **Owner-flagged again.** Corrected in four places in the assessment (§3.7 point 3, §4.2 table, §4.3 point 4, F-9), in the DPG-04 acceptance text, and in the **DPG-32 status-board row above**, which still carried the original wording. ⚠ **And D-08's own correction was incomplete:** sweeping for the phrase found it still live in **[`README.md`](README.md) §Decisions** and **[`00-dpg-context-and-decisions.md`](00-dpg-context-and-decisions.md) open-question 10** — two documents D-08 never touched. Both corrected here. A correction that fixes the documents you remember writing, rather than the ones a `grep` finds, is how the same error comes back | `privacy-assessment.md`, `01-…-spec.md`, this file, `README.md`, `00-dpg-context-and-decisions.md` | ✅ corrected · **root cause below** |
| D-23 | ✅ Closed | — | **Deviation IDs collided.** The block I added on 2026-08-18 was numbered D-08…D-14 while D-08…D-11 were already taken by the corrections logged in the pre-sprint findings table below. Renumbered to **D-15…D-21**, and every cross-reference updated. **This is not cosmetic — it is the mechanism of D-22:** the numbers I reused belong to *corrections*, and if I had read the rows I was about to overwrite I would have read D-08 and not repeated it. The two tables in this file are one namespace and should be merged | this file | ✅ renumbered |
| D-24 | ✅ Closed 2026-08-18 | DPG-01 | ⚠ **The sprint wired tests into CI on a branch pattern CI does not watch.** DPG-01 added `tests/repo` to `ci.yml`'s pytest step — the SPDX walker and the licence-classifier pins, whose entire purpose is to fail when a commit breaks a compliance claim. But `ci.yml`'s push triggers were `main`, `integration/**`, `dev/**`, and this sprint works off `dpg/sprint0-licensing`. **So the pins ran nowhere but my own machine for nine commits**, and every "green in CI" line in this tracker was prospective. Found by pushing the branch and watching nothing happen | `.github/workflows/ci.yml:15-21` · **Same defect as D-01** — a gate that does not run is decoration. The general lesson: wiring a test into CI is **two** edits, the step and the trigger | Fixed by adding `dpg/**`, with the reason in a comment beside it |
| D-25 | 🔵 **Open — config** | DPG-02 | ⚠ **The ops container writes nothing if `OPS_DB_PASSWORD` is unset — and that disables every ops finding, not just the licence scan.** `ops/config.py:104-111` builds the URL as `user = ops_db_user or postgres_user`, but `ops_db_user` **defaults to the non-empty string `"ops_app"`**, so that fallback can never fire — while the password *does* fall back, to the admin's. Result: login as `ops_app` with the admin password. `ops001_init.py:54-64` meanwhile creates the role **with no password at all** when the env var is unset, so nothing can authenticate it. Found by running `licence_scan()` for real: `FATAL: password authentication failed for user "ops_app"` | **The failure is silent.** `licence_scan()` returns normally, logs `Failed to record check`, and tries to alert — which also fails when `HEALTH_ALERT_EMAIL` is unset. A deployment missing one env var records nothing and says nothing: the same *manufactured confidence* as D-07's classifier bug, one layer down. `env.local` does not set it. The migration comment documents it as an operator step, so this is a **configuration precondition, not a code defect** — but one whose omission is undetectable | Recorded in [`dependency-licenses.md`](../../dpg/dependency-licenses.md) §Staying true. ⚠ **Not yet a followup** — the fix is a one-line config change (`ops_db_user: str = ""`) plus a documented env requirement, and it belongs to whoever owns the ops container |
| D-26 | ✅ **Fixed 2026-08-19** | DPG-01 / DPG-02 | ⚠ **CI has been red on `integration/stage` for its last 10 runs, 2026-08-08 → 2026-08-17 — so \"green in CI\" was never available to claim, and I claimed it anyway.** Pushing this branch produced the **identical failure shape**: `ui-checks` fails at **Lint** (2 `react-hooks/set-state-in-effect` errors out of 166 problems), which **skips the Build step**; `backend-tests` fails pytest (18 failed / 1143 passed **as measured that day** (the passing count is now 1166 — `tests/repo` grew; **the failure count, 18, is the number that matters and it has not moved**)) in project-type, back-fill and grievance-sync tests. ✅ `tests/repo` passes. **This sprint introduced no new failure** — verified step by step against the baseline run | **A permanently red build is indistinguishable from one nobody watches**, which is the root the sprint kept meeting from different angles: D-01 (27 files never collected), D-24 (branch prefix missing from triggers), and now this. ⚠ **It also blocks D-02b properly:** Build is skipped when Lint fails, and Build is the `next build` gate that deviation named for the `"use client"` question — so that gate **has never run in CI and cannot until the 2 lint errors are fixed**. The Docker build (D-02) remains the evidence, and its limits are now stated rather than glossed | ✅ [`followups/ci-has-been-red-for-ten-days.md`](followups/ci-has-been-red-for-ten-days.md) + `TODO.md` row · ✅ **Both jobs green.** **Eighteen failures, one root cause, said four different ways:** something reads data the seeder writes afterwards. CI's order is migrations-then-seed, so a back-fill that derives its rows from seeded data finds nothing **on a fresh database, and only on a fresh database** — on the deployment where a human looks it works perfectly, nothing raises, and a table is simply empty. Skipped: `project_workflows` (`b3c5d7e9`), project types (`n0p2r4t6`, which reads the former), per-lot Level 1 staffing (`p2r4t6v8` — and C1 gates **intake**, so every ticket POST was refused), and `apply_donor_informed_defaults`, a **service-layer side effect** the API performs and the seed bypassed. That last one presented as **flaky, not broken**: the test exercising it commits, so one full-suite run repaired the database permanently and anyone re-running to check saw green. Fixed in the seeder — a migration must not invent data that never existed, and `n0p2r4t6` says so deliberately — with every value **derived** from the project's own rows so it cannot drift from `seed_standard()`. Pinned by `tests/ticketing/test_seed_completeness.py` (8 tests on the *outcome*, not the mechanism); 5 mutations red, each reproducing its original failure. ⚠ **Three tests were asserting the absence of seed data** and went red when the gap was filled — they now take `without_seeded_package_l1s` and say the precondition out loud. `ui-checks`: 2 `react-hooks/refs` errors from a ref assigned during render to bridge two callbacks in the wrong order; there was no cycle, so reordering removed the ref, the errors and a stale-closure risk. **backend-tests 18→0 failed, 1166→1388 passed; ui-checks 2→0 errors** — verified by rebuilding the database exactly as CI does |
| D-27 | ✅ Closed 2026-08-18 | T-03 | ⚠ **The reference pin passed locally and failed in CI within minutes of landing — and the cause was worth the trip.** `_python_index()` walked `rglob("*.py")`, which picks up **agent worktrees under `.claude/`**: whole *gitignored* copies of this repository at older commits. Four genuinely dangling `tickets.py:NNNN` citations (H2-02 split that module into a package in July) resolved against one of those ghosts. CI has no worktrees, so CI was right and my machine was wrong | **A filesystem walk is the wrong index for "does this repo contain the file".** Now reads `git ls-files`, which is exactly what CI checks out, with the filesystem walk kept only as a no-git fallback and `.claude` added to its skip list. `tickets.py` added to `KNOWN_ABSENT` with the package-split reason. **Mutation-rechecked all four against the new index** — the resurrection check is now faithful (`git add -N` then red) | — (fixed in place; the finding is recorded in the test's own comments, where the next person to touch the index will read it) |
| D-28 | ✅ **Closed 2026-08-18 (DPG-14)** | DPG-10 | ⚠ **`extract_contact_info`'s declared failure contract is unreachable.** The handler's first statement is `if not response:` — but `response` is bound *after* the API call. A provider failure, a missing/invalid `field_name`, or an absent client all raise **`UnboundLocalError` from inside the `except`**, not the documented `{field_name: ""}`. Only a malformed-JSON body reaches the declared contract. Predicted by the spec (DPG-10 trap 1) and **confirmed by the net** | `backend/services/LLM_services.py:109-118` · Pinned as-is by `test_extract_contact_info_raises_unbound_local_error_when_the_call_fails` and `…_without_a_client`. **Characterize the real behaviour, not the intended one** — otherwise the net asserts a contract the product does not have | ✅ **Fixed under the net in DPG-14.** `field_name` and `response` resolve before the `try`, so the declared contract is reachable; the no-valid-field case raises a `ValueError` naming the **keys, not the values**, because that message reaches the Celery log. Both pinning tests flipped in the same commit and were mutation-rechecked |
| D-29 | ✅ **Closed 2026-08-19 (DPG-19.3)** | DPG-10 | ⚠ **The same defect a second time, and nobody knew.** `translate_grievance_to_english_LLM` interpolates `result` into its `ValueError` message, but `result` is bound *after* the API call — so every pre-call failure raises **`UnboundLocalError`**, and the documented `ValueError` is reachable only on a malformed body. **Not in the spec's inventory**; found by writing the failure-contract test | `backend/services/LLM_services.py` · `translate_grievance_to_english_LLM` (⚠ a line number was here and rotted twice — the function name does not) · ⚠ **The one-line fix is a privacy regression.** Binding `result` early makes the declared `ValueError` reachable — and its message interpolates **`input_data` in full**, grievance narrative included, straight into the Celery logs. The correct fix is that binding **plus** removing the interpolation, which is exactly Sprint 3's **T-34-b**. Doing half of it now would be a bug fix that leaks | ✅ [`followups/translate-error-path-unbound-local.md`](followups/translate-error-path-unbound-local.md) + `TODO.md` 🔵 TECH DEBT row |
| D-30 | ✅ **Closed 2026-08-19 (DPG-15b)** | DPG-14 | ⚠ **A fourth defect, found by checking the third.** DPG-14.1 asked whether `gpt-5-nano` was silently failing or truncating. It is doing neither — three live calls returned `finish_reason: stop` and parsed cleanly. But they took **20.5 s, 15.2 s and 14.0 s** of server processing, and the retrieve step's poll deadline is **20.0 s** (`CLASSIFICATION_POLL_MAX_SECONDS`). One run of three missed it. When that happens **the complainant sees the empty-classification path even though the model succeeded** — indistinguishable, from the outside, from the failure DPG-14.1 was hunting | Measured, not inferred: 92% of run 1's completion budget was **reasoning tokens** (2,880 of 3,087) and the prompt is 20,725 characters because the category catalogue is injected twice in two shapes. Both halves are addressable; neither should be guessed at. ⚠ Three samples is an observation, not a distribution — and lowering reasoning effort is a **cost/quality** change against an unanswered budget question (Q-19) and the very baseline DPG-23 benchmarks against | ✅ [`followups/classification-latency-exceeds-the-poll-deadline.md`](followups/classification-latency-exceeds-the-poll-deadline.md) + `TODO.md` 🔵 row |
| D-31 | ✅ Closed 2026-08-18 | DPG-13 | ⚠ **The spec put the structured-output capability on the *endpoint*. It belongs on the (endpoint, model) pair, and the difference would have broken three call sites.** Measured against the live provider, one request per cell: `gpt-5-nano` ✅ schema · `gpt-4o-mini` ✅ schema · `gpt-3.5-turbo` **400** on schema · `gpt-4` **400 on both** — it has no JSON mode at all, which is also why the translation site never had a `response_format` and nobody knew why | An endpoint-only flag set to `json_schema` (the obvious default for the current provider) would have sent it to `gpt-3.5-turbo` twice and `gpt-4` once, breaking contact extraction, SEAH detection and translation **in production**, on a path with no tests until this sprint. The registry now carries a per-task capability; `LLM_STRUCTURED_OUTPUT` is the endpoint ceiling and the effective mode is the weaker of the two | — (spec corrected in place; `docs/services/06_llm_service.md` carries the measured table) |
| D-32 | ✅ Closed 2026-08-18 | DPG-15 | ⚠ **A failed classification was recorded as a successful one.** With the endpoint on a dead port, `classify_and_summarize_grievance_task` returned `status: "SUCCESS"` and wrote `grievance_classification_status = LLM_generated` — a **success** code — with an empty summary, an empty category list, and `error: "Connection error."` sitting in the same payload, contradicted by the status beside it. Cause: the task's only guard was `if not values:`, and the service's documented failure dict is **truthy** | Found by *driving* the path, not reading it (grievance `DPG15-c2b3e821`). Consequences: the terminal `LLM_failed` code was unreachable on the one path that most needs it; the retry never fired; the complainant waited out the 20 s poll and got an empty classification; and the row claimed the model had produced it. **A grievance mechanism that records a failed AI step as a completed one is worse than one with no AI**, because the failure becomes invisible to the officer reviewing the queue | Fixed: `is_failed_classification()` + the task raises on it. Re-run recorded in the audit table |
| D-33 | ✅ Closed 2026-08-18 | DPG-15 | ⚠ **An outage erased a complainant's phone number.** `extract_contact_info_task` reported SUCCESS and overwrote the stored `+9779841234567` with `""` (complainant `DPG15C-ef87917d`). The complainant had typed it; the model was down; the number was gone | ⚠ **The service function did exactly what its contract said** — DPG-14 had just made that `{field: ""}` sentinel *reachable* (it previously died as an `UnboundLocalError`, which by accident wrote nothing). So this is a fault the characterization net could not have caught: the contract itself was harmful one layer up, and only a dead-port run against a real database showed it. **That is the argument for DPG-15 being an experiment rather than a code review** | Fixed: `is_empty_extraction()` + the task refuses to persist an all-empty extraction. Losing the enrichment is recoverable; losing the phone number is how a GRM loses the ability to contact a complainant |
| D-34 | ✅ **Closed 2026-08-19 (DPG-15b)** | DPG-15 | ⚠ **`LLM_failed` is unreachable.** `_persist_classification_failed_if_final` writes it only once Celery retries are exhausted — correct in intent — but the task's `except` **returns a FAILED dict instead of re-raising**, so `autoretry_for` never fires, `request.retries` never increments, and the condition is never true. The row sits at `pending`, where the retrieve step polls the **full 20 s** before giving up | Not fixed here: re-raising changes the task's terminal state from SUCCESS-with-a-FAILED-payload to Celery FAILURE, and the frontend polls task status. That is a retry-topology change on the live intake path and it needs its own tests | ✅ [`followups/llm-failed-is-unreachable-and-the-closure-doc-never-retries.md`](followups/llm-failed-is-unreachable-and-the-closure-doc-never-retries.md) + `TODO.md` row |
| D-35 | ✅ **Closed 2026-08-18 (DPG-19b)** — declared parked, not deleted (Q-22) | DPG-15 | ⚠ **Neither contact-extraction function has a production caller — and it is worse than the one dead function first logged.** `extract_all_contact_info` has no reference at all; `extract_contact_info` is reached only by `extract_contact_info_task`, which nothing enqueues outside `backend/task_queue/test_tasks.py`. Whole-repo grep across `.py`/`.js`/`.ts`/`.yml`. **Two of the nine call sites in every inventory this sprint wrote are unreachable**, including two of the six in the privacy assessment's leg L4 | The live contact path is **deterministic** — `backend/actions/services/contact/phone.py::validate_complainant_phone` → `helpers.is_valid_phone` — and no model is involved. So the answer to *"how is the phone number sent to the LLM"* is **it is not**, and the compliance document that implies otherwise is the thing to fix | DPG-19 corrects L4 and decides the code's fate (delete, or wire up deterministically first — **Q-22**) |
| D-36 | ✅ **Fixed 2026-08-19** | DPG-15 | ⚠ **A closure document that fails to generate is never rebuilt.** `generate_resolved_case_summary` sets `generation_status = "llm_failed"` and returns normally; nothing retries it (the retry is on the `except` path, and a `None` return is not an exception) and nothing sweeps those rows. The complainant **is** told the case is resolved — that notification comes from the resolve action — but never receives the outcome narrative or its link | **Complainant-facing.** The deterministic half of that document (dates, levels, resolution category, officer) does not need a model at all; publishing it with an honest gap where the narrative would be is probably better than publishing nothing | ✅ same followup as D-34 + `TODO.md` row · ✅ **Fixed by asking a different question.** Both public routes gated on `generation_status == "complete"` — *did the AI succeed* — when the thing that matters is *is there something a complainant can read*. `build_public_summary_json` produces the whole deterministic record (dates, level, category, status) even when the model call returns nothing, so the endpoint was withholding a usable document because one optional narrative field was missing. `_is_publishable(row)` now asks for `summary_public_json["resolution_text_public"]`, and the task retries twice (30s, 60s) when the narrative is absent, then logs — at ERROR, naming the ticket — that the complainant was **not** notified. 5 tests in `tests/ticketing/test_closure_publishable.py`, 4 mutations red |
| D-37 | ✅ Closed 2026-08-18 | DPG-15 → DPG-19 | ⚠ **I overstated D-33, and the overstatement was in the headline.** The commit says an outage *"erased a complainant's phone number"*. The defect was real, reproduced against the database, and the fix is right — **but `extract_contact_info_task` has no production caller**, so no complainant's number has ever been erased by it. The only reference outside its own definition is `backend/task_queue/test_tasks.py`, a manual script. Found while answering the owner's question *"I don't understand how the phone number is sent to the LLM"* — the honest answer being that it is not | **Severity, not existence.** The code is reachable by Celery name, the fault was real, and the guard belongs there. But "a complainant's phone number was erased" describes production impact that did not happen, and this project has been bitten twice by claims that outran their evidence (D-22, D-26). ⚠ It also means the **privacy assessment's leg L4 overstates egress by two of six call sites** — an inventory that overstates exposure spends the reader's trust on paths that do not exist | Corrected here, in the audit table, and in the DPG-15 status row. L4 corrected by **DPG-19** |
| D-38 | ✅ **Closed 2026-08-18 (DPG-19b)** — `PARKED_TASKS` + the four documents corrected | DPG-18 | ⚠ **Four of the nine LLM call sites are unreachable, and every DPG document counts nine.** Established per site by asking whether anything **enqueues** the task outside `test_tasks.py`: ✅ live — classification (`gpt-5-nano`), SEAH detection (`gpt-3.5-turbo`), and the three ticketing paths. ❌ not live — contact extraction ×2, grievance translation, and ASR. **Two live call sites on the chatbot surface, not six** | Raised by the owner's claim that only nano is called. **He is right about what runs**, with one exception: `gpt-3.5-turbo` is still live on the **SEAH detection** path — the oldest model in the repository, on the harassment-detection call. Two consequences nobody had written down: (1) **nothing translates the narrative on the chatbot side** — the English officers read is produced by ticketing's `generate_case_findings`, which is coherent but undocumented; (2) the nano call is a **third SEAH signal**, because the review step screens its categories for "gender" (`detect_sensitive_categories`). ⚠ ASR is dead **by decision** — `registered_tasks.py:200`, *"CB-01 proto: store audio only"* — which is different from rot and must not be "cleaned up" | ✅ **DPG-19b** — ⚠ *not* deletion (see **D-39**): the four are the parked voice-notes flow. Declares them, labels them, corrects the four documents that count nine, and pins the live/parked distinction |
| D-39 | ✅ Closed 2026-08-18 | DPG-19b | ⚠ **I read "unreachable" as "legacy" and recommended deleting three working features.** D-38 established that four LLM paths have no production caller; I proposed deleting contact extraction ×2 and grievance translation, keeping only ASR. The owner: *"they are functions part of the voice notes flow, which I have not updated as we have no budget for the transcription part."* **All four are one coherent, deliberately parked feature** | **The evidence was already in the repository and I had read past it.** `test_tasks.py` holds the chains — `transcribe → classify` and `transcribe → extract_contact_info` — so contact extraction consumes a *transcription of spoken contact details*, which is why the typed path never needed it. I had even quoted that file as proof the paths were dead, without reading what it was testing. **Reachability tells you what runs; it does not tell you why something does not** — and deleting on that evidence alone would have destroyed a designed-for capability and left the next person to rebuild it from a spec | DPG-19b rewritten: declare and label, delete nothing. The **pin** changes with it — enqueued **or** declared parked, which is the distinction that was missing |
| D-40 | ✅ **Closed 2026-08-18 (DPG-18 §18.3)** — `ModelProfile` drops, renames and budgets | DPG-18 | ⚠ **Moving the ticketing calls to `gpt-5-nano` would have broken them three ways, and the third is silent and complainant-facing.** Measured against the live provider: `temperature=0.0` → **400** (*"Only the default (1) value is supported"*); `max_tokens` → **400** (*"Use 'max_completion_tokens' instead"*); and once renamed, `max_completion_tokens=400` **and even 2000** → **200 OK, `finish_reason: length`, content EMPTY** — the reasoning consumed the whole budget. The resolved-case summary actually needs **4,287 completion tokens, 3,904 of them reasoning**, against a current cap of 1,200 | **The empty-content case is the dangerous one.** Two 400s are loud and get fixed in minutes. A 200 with empty content returns `None` on the resolved-summary path → `generation_status = "llm_failed"` → and per **D-36** nothing retries it: **the complainant is told the case is resolved and never receives the closure document**. It would have read as a model-quality problem, not a parameter one. ⚠ It also means the consolidation's **cost is not obviously lower**: nano spends 2,000–4,000 reasoning tokens per call, so DPG-23 must price **per call**, not per thousand tokens (a Q-19 input) | ✅ **DPG-18 §18.3** — `ModelProfile` in the registry (structured output · temperature support · cap parameter name · measured reasoning overhead); `request_for()` drops, renames and budgets. Plus the rule that `finish_reason == "length"` is a **failure**, not an empty answer |
| D-41 | 🟡 **Partly closed 2026-08-19 (DPG-15b)** — the risk is mitigated (30 s wait, terminal failure, a message in every branch) and the short path is documented in `02_flow_spec.md`. ⚠ **Still open: nobody has measured how long the no-contact path actually takes.** Fold it into the next manual browser sweep rather than making it a ticket. ✅ **Written up 2026-08-27** — [`followups/no-contact-path-latency-unmeasured.md`](followups/no-contact-path-latency-unmeasured.md) + a `TODO.md` row, because "fold it into the sweep" with no doc and no row is how a finding stops existing | DPG-15b | ⚠ **The complainants most exposed to a slow classification are the ones who refuse to give contact details.** Q-20's tracing established the review step runs *after* submission, at the end of `form_contact` + `form_otp` (an SMS round-trip) — so the measured 14–20.5 s classification is normally long finished. **But `complainant_consent is False` makes `form_otp.required_slots()` return `[]`** (`form_otp.py:181-183`), the SMS round-trip disappears, `form_contact` shrinks, and the gap collapses to seconds | **The fast path through the flow is the privacy-conscious path.** An anonymous or contact-refusing complainant is the one who reaches the review step before the classification does, and is shown nothing. ⚠ **And raising the budget to 90 s makes D-34 worse until it is fixed**: a failed classification is not terminal, so the poll runs its full length — a 20-second pause becomes a **90-second stall**. The raised budget and reachable `LLM_failed` ship together or not at all | ✅ **DPG-15b** — measures the no-contact path end to end rather than assuming a number, and couples the budget to the terminal state |
| D-42 | ✅ Closed 2026-08-18 | DPG-19 | ⚠ **I wrote a test that could not go red, and the mutation check caught it — which is the only reason it is not still there.** D-29's fix was drafted as *"bind `result` before the `try`"*, with a test asserting a pre-call failure raises `ValueError` rather than `UnboundLocalError`. Deleting the binding left the test **green**: the real fix was bounding the error **message** to `grievance_id` + three words, which stopped the handler reading `result` at all. The binding protected nothing | **Dead defensive code that reads as the fix is worse than none**, because the next person maintains it believing it matters — and the test that "proves" it manufactures the same confidence a decorative test always does (`test_the_fixture_really_is_encrypted`, and the boundary test that pinned code against a dict inside itself). Binding removed; the honest mutation — **restore the old message** — turns two tests red, and is recorded in the test's own docstring | — (found and closed inside the ticket; TESTS.md rule 1 doing exactly its job) |
| D-43 | 🔵 **Open — note for T-03's owner** | DPG-19b | ⚠ **`LLM_services.py`'s anchors were re-pointed FOUR times in one day** — DPG-14 (+14 lines), DPG-11 (model names left the file), DPG-13 (the schema layer), DPG-19 (the length gate), DPG-19b (parked-flow docstrings). Each time: recompute the line numbers, rewrite ANCHORS, sweep every citing document. The pin is doing exactly what it was built for and it caught real drift each time — **but on a file under active refactor the cost is a re-point per commit**, and a mechanical edit repeated five times is where a tired author starts editing ANCHORS alone, which is the one failure the pin exists to prevent | **A cheap improvement, for whoever owns T-03:** anchor call sites on the **enclosing function name** (`def classify_and_summarize_grievance` + an expected token) rather than a line number. It still catches deletion and renaming — the drift that matters — and stops charging a re-point for every line added above. Line anchors remain right for the stable files (`base_manager.py`, `keyword_detector.py`), which have not moved once | — (recorded, not acted on: `test_doc_code_refs.py` is Sprint 0's and changing its anchoring strategy mid-refactor is its own decision) |
| D-44 | ✅ Closed 2026-08-18 | DPG-18 §18.2 | ⚠ **Fail-open pretended nano could not detect harassment, and it took a debug probe to disbelieve it.** Verifying the SEAH model change, both positive cases came back `detected: False` — which reads as "nano is worse than turbo at the thing that matters most". It was a **401**: my fast iteration container used `docker run --env-file`, which does not parse the quoted `OPENAI_API_KEY` in `env.local` the way compose does. Re-run through compose: **4/4 correct**, both harassment cases `detected: True, level: high`, both non-cases correctly ignored | **This is Q-14's fail-open, demonstrated on me rather than argued about.** A configuration error and "no harassment here" are the same observation from outside — which is exactly why the deterministic keyword pre-filter has to keep running underneath, and why `/health/llm` (DPG-15) reports reachability separately from application health. ⚠ It also means **any future SEAH benchmark must prove it is authenticated before it reports a miss**, or it will publish a config error as a quality finding | — (the finding is the lesson; the fast container was rebuilt to match compose, then removed) |
| D-45 | ✅ **Closed 2026-08-19** | DPG-15b | ⚠ **No Celery task has ever had its retry configuration applied.** `TASK_CONFIG` declares the settings under `'retries'`; the decorator reads `config.get('retry', {})`. So `retry_config` is always `{}` and every task type runs on Celery defaults: a **180-second** delay instead of 2 s doubling, and **no `autoretry_for` at all** — which is why nothing in this queue has ever retried automatically. Found by reading the countdown Celery actually chose (*"Retry in 180s"*), not the config | It half-explains **D-34**: the retry counter that the terminal-status guard waited on could never move, because nothing was configured to move it. ⚠ **Not fixed here** — correcting the key switches on `autoretry_for=(Exception,)` for *every* task type at once, since `getattr(__builtins__, 'RateLimitError', Exception)` resolves unknown names to `Exception`. Queue-wide behaviour change, own ticket, own tests | ✅ **Fixed 2026-08-19** — key corrected **and** `retry_on` resolved to real classes, because the key alone would have switched on retry-on-anything. Unknown names are now dropped **loudly**. Verified on live task objects: `classify…task max_retries=3 delay=2s backoff=2 autoretry=[ConnectionError, TimeoutError, RateLimitError]`. Pinned by `tests/backend/test_task_retry_config.py` |
| D-46 | ✅ Closed 2026-08-19 | DPG-15b | ⚠ **My first fix for D-34 reproduced D-34, one layer up.** The terminal write was placed in `except MaxRetriesExceededError:` around `self.retry(exc=e)` — but Celery **re-raises the original exception** when retries are spent and an `exc` is given; `MaxRetriesExceededError` is only raised when it is not. So the handler never fired and the terminal write was unreachable again | **Exactly the shape D-34 was**: a guard placed where the condition it waits for can never be true. Found by driving the ladder against the database rather than reading the retry docs — the third time in this sprint that running something beat reasoning about it. The decision now happens **before** the retry: `if self.request.retries >= max_retries: …`, which cannot be bypassed by whichever exception Celery picks | — (found and fixed inside the ticket; the test asserts the construct is absent, and strips comments first so the warning about it does not trip the check) |
| D-01 | 🔵 **Open — backlog** | DPG-02 | ⚠ **CI never runs 21 test files.** `ci.yml`'s pytest step names four paths explicitly (`tests/ticketing tests/orchestrator tests/actions tests/backend`), so the **21 test files sitting directly in `tests/`** — `test_complainant_functions.py`, `test_postgres_services.py`, `test_seah_*.py` and 18 more — plus `tests/shared/`, have never executed in CI. Found while deciding where T-01 should live; it is why `tests/repo/` exists rather than `tests/test_spdx_headers.py`. **Not fixed here** — those files may not pass, and finding out is its own ticket, not a licence commit. Logged as a followup | Location: `.github/workflows/ci.yml:178` | [`followups/ci-untested-root-test-files.md`](followups/ci-untested-root-test-files.md) · `TODO.md` 🔵 |
| D-02 | ✅ Closed | DPG-01 | ✅ **CLOSED 2026-08-18 — `next build` succeeded.** Rebuilt `grm_ui` in Docker after the SPDX pass and the container came up healthy; the built image carries the current sources (verified via the new `license` field), so the build genuinely re-ran over all 110 `.tsx` files that now have the header above `"use client"`. The reasoning below was right — comments are trivia and do not break a Directive Prologue — but it is now confirmed by the actual Next.js compiler rather than by argument. Original note: | Location: 110 files under `channels/ticketing-ui/` · ✅ verified | — (verified by rebuild) ⚠ **Qualified 2026-08-18 (D-26):** the Docker `next build` is real evidence and stands, but **`ui-checks`' Build step — the CI gate D-02b actually named — is skipped whenever Lint fails, and Lint has failed on every run since 2026-08-08.** So the named gate has never run and cannot until 2 lint errors are fixed. Closing D-02 on a local build was right; describing it as the CI confirmation was not |
| D-02b | ✅ Superseded by D-02 | DPG-01 | ⚠ **The `"use client"` directive placement was reasoned, not empirically confirmed** (superseded by D-02). 110 `.tsx` files now carry `// SPDX-License-Identifier` **above** their `"use client"` directive. Per ECMAScript, comments are trivia and do not break a Directive Prologue, so the directive is still the first *statement* — and `tsc --noEmit` is clean plus all 88 vitest tests pass. But **vitest does not exercise the directive** and there was no prior comment-above-directive precedent in the repo to lean on. `next build` is the confirming gate and it must not run on the host (CLAUDE.md §Docker-only). **Check `ui-checks` on the first CI run of this branch** — if it fails, the fix is to move the header below the directive for `.tsx` only, which is a three-line change to `insertion_line()` | Location: 110 files under `channels/ticketing-ui/` · **verify in CI** | — |
| D-03 | ✅ Closed — false positive | DPG-01 | The pre-sprint reading of "encoding declarations in scope" was a **false positive** — the grep matched `encoding="utf-8"` inside `open()` calls, not PEP 263 headers. There are **no** encoding declarations in scope; the shebang case (13 files) is real and handled | Location: — · closed | — |
| D-04 | ✅ Closed — spec corrected | DPG-02 | ⚠ **The DPG-02 spec's "two images" premise is wrong.** One `Dockerfile` serves every Python service and installs **both** `requirements.txt` and `requirements.grm.txt` into a single image — verified by importing packages from each inside `ticketing_api`. The report therefore splits by **manifest** (parsed from the requirements files), which is the only split that exists: 35 declared, 98 transitive | Location: `Dockerfile:22-25` · DPG-02 (spec corrected) | — |
| D-05 | ✅ Closed | DPG-02 | **Our own npm manifest declared no licence** — fixed (`"license": "Apache-2.0"` in both). ⚠ **But the follow-on claim was wrong and is corrected:** the report predicted the scanner row would flip to `Apache-2.0` after an image rebuild. It does not. `license-checker` hard-codes `UNLICENSED` for any `"private": true` package and ignores the `license` field — verified by rebuilding `grm_ui` and re-scanning (`{"licenses": "UNLICENSED", "private": true}`). The row is a tool artefact for an unpublished package, not a finding; the fix stands on its own merits | Location: `channels/*/package.json` · DPG-02 ✅ fixed, claim corrected | — |
| D-06 | ✅ Closed — recorded | DPG-02 | ⚠ **Two LGPL dependencies nobody knew about**, both transitive so no manifest read would show them: `jwcrypto` (LGPL-3.0-or-later, via the Keycloak JWT path) and `@img/sharp-libvips-linux*-x64` (LGPL-3.0-or-later, via Next.js image optimisation). Both are unmodified dynamically-loaded libraries — the LGPL-compliant pattern — and both are now dispositioned in the report | Location: `docs/dpg/dependency-licenses.md` · DPG-02 ✅ recorded | `docs/dpg/dependency-licenses.md` |
| D-07 | ✅ Closed — fixed + pinned | DPG-02 | ⚠ **The first licence classifier passed a proprietary licence silently.** `"Proprietary Limited License"` contains the substring `MIT`, so `token in text` classified it as OSI-approved and the nightly scan would have reported clean. Fixed with word-boundary matching and pinned by a mutation-checked test | Location: `ops/licences.py` · DPG-02 ✅ fixed | `tests/repo/test_licence_scan.py` |
| D-08 | ✅ Closed | DPG-02 / DPG-04 | ⚠ **I under-scoped the deterministic redaction layer, and the DPG docs inherited the error.** DPG-31 listed only numeric identifiers and email, so the briefing and compliance status both said person names would reach the provider unaddressed. **Names are substantially tractable without ML** — honorific/role-title triggers (`Er.`, Engineer, overseer, ward chairperson), a Nepali family-name gazetteer, and self-identification patterns. Added as §31.2b + §31.2c (addresses); DPG-32's deferral banner, DPG-35's metrics and both DPG docs corrected. **Owner-flagged** | Location: `04-pii-redaction-spec.md`, `docs/dpg/*` · ✅ corrected | see **D-22** — this correction was incomplete and was finished 2026-08-18 |
| D-09 | ✅ Closed · Q16 open | DPG-02 | ⚠ **I read the wrong Hugging Face page and understated their commitments.** The general privacy policy has no inference-specific clause, and I concluded there were none. They are on [Inference Providers → Security & Compliance](https://huggingface.co/docs/inference-providers/en/security): no storage of request body or response when routing, no user data stored for training, debug logs ≤30 days with no user data, TLS, SOC 2 Type 2 on the Hub. **Corrected in both DPG docs.** The caveat that survives is theirs — *"External providers are responsible for their own security measures"* — plus per-request routing making the processor unknowable, and no DPA in the ToS. Fix: pin `model:provider` in prod, route freely in CI (synthetic data only). Q-07-05 | Location: `docs/dpg/*` §4.3a · ✅ corrected · Q16 open | — |
| D-10 | ✅ Closed | DPG-04 | ⚠ **The transfer analysis was pointed at the wrong event.** Retention and training use are mitigations; **the trigger is the transmission itself** — a disclosure and cross-border transfer under the Individual Privacy Act regardless of what the recipient does. DPG-04 now states this, and states that **openness is a licensing property, not a privacy one** | Location: `01-licensing-…#dpg-04` · ✅ corrected | — |
| D-11 | ✅ Closed | DPG-04 | ⚠ **"Anonymised" would have been an overstatement to the ministry.** We hold the mapping, so output is **pseudonymised** and remains personal data. The defensible claim is *only pseudonymised text crosses the border, and the key never leaves Nepal* — which makes **key residency and storage separation acceptance criteria with a test**, since one careless serialisation into a Celery payload voids the argument silently | Location: `04-pii-redaction-spec.md#dpg-31` · ✅ criteria added | — |
| D-47 | ✅ **Fixed 2026-08-19** | D-26 | ⚠ **A tracked file is rewritten as an import side effect.** `backend/task_queue/config.py` calls `update_shell_config()` at module scope, which writes `backend/scripts/task_queue/config.sh` — **a file tracked in git** — from the importing process's environment. Inside the stack it writes `REDIS_HOST="redis"`; on the host, `"localhost"`. So the working tree goes dirty on its own after every containerised test run, the committed content records whichever environment imported the module last, and nothing announces any of it | Found while fixing D-26: it appeared in `git status` twice, was swept into the D-26 commit by `git add -A`, and had to be pulled back out — which is precisely how an environment-specific value gets shipped by accident. Not fixed in that commit because `backend/task_queue/` is a stable chatbot service (CLAUDE.md: deliberate intent + tests), and because the deploy path sources this file — a stale or missing one breaks worker startup, a worse failure than a dirty tree | ✅ Tracked: [`followups/a-tracked-file-is-rewritten-on-import.md`](followups/a-tracked-file-is-rewritten-on-import.md) — recommendation is to generate to an untracked `config.generated.sh` and stop tracking the artefact · ✅ **Deleted, not redirected — and the deferral's stated reason turned out to be wrong.** I wrote "the deploy path sources this file" from a `grep -rn "config.sh"` hit that belongs to a *different* `config.sh` (`scripts/database/config.sh`, sourced by `scripts/docker/init_db.sh`). **Nothing sourced this one**: its directory held no sibling scripts, nothing outside `config.py` referenced it, and its consumers — the legacy systemd/shell runtime — went in `28508aec` when the stack moved to Docker, leaving the generator and its output behind. Every commit touching it since is an accidental sweep; its whole history is `REDIS_HOST` alternating. Removed ~78 lines (`generate_shell_config`, `update_shell_config`, the module-scope call), the artefact, and the empty `backend/scripts/` tree; confirmed no dataclass instance became dead. **Pinned by `tests/backend/test_import_side_effects.py`, which asserts the *property* — importing writes nothing to the source tree — not the deletion, since a test for "the function is gone" is satisfied by a rename.** Snapshots four source roots, imports each startup module in a **subprocess** (in-process proves nothing: pytest has already imported them and a module-scope write happens once), 1 mutation red. ⚠ **The lesson is the caveat, not the bug:** a caution asserted from a grep that was never narrowed is a guess wearing the clothes of a finding, and it nearly bought a generate-to-untracked-path mechanism for a file that should not exist |
| D-48 | 🔵 **Open — backlog** | D-47 | ⚠ **112 backticked paths in `docs/` point at files that do not exist** — 39 distinct targets, worst of them `ticketing/api/routers/tickets.py` with **22 references** to a module that became a package. Measured while widening the `docs-links` gate after `CLAUDE.md` was found pointing at a file renamed months earlier | The gate now checks backticked paths in the **root** `*.md` only (verified 0 broken before turning it on), and **not** in `docs/` — not because of the count, but because a backticked path in prose does not always mean "go read this": a post-mortem legitimately names the file it just deleted, a spec legitimately names one it proposes creating. Both would be flagged, both flags would be wrong. Markdown **links** carry that distinction on their own, so the link check runs everywhere | ✅ Tracked: [`followups/backticked-paths-in-docs-are-unchecked.md`](followups/backticked-paths-in-docs-are-unchecked.md) — needs a convention for "planned" and "deliberately gone" before the check can extend to `docs/` without an ignore list, which is how a gate quietly stops gating |
| D-49 | ✅ **Fixed 2026-08-21 (owner approved the priority change)** | DPG-20 | ⚠ **Four grievance categories silently lost their high-priority flag, and two of them are the ones that matter.** `grievances_categorization_v1.1.csv` declares 13 columns; five of its 24 rows do not have 13 fields — four carry a **third** follow-up-question pair the header never names, one is short. `csv.DictReader` files the surplus under the `None` key, so `row["high_priority"]` receives a question string — `'"…on a scale from 1 to 5?"'.lower() == "true"` is `False` — and the short row yields `None`, which the guard also reads as `False`. Live result: **`Environmental - Air Pollution`** (the dust complaint — demo scenario 1, and the highest-volume category on a road project) and **`Gender - Gender Discrimination And Harrassment`** (the category the SEAH review step keys on) are authored `True` and load as `False`, along with `Cutting Of Trees` and `Wildlife Destruction`. Ticket priority reads the flag directly (`ticketing_dispatch.py:117`), so SLA clocks and queue badges follow it | Found by building DPG-20's benchmark, which labels severity from the taxonomy and therefore had to read it as raw rows rather than through `DictReader`. ⚠ **It is in Postgres too** — the DB copy was seeded from this CSV and the loader prefers the DB, so re-seeding re-applies the defect instead of clearing it verified two ways: the taxonomy loads *from Postgres* and still reports Air Pollution as normal priority, and forcing the CSV fallback reproduces the identical four disagreements — so neither source is the clean one. **Why it stayed invisible:** the flag travels one way, CSV → dict → ticket priority, and a `False` where `True` was meant produces a ticket that looks entirely normal — no error, no log line, no test. **Not fixed in a benchmark commit on purpose:** flipping four categories moves SLA timing, escalation and seeded demo tickets, which is a product decision with a blast radius, not a data-labelling decision | ✅ **CLOSED 2026-08-21 — owner approved the priority change.** Repaired the **CSV**, not the parser: the five ragged rows had three different shapes (a genuine third question pair ×2, a duplicated field ×2, one short row), and the repair rule that covered all of them was *the final field is the authored flag* — which is where every malformed row already had it. Two extra columns declared, nothing lost, and the duplicate was **asserted identical before being dropped**. ⚠ **Not live until each environment is re-seeded**: CSV ✅, local DB ✅, **staging and prod ⬜** — and re-seeding prod shifts SLA clocks and queue badges for four categories, Air Pollution being the highest-volume one, so it wants a known window rather than a deploy. ⭐ **The canary was deleted and replaced by two positive pins** — one asserting **no row is ragged** (so a *new* malformed row is caught, not only the five known ones), and an `@integration` one that fails when an environment's seeded taxonomy has drifted from the CSV, which is the half that actually bites. Tracked: [`followups/taxonomy-csv-column-drift-silently-clears-high-priority.md`](followups/taxonomy-csv-column-drift-silently-clears-high-priority.md) + `TODO.md` row. **Two tests carry it forward**: the benchmark's severity labels are read from the CSV's final field *positionally*, so the set is already correct when the loader is fixed; and a **canary** pins the known extent of the defect so that repairing it fails loudly and this row gets closed rather than the fix landing unnoticed. ⚠ The fix is the CSV header, not the parser — a header that does not describe its rows is the actual defect, and a tolerant parser only hides the next one |
| D-50 | ✅ **Diagnosis corrected same day; worked around** | DPG-21 | ⚠ **The Hugging Face account has no inference credit, so the measurement half of Sprint 2 cannot run.** The token authenticates and the first probes succeeded — `gpt-oss-20b` completed all six, served by Groq — then every subsequent request returned **HTTP 402, *"You have depleted your monthly included credits"***. This is **not** Q-19's old "there is no budget" blocker returning: the budget was answered on 2026-08-20. It is the narrower one — **the funded budget is not on the account the code points at** | ⭐ **What it nearly cost is the part worth recording.** The first version of `llm_smoke.py` rendered those 402s as ❌ in the capability matrix, in the same visual language it uses for a real 400 — so *"credits depleted"* read as *"this model does not support `temperature`"*, and the script prints those cells as a `ModelProfile` tuple **to paste into the running code path**. A permanent conservative pin, derived from an empty wallet, indistinguishable from a measured row. That is exactly the shape `01_seah_detection_benchmark.md` §6.1 makes a standing rule about (a 401 reading as *"the model cannot detect harassment"*, D-44) — arriving from a third direction, in a tool written by someone who had read that rule the same morning | ✅ **Fixed same-day, and pinned.** The probe now separates `blocked` (the account) from `refused` (the model), renders **three** states not two, prints a banner, **refuses to emit a ModelProfile for any model with a blocked cell**, and exits **3** rather than 0 — because a scheduled run that exits green beside an empty matrix is how this rots. `tests/backend/test_llm_smoke.py` (16) pins the distinction, including that a `json_schema` reply omitting a required field is *accepted and ignored*, not a pass. ⚠ **AND THE DIAGNOSIS ABOVE WAS WRONG — corrected the same day.** It is a **short-window rate limit**, not a depleted account. Evidence: seven candidates probed back to back → model 1 passed all six and every later model 402'd; the same models probed **one at a time seconds later** → all six pass, each. The message says *monthly* and the behaviour is about *the last minute*. ⭐ It nearly declared six of seven candidates unmeasurable on the strength of an error string — the same class of error as the one this row was already about, one level up, and the third time this sprint has taught **believe the behaviour, not the message**. Fixed: backoff-and-retry (20/45/90 s) on account-level refusals, **never** on a model refusal (a 400 on `json_schema` is the answer, not an obstacle), plus `--pause` between models. Three more tests pin it, with the clock **injected** rather than endured. ⚠ **And "rate limit" is itself more confident than the evidence.** What is established is that **the message does not mean what it says** — the account served **250+ further calls** after announcing depletion. The mechanism is not established. ⭐ **Separately checked the same day and this one is settled:** a configurable spending limit is a **Team/Enterprise** feature and this is a personal free account, so DPG-24's *"set a hard token cap"* acceptance criterion was **unactionable as written**. The real ceiling is structural — free tier ~$0.10/month, extra usage **pre-paid**, so the balance loaded *is* the cap. The job header records that instead. [`followups/hf-router-rate-limit-reads-as-credit-exhaustion.md`](followups/hf-router-rate-limit-reads-as-credit-exhaustion.md) + `TODO.md` row |
| D-51 | ✅ **Closed 2026-08-25 — taxonomy 08-21, storage guard 08-25** | DPG-23 | ⚠ **The classifier invents categories on 17% of grievances, and they are stored.** Measured over all 105 benchmark items against `gpt-5-nano`, the model production runs: **18 of 105** received at least one category that exists nowhere in the taxonomy — and not as noise, but as a coherent fictional family (`Road Hazard - Dust` ×7, `- Accident` ×5, `- Animal On Road` ×3, `- Potholes`, `- Flood And Landslide`, `- Others`, plus a malformed `Wildlife Passage` missing its classification half). **21 of the run's 32 false positives are invented categories**; precision against the real taxonomy alone would be ≈0.88 rather than the measured 0.740 | The prompt already forbids it (*"Do not create new categories"*). `_warn_about_unlisted_categories` logs and **never rejects** — deliberately, and the stated reason is sound for a *near-miss name*: *"a complainant's classification is not worth discarding because the model named a category slightly wrong."* ⚠ `Road Hazard - Dust` is not a near-miss. It is **stored**, **shown to the complainant** as the system's understanding of their own complaint, **synced to ticketing**, invisible to every category filter and report, and **not found** by the `high_priority` lookup (`ticketing_dispatch.py:117` does `if category in CLASSIFICATION_DATA`). ⭐ **It compounds D-49**: `Air Pollution` already loses its priority flag to the CSV parse defect, and on 7 items the model *also* offered `Road Hazard - Dust` instead — so a dust complaint can arrive with **no priority signal from either direction**, and neither half is visible alone | ✅ **CLOSED 2026-08-21.** The owner chose the taxonomy fix over the storage guard, and the measurement says that was right: adding all **six** `Road Hazard - *` categories the models kept reaching for took invention from **18 of 105 items to 4** — and **3 of those 4 are not new concepts**, they are `Cultural Site Disturbances` and `Wildlife Passage` with the classification half missing, a formatting failure that predates this. **Exactly one genuinely new concept survived** (`Road Hazard - Noise Pollution`), which is the same signal that produced this change and is worth watching. All six new categories were used — 20 predictions — so none is dead weight. ⭐ **On the owner's reasoning for keeping Dust in both places:** *"air pollution can be related to cement plants as well as road construction, anyway we allow multiple classification"* — which is right, and it is why the benchmark carries `Road Hazard - Dust` as an **acceptable alternate** on the dust items rather than replacing the gold label. ⚠ **Still unmeasured:** the six have no gold items, so nothing scores whether a model picks them *correctly* — only that it stops inventing them ([logged](followups/road-hazard-categories-have-no-benchmark-items.md)). Tracked: [`followups/the-model-invents-categories-and-they-are-stored.md`](followups/the-model-invents-categories-and-they-are-stored.md) + `TODO.md`. ⚠ **Why it was invisible:** the warning is logged on the Celery worker and nothing aggregates it — no counter, no metric, no test — and a grievance with an invented category looks entirely normal in the UI. Scoring classifications against the catalogue is the only thing that shows it, and nothing did that until the benchmark existed. Recommended fix is **both** a parse-time guard *and* a prompt repair — and separately, **seven items independently wanting a road-hazard category is a taxonomy question for the owner**, not only a model defect ⭐ **CLOSED 2026-08-25 — the storage guard landed, and the four survivors turned out to be one shape.** Every one of them is a **correct leaf under a wrong or missing parent** (`Wildlife Passage`; `Road Hazard - Noise Pollution`, where `Noise Pollution` is real and lives under `Environmental`), and leaves are unique across the taxonomy — so the repair is a dictionary lookup, not a judgement call. Three tiers, in `backend/services/category_resolution.py`: the live catalogue is sent as a **JSON-Schema enum** rebuilt per call (invention is impossible on a provider that honours it — ⚠ `Qwen3.5-9B` does not); an unmatched value is folded and resolved by its leaf; anything left is **dropped and logged, never stored**, and if that would leave an item with no category the model's own top alternative is promoted. ⛔ **No fuzzy matching and no repair prompt** — an ambiguous leaf is refused rather than guessed, and a second model call would have cost an interactive round trip *and* buried the taxonomy signal that produced the 08-21 change. ⚠ **The fix hid its own measurement**, since the harness calls the repaired function: `InventionMeter` (`scripts/ops/llm_benchmark.py`) now reads the rate from the resolution log, so the model metric survives — pinned by a test against that log's wording. ⏳ The benchmark has **not** been re-run; precision should rise from 0.773 and by how much is unmeasured. Also closes the DoD item on the alternatives list, which was **never validated at all** even though the complainant picks from it |
| D-52 | 🟡 **Open — ⭐ REFRAMED 2026-08-24: intentional, and the gap moved to the return path** | DPG-23 | ⚠ **The SEAH detector flags gendered complaints that are not harassment — 5 of the 8 items authored as exactly that hard case.** Over 105 items, **none of which is a harassment report**, the LLM detection call flagged 7 (false-alarm rate 6.7%). The five confusables it caught are: no separate toilet for women workers · an unlit route home from school · **unequal pay for the same work** · one tap for the whole labour camp · **refused work for being a woman**. Plus non-sexual abuse (`gen-0073`) and a resettlement complaint (`gen-0093`) | ⭐ **REFRAMED 2026-08-24 by the project owner — this is the design, not a defect.** The detector is tuned **recall-first** deliberately: a **miss** leaves a harassment report in the ordinary queue where nobody knows to look and is **unrecoverable**; a **false alarm** costs a SEAH officer a review and carries **no risk to the complainant**, because a SEAH officer reading a dust complaint discloses nothing to anyone. The system prefers the cheap, recoverable error on purpose. ⛔ **The prompt fix this row originally proposed is therefore withdrawn as the recommendation** — reducing false alarms trades away the property the design rests on. ⚠ **What the trade obligates is the return path**, and that is the real gap: an officer correcting the classification does re-resolve the workflow with `legacy_is_seah=False` and clear `is_seah` (`ticket_workflow_reroute.py:28`), but only as a **side effect of a category edit** — no explicit action, no de-flag audit event, no test pinning it, no round-trip measurement. Raised as its own followup. ⚠ Residual: these five are **women's access and discrimination grievances**, and they are **delayed** by a round trip nobody currently measures. ⚠ There is a **third signal** too — classification returned `Gender - Gender Discrimination And Harrassment` as a false positive 3 times in the same run, and the review step keeps any category containing `"gender"`, so what a complainant experiences is the **union** and is worse than 6.7% | ⚠ **DO NOT TIGHTEN THIS WITHOUT MEASURING RECALL.** The committed set contains **no positives** (§0.2, the owner's 2026-08-19 decision), so a prompt change that removes these five could equally remove real detections and **nothing here would notice**: false alarms drop, the benchmark goes green, and an oblique report — *"he keeps asking me to come to the office alone"* — now reads as a workplace-access issue. Any change is gated on a **paired** re-run against the owner's held-out positive set (`--seah-set`, which the harness **refuses** to load from inside the repo), reporting recall AND false-alarm together. There is currently **no baseline recall figure at all**, which is the most important item in the followup. ✅ Tracked: [`followups/seah-detector-flags-gendered-non-harassment-complaints.md`](followups/seah-detector-flags-gendered-non-harassment-complaints.md) + `TODO.md` |
| D-53 | 🔴 **Open — the committed open configuration cannot transcribe** | DPG-22 | ⚠ **`.env.open`'s ASR endpoint does not exist.** It ships `ASR_BASE_URL=https://router.huggingface.co/v1` with `MODEL_ASR=openai/whisper-large-v3`, and `POST /v1/audio/transcriptions` returns **HTTP 404** for every model id tried, including the turbo variant and a provider-pinned form. **Not auth, not billing**, and three controls say so: `GET /v1/models` → 200, `POST /v1/chat/completions` → 200, the same audio route **without** a token → **401** (so it exists and authenticates) and **with** a token → 404. The router's own catalogue confirms it: 132 models, **none of them audio** — Hugging Face serves ASR, but not through the OpenAI-compatible `/v1/audio/*` surface this code calls | **Why nothing noticed:** three layers of *switched off* stacked up — voice has never been live (Q-13.2) and its four flow functions are declared `PARKED`; `.env.open` is a template nothing loads, so its values were plausible and never resolved; and ⚠ **DPG-21's own `--audio` probe was written the same day and skipped for want of a one-second WAV**. That third layer is the one worth learning from | ✅ Tracked: [`followups/the-open-config-has-no-working-asr-endpoint.md`](followups/the-open-config-has-no-working-asr-endpoint.md) + `TODO.md`. ⚠ **The indicator-4 claim is unaffected for text** — nine of ten call sites and all the live ones. What is affected is *"the open configuration runs the whole system"*. Nothing breaks in production because voice is off; this is a documentation defect that **would have become a runtime defect** the day voice was switched on with `.env.open` loaded. ⭐ The live test now **generates** its probe audio from the `wave` module rather than depending on a committed fixture, so this class of check cannot be blocked by a missing file again. ⚠ Do **not** close it by deleting the ASR variables — their separation from the chat endpoint is what makes pointing at a different provider a two-line change |
| D-54 | 🔴 **Open — a candidate is provisionally disqualified for a non-quality reason** | DPG-21 | ⚠ **`swiss-ai/Apertus-70B-Instruct-2509` refused a grievance about children falling ill.** Its only reply to the capability probe was **`Your request was blocked.`** — in **0.42 s**, far too fast to be generation, so a filter in front of the model rather than the model declining. The prompt was the benchmark's flagship item: *"road construction dust is entering their house and their children have become ill."* Every other candidate answered it | ⭐ **Why this is disqualifying rather than inconvenient: this system's entire input distribution is human harm.** Dust and sick children is the *mildest* end; the rest is land seizure, unpaid wages, forced relocation and, on the SEAH path, sexual harassment. A filter tuned to refuse discussion of harm to children refuses hardest on the reports that matter most. ⚠ **And the SEAH path fails open by design** — so a harassment report the filter blocks is silently handled as an ordinary complaint. That is a **safeguarding** failure mode, the same shape as D-44 with a different cause. ⚠ It is also a genuine loss: Apertus was the strongest **DPG story** in the shortlist (fully open weights *and* open training data, built for low-resource coverage), excluded on a property unrelated to its quality | ✅ Tracked: [`followups/apertus-content-filter-blocks-a-grievance.md`](followups/apertus-content-filter-blocks-a-grievance.md) + `TODO.md`. ⚠ **Deliberately NOT concluded:** one prompt is one data point, and *"Your request was blocked"* is infrastructure phrasing — it may be the **provider's** filter, not the model's, in which case another route to the same weights has no such problem and Apertus returns to the shortlist. It is therefore kept in `llm_candidates.json` with the measurement attached rather than moved to `excluded`, and no `_PROFILES` entry was written (an unmeasured model takes the conservative default, never a guess). ⭐ **The generalisation is the real outcome:** *"does this model refuse our actual input distribution?"* belongs in the standard capability probe next to `json_schema` — it is a capability, and this is the second time this sprint a non-quality property nearly decided a model choice invisibly |
| D-55 | ✅ **Fixed 2026-08-25 (found while closing D-51)** | D-51 | ⚠ **Every Nepali grievance stored a category that is not in the taxonomy — our own code, not the model.** `categories_in_local_language` looked the **leaf** up in `CLASSIFICATION_DATA`, whose keys are the full `Classification - Leaf`. The lookup never hit, both halves fell back to the leaf, and the slot got `Air Pollution - Air Pollution`. `_resolve_grievance_categories_for_db` prefers that slot, `categories_in_english` could not match it back, and it was **written to the database and synced to ticketing** | `backend/actions/utils/language.py:125`. ⭐ **Same damage as D-51 — invisible to every filter and report, and `categories_high_priority` returns False for it — but on *every* Nepali session rather than 4 items in 105.** ⚠ **The test suite pinned it**: `tests/shared/test_grievance_categories_resolve.py` asserted `"Air Pollution - Air Pollution"` as the expected value, because the fixtures were copied from the observed output. A net written from behaviour holds a defect in place unless the behaviour is checked against something else — here, the catalogue. ⚠ It also raised **`IndexError`** on any value without `" - "`, which is exactly the shape the classifier produces when it drops the classification half | ✅ Fixed with D-51: the key is looked up whole, an unknown label passes through untranslated instead of crashing, and `categories_in_english` resolves the legacy doubled form home so slots already in flight heal rather than store junk. That test file now pins the round trip |
| D-56 | 🔵 **Open — owned by [DPG-40](05-prompt-engineering-spec.md#dpg-40)** | Sprint 4 planning | ⚠ **The SEAH detector's graded confidence is computed on every call and discarded three times.** The prompt asks for `level: "high"|"medium"|"low"` (`LLM_services.py:633`) and the schema keeps it as a `Literal` so the enum reaches the provider — then: (1) it is **never persisted** — `public.grievances` has no such column, yet `backend/actions/grievance_intake/sensitive.py:103` reads `grievance.get("sensitive_issues_level", "low")` and always gets its default; (2) it **never reaches the ticket** — `is_seah` is a boolean (`ticketing/models/ticket.py:94`) and `case_sensitivity` is derived from it; (3) so the SEAH queue — a queue that **by design** holds deliberate false positives — is presented to the officer with **no ordering signal** | Found 2026-08-25 while scoping the prompt sprint. ⭐ Same defect class as D-51/D-55: a value that looks present, is a default, and nothing can see is wrong. `sensitive_issues_confidence` is the same read, and the LLM path never returns a `confidence` key at all — only the keyword detector does | ⏳ Fix is **plumbing, not a prompt change**, and is ungated: DPG-40. ⚠ Two constraints it must respect — `is_seah` stays boolean and stays the access control (a grade that gates access is a recall cut with a database column), and whether the officer *sees* the grade rather than merely being ordered by it was [Q-26](DECISIONS.md#q-26) — ✅ **decided 2026-08-25: order only**, because *"the model was not confident"* can bias a reviewer toward the one unrecoverable error |
| D-57 | 🔵 **Open — owned by [DPG-46](05-prompt-engineering-spec.md#dpg-46)** | Q-27 | ⚠ **The officer's correction overwrites the classifier's answer with no record of what it proposed** — so the one number that would say whether the AI baseline is any good in production cannot be computed. `PATCH /tickets/{id}/classification` (`ticketing/api/routers/tickets/crud.py:523`) calls `patch_grievance_classification` → `grievance_manager.update_grievance` (`grievance_manager.py:83`), a plain `UPDATE grievances` with **no change logging**. The tracking variant — `update_grievance_with_tracking` (`:383`), which calls `compare_and_log_field_changes` and writes old→new into `grievance_status_history.field_changes` — **exists, is used elsewhere, and is not used here** | Found 2026-08-25 while recording Q-27. ⭐ **It matters because of what Q-27 decided:** the classifier is a **baseline the officer corrects**, so *how often the officer changes it* is the classifier's real-world score — on real Nepali grievances, at no authoring cost. The ticket event `CLASSIFICATION_VALIDATED` records **that** a validation happened and by whom, so an officer who confirmed the AI unchanged is indistinguishable from one who replaced every category | ⏳ DPG-46: route this path through the tracking variant and report the correction rate. ⚠ **Aggregate metric only** — a correction *rate* is not PII; building a labelled corpus out of real grievances is a separate privacy decision and is **not** part of this ticket |

| D-58 | 🟡 **Drift repaired + pinned 2026-08-27; the duplication remains** | D-49 / D-51 fallout | ⚠ **The taxonomy is authored twice, and the copy that seeds every database is not the one anybody edits.** `ticketing/constants/grievance_categories_default.json` is what `mock_tickets` DELETE+INSERTs into `public.grievance_classification_taxonomy`; the CSV is what D-49 and D-51 edited. They had drifted to **64 fields**: the dust complaint and the SEAH route still `high_priority=False` in every seeded DB, the six Road Hazard categories still on June's text, and one `description` holding the literal string `"True"`. `test_the_seeded_taxonomy_matches_the_authored_csv` had been red **every CI run since 2026-08-23** — it saw the `high_priority` half and prescribed a re-seed, which re-applies the stale JSON. Fixed by `scripts/ops/sync_category_catalog_json.py` + `tests/repo/test_category_catalog_sources.py`, which compares the two authored files so drift fails before a DB exists. Still owed: one source, not two in sync — [`followups/the-taxonomy-is-authored-twice.md`](followups/the-taxonomy-is-authored-twice.md) |
| D-59 | ✅ **Fixed 2026-08-27** | spec reconciliation | ⚠ **The privacy assessment's leg L4 claimed *"two reachable call sites"* and cited one live site and one PARKED one.** It pointed at `LLM_services.py:306` and `:535`; `:535` is `model_for("translate")`, on the **parked** voice-notes path (DPG-19b), and `:306` was six lines off the classification call. So an evidence-pack row whose whole authority is *verified at the line cited* named a path that does not run, in the leg that describes **grievance text leaving the country**. Now `:312` (classification) and `:618` (SEAH detection). ⭐ **The transferable point: the live/parked distinction is invisible to a line number**, so re-pointing citations is a read-and-verify job, never a find-and-replace — this row was created by doing it the slow way | ✅ Closed in the sweep; `TODO.md` row closed with it |
| D-60 | 🔵 **Open** | DPG-18 | ⚠ **DPG-18's acceptance item *"§18.0's table is reproduced in `docs/services/06_llm_service.md`"* was never done** — found 2026-08-27 by ticking the box rather than by reading the file. That file gained DPG-19b's SEAH triple-signal table and DPG-13's capability matrix, but **not** the live/parked call-site table: the word *parked* does not appear in it. So the live spec still describes nine capabilities with no sign that four are switched off — the exact confusion DPG-19b spent a ticket removing, one document downstream | ✅ [`followups/the-live-spec-never-says-which-call-sites-run.md`](followups/the-live-spec-never-says-which-call-sites-run.md) + `TODO.md` 🔵 TECH DEBT row |
| D-61 | 🔵 **Open** | DPG-15b | ⚠ **The two-minute back-fill was documented in the wrong file, and the *"verified with a real late arrival"* limb was never done.** DPG-15b's acceptance asked for it in `11_llm_pipeline_policy.md` — *"it is undocumented and it is what makes the whole design work"*. T-15b-c found the back-fill **already existed** (`grievance_sync`, every two minutes, Q-20) and documented it in `02_flow_spec.md` instead. Reasonable, and incomplete: the guarantee that a late classification still reaches the officer is written where the **chatbot** reader looks, not where the **ticketing** reader does. ⚠ And nobody has watched a real late arrival land — including whether the sync can overwrite the complainant's own review edits, which is the half with a data-loss shape | ✅ [`followups/the-two-minute-backfill-is-undocumented-for-ticketing.md`](followups/the-two-minute-backfill-is-undocumented-for-ticketing.md) + `TODO.md` 🔵 TECH DEBT row |
| D-62 | 🟡 **Open — ⚠ DOWNGRADED from 🔴 the same day, my claim was wrong** | DPG-30 | **A credential is written to the application log at INFO.** `form_otp.py:312` logs the OTP the complainant typed, `:343` again on the invalid path. ⚠ **The original finding said this «completes a status-check impersonation». It does not.** Verification is `otp_matches(slot_value, tracker.get_slot("otp_number"))` (`:349-352`) — the expected value is in **that conversation's own slot**, so an attacker's session holds its own and a leaked OTP string authenticates nothing. Exploiting it needs session replay too, which nobody has established. **The attack was written from the shape of the finding — credential + identifier in one log — without checking the one thing that decides it: whether the secret is verified against per-session or global state. Trace the check before writing the attack.** **What survives:** a credential in a log is a finding on its own terms, and `len(slot_value)` gives every diagnostic the line does. Two lines; cheap, not urgent. ⭐ **Two things found while checking, both more interesting:** (1) **the OTP has no expiry at all** — `services/otp/verification.py` is generate-six-digits, check-six-digits, `==`; no timestamp, no TTL, so the bound is the slot's lifetime, not a clock, and *"an OTP stops working after a few minutes"* is false here; (2) **`otp_number` is not cleared on success** (`:352-364` sets four other slots, never this one). ✅ **Both are now owner decisions, 2026-08-27 — add a validity window, and erase the OTP once the number is verified.** Tracked as an 🟠 `TODO.md` row; the **window length is the one parameter still to confirm** (suggest 10 min), because it is a live-intake change and the people it bites hardest are complainants on slow connections. Clearing the slot is one line and should land first, independently. ⚠ **Open, flagged not claimed:** on SMS failure `:121` prints the OTP into the chat as the designed fallback — on the **status-check** path that would defeat the control, and DOIT has no fallback transport since the SNS deletion. Needs tracing. ⭐ Unaffected by any of this: `form_status_check.py:76`'s `slot_value[:20]` truncation **never fires** — a Nepali mobile is 10 digits — so it reads as a redaction and is not one | ✅ [`followups/otp-and-phone-logged-at-info.md`](followups/otp-and-phone-logged-at-info.md) + `TODO.md` row |
| D-63 | 🔴 **Open — unresolvable here (no Docker)** | DPG-30 | 🔴 **"Redis has no persistence volume" is an inference, and four documents state it as fact** — `privacy-assessment.md` L3 (where it is the *mitigation* on the leg carrying unredacted SEAH text), `19_incident_response.md` `:107`/`:125` (where it is a **containment property** someone acts on during an incident), `14_key_and_secret_lifecycle.md` `:34`, and `TODO.md`. ✅ Verified from source: compose declares no volume, no `--save`, no `--appendonly`, no mounted `redis.conf`, and no override in grm/prod/aws. ❌ **Not established: that any of that means nothing reaches disk.** The official image declares its own `VOLUME /data`, and `redis-server` with no config file uses default RDB save points — so grievance narratives may be snapshotted to a host volume no backup covers and no retention policy names. ⚠ **The 2026-08-18 "VERIFIED LIVE" note did not settle it**: it checked licence and usage, and its persistence sentence restates the inference. Three commands settle it | ✅ [`followups/redis-persistence-is-inferred-not-verified.md`](followups/redis-persistence-is-inferred-not-verified.md) + `TODO.md` 🔴 row |

### Pre-sprint findings — carried in from planning

> ⚠ **The `Location` column is where the finding WAS, at planning time — not where the code is now.**
> This note said the opposite until 2026-08-27, and it was true when it was written: the citations had
> been re-pointed three times in one day (DPG-14 shifted `LLM_services.py` by +14, DPG-11 moved the
> model names out, DPG-13 added the schema layer). **Sprint 2 moved them again and nobody re-pointed a
> fourth time**, which is the honest reason this note is inverted rather than refreshed — and it is the
> argument for the reference pin (`tests/repo/test_doc_code_refs.py`), not against citing lines.
>
> **Re-pointing these rows would falsify the record.** A finding is a claim about the code on the day
> it was found, and several rows name code that was **deleted** rather than moved (P-03's shadow
> client, P-07's `load_dotenv`, P-15/P-16's copied ternaries). Where a finding no longer exists, the
> row records what it *was* and what closed it. Where a row's **claim** stopped being true, it says so
> in place — see **P-10**. For today's locations use
> [`02` §DPG-17's registry table](02-llm-agnostic-spec.md#dpg-17) or `ANCHORS`, the maintained index.

These were found while writing the specs, against `integration/stage` @ `f0d4552d`. They are **not yet
deviations** (no code has changed); they are the inbox DPG-10 and DPG-14 inherit. Move each to the table
above once a ticket touches it.

> ⚠ **`P-##` only.** Twelve `D-##` rows had accumulated here and were moved to the Deviations table on
> 2026-08-18 (D-23). **Do not add a `D-##` row to this table again** — two tables sharing one ID
> namespace is what produced the collision, and reusing a number that belonged to a correction is what
> let a corrected error come back. If a `P-##` finding turns into a deviation, give it the next free
> `D-##` **in the table above** and leave a `→ D-##` pointer here.

| # | Finding | Location | Owner |
|---|---|---|---|
| P-01 | A **second LLM surface** the source narrative missed entirely — 3 more hard-coded models, its own client, its own settings | `ticketing/clients/llm_client.py` | DPG-12 |
| P-02 | `gpt-5-nano` is the **live classification model**, not a stray reference | `LLM_services.py:247` | DPG-14.1 |
| P-03 | ✅ **Removed by DPG-11.** Classification built a **shadow client** shadowing the module-level one, then tested it with a guard that could never fire | `LLM_services.py:260` (the one client) | DPG-14.2 ✅ |
| P-04 | ✅ **Confirmed and fixed 2026-08-18.** The SDK rejects `language_code=` — `openai==1.70.0` declares `file, model, include, language, prompt, …` and no `**kwargs`, checked in-container — so **every transcription raised `TypeError`**. Now `language=`, pinned by T-14-c against the installed signature | `LLM_services.py:52` | DPG-14.3 ✅ |
| P-05 | `extract_contact_info`'s `except` reads `response` at `:109`, which was unbound if the failure happened before the API call → `UnboundLocalError` instead of the documented `{field_name: ""}`. ✅ **Fixed in DPG-14** (D-28) | `LLM_services.py:145` | DPG-10 / DPG-14 ✅ |
| P-06 | Two JSON-producing calls have **no `response_format` at all** — classification and translation ask for "strict JSON" in the prompt and absorb parse failures as empty results | ✅ **Closed by DPG-13** — both now send a `response_format` the model actually supports (`json_schema` for `gpt-5-nano`; `prompt` for `gpt-4`, which rejects JSON mode outright — measured) | DPG-13 ✅ |
| P-07 | ✅ **Deleted 2026-08-18 (DPG-11).** `load_dotenv('/home/ubuntu/nepal_chatbot/.env')` — an absolute EC2 path in a Docker-only stack. Dead code that reads as live config, which is the worst kind: it invites the next reader to *fix the path* rather than delete the line | `LLM_services.py` (removed) | DPG-11 ✅ |
| P-08 | **Zero tests** import either LLM module. The guide's CI snippet targets `tests/test_llm_services.py`, which does not exist | `tests/` | DPG-10 |
| P-09 | Translation error paths interpolate the whole `input_data` dict — including `grievance_description` — into a `ValueError` message that is then logged and may reach the Celery result backend | `LLM_services.py:500`, `:463` | DPG-34 |
| P-10 | `parse_llm_response` logs a JSON parse error — ⚠ **length only, not the raw response**, since DPG-13; this row's original claim is superseded and the redaction work should re-verify it | `LLM_services.py:490` | DPG-34 |
| P-11 | `detect_sensitive_content_llm` **fails open** — LLM unreachable ⇒ `detected: False` on the SEAH path | `LLM_services.py:618-586` | DPG-15 / Q-14 |
| P-12 | `11_llm_pipeline_policy.md` asserts summaries "MUST be PII-scrubbed before storage". **Nothing scrubs them** | `docs/deployment/11_llm_pipeline_policy.md` | DPG-36 |
| P-13 | CLAUDE.md §Folder structure lists `rasa_chatbot/` — the directory does not exist | `CLAUDE.md` | DPG-02 (cheap fix alongside) |
| P-14 | `env.local` holds a live OpenAI key in plaintext. Gitignored and normal for a local env file, but it is the credential this sprint replaces — rotate when the migration lands | `env.local:39` | DPG-16 |
| P-15 | ⚠ **A second copy of `_MODEL_STANDARD` / `_MODEL_SEAH`**, in a module the inventory missed — written into the resolved-case summary as `llm.model` (`:299`), i.e. a **persisted provenance field** computed from a copy rather than from the module that made the call. Change the client's mapping and miss this file → every resolved case records a model that never ran it | `ticketing/services/resolved_summary_builder.py` (the copy that stood at `:28-29`, deleted by DPG-12), `:301` | DPG-17 / DPG-12 |
| P-16 | A **third** copy of the same SEAH ternary, re-derived for a log line — the log can disagree with the call it is logging | `ticketing/tasks/llm.py:165` (and a stale key reference at `:15`) | DPG-17 / DPG-12 |
| P-22 | ~~**There is no LLM budget**~~ → ✅ **there is one, as of 2026-08-20**: a few hundred USD, owner-funded, covering benchmarking and the pilot's inference in two districts (Q-19). Voice transcription stayed **not live** on the original premise and is unaffected — it is sequenced last for decision relevance, not for money now. ⚠ **The premise that replaced it is subtler and easier to forget:** the envelope is shared with production and ends with the demo months, while DPG-24's cost recurs indefinitely | Q-13 · DPG-22/23/24 | **Q-19** ✅ |
| P-23 | **T2 parked** (Q-03/Q-05) — no run-cost owner. T1 becomes the steady state, so grievance egress to a third party is **permanent, not transitional**, which promotes Sprint 3 and removes the vLLM-in-production sentence from the indicator-4 answer | Q-03, Q-05 | DPG-25, DPG-04, Sprint 3 |
| P-24 | ✅ **A deterministic SEAH pre-filter exists and is wired in** — `shared_functions/keyword_detector.py:259` (scored, `:343`) via `helpers_repo.py:58` → `services/seah/sensitive_detection.py:25` → `base_mixins.py:170`, as synchronous slot validation. So the LLM leg's fail-open degrades a **second** pass. Strengthens indicator 9b, which the compliance doc credited to the LLM alone | verified 2026-08-17 | DPG-15, compliance §2.6 |
| P-25 | Owner's Q-11 answer states translation and classification run **in one call**; the code has them as **two** (`:204-231` classify/summarise/follow-up; `:322` translate). The true and useful version: there is no translate-then-classify pipeline — the model consumes Nepali directly | `LLM_services.py:221-247`, `:324` | DPG-23, Q-11 |
| P-26 | Q-12b's *"in practice we store both"* implies a persisted `restore()` mapping — **a plaintext PII store by construction**. Must not land in `ticketing.*` (data rule 3); prefer never persisting it | Q-12b | DPG-31 |
| P-18 | Container images were a **fourth dependency set nobody was auditing** — and the one place licence drift actually happened (`redis:7`, a floating tag onto the non-OSI 7.4 line). Fixed outside the sprint plan: `redis:8.10`, AGPLv3 elected. DPG-02 now audits four sets + a pin-drift check | `docker-compose.yml:222`, `.github/workflows/ci.yml:42` | DPG-02 |
| P-19 | No ticket covered indicator-8 project hygiene (`SECURITY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, templates) — all verified absent | repo root | **DPG-05** (new) |
| P-20 | Root `README.md` advertises a **Rasa service on port 5005** plus an Action Server on 5055, and names a stale active branch. It contradicts the compliance briefing's own indicator-2 argument, on the most-read file in the repo | `README.md:6`, `:10`, `:46-47`, `:60`, `:76`, `:116` | **DPG-06** (new) |
| P-21 | The compliance audit asserted "**No `.env.example`**". It exists (5982 B, `OPENAI_API_KEY=` at `:61`) — the spec was right and the audit wrong. Corrected in the audit doc; the real gap is that it documents only the key, not the endpoint/model surface | `.env.example:61` | DPG-16 |
| P-17 | DPG-11's "open configuration by default" cannot ship in Sprint 1: an HF-router base URL with `gpt-3.5-turbo` / `whisper-1` model IDs is a default configuration that serves **no** request, and it turns DPG-10's tests red in the commit where red means *stop*. Deferred to Sprint 2 against DPG-17's single file | — (planning finding) | DPG-17, DPG-23, Q-10 |

---

## Sprint 1 baseline — measured, not quoted

Measured in-container on `dpg/sprint1-llm-agnostic` @ `a6b6341d`, immediately before DPG-10's first
commit, with the same path list `ci.yml` runs:

| Where | Result | Notes |
|---|---|---|
| **Local, in-container** (before DPG-10) | **16 failed · 1170 passed · 6 skipped** | The number this branch measures itself against |
| **Local, in-container** (after DPG-10) | **16 failed · 1235 passed · 6 skipped** | +65 passing, **failure count unchanged** |
| **CI** (`dpg/sprint0-licensing`, last run) | 18 failed · 1166 passed | Different failing *set*, same conclusion |

⚠ **The local and CI numbers are not the same, and the difference is instructive rather than alarming.**

* **6 of the 16 local failures are `tests/repo/test_spdx_headers.py`, and they are an artefact of the
  container, not a defect**: the image has no `git` binary, and the test resolves its file scope through
  `git ls-files` — deliberately, because a filesystem walk resolved citations against gitignored agent
  worktrees (D-27). CI runs on a runner that has git, where these pass.
* The remaining 10 (`test_grievance_sync`, `test_ticket_uniqueness`) differ from CI's set
  (`test_donor_guardrail`, `test_project_type_authoring`, `test_roles_crud`) because the local database
  carries accumulated state that a CI run seeds from empty. **Neither set has anything to do with LLMs.**

**So the invariant this sprint holds itself to is the one D-26 named: the failure count does not
increase.** Re-measure locally before each commit; re-read CI after each push. A single number copied
from a document is what made D-26 necessary in the first place.

---

## Degraded-mode audit table (DPG-15) — completed 2026-08-18

**Method: drive the path with `LLM_BASE_URL` on a dead port and read the database back.** Not a code
review. Two of the three findings below were invisible to reading, and one of them destroys data —
`extract_contact_info` did exactly what its contract said, and the contract was the problem.

| # | Call site | Durable before the call? | On failure, the user gets | Gap |
|---|---|---|---|---|
| 1 | `transcribe_audio_file` | ✅ the recording row is written by the upload path first | The transcription is absent and the description slot stays empty; the task returns FAILED | ⚠ **Unverifiable in the field** — voice is not live (Q-13.2). Signature-correct and unit-tested since DPG-14.3, nothing more |
| 2 | `extract_contact_info` | ✅ complainant row exists | ✅ **Now: nothing is written.** The contact details survive | ✅ **Was: the stored phone number was overwritten with `""`** (**D-33**, fixed) — ⚠ **but on a path with no production caller** (**D-37**, **D-35**). Real defect, no complainant harmed |
| 3 | `extract_all_contact_info` | — | — | ⚠ **No caller at all** — and neither has site 2, in production. Both are in every inventory this sprint wrote (**D-35**, **D-37**); DPG-19 corrects the privacy assessment and decides their fate |
| 4 | `classify_and_summarize_grievance` | ✅ (row written, then `.delay()`) | ✅ **Now: the task fails honestly** and the row stays `pending` · ⏱ 20 s poll, then the localized "not enough information" path | ✅ **Was: recorded as `LLM_generated` — a SUCCESS code — with an empty summary** (**D-32**, fixed) · ⚠ `LLM_failed` is still unreachable: nothing retries, so the "after retries are exhausted" guard never fires (**D-34**, deferred) |
| 5 | `translate_grievance_to_english_LLM` | ✅ grievance stored; translation is enrichment | No English record; the officer sees the Nepali original. Task returns FAILED | ⚠ Raises `UnboundLocalError` rather than the documented `ValueError` on pre-call failures (**D-29**, deferred to T-34-b — the fix makes a PII-leaking message reachable) |
| 6 | `detect_sensitive_content_llm` | ✅ | Detection falls back to the deterministic path; the complainant sees no difference | ✅ **Fail-open justified, and re-verified here** (Q-14): `backend/shared_functions/keyword_detector.py` imports nothing LLM-related and runs as **slot validation inside the conversation** (`base_mixins.py:172` → `actions/services/seah/sensitive_detection.py`). ⚠ **The justification depends on that staying true** |
| 7 | `translate_to_english` (ticketing) | ✅ the note is stored first | The note stays in its original language; `payload.translation_en` is absent | — |
| 8 | `generate_case_findings` | ✅ events and cache are stored first | No AI summary on the ticket; the officer reads the timeline | — the task returns `llm_error` and the officer sees an empty summary, not an error |
| 9 | `generate_resolved_case_summary_llm` | ✅ the resolution event is already recorded | The complainant **is** told the case is resolved (that notification comes from the resolve action, not this task) but gets **no closure-document link** | ⚠ `generation_status` is set to `llm_failed` and nothing retries it — the closure document stays unbuilt until someone re-triggers. **D-36**, logged |

**The probe:** `GET /health/llm` on the backend API — host only, never the key, reachability
separate from auth (a 401 means *reachable*), and `scope: "probe-only"` because it observes its own
requests, not the workers'. **Not wired into any container healthcheck**, and a test reads the
compose files to keep it that way: an LLM outage must degrade classification, never restart the
chatbot.

**The dead-port runs, recorded as the spec asks:**

| Grievance / complainant | Before | After | Verdict |
|---|---|---|---|
| `DPG15-c2b3e821` | `pending`, empty | **`LLM_generated`**, empty summary, `error: "Connection error."` beside `status: "SUCCESS"` | ❌ the finding (D-32) |
| `DPG15-ed2c9eb5` | `pending`, empty | `pending`, row intact, task returns `failed` with the real reason | ✅ after the fix |
| `DPG15C-ef87917d` | phone `+9779841234567` | **`''`** — erased, task reported SUCCESS | ❌ the finding (D-33) |
| `DPG15C-ef87917d` (re-run) | phone `+9779841234567` | `+9779841234567` — untouched, task returns `failed` | ✅ after the fix |

---

## Commit log

> ⚠ **This table stopped at 2026-08-19 and was back-filled on 2026-08-27**, which is the convention
> slipping, not a gap in the record: the **Status board** above was kept current at every commit and is
> the authority. The Sprint 2 rows below are therefore deliberately terse — they say what landed and
> point up, rather than restating a row that already exists. **The habit is the thing to fix**
> ([`06_documentation_lifecycle.md`](../../engineering/06_documentation_lifecycle.md) §3c.1): a log
> written weekly is a log written from memory.

| Date | Ticket | Commit | What landed |
|---|---|---|---|
| 2026-08-27 | **D-58** | `efb1abe9` · `ca5e3c3e` | **The taxonomy is authored twice, and the copy that seeds every database is not the one anybody edits.** The CSV and `ticketing/constants/grievance_categories_default.json` had drifted to **64 fields** — D-49's `high_priority` repair and D-51's six Road Hazard rewrites were both CSV-only, so no seeded database ever received them, and one `description` held the literal string `"True"`. `backend-tests` had been red on `test_the_seeded_taxonomy_matches_the_authored_csv` **every run since 08-23**, prescribing a re-seed, which re-applies the stale JSON. Fixed by `scripts/ops/sync_category_catalog_json.py` + `tests/repo/test_category_catalog_sources.py`, which compares the two **authored files** so drift fails before a database exists. ⚠ Still owed: one source, not two in sync. Same day, `ca5e3c3e` corrected three claims in the privacy assessment's §1.4 — a test cited by the wrong filename, a log pin that does not exist, and "fail the build" written for a reader who is a lawyer |
| 2026-08-25 | **D-51 / D-55** | `921274ca` | **An invented category no longer reaches storage — and every Nepali session was storing one.** Three tiers in `backend/services/category_resolution.py`: the live catalogue reaches the provider as a per-call JSON-Schema enum (nothing frozen, so the admin-configurable taxonomy is untouched), then a fold-and-resolve by **leaf** (unique across the taxonomy, so the repair is a dictionary lookup rather than a second model call on the interactive path), then drop-and-log, promoting the top alternative if the item would otherwise end with no category. **Both** lists are resolved now — `grievance_categories_alternative`, which the complainant is *offered to pick from*, had never been validated at all. **D-55, found while fixing it and worse:** `categories_in_local_language` looked a category's **leaf** up in a catalogue keyed `Classification - Leaf`, so **every Nepali session stored `Air Pollution - Air Pollution`** — outside the taxonomy, invisible to every filter. Not 4 items in 105; every Nepali grievance. ⚠ The fix hid its own measurement, so `InventionMeter` reads the rate from the **resolution log** instead, pinned by a test that reddens if either log line is reworded |
| 2026-08-24 | **messaging** | `fc12b1b8` | **The AWS SNS SMS fallback is deleted.** SMS providers serving Nepal must be in Nepal; the SNS path could only format PH numbers anyway. With no DOIT token the provider now resolves to `disabled` and sends nothing — **that is intended**, and there is deliberately no cross-border fallback |
| 2026-08-21 | **D-49 / D-51** | `1d794c6c` · `71a91b8a` | **The taxonomy answered back.** D-49: four categories had silently lost the `high_priority` they were authored with — including the dust complaint and the SEAH route — from a CSV column shift read positionally. D-51: six `Road Hazard - *` categories added and the classification prompt cut **~70%**, after both `gpt-5-nano` and `gpt-oss-20b` invented the *same* `Road Hazard - Dust`, which made it a **taxonomy** finding rather than a model one. ⚠ Neither reached a seeded database until D-58, six days later |
| 2026-08-20 | **DPG-20…25** | `7f2408e6` · `2bdc9e7e` · `747916a0` · `97e67bbe` · `aee9046c` · `6675b209` · `5a4a95fa` | **Sprint 2's measuring apparatus, in one day — and it measured the closed column only.** The benchmark set (105 items, 8 gate fixtures, CC0-1.0); `llm_smoke.py` probing six capabilities per model and verifying every licence **from the model card at probe time**; `llm_benchmark.py` running the product's own call path, scoring set-level and recall-first; the shortlist as **data** in `llm_candidates.json`; the fifth CI job with its empty-gate guard; and `vllm-deployment.md`, whose costing **changed DPG-25's conclusion** — T1 wins by three to four orders of magnitude, so T2 is a sovereignty decision, not a cost one. Findings: **D-49, D-50, D-51, D-52, D-53, D-54**. ⚠ `5a4a95fa` retracted one of this sprint's own acceptance criteria — *"set a hard token cap"* was **unactionable** on a personal free account, and the header now states what actually bounds the spend. **Detail per ticket is in the Status board; it is not repeated here** |
| 2026-08-19 | **DPG-15b** | _(this branch)_ | **The wait was lowered, not raised, and failure finally reaches a terminal state.** `CLASSIFICATION_WAIT_SECONDS=30` from the registry (the literal was 20); the **first attempt gets 30 s and retries get 120 s**, because a person is waiting on the first one and nobody is waiting on the fourth — the two numbers were 20 vs 120 before, so the conversation gave up six times over before one attempt was due to finish. **D-34 closed and verified against the database**: with the endpoint dead the row now reaches `LLM_failed`, and **the poll returns in 0.19 s instead of running its full budget** — the wait ends on knowledge, not on the clock. Three messages in the review step: ready · will not arrive · **not ready yet**, which used to render nothing at all. Two findings on the way: **D-45** (no task has ever had its retry config applied — 180 s instead of 2 s, and no `autoretry_for`) and **D-46** (my first fix reproduced D-34 one layer up, because `self.retry(exc=…)` re-raises the original exception instead of `MaxRetriesExceededError`). ⚠ **And one of my own tests was decorative**: hard-coding the poll deadline left it green, because it asserted the registry *returns* 30 s rather than that the poll *uses* it. Replaced with a behavioural one that drives the poll and measures the wait. Mutations checked red: deadline hard-coded · first attempt given the background timeout · retry-gated terminal write restored · review step silenced · terminal short-circuit removed |
| 2026-08-18 | **DPG-18 §18.2** | _(this branch)_ | **Two models: `whisper-1` for transcription, `gpt-5-nano` for all seven text tasks** (Q-21). Eight keys, two values — the keys stay, because they are the seam that lets a task be moved back without touching code. ⚠ **The one live behaviour change is SEAH detection**, moving off a July-2025 `gpt-3.5-turbo`, so it was **verified against the live provider before shipping**: 4/4 — both harassment cases `detected: True, level: high`, dust and a land dispute correctly not flagged. **Second dividend: `json_schema` everywhere.** Three of the four sites on a weaker rung were there because of the *model*, so the ladder goes back to being a provider fallback for the open configuration rather than a workaround for four OpenAI vintages. The per-task `STRUCTURED_*` vars become empty overrides. ⚠ **D-44** — during that verification, fail-open told me nano could not detect harassment. It was a 401 from my own iteration container. A config error and *no harassment here* are the same observation from outside |
| 2026-08-18 | **DPG-18** (layer) | _(this branch)_ | **One entry point per surface, and the shaping shared between them.** A call site used to carry five concerns; three of them vary by provider and none varies by call site, which is the test for what moved. `call_llm(task, messages, schema=…)` on each surface; `request_for()`, `augment_prompt_for_schema()` and `parse_response()` in the registry. **`ModelProfile`** is the part the measurements forced (D-40): structured-output rung, whether the model accepts `temperature` at all, **the name of its token-cap parameter**, and its **reasoning overhead** — `gpt-5-nano` refuses `temperature`, refuses `max_tokens` by name, and returns empty content if the cap does not budget ~4,000 reasoning tokens. Unknown ids (every open-weights model) fall through to a conservative default. **The `prompt` rung is now generated from the schema**, replacing nine hand-written 'return strict JSON' paragraphs that had drifted into three phrasings. **Truncation is a failure**: `finish_reason: "length"` raises instead of being parsed as an empty answer — the exact route by which a complainant was told their case was resolved and never sent the closure document. ⚠ **And the migration introduced a leak I caught with a test**: `LLMParseError` first interpolated pydantic's `ValidationError`, which embeds `input_value=…` — model output derived from the grievance — into a message that reaches the Celery log. It now reports the *shape* of the failure (field paths and error types), never the reply. The SEAH clamps became **structural and forgiving**: an out-of-range `level` is normalised rather than rejected, because rejecting the reply on that path fails open to `detected: False` — a bad label becoming a missed report. Mutations checked red: temperature sent to a `gpt-5` model · the reasoning overhead dropped · truncation parsed as empty · the prompt fallback hand-written · a request built at a call site |
| 2026-08-18 | **DPG-19b** | _(this branch)_ | **Declared the parked voice-notes flow, and pinned the distinction that was missing.** `PARKED_TASKS` in `registered_tasks.py` names the three parked LLM tasks with the decision that parked each — CB-01 for transcription, and the two that consume a *transcription of spoken contact details* / feed the English record. Docstring labels on each parked function say **parked, not dead**, and where the live equivalent is (typed phone numbers are validated deterministically; officers read English from ticketing). **The pin (T-19b-b): every LLM task is enqueued in production or declared parked with a reason — and `test_tasks.py` does not count as production**, since treating a manual script as a caller is exactly what made four paths look live for eleven months. Two supporting pins: a parked entry must name a real task and give a reason longer than an excuse, and the parked set is exactly the voice flow — **if the classification or SEAH task ever appears there, the product has stopped classifying and a test says so**. Documents corrected: this spec's §0.3 inventory (nine exist, **five run**), `README.md`'s premise table, and — the acceptance item that was in no spec at all — **the three SEAH signals** written into `06_llm_service.md`, because Q-14's fail-open decision rests on the deterministic one existing. ⚠ **D-43**: `LLM_services.py`'s anchors were re-pointed a fourth time today. Recorded with a cheap fix for T-03's owner — anchor on the enclosing function name, not the line. Mutations checked red: a task neither enqueued nor parked (3 tests) · a parked entry with a one-word reason · the live classification task parked by mistake |
| 2026-08-18 | **DPG-19** | _(this branch)_ | **The guardrail, and the distinction it protects.** `MIN_CLASSIFY_CHARS=25`, measured on **whitespace-stripped** length, in the registry so it can differ by language later (25 Devanagari characters is a sentence; 25 English ones are four words, and the code says so). Below it the model is **not called**: `skipped: "too_short"`, no `status` key, so the task layer does not read it as a failure. At or above it, **an empty answer stays an answer** — the `"{}"` sentinel produces the localized *not enough information* response, is logged with the **length only**, and `is_failed_classification()` stays False. That last property is pinned first, because the cheapest way to build a length gate is to start treating every empty answer as an error on the way past. **D-29 closed in-sprint**: the translation error now names the `grievance_id` plus at most three words (60-char cap), never `input_data` — the owner's trim, which removed the reason it was deferred to Sprint 3. ⚠ **D-42** — one mutation stayed green and exposed my own decorative test: the `result` pre-binding fixed nothing once the message stopped reading it. Binding deleted, honest mutation recorded. Mutations checked red: the gate bypassed · **an empty result made a failure** (the regression this ticket could have introduced) · raw length instead of stripped · `input_data` restored to the message · the old message restored (2 tests) |
| 2026-08-18 | **DPG-15** | _(this branch)_ | **The audit was supposed to confirm what was already built. It found two faults, one of which destroys data.** Method: point `LLM_BASE_URL` at a dead port, run the real Celery tasks against the real database, read the rows back. **D-32** — a failed classification was written as `LLM_generated`, a *success* code, with `error: "Connection error."` beside `status: "SUCCESS"`; the task's only guard was `if not values:` and the failure dict is truthy. **D-33** — `extract_contact_info_task` reported SUCCESS and **overwrote a stored phone number with `""`**; the complainant had typed it, the model was down, the number was gone. Both fixed, both re-verified against the database. ⚠ **D-33 is a fault no characterization test could have caught**: the service function did exactly what its contract said, and DPG-14 had just made that contract *reachable*. The contract was harmful one layer up. That is the case for driving the path rather than reading it. `GET /health/llm` reports host only, treats reachability as a network property (a 401 means *reachable*), labels its `last_success_at` `scope: "probe-only"` rather than implying it observes the workers, and returns **200 even when degraded** — a non-200 invites someone to wire it into a healthcheck, and a test reads the compose files to make sure nobody has. Three absences logged rather than fixed: `LLM_failed` unreachable (**D-34**), `extract_all_contact_info` has no caller (**D-35**), closure documents never rebuilt after an LLM failure (**D-36**). Mutations checked red: the failure dict treated as success again · the key added to the probe response · the probe wired into a container healthcheck |
| 2026-08-18 | **DPG-16** | _(this branch)_ | **The configuration surface — which *is* the indicator-4 evidence.** `.env.example`'s single `OPENAI_API_KEY=` line replaced by 23 annotated variables generated from `declared_env_vars()`, pinned **in both directions**: an undocumented variable fails the build, and so does a stale one (the rot case, which reads as configuration and does nothing). `.env.open` and `.env.openai` committed — the same system pointed at two providers, so `diff` is the answer to indicator 4 — with **empty secrets**, a test that they carry none, and a test that they declare **identical variables** (differ in values or the diff stops meaning what it claims). Verified against `.gitignore` rather than assumed: `.env` is an exact match, not a glob. ⚠ `.env.open`'s model ids are **placeholders** and say so in the file, in `open-model-configuration.md`, and in this row — DPG-23 has not measured them, and Q-19 gates that. **Verified by exec, not by reading compose**: all five LLM-touching services rebuilt and each asked what it resolves — identical answers — plus one run with `-e LLM_BASE_URL=…` proving the *plumbing*, since matching defaults would prove nothing. No service hardcodes an LLM variable in its `environment:` block. Mutations checked red: a variable the code reads and the template omits · a stale variable left in the template · a key pasted into a committed template |
| 2026-08-18 | **DPG-13** | _(this branch)_ | **Structured output, and the silent failure it removes.** Seven Pydantic models in two schema modules (one per surface — the service-layer boundary stands); a strict `json_schema` wherever the model supports it; the degradation ladder implemented **once**, in the shared registry, with the rung logged per call. ⚠ **The spec's endpoint-level capability flag was wrong** and it would have broken three call sites — see **D-31** and the measured table now in `06_llm_service.md`. **`parse_llm_response` no longer returns `{}` on malformed JSON**: it raises `LLMResponseParseError`, so on the primary AI path a parse failure (`status="error"`) is finally distinguishable from the model saying *not enough information* (the localized fallback). Its log carries the response **length, not the body** — one item off Sprint 3's T-34-c list, for free. Category values validate against the **live catalogue** built from `CLASSIFICATION_DATA` at call time, and unlisted values are **logged, not rejected**: freezing an enum would break the admin-configurable taxonomy's resync path, and a mis-named category is not worth discarding a complainant's classification over. Mutations checked red: a site reverted to `json_object` (2) · `model_validate` swapped for `json.loads` (1 — and its log printed `['D','u','s','t',…]`, the exact defect validation prevents) · a ladder rung skipped (3) · the bare `except JSONDecodeError: return {}` restored (3) · a `Literal[...]` of categories frozen into the schema (3) |
| 2026-08-18 | **DPG-12** | _(this branch)_ | **The ticketing surface joins the registry — all five sites, not the three the guide knew about.** `_get_client()` builds from `llm_endpoint()`; `_MODEL_STANDARD`/`_MODEL_SEAH` deleted; the three call sites resolve `model_for(findings_task(is_seah))`. Then the copies: **`resolved_summary_builder`** (a **persisted provenance field** — miss it and every resolved case records a model that did not run it), the **log line** in `tasks/llm.py` that re-derived the ternary as a literal, and `tasks/llm.py`'s reach into `_llm._MODEL_SEAH` — **invisible to `grep "gpt-"`**, so this ticket's own acceptance grep would have passed with it untouched. `ticketing/api/routers/tickets/summary.py`'s **endpoint description** no longer names a model: it is published in the OpenAPI document, and indicator 5 is graded on that. `TicketingSettings.openai_api_key` removed, with the reason left in its place. T-17-c now parses the AST rather than grepping — the only check that could have caught the privates reach — and T-17-d constructs **both** factories from one env change. ⚠ **A sixth disagreement, found by reading both:** `11_llm_pipeline_policy.md` documented note translation as `gpt-4o-mini`; the code called `gpt-4`, and had since the pipeline was written. Corrected, and the table now names **registry keys** so the failure mode cannot recur. Mutations checked red: `_MODEL_STANDARD` restored in the builder (2 pins) · base URL hard-coded in the ticketing factory (2) · the SEAH split collapsed onto one key (2) |
| 2026-08-18 | **DPG-11** | _(this branch)_ | **`backend/services/llm_client.py` — the chatbot surface's only client constructor, and it names nothing.** Two clients (chat + ASR, separate endpoints and deadlines), lazy and cached, built from DPG-17's registry. Gone: the **import-time** client that swallowed its own failure into `client = None`; the **shadow client** inside `classify_and_summarize_grievance` with its own timeout and a guard that could never fire (DPG-14.2); `load_dotenv('/home/ubuntu/nepal_chatbot/.env')`, an absolute path to a directory that exists in no container. All six call sites resolve `model_for(...)`, and each keeps its own failure contract — raise / sentinel dict / fail-open — because unifying three idioms is a behaviour change and not this ticket. **No key still means no client** (not a keyless client that 401s per request), which is what keeps those contracts reachable. T-11-d pins it: no model literal and no `OpenAI(` outside the registry and the factory, AST-parsed, docstrings exempt. Mutations checked red: base URL hard-coded in the factory · ASR pointed at the chat endpoint · a `gpt-4o` literal re-introduced · the shadow client re-introduced |
| 2026-08-18 | **DPG-17** | _(this branch)_ | **`backend/config/llm_config.py` — the one registry both surfaces read, and neither owns.** 8 task keys (`classify`, `extract`, `translate`, `detect`, `asr`, `ticket_translate`, `ticket_findings`, `ticket_findings_seah`), 2 endpoints (chat + ASR, ASR falling back to chat per field), 18 env vars, per-task timeouts, `findings_task()` as the **only** SEAH ternary, `declared_env_vars()` for DPG-16 to generate `.env.example` from. `pydantic-settings>=2.0` moved from `requirements.grm.txt` to `requirements.txt` — with the reason written into **both** manifests, and the import verified in a **rebuilt** `celery_llm` container, not just in the file. `dependency-licenses.md` amended (attribution moved; counts unchanged, and that is stated rather than left to be rediscovered). ⚠ **Defaults are today's models, endpoint and timeouts** — the open-default flip is a Sprint-2 commit against this one file (Q-10). An open base URL with `gpt-4o` model ids is a repository that cannot answer a request on a fresh clone. Mutations checked red: default model changed · a task key pointed at the wrong model attribute · a first-party import added · the alias warning removed |
| 2026-08-18 | **DPG-14** | _(this branch)_ | **Two defects fixed, one non-finding recorded, one new defect found.** **14.3** — confirmed in-container against `openai==1.70.0`: `Transcriptions.create` takes `language`, declares its parameters explicitly, and accepts no `**kwargs`, so `language_code=` made **every transcription raise `TypeError`**. Fixed, with T-14-c binding the request against the **installed SDK signature** (a mock accepts any keyword — which is why nothing caught it). **D-28** — `extract_contact_info`'s declared `{field_name: ""}` made reachable: `field_name` and `response` now resolve before the `try`. The caller-error message names the **keys**, never the values, because it reaches the Celery log. **14.1** — three live `gpt-5-nano` calls: `finish_reason: stop`, no truncation. The suspicion is a corrected non-finding; the model choice is now recorded in the live spec with its measurements. **14.2** — deferred to DPG-11 by design; the factory deletes the shadow client and its dead guard. **D-30** — 14–20.5 s latency against a 20 s poll deadline: the complainant can see the empty-classification path on a **successful** classification. Mutations checked red: ASR kwarg reverted (2) · pre-try `response` binding dropped (2) · values restored to the caller-error message (1) |
| 2026-08-18 | **DPG-10** | _(this branch)_ | **The test net — the first commit of Sprint 1, and it changes no production code.** `tests/backend/test_llm_services.py` (44 assertions) + `tests/ticketing/test_llm_client.py` (21), green against **unmodified** code. All nine call sites pinned for request shape (model name + `response_format`), happy-path parse and failure contract; the `client is None` guard pinned per function; `parse_llm_response` covered on all four branches and five language codes; the SEAH level clamp pinned. Mocked at the **`OpenAI` class**, not the module client — otherwise classification's shadow client escapes the net. **6 mutations checked red**: `gpt-5-nano`→`gpt-4o-mini`, clamp removed, `en` fallback string changed, SEAH split collapsed, `return None`→`return {}`, translation's client guard removed. Found **two** latent `UnboundLocalError`s (D-28, D-29) where the spec predicted one |
| 2026-08-18 | **T-03** | _(this branch)_ | `tests/repo/test_doc_code_refs.py` — the documentation reference pin, written **because of** this sprint's own churn. Parses every `file.py:NNN` citation from the live docs (329 distinct, `archive/` excluded): file exists · line in range · **20 load-bearing citations land on the expected token**. 23 assertions, **4 mutations checked** (anchor shifted by one, dangling file, line past EOF, `KNOWN_ABSENT` file resurrected). `tests/repo` 46 → **69**. ⚠ Sprint 1 turns T-03-c red on purpose when DPG-11/12 move the nine call sites — that is the reminder to re-point the docs, not a reason to edit `ANCHORS` alone |
| 2026-08-18 | **DPG-04** | _(this branch)_ | `docs/dpg/privacy-assessment.md` — **13 data-flow legs verified against the code**, assessed against Nepal's Individual Privacy Act 2018 · mandatory honesty marker (AI author, no legal review — Q-07) · **17-item findings register**, 3 of them new and previously unknown (F-2 encryption-at-rest fails open, F-3 unsalted phone hashes, F-4 unencrypted-by-default backups) · third-party PII position stated as the load-bearing section (T2 parked ⇒ egress is permanent) · retention/deletion/breach written as `⚠ Not built` where they are · cross-linked from `13_security.md`; evidence-pack rows 5/7/8/9a now point at real files |
| 2026-08-18 | **DPG-05** | _(this branch)_ | `SECURITY.md` (private channel, named recipient, 3/10-working-day targets, SEAH-aware in/out-of-scope, known-and-accepted list) · `CONTRIBUTING.md` (points at the rules, does not restate them) · `CODE_OF_CONDUCT.md` (Covenant 2.1 + a grievance-confidentiality clause) · `.github/ISSUE_TEMPLATE/` with `blank_issues_enabled: false` and a security **redirect** contact link · `PULL_REQUEST_TEMPLATE.md` with a sensitive-path section · governance/versioning deferred + logged (D-08) |
| 2026-08-18 | **DPG-06** | _(this branch)_ | Root `README.md` **rewritten from the compose files** — Rasa service (:5005) and Action Server (:5055) rows gone, `rasa-sdk`'s real type-shim role stated in one line, **13-service table verified against `docker compose config --services`**, folder tree matched to disk, branch corrected to `integration/stage`, **three dead environment URLs replaced with the two real hostnames** (D-10), Contributing/Security/Roadmap section added · `rasa_chatbot/` removed from CLAUDE.md §Service boundaries (P-13 closed) · `NOTICE` stale-claim fix (D-13) |
| 2026-08-18 | **DPG-02** | _(this branch)_ | `docs/dpg/dependency-licenses.md` — 153 packages, in-container scans · `ops/licences.py` + `licence_scan()` scheduled nightly at 01:50 (Q-06) · `pip-licenses` declared · pin-drift + classifier tests (`tests/repo/`, 16 assertions) · **`redis:8.10` pin verified live** — the caveat on commit `800aa803` is closed · npm manifests now declare Apache-2.0 |
| 2026-08-17 | **DPG-01** | _(this branch)_ | `LICENSE` (canonical Apache-2.0, 202 lines, md5 `3b83ef96…`) · `NOTICE` with an explicit pending-holder disclosure · `scripts/ops/add_spdx_headers.py` (idempotent, `--check` mode) · **583 files stamped** (408 `.py`, 175 `.ts`/`.tsx`) · `tests/repo/test_spdx_headers.py` (T-01, 9 assertions) · `tests/repo` wired into `ci.yml` |

| Date | Ticket | Commit | Summary |
|---|---|---|---|
| | | | |

---

## Sprint 0 — definition of done

Against [`01-licensing-and-governance-spec.md`](01-licensing-and-governance-spec.md) §Definition of done:

- [x] **DPG-01, DPG-02, DPG-05, DPG-06 landed** — ⚠ **but not "green in CI", and that phrase was false when I wrote it.** The branch was pushed 2026-08-18 and CI ran for the first time: `docs-links` ✅, `webchat-checks` ✅, **`ui-checks` ❌**, **`backend-tests` ❌**. ✅ **`tests/repo` — the DPG pins — passes**, among 1166 passing tests. **The two failures are pre-existing and identical in shape on `integration/stage`, which has failed all 10 of its last runs since 2026-08-08** — verified step by step, not inferred. Nothing in this sprint made CI red; it was already red, which is why nobody noticed the sprint's own branch prefix was missing from the push triggers (D-24). Tracked: [`followups/ci-has-been-red-for-ten-days.md`](followups/ci-has-been-red-for-ten-days.md) (D-26)
- [ ] **DPG-03 request sent** — ⏸ **not sent to ADB OGC.** Named as a blocker with its clock above. The sprint is not blocked on the response, but this box stays unticked until the letter goes
- [x] **DPG-04 written** — `docs/dpg/privacy-assessment.md`, with a named author and date, and an explicit statement that no legal review was performed
- [x] **`README.md` and CLAUDE.md make the same claim about Rasa as the compliance briefing** — verified by grep: zero occurrences of `rasa` in CLAUDE.md, and the only `5005` in `README.md` is the sentence explaining that the row was removed
- [x] **`docs/dpg/` exists and is indexed from `docs/README.md`** — four files, each with its own row
- [x] **Evidence-pack rows 2, 3, 7, 9a point at real files** — 2 ✅ (`LICENSE`, `NOTICE`, `dependency-licenses.md`), 7 and 9a ✅ (`privacy-assessment.md`), **3 ⏸ still has no file and cannot until ADB OGC responds** — rows 5 and 8 were updated too
- [x] **Every deferral logged in `followups/` + `TODO.md`, same commit** — ✅ **now complete.** Three followups: `ci-untested-root-test-files.md`, `governance-and-versioning-policy.md`, and `storage-layer-privacy-defects.md`, each with a `TODO.md` row. The last one closed the exception this line used to carry: D-19's three privacy defects were recorded only in the assessment's findings register, which is the half-logged pattern D-16 exists to name

### Deviation disposition — 21 rows, all triaged (2026-08-18)

| Disposition | Count | Rows |
|---|---|---|
| ✅ **Closed** — fixed in place, corrected, or superseded | **19** | D-02 *(qualified)*, D-02b, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-16, D-17, D-18, D-20, D-21, D-22, D-23, D-24 |
| 🔵 **Open — tracked in `followups/` + `TODO.md`** | **3** | **D-01** (27 test files CI has never run) · **D-19** (three storage-layer privacy defects — **go-live deadline**) · **D-26** (CI red for ten days; blocks D-02b's gate) |
| 🔵 **Open — config, no followup yet** | **1** | **D-25** (`OPS_DB_PASSWORD` unset ⇒ ops writes nothing) |
| ⏸ **Blocked on an external answer** | **1** | **D-15** (governance model + versioning — Q-08-01, and DPG-03) |

**Nothing is untriaged.** Three 🔵 rows have followup docs with a measured definition of done; D-25 is a
one-line config fix awaiting the ops owner; the ⏸ row waits on someone else. Of the 19 closed,
**ten are corrections of this sprint's own errors** (D-04, D-05, D-08, D-09, D-10, D-11, D-22, D-23, D-24,
and D-02's qualification) — worth stating plainly rather than burying, because the rate at which a
process catches itself is the only evidence that it does. **D-24 and D-26 were both found by doing the
thing rather than reasoning about it**: pushing the branch and reading the result

---

## Sprint 1 — definition of done

Against [`02-llm-agnostic-spec.md`](02-llm-agnostic-spec.md) §Sprint 1 acceptance criteria. Ticked
means verified, not intended.

- [x] **One config file.** Every model name, endpoint, timeout, retry count and capability flag is
      declared exactly once, in `backend/config/llm_config.py`; both surfaces read it and neither
      restates it — pinned by an AST parse, not a grep (T-17-c)
- [x] **No `OpenAI(` outside the two factories** (T-11-d)
- [x] **No hard-coded model name anywhere**, and a test enforces it — including the two forms
      `grep "gpt-"` could not see: a reach into another module's privates, and an OpenAPI
      endpoint description
- [x] **One `LLM_BASE_URL` change moves both surfaces — proven by test** (T-17-d), not by inspection
- [x] `.env.example`, `.env.open`, `.env.openai` present, documented, secret-free, and pinned against
      the registry in both directions
- [x] **All seven structured calls carry a schema *where the model supports one*, with Pydantic
      validation and a configured degradation ladder.** ⚠ Stated precisely because the spec's version
      — "all seven use `json_schema`" — is not achievable against today's models and would 400:
      `gpt-3.5-turbo` rejects schemas and `gpt-4` rejects JSON mode entirely (**D-31**, measured)
- [x] **Intake survives a dead endpoint, as an automated test** — and driving it for real found two
      faults the test alone would not have (D-32, D-33)
- [ ] **The full suite passes against both configurations.** ⏸ The second configuration is DPG-21
      (Sprint 2). Everything here runs against mocks plus the current provider
- [x] `docs/services/06_llm_service.md` and `docs/deployment/11_llm_pipeline_policy.md` reconciled
      with the code — the second one had documented the wrong model for note translation since the
      pipeline was written
- [x] Every deferral logged in `followups/` **and** `TODO.md`, same commit — D-29, D-30, D-34, D-36
- [x] **No increase in the failure baseline**, measured locally in-container at every commit:
      16 failed / 1170 passed → 16 failed / 1316 passed. ⚠ **Not yet confirmed in CI** — this branch
      has not been pushed, and D-26's lesson is that a local green is not a CI green
- [x] Every new `.py` file carries an SPDX header (590/590)
- [x] `privacy-assessment.md` legs L4 and L5 re-pointed — **three times**, as the call sites moved
      under them — and `dependency-licenses.md` reconciled with the `pydantic-settings` move

**What the sprint changed that nobody planned for.** Six of the eight tickets found something the
spec did not know: a second `UnboundLocalError` (D-29), classification latency brushing the poll
deadline (D-30), the capability flag on the wrong object (D-31), a failure stored as a success
(D-32), a phone number erased by an outage (D-33), and three terminal states nothing reaches
(D-34/35/36). Five of those six came from **running** something — a live call, a dead port, a real
database — rather than from reading code. That is the transferable lesson for Sprints 2 and 3.

---

## Sprint close checklist

- [ ] All 27 tickets ✅, or ❌ with a logged reason
- [ ] Every test in [`TESTS.md`](TESTS.md) written and mutation-checked
- [ ] Every question in [`QUESTIONS.md`](QUESTIONS.md) answered **and its answer written into the spec**
- [ ] All P-## findings above dispositioned (fixed, or moved to a followup + `TODO.md` row)
- [ ] `docs/dpg/` evidence pack complete; every indicator row points at a real file
- [ ] Live specs updated with honest verification markers (see [`README.md`](README.md) → domain specs table)
- [ ] Indicator-4 submission text checked **clause by clause** against reality before pasting
- [ ] Summary written as `docs/sprints/2026-08_dpg_llm_independence.md`
- [ ] This folder moved to [`../archive/`](../archive/); [`../README.md`](../README.md) tables updated
- [ ] `docs/PROGRESS.md` and `docs/TODO.md` updated
