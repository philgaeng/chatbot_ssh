# Tier-3 Structural Sprint — Progress

> Update at **every commit** on `dev/tier3-structural`. Status values: `todo` · `in_progress` · `blocked` · `review` · `done`.
> Sprint definition: [README.md](README.md) · **Reassessment (read first): [00-reassessment.md](00-reassessment.md)** · Source review: [`../../reviews/devils_advocate_codebase.md`](../../../reviews/devils_advocate_codebase.md)

## Ticket status

| ID | Title | WS | Phase | Status | Commits | Notes |
|---|---|---|---|---|---|---|
| T3-01 | Explicit utterance keys + repair 3 non-resolving keys + delete dead `get_buttons` | A | **1** | **done** | `810304b1` | **§0 gate answered (D-13): Phase 1 CONFIRMED — two live bugs, not latent.** `form_seah_1` was a **HTTP 500** on SEAH intake; the category-review site was **silent** (0 messages + wrong slots). Site 4 proved **dead** → logged (D-22), not fixed. Introspection deleted, `key` now required; dead `get_buttons` + dead `check_form_function_name` deleted; swallow narrowed. **Only 1 new mapping entry needed — sites 2/3 reuse existing reviewed copy (D-23).** 15 new call-site tests (10 red pre-fix). Suites 188/1 vs 173/1 baseline. Manual EN/NE sweep pending-human (D-24). Spec [01](01-conversation-layer-spec.md) §1. |
| T3-02 p1 | `run_flow_turn`: delete `form_dust` + `submit_grievance`, add terminal `else` | B | 1 | **done** | `27cabcad` | **Live bug fixed (D-08).** Both deletion proofs re-run and still hold — each string appears repo-wide only as its own `elif state ==` comparison, and the precondition holds (nothing outside `state_machine.py` writes `session["state"]`; only tests do, none of them these two). `form_loop.py:332`'s `"form_dust"` is an `active_loop` value — different namespace, left alone. No helper stranded by the deletion. Terminal `else` logs at **error** + **recovers** (not just a message: leaving `next_state` on the bad state soft-locks the session). `_recover_from_unknown_state` follows the file's own answer (`_handle_attachment_ids_sync`): no language ⇒ intro, else ⇒ main menu; and takes the in-file handler signature, which is what p4's dict default will want. Copy composed from **existing reviewed strings** (D-23's lesson) ⇒ no new mapping entries, no new AI translations. **Verified red: 7 failed / 1 passed, the 1 being the control.** AST: chain 22 → 20 arms, terminal else present. Suites 196/1 vs 188/1 (+8 = exactly the new file). Spec [01](01-conversation-layer-spec.md) §2. |
| T3-03 | Serialize voice-chunk uploads behind chunk-0's `upload_id` | C | 1 | **done** | `45127407` | **Live bug — fixed.** Queue serialized; `FormData` rebuilt per attempt; `"out of order"` swallow gated on `isStopping`. Server untouched. Harness decision: **(a)** — vitest seeded for `channels/REST_webchat/` + wired into CI (closes D-11). 7 client tests (4 verified red pre-fix) + server contract test. Manual Slow-3G sweep **pending-human** (D-17). Spec [02](02-webchat-voice-spec.md). |
| T3-04 | Unify the PII boundary (backend decrypt → delete workaround → drop key) | D | 2 | **done** | `57acf7ff` · `f0a6c4a9` · `0ca98d85` · `c47fd4a9` | **Binding order followed; commit 1's net green at all four commits — the ticket's invariant.** The review was wrong on diagnosis, location and ordering, and the spec was right about all three: the fix is **one line in `backend/`**, and dropping the key first would have shown "—" on every officer card with no error and no failing test. **D-09 confirmed: nothing imported `pii_vault`** ⇒ commit 1 is a test. **Deviation (D-54): the spec's ciphertext/plaintext parametrize cannot survive steps 2-4** — today ciphertext decrypts, after step 3 it masks; no assertion is true at both ends. Pinned the durable property instead. **Deviation (D-55): step 4's compose/env edit is not implementable** — `DB_ENCRYPTION_KEY` is in **no** compose file and every service shares one `env_file`, so removing it would strip it from `backend`. Deleted the **field** instead — stronger: ticketing has no accessor at all. **Verified E2E in rebuilt containers** (D-56): real API + real ciphertext at rest ⇒ **plaintext**; ticketing's real client ⇒ standard card shows real PII, SEAH masked; `ticketing_api` boots with the env var present but unreadable. Suites: backend 147/1, ticketing 602/5, actions+orch unchanged ⇒ chatbot not regressed. Spec [03](03-pii-boundary-spec.md). |
| T3-06 | Harden the grievance API: authn + authz + read audit + `response_model` | F | **2** | **done** | `96d6eb33` · `aead11eb` · `e5fdc4c4` · `28c73f13` | **§0 gate answered (D-25): `:5001` is NOT internet-open ⇒ Phase 2 confirmed, not an incident.** Read live from the AWS SG, not inferred. Order followed: contract → audit → client-key → authn. **Verified red: unauthenticated the GET returned 200 with `grievance_description`** (D-30). Spec's "commit 3" **split in two** (D-29) — the clients sent *no* key, so one commit couldn't meet its own precondition. `response_model` = 51 fields, `extra="allow"` (a strict model **silently drops** columns — verified); text fields typed `Any` because `_parse_field_from_database` json-parses every string (D-28). Audit = structured log at **INFO** (D-27). Rate limit **deferred + logged** (D-31) ⇒ §6's chokepoint is **2/3 built**. Suites: backend 140/1 (+33), ticketing 549/5 (+5), actions+orch 188/1 (`+15` = T3-01's). **Authz deferred by design** — see note. Manual sweep pending-human (D-33). **Bonus finding: orchestrator `:8000` IS open + unauthenticated (D-35).** Spec [05](05-grievance-api-hardening-spec.md). |
| T3-07 | Amend the data rules to as-built; pin no-FK + no-PII-columns with tests | F | 3 | **done** | `2ad2cf21` · `d6bdd9fa` · `e6a0b473` | **Docs + tests only — no runtime source touched** (verified: `git diff --name-only` has no non-test `.py`/`.ts`). Implements the §6 boundary DECISION. **Inventory re-verified independently: 11 statements / 5 tables / 3 writes reproduces exactly.** 13 guard tests, **every one mutation-verified to go red** (D-41). **Method finding: the SQL collector must scope to `text()` args — prose says *"…fields from `public.grievances`"*, so a FROM-anchored regex finds 13 "statements" where 11 execute (D-42).** `create_scoped_roles.sql` fixed *and* found **non-runnable since it was written** (D-40). Absorbed the `grievance_sync.py` column-list TODO row + 2 stale TODO rows it disproved. Suite 562/5 vs 549/5 (+13 = exactly the new file). Spec [06](06-boundary-policy-spec.md). |
| T3-08 | Test hygiene: host loop, load-dependent assertion, CI coverage | F | 3 | **done** | `ac0b9d29` · `9e74e457` · `d815ddf1` | **Not in the original sprint — opened from T3-07's D-36/D-37/D-44 rows.** The three were one story: *the only tests that exercise the seeded DB were excluded from CI, and the one place they could run (the host) was broken by default, so they rotted silently.* **CI: 364 passed / 390 deselected → 897 passed / 0 deselected (+533 tests, +16s).** Host: `pytest tests/ticketing` **110 failed + 234 errors → 567 passed / 0 failed**, 132s → 14s, no env workaround. Closes [`integration-seed-reconciliation`](../../2026-07_schema_and_legacy_cleanup/followups/integration-seed-reconciliation.md) (open since July — the work was already done, nobody re-enabled the gate). **D-44's published mechanism was wrong and is corrected in D-45.** |
| T3-02 p2 | `run_flow_turn`: characterization tests (`status_check_form` + 3 `add_*` branches) | B | 3 | **done** | `a6519a6b` · `eccbfeb5` | **The gate for p3 — and it earned its keep: it found two bugs.** 12 tests over HTTP (`post_turn` → `/message`), all written **from probes of the real code, not from reading it** — the only honest way to characterize, and how both findings surfaced. The 3 `add_*` branches had **zero** state references in any test (202 lines); `status_check_form` (303 lines, depth 9) was touched only at entry level. **Two bugs pinned, not fixed** (spec: record, don't fix): **D-51** `add_more_info_flow` returns 0 messages when `story_route` is unset, **D-52** an unknown `active_loop` silently runs status form 1 ⇒ 0 messages + wedged session. **⇒ Both are D-08's dead-air class *inside a recognized branch*, so neither p1's terminal `else` nor p4's table can catch them — the pattern is the finding, and a `run_flow_turn` postcondition subsumes all three.** Coverage gap recorded, not papered over: the OTP arm is uncovered (the demo grievance has no phone, so the check short-circuits). Suites 203/1 then 208/1. |
| T3-05 | Extract the 6 remaining settings tab clusters | E | 3 | **done** | `977375f0` · `94e0ab13` · `c2f978df` · `0976ad58` · `e31d472a` · `ba2fe75d` · `4228d72e` · `8c747fcd` | **`page.tsx` 4,372 → 301 lines**, 8 commits, gate green at **every** one (tsc / eslint 0 errors / vitest / build). Both cross-cluster seams resolved as plain imports, as spec'd. **The headline claim does not survive measurement: bundle size did not move — 1,461,565 → 1,461,373 B (−0.01%), settings server chunk byte-identical (D-46).** A verbatim extraction with static imports cannot move it; only `dynamic()` per tab would, which the spec rules out. **Reviewability is the real (and only) win.** Deps resolved by scanning each body against `lib/api`'s exports, not by eye — a guess-based pass missed `getOrgRoles` (D-50). 27 helper tests added — first-ever coverage under `components/settings/`, mutation-checked (closes half of D-12); render tests deferred, **no DOM harness exists** (D-48). Found `ProjectWorkflowSelect` dead → logged, and **extraction silenced its warning** (D-47). Manual sweep pending-human (D-49). Spec [04](04-portal-settings-spec.md). |
| T3-02 p3 | `run_flow_turn`: split `status_check_form` (303 lines, depth 9) | B | 3 | **done** | `7723bdfb` | Gated on p2 as the spec requires: **the characterization net stayed green *unchanged* throughout**, which is what makes this verifiable rather than hopeful. Split along the code's own seam (AST-confirmed, no shared locals): `_status_check_route_intent` (101) + `_status_check_run_active_form` (231), both on the in-file convention with extras keyword-only. `next_state` passed in **and** returned — redundant today (AST proves neither arm reads it) but there so an unassigning path keeps the caller's value rather than depending on my reading of 300 lines being exhaustive. Extracted **by line range, never retyped** (D-50): **all 288 non-blank moved lines reappear verbatim, 0 missing.** D-52 preserved deliberately — moving code and fixing behaviour in one commit is how a refactor hides a bug. **`run_flow_turn` 1,485 → 1,175 lines (−21%)** across p1+p3; chain intact (20 arms + terminal else). Suites 208/1. |
| T3-02 p4 | `run_flow_turn`: handler table | B | 3 | **not done — logged** | `4c454c5a` | **STRETCH, deliberately not started, logged in all three places (D-53).** The interesting part is not that it slipped: **its estimate does not survive measurement.** The spec sizes it *"the cheap, safe part — 22 one-line entries against a proven signature"*, which holds only if every branch already has the signature. p3 extracted **one** (correctly — `status_check_form` is where the risk lives), leaving **19 arms / ~1,058 lines still inline**, ten of them > 40 lines. ⇒ p4 is *"extract 19 more branches, then write a table"* — an **M**, and the table is its last 5%. **It also does not fix D-51/D-52** (the lookup succeeds; the default never fires) — the postcondition does, cheaper. → [`followups/run-flow-turn-handler-table.md`](followups/run-flow-turn-handler-table.md) + TODO row. |

## Baselines at sprint start (`dev/tier3-structural` @ `87fbba86`, 2026-07-15)

Record these now so regressions are attributable:

| Suite | Baseline |
|---|---|
| `tests/ticketing` | 545 passed / 5 skipped / 0 xfailed |
| ↳ **caveat (added by T3-08)** | These are **host** numbers and include `@integration`. **CI gated none of them** until T3-08: it ran `-m "not integration"`, so 390 of the tests this sprint measured against were **ungated**, and `tests/backend` was absent entirely. Post-T3-08 CI runs **897 passed / 7 skipped / 0 deselected**. Reaching the host numbers also needed a `POSTGRES_*=...` prefix (D-36) — no longer. |
| `tests/orchestrator` + `tests/actions` | 173 passed / 1 skipped |
| Portal vitest | 61 passed (8 files) |
| Portal `tsc` | clean |
| Portal `eslint` | 0 errors / 141 warnings |
| Portal `next build` | green |
| `app/settings/page.tsx` | 4,372 lines |
| Portal bundle (T3-05) | **total client JS 1,461,565 B / 38 chunks · settings server chunk 32,853 B · `.next/static` 1,719,279 B** |
| `state_machine.py` / `run_flow_turn` | 2,303 / 1,485 lines, CC 189 |
| ↳ **at sprint close (T3-02 p1+p3)** | `run_flow_turn` **1,175 lines (−21%)**; chain **20 arms** (was 22) **+ a terminal `else`** (was none). Two handlers extracted: `_status_check_route_intent` (101) + `_status_check_run_active_form` (231). **CC not re-measured** — `radon` is not installed and the Docker rule forbids `pip install`ing it to find out; the 189 baseline came from the reassessment's own tooling. **Do not publish a CC delta without measuring one** (D-46's lesson). |

> **Portal bundle baseline added by T3-05** — the sprint README asks it to be measured before/after and the table had no row for it. Method is **not** the spec's (`next build` output): Next 16.2.6 prints no per-route sizes and Turbopack emits no per-route manifest, so this is whole-app (see D-46). Reproduce: `docker build --target builder -t grm_ui_builder:dev ./channels/ticketing-ui`, then `npx next build` in it and sum `.next/static/chunks/*.js` (D-45).
>
> ⚠️ **All 5 portal rows above were re-measured and confirmed exactly at T3-05 (2026-07-15).** Note the two Python rows are only reproducible **against a freshly built image** — the running `ticketing_api` container predated `184bd461` and silently collected **108** tests, not 173, because this stack **bakes source into images (no bind-mounts)**. `make wsl-up` (or `compose build ticketing_api`) first, or the baseline is a mirage. Same family as D-36 / D-44.

## Acceptance checklists

### T3-01 — Explicit utterance keys ✅ DONE

- [x] **§0 reachability traced — verdict in D-13. Phase 1 CONFIRMED (live bug fix).** Sites 2 + 3 **reachable**; site 4 **dead**. Traced statically (importer graph, `_FORMS` registry, compose services) **and empirically** (each validator invoked directly, both languages).
- [x] `try/except` checked at the 2 previously-unchecked sites: `form_seah_1.py:199` has **none** (⇒ 500); `form_story_main_route_step.py:33` has none (moot — dead).
- [x] **`BaseFormValidationAction.get_buttons` DELETED** — 0 callers **re-confirmed by AST+MRO myself** (D-06): every `Validate*` class on that base has *only* a `get_utterance` call; all 76 `get_buttons` calls sit in sibling `Ask*` classes on `BaseAction`/`BaseOtpAction`. The grep trap was real — `form_grievance_complainant_review.py:621/648/651/657` are `Ask*` classes *below* the `Validate` class in the same file.
- [x] `key=` param added to `get_utterance` (`base_classes.py`) — **keyword-only and required**, not optional
- [x] All 4 call sites pass explicit keys. **AST+MRO census re-run on today's tree: 4 introspection / 196 explicit / 0 unresolved — reproduces exactly** (`introspection by method: {'get_utterance': 4}`, zero `get_buttons`).
- [x] Mapping entries — **only 1 new entry was needed, not 3 (see D-23)**. Sites 2 and 3 point at existing, already-reviewed, already-translated copy authored *for those very branches*; only the dead site 4 needed new copy. NE AI-translated per the standing directive → [`translations_t3-01_review.csv`](translations_t3-01_review.csv), **1 row, `needs_translator=0`**. `form_story_main_route_step` added as a top-level key (module **not** renamed — 200-site blast radius).
- [x] Introspection deleted (`f_back` line + `inspect` import + the now-unused `get_buttons_base` import); `key` is required
- [x] Swallow narrowed at `form_grievance_complainant_review` — `except Exception` → `except (ValueError, KeyError)`, and a `ValueError` from the utterance layer is **re-raised** rather than becoming a `SKIP_VALUE`. The recoverable `.remove()`-on-absent-category case it actually existed for stays handled.
- [x] `check_form_function_name` (`base_mixins.py`) **DELETED** (D-05). It is an *instance method* needing `self.logger` (so it could not be called from a test as the spec's "use it in the new test" suggested), and it only checks `[form][action]` exists — strictly weaker than the new test, which checks form + action + **index** + **both languages** across every call site. Two mechanisms for one invariant is how the weaker one rots.
- [x] `tests/actions/test_utterance_key_integrity.py` — walks **call sites** by MRO, not the dict; scoped to `get_utterance`. Includes a *guard-the-guard* test (a resolver that finds nothing must fail loudly, not vacuously pass), a census pin (4 sites), and a `get_buttons`-never-returns pin.
- [x] **Verified red pre-fix** — see the evidence table below
- [x] Swallow regression covered: the branch **is** reachable, so it is exercised directly (probe + `test_call_site_key_resolves`) — no need for the D-13 fallback the spec described
- [x] Full `tests/actions` + `tests/orchestrator`: **188 passed / 1 skipped** vs baseline **173 / 1** (+15 = exactly the new file). No regressions.
- [ ] Manual: EN + NE re-prompts render at the repaired sites — **PENDING-HUMAN (D-24)**: no browser in the build env. **Largely pre-empted**: the §0 probe drove both live sites in EN *and* NE and captured the rendered strings (below). Combine with H2-08's pending SEAH walk-through.
- [x] §0 found site 4 dead ⇒ dead-code finding logged (D-22 + followup + TODO row), **not fixed here**

**Red-pre-fix evidence (HR-04 standard).** `tests/actions/test_utterance_key_integrity.py` on pre-fix source ⇒ **10 failed / 5 passed**; after ⇒ **15 passed**. The `form_otp` control passing pre-fix is what proves the suite isn't failing on everything:

| Test | Pre-fix | Evidence |
|---|---|---|
| `test_call_site_key_resolves` × 3 broken sites | 🔴 | `ValueError` — key resolves nowhere |
| `test_call_site_key_resolves[form_otp:352]` | 🟢 | **control** — the one site whose key exists |
| `test_call_site_ne_is_translated` × 3 | 🔴 | can't resolve to compare |
| `test_call_site_passes_an_explicit_key` × 4 | 🔴 | all 4 relied on `f_back` introspection |
| `test_no_introspecting_get_buttons_call_sites` | 🟢 | pins D-06 (already 0 callers) |
| `test_introspection_sites_are_the_known_four` | 🟢 | census pin |

**Empirical before/after (the actual user impact, both languages).** Each validator invoked directly:

| Site | Before | After |
|---|---|---|
| 3 `form_seah_1` (SEAH intake, unrecognized reply) | `ValueError` → `form_loop.py:231` (unguarded `getattr` call) → `main.py:135` ⇒ **HTTP 500** | EN *"Please choose anonymous grievance or grievance with contact details."* · NE *"कृपया बेनामी गुनासो …"* |
| 2 `form_grievance_complainant_review` (user happy with categories) | **0 messages**, slots `{status: 'LLM_generated', cat_modify: 'slot_skipped'}` — the `ValueError` laundered into a skip | EN *"No category selected. skipping this step."* · NE *"यदि कुनै समूह चयन गरिएको छैन …"*, slots `{status: None, …}` — **the correct confirm shape** |

Note site 2's fix restores **both** the message and the slot state: `grievance_categories_status` was being set to `LLM_GENERATED` by the swallow, not just silenced.

### T3-02 p1 — Dead branches + terminal `else` ✅ DONE (`27cabcad`)

- [x] Both deletion proofs **re-run and still hold**: `"form_dust"` returns exactly 2 hits — `state_machine.py:1039` (its own comparison) and `form_loop.py:332`, an **`active_loop`** value in a different namespace (left alone). `"submit_grievance"` appears **only** as its own comparison.
- [x] The proof's precondition **re-verified**: only `state_machine.py` (7 sites) and tests write `session["state"]` — and **no test writes either deleted state**, so nothing depended on them.
- [x] `form_dust` deleted — a **verbatim duplicate** of `form_road_hazard`, down to calling `_get_form_road_hazard()`.
- [x] `submit_grievance` deleted. **No helper stranded**: `_append_seah_outro_after_submit_if_applicable`, `_start_grievance_review_after_submit` and `_get_form_road_hazard` all keep live callers (checked, not assumed).
- [x] SEAH branches **NOT** touched · `location_consent` / `location_method` / `grievance_review` / `map_location` **NOT** touched. AST census: chain **22 → 20** arms, exactly the two intended.
- [x] Terminal `else` added: logs at **error** with the offending state **and recovers**. Recovery, not just a message — leaving `next_state` on the unrecognized state **soft-locks** the session (every later turn re-enters the fallback; only `/introduce` escapes). `_recover_from_unknown_state` follows `_handle_attachment_ids_sync`'s own answer (no language ⇒ intro, else ⇒ main menu) and takes the **in-file handler signature**, which is what p4's dict default will want.
- [x] Copy composed from **existing reviewed strings**, not authored (D-23's lesson): `माफ गर्नुहोस्` + `त्रुटि भयो` + a verbatim clause from `_handle_attachment_ids_sync`. ⇒ **no new mapping entries, no new AI translations** on a user-facing path.
- [x] Dead-air test — **verified red pre-fix: 7 failed / 1 passed**, the 1 being the control (a known state must not hit the fallback), which is what proves the suite isn't failing on everything. Post-fix 8/8.
- [x] Full `tests/orchestrator` + `tests/actions`: **196 passed / 1 skipped** vs **188/1** (+8 = exactly the new file).

**Red-pre-fix evidence (HR-04 standard).** Note what the red run *disproved*: the two deleted branches were **not** dead air when seeded — `submit_grievance` executed and filed a grievance, advancing to `grievance_review`. They are dead because nothing **assigns** those states, not because the bodies were inert. The test says so rather than implying otherwise.

| Test | Pre-fix | Evidence |
|---|---|---|
| unknown state returns a message | 🔴 | `assert []` — zero messages, the dead-air bug |
| unknown state logs an error | 🔴 | `assert []` — nothing in the logs either |
| unknown state recovers to `main_menu` | 🔴 | `'nonexistent_state' == 'main_menu'` — soft-locked |
| no language ⇒ recovers to `intro` | 🔴 | stays on the bad state |
| recovery is bilingual (NE session) | 🔴 | `'माफ गर्नुहोस्' in ''` |
| deleted states recover × 2 | 🔴 | `'form_dust' == 'main_menu'`; `'grievance_review' == 'main_menu'` (submit_grievance **ran**) |
| **known state does NOT hit the fallback** | 🟢 | **control** — proves the suite isn't failing on everything |

### T3-03 — Voice-chunk serialization ✅ DONE

- [x] Chunk queue serialized (chunk N chains on N-1) in `voiceNote.js` — `enqueueRecordingChunk` chains on the previous chunk's **settlement** (`uploadChain`), so one failed chunk is not re-reported once per queued chunk. `resetUploadState` resets the chain. `Promise.all(pendingChunkUploads)` join at stop preserved.
- [x] `FormData` built **inside** the retry closure (`voiceChunkUpload.js`) — follows the `authedFetch` precedent. **`uploadId` value → `getUploadId` resolver**: rebuilding the body alone was *not* enough — the param was captured at call time, so a rebuilt body would have re-read the same stale `null`. Only caller was `voiceNote.js:74`, so the signature change is contained.
- [x] `"out of order"` swallow gated on `isStopping` (`voiceNote.js`) — kills the silent-truncation path
- [x] Server **not** changed (it is correct)
- [x] **Test-harness decision: (a)** — `channels/REST_webchat/{package.json,vitest.config.js,.gitignore}` + `modules/__tests__/`. Node-env vitest, hand-rolled fakes, **no jsdom** (the modules are transport/ordering logic, not DOM). The fake server mirrors `files.py`'s real contract so a client bug fails here the way it fails in prod. **CI wired in the same commit** (`webchat-checks` job) ⇒ **D-11 closed, no followup owed.**
- [x] Race test — **verified red pre-fix**
- [x] Ordering / retry-heals / truncation-guard / fast-network-no-regression tests
- [x] `tests/backend/test_fastapi_files.py` extended: id-less chunk > 0 ⇒ 400 (`test_upload_voice_chunk_without_upload_id_after_first_is_rejected`)
- [ ] Manual: Slow 3G ≥5 s recording completes + plays back full length — **PENDING-HUMAN (D-17)**: no browser in the build env. Do together with HR-07's webchat sweep.

**Red-pre-fix evidence (HR-04 standard).** Final tests run against pre-fix source via `git stash push -- <the 2 modules>` ⇒ **4 failed / 3 passed**; restored ⇒ **7 passed**. The 3 that pass pre-fix are the guard tests — they are what proves the suite isn't just failing on everything:

| Test | Pre-fix | Evidence |
|---|---|---|
| Race — every chunk > 0 carries an `upload_id` | 🔴 | later chunks id-less ⇒ 400 |
| Ordering — one at a time, in index order | 🔴 | `[0,1,2,1,2,1,2]` — the concurrent retry storm, caught verbatim |
| Mid-recording 409 surfaces | 🔴 | `['recording']` only — no `upload_error`; the silent truncation |
| Retry re-reads `upload_id` | 🔴 | `[null, null]` — the frozen body, caught verbatim |
| Late 409 **after** stop still ignored | 🟢 | guard: the legitimate swallow still works |
| Fast network unchanged | 🟢 | no-regression baseline |
| Retry exhaustion surfaces server status | 🟢 | existing behavior pinned |

**Suites:** webchat vitest **7/7** (new baseline) · `tests/backend/test_fastapi_files.py` **20 passed** (was 19 + 1 new).

### T3-04 — PII boundary ✅ DONE (manual browser sweep pending-human)

> **The binding invariant held: commit 1's net was green at all four commits** — each time for a different reason (vault workaround ⇒ backend decrypts ⇒ API alone ⇒ key absent). That is the whole design of the order, and it is why nothing here was flying blind.

- [x] **Commit 1** (`57acf7ff`) — `tests/ticketing/test_pii_boundary.py`, **31 tests green on today's unmodified code**. **D-09 re-verified: nothing imported `pii_vault`** (the only `pii_vault` string in `tests/` was a docstring), so `decrypt_ciphertext` / `reveal_field` / `scrub_pii_value` / `grievance_pii_for_officer_card` were **entirely** uncovered — and `test_ticket_access_matrix.py` passes identically whether decryption works or is deleted, because it never mocks the client and lands in the `_backend_unavailable` branch. The new API tests assert `_backend_unavailable is not True` so they **cannot** pass vacuously the same way.
- [x] **Parametrized ciphertext/plaintext — but not as specced (D-54).** The spec's version cannot survive its own steps 2-4: ciphertext decrypts today and is *masked* after step 3. Pinned the durable property instead: *plaintext in ⇒ plaintext out*, *ciphertext in ⇒ **never ciphertext on the card***.
- [x] **Commit 2** (`f0a6c4a9`) — `get_grievance_by_id` decrypts, mirroring `get_grievance_by_complainant_phone:537`. **Order matters and is not cosmetic:** decrypt **after** `_parse_database_result`, since the parser json-parses every string (T3-06/D-28's reason for typing text fields `Any`) — decrypting first would hand it plaintext PII and a complainant named `"123"` would come back an **int**.
- [x] **Fallback path checked, not assumed:** `get_grievance_core_by_id` reads `SELECT * FROM grievances`, and **none of the four encrypted columns are on that table** — all four are on `public.complainants` (verified against the live schema). ⇒ needs no treatment. Pinned by a test.
- [x] **`postgres_services.DatabaseManager.get_grievance_by_id:795` merely delegates** to this method ⇒ one site covers both entry points.
- [x] Blast radius: **no caller decrypts this method's result** (checked) ⇒ no double-decryption. **Decrypting already-plaintext data measured directly against the db container:** pgcrypto raises `invalid hexadecimal digit`, and since the loop reuses one cursor the first failure **aborts the transaction** for the rest (`current transaction is aborted`); the manager catches it and falls back per-field, returning the original. Non-destructive but noisy — **pinned** so a future refactor of that fallback cannot silently start dropping plaintext.
- [x] `ticketing_dispatch.py:96` verified: it selects **only** `grievance_summary` / `grievance_categories` / `grievance_location` — **none of the four PII fields** ⇒ no PII lands in `ticketing.*` (rule 3). T3-07's guard agrees independently, and there is **no column to put it in**.
- [x] Full `tests/actions` + `tests/orchestrator` **unchanged at 196/1** across commit 2 ⇒ **the live chatbot is not regressed**, which was this ticket's one real risk. `tests/backend` 147/1 (+5).
- [x] **Commit 3** (`0ca98d85`) — `decrypt_ciphertext` + `reveal_field` deleted, with the `engine` / `text` / `get_settings` imports: **ticketing's PII path no longer opens a DB connection at all.** `reveal_field`'s one external caller (`resolved_summary_builder.py:150`) collapsed to a passthrough exactly as the spec predicted — it was called on `grievance_description`, which is **not** an ENCRYPTED_FIELD and is stored plaintext (verified against real rows) ⇒ always a no-op.
- [x] **DECISION recorded — `scrub_pii_value` is now a fail-loud assertion, not a silent mask.** Ciphertext can no longer legitimately arrive; if it does, the backend regressed. Still fails closed (never show an officer hex) but logs at **error**, and **never logs the value**. Silence is exactly what made the review's ordering dangerous. Pinned by 2 tests incl. a control proving the normal path stays silent.
- [x] **Commit 4** (`c47fd4a9`) — `db_encryption_key` removed from ticketing settings; **left on `backend`** (`base_manager.py:57`, `complainant_manager.py:43`, `scripts/database/init.py:203`). **Deviation (D-55): the compose/`env.local` edit is not implementable** — the var is in **no** compose file and all services share one `env_file`, so removing it would strip `backend`'s. Deleting the **field** is stronger anyway.
- [x] **Guard test — and it is stronger than a grep:** asserts the settings object has **no attribute**, plus an **AST** sweep of `ticketing/` for any live reference (comments allowed — `pii_vault.py`'s history is prose, and that prose is what stops the next person re-adding it). **Mutation-verified**: re-adding the field ⇒ 2 red; a module reading `get_settings().db_encryption_key` ⇒ sweep red.
- [x] Backend test: `get_grievance_by_id` returns plaintext — **verified red pre-fix (1 failed / 4 passed)**, the 4 passers being controls incl. a **guard-the-guard** that fails if the fixture was never encrypted. Uses a **real pgcrypto round-trip**, not a hand-written fake: a fake proves only that some string changed.
- [x] Docs updated: **`CLAUDE.md:121` — the claim is now TRUE** and T3-07's "delete this caveat when T3-04 lands" instruction followed exactly (§Data rules' §6 pointer left alone, as instructed); rule 5's *"(Fully true when T3-04 lands…)"* conditional resolved; `13_security.md` records `backend` as sole key holder; `seah/02_vault_privacy_and_reveal.md:58` rewritten, keeping the history so the workaround is not mistaken for a design.
- [x] **Manual checks driven without a browser (D-56) — the check the whole ticket exists for PASSES.** Both images rebuilt; grievance seeded with **real ciphertext at rest** (`c30d0407…`): real HTTP API ⇒ **plaintext all four fields**; ticketing's **real client + real `pii_vault`** ⇒ standard card `Sita Rai` / `+9779812345678` / email / address, **SEAH card all `None`**; `ticketing_api` boots with `DB_ENCRYPTION_KEY` **present in its env but unreadable** (no accessor) ⇒ *"ticketing serves with the key absent"* proven structurally. **Seeded rows deleted afterwards, 0 leftover.**
- [ ] Manual: the **rendered UI** — **PENDING-HUMAN (D-56)**. Joins D-24 / D-17 / D-33 / D-49; one session covers all five.

### T3-02 p3 — Split `status_check_form` ✅ DONE (`7723bdfb`)

- [x] Gated on p2 — **the characterization net stayed green *unchanged* throughout**, which is what makes this verifiable rather than hopeful.
- [x] Split along the code's **own** seam, AST-confirmed with no shared locals across it: `_status_check_route_intent` (101 lines, intent routing) + `_status_check_run_active_form` (231 lines, a form in flight).
- [x] Follows the **existing in-file convention** `(session, dispatcher, domain, slot_updates, latest_message) -> next_state`, extras keyword-only (`_begin_road_hazard_intake` is the precedent; `_restart_intake_from_done` for passing `intent`).
- [x] `next_state` passed **in** and returned — redundant today (AST proves neither arm reads it, both only assign) but there so an unassigning path keeps the caller's value **rather than depending on my reading of 300 lines being exhaustive**.
- [x] **Extracted by line range, never retyped** (D-50's lesson). **Proof: all 288 non-blank moved lines reappear verbatim (modulo the 8-space dedent); 0 missing.**
- [x] **D-52 preserved deliberately** — moving code and fixing behaviour in one commit is how a refactor hides a bug.
- [x] **`run_flow_turn` 1,485 → 1,175 lines (−21%)** across p1+p3; chain integrity intact (20 arms + p1's terminal `else`). Suites **208/1**.

### T3-02 p4 — Handler table ❌ NOT DONE (STRETCH — logged, `4c454c5a`)

- [x] Logged in all three required places → [`followups/run-flow-turn-handler-table.md`](followups/run-flow-turn-handler-table.md) + TODO row + commit.
- [x] **Re-sized honestly (D-53): M, not the spec's S.** "22 one-line entries against a proven signature" holds only if every branch **has** the signature; p3 extracted one, leaving **19 arms / ~1,058 lines inline**. p4 is *"extract 19 more branches, then write a table"*.
- [x] Recorded that **p4 does not fix D-51/D-52** (the lookup succeeds; the default never fires) — the `run_flow_turn` postcondition does, cheaper and better.

### T3-06 — Grievance API hardening ✅ DONE (manual sweep pending-human)

- [x] **§0 FIRST — EC2 :5001 inbound checked** (staging security groups). **ANSWERED: NOT open ⇒ Phase 2 confirmed, not an incident (D-25).** Read live from the AWS account, not inferred from the repo. **Prod (DOR infra) remains unverified — carried as an open TODO row (D-26).**
- [x] Full HTTP caller inventory recorded in PROGRESS.md **before** commit 1 — see §T3-06 caller inventory below. **Confirmed no browser/webchat JS calls `/api/grievance/*`** (the sole `channels/` hit, `ticketing-ui/lib/api.ts:2719`, is a comment).
- [x] **Commit 1** (`96d6eb33`) — `response_model` on the GET; **payload not narrowed**; the 9 `grievance_content.py` fields present. **Inertness proved against a real row: 51 fields before and after, envelope byte-identical.**
- [x] Schema-drift test — model fields checked against their **owning** tables (D-28), both directions, mutation-verified.
- [x] **Commit 2** (`aead11eb`) — read audit incl. **caller identity**; emits at **INFO**; **verified red** (switching to `.debug` fails 5 tests). Log-vs-table decision recorded (D-27).
- [x] **Commit 3** (`e5fdc4c4` client key) + **Commit 4** (`28c73f13` enforce) — `Depends(_ticketing_auth_check)` on **both** endpoints; every caller confirmed sending the key **first** (D-29); `AUTH_MODE=bypass` dev loop still works (pinned by test).
- [x] 401-without-key / 200-with-key on **both** endpoints — **verified red pre-fix**: unauthenticated, the GET returns **200 carrying `grievance_description`** (D-30).
- [x] Rate limit **deferred, logged** ([`followups/grievance-api-rate-limiting.md`](followups/grievance-api-rate-limiting.md) + TODO row) — measured: no reusable mechanism reaches these endpoints (D-31).
- [x] Full `tests/backend` (140/1, +33) + `tests/actions` + `tests/orchestrator` (188/1 — `+15` is T3-01's, not this ticket) + `tests/ticketing` (549/5, +5) green vs baselines.
- [x] **`curl` GET without key ⇒ 401 — verified against the rebuilt container** in the deployed posture; valid key ⇒ 200 with all 51 fields; dev bypass still 200 (D-32).
- [x] **Portal integration paths verified end-to-end without a browser (D-33):** ticketing's real client → keyed backend ⇒ `get_grievance_detail` 200/401 and `update_grievance_status` 200 (**history row written**)/401. Covers ticket-detail + acknowledge/resolve.
- [ ] **Manual browser sweep (rendered UI, chatbot file+status-check) — PENDING-HUMAN (D-33).** Joins D-24 / D-17; one session covers all three.
- [x] Prod `5001:5001` host mapping — **necessity recorded, not dropped**: it is in `grm.yml` (shared, not a prod overlay) and compose **merges** `ports`, so it cannot be cleanly overridden per-overlay (D-34) → [`followups/grievance-api-host-port-binding.md`](followups/grievance-api-host-port-binding.md).
- [ ] **`authz` — NOT DONE. The ticket's title promises it; its steps never define one, and the `x-api-key` scheme cannot express it (D-38).** A service key says "this is ticketing", not "this is officer X in district Y", so the SEAH/jurisdiction gate the §6 table compares against is not derivable from it. Left open deliberately rather than silently dropped.

#### What T3-06 actually closed — the §6 scorecard

The ticket exists because §6 measured `GET /api/grievance/{id}` on four properties and found it worse than the direct SQL read it was supposed to replace. Post-T3-06:

| Property | Before | After T3-06 |
|---|---|---|
| Authn | none | ✅ `Depends(_ticketing_auth_check)`, both endpoints |
| Response contract | none (`SELECT g.*` → untyped dict) | ✅ `response_model`, 51 fields, drift-gated |
| Read audit | none | ✅ structured log at INFO, with principal (**not** a queryable table — D-27) |
| **Authz** | none | ❌ **still none** (D-38) — needs officer identity, not a service key |
| Rate limit | none | ❌ **still none** (D-31) — no reusable mechanism reaches `:5001` |

**⇒ The endpoint is no longer the least-protected path to grievance data — but it is still not an authorization chokepoint.** The §6 DECISION to legitimize ticketing's direct reads as-built **survives unchanged**: the direct read keeps a JWT + jurisdiction gate the API still cannot offer. **Do not cite T3-06 as having made "route reads through the API" viable.** It made the *option* discussable, which is exactly what the spec claimed for it — no more.

#### T3-06 caller inventory (recorded before commit 1, per the spec's step 0)

Grepped repo-wide. **Every HTTP call funnels through two functions in one file** — `ticketing/clients/grievance_api.py`:

| Endpoint | Client fn | Callers | Sent `x-api-key` before T3-06? |
|---|---|---|---|
| `GET /api/grievance/{id}` | `get_grievance_detail` | `api/routers/tickets/pii.py:55`, `services/resolved_summary_builder.py:139`, `clients/grievance_api.py:218` (`begin_reveal_session` proto fallback) | **NO** ⇒ fixed in `e5fdc4c4` |
| `POST /{id}/status` | `update_grievance_status` | `engine/ticket_actions.py:375`, `services/archiving.py:247`, `:312` | **NO** ⇒ fixed in `e5fdc4c4` |
| `GET /api/grievance/statuses` | `get_grievance_statuses` | no non-test caller | n/a — endpoint stays public |
| `PATCH /{id}/classification` | `patch_grievance_classification` | — | yes (already) |
| `PATCH /api/complainant/{id}` | `patch_complainant` | — | yes (already) |
| chatbot in-process | — | ~20 non-test callers of `grievance_manager.get_grievance_by_id` | n/a — **not HTTP**, unaffected by authn (confirmed: 173 baseline tests still green) |
| browser / webchat | — | **none** | n/a |

**The spec's table said "verify" for the first two rows. Verdict: neither authenticated.** Only the two PATCHes did — which is why this was 2 of 5 endpoints rather than a missing convention.

### T3-07 — Boundary policy amendment ✅ DONE

- [x] **Commit 1** (`2ad2cf21`) — `CLAUDE.md` §Data rules amended: rule 1 → enumerated read/write contract (5 tables, with the 3 writes named); rules 2/3 kept + marked pinned; rule 4's free-text caveat stated (**and the summary/description asymmetry marked intentional so nobody "fixes" it**); rule 5 scoped to complainant PII only. Also fixed the schema diagram's *"`public.*` — READ ONLY, never query directly from ticketing"* (`CLAUDE.md:151`), which was the same false claim one line up and is not in the spec's list.
- [x] `CLAUDE.md:121` fixed — **coordinated with T3-04**: marked **false-today** with a pointer rather than rewritten, since T3-04 step 2 makes the claim true and its step 4 owns the line. T3-04 deletes the caveat; the §Data rules §6 pointer stays. Nothing to revert either way.
- [x] **Dated pointer to §6 added** — inline in §Data rules, in `04_ticketing_schema.md` (with the March rationale quoted verbatim), `09_privacy.md`, `03_ticketing_api_integration.md` §3b, and the test module docstring. **The rationale now lives in 5 places; `21631051` deleted it from 1.**
- [x] **Commit 2** (`d6bdd9fa`) — no-cross-schema-FK guard, **green today**, docstring says it pins an invariant rather than fixing a bug. **Two tests, not one:** models (via `Base.metadata`) *and* migrations (via AST) — a migration can declare an FK no model reflects, and metadata would never see it.
- [x] No-complainant-PII-columns guard (green today), read from `Base.metadata` — `services/demo_reveal.py` holds those four names as in-memory demo dict keys, which is not a column and correctly does not trip it. Plus a broader `complainant_*` namespace fence (D-43).
- [x] Contract-drift guard over the enumerated reads — **both directions** (code→contract, contract→baseline), plus a closed-table-set gate and an 11-statement census pin.
- [x] **Proven to go red** — all 5 live guards mutation-verified against the real tree, then restored green (D-41): renamed baseline column, undeclared real column, undeclared table, PII column on a model, cross-schema FK on a model **and** in a migration.
- [x] `grievance_sync.py` hardcoded-column-list TODO row **closed**. Also retired **2 further TODO rows the guard disproved** — a manual "validate the sync column names" task (now automatic) and a row asserting `grievance_sync.py` reads 5 complainant location fields, which it never has (it reads `location_code`).
- [x] **Commit 3** (`e6a0b473`) — `04_ticketing_schema.md` (March rationale restored verbatim + which half retired and why), `09_privacy.md` (re-motivated: heading was justified by parallel worktrees, retired June 2026; its rule 4 was **factually false** and is corrected), `03_ticketing_api_integration.md` (new **§3b** documents the contract; the doc's opening *"integration is API-only"* claim also corrected — it was never true), `00_ticketing_overview_and_questions.md` (§Principles aligned; "Ticketing is independent" marked aspirational, since chatbot intake needs `ticketing.locations`).
- [x] `followups/ticketing-cross-schema-direct-reads.md` — was **already** closed with the decision + §6 pointer before this ticket started; added a **DoD-outcome table** recording what each of its 5 items became. Its TODO.md row marked T3-07-landed. **Its DoD item 1 (escalate the decision) was the doc's best call and is why the outcome was (b), not its recommended (c).**
- [x] `scripts/ops/create_scoped_roles.sql` — comment **and** grants fixed together (spec's preferred option): grants now mirror the 5-table contract, with `complainants` **column-scoped** to `(complainant_id, location_code)` so rule 3 is enforced by **Postgres**, not convention. **Also found and fixed a defect that made the script non-runnable since it was written (D-40).** Applied end-to-end against the dev DB inside a transaction and **rolled back** — grants verified, no role leaked.
- [x] Full `tests/ticketing`: **562 passed / 5 skipped** vs the post-T3-06 549/5 baseline (+13 = exactly the new file). 1 failure is **pre-existing and environmental** — not T3-07 (D-44).
- [x] ~~**`git diff --name-only` shows docs + tests only** — no runtime `.py`/`.ts` touched.~~ ⚠️ **OVERSTATED — corrected 2026-07-15 (D-57).** The parenthetical hedge (*no non-test `.py`/`.ts`*) is literally true, but the headline it supported is not: `e6a0b473` also changes **`scripts/ops/create_scoped_roles.sql`** (78 lines) — a runtime ops script that is neither a doc nor a test. `.sql` is simply neither `.py` nor `.ts`, so the narrow predicate was doing load-bearing work the headline had not earned. **D-40 conceded this in the same document** (*"This is the one place T3-07 touched a non-doc/non-test file"*) — i.e. the summary row and its own deviation row contradicted each other. The *work* was correct and in scope (the spec puts the script in step 3); only the claim about it was wrong. **Accurate statement: docs + tests + one ops script.** (`channels/ticketing-ui/*` in the working tree was **T3-05's** in-progress work, deliberately left unstaged.)

### T3-08 — Test hygiene ✅ DONE

> Opened from T3-07's deferrals (D-36 / D-37 / D-44). They looked like three unrelated
> annoyances; they are one failure: **the `@integration` tests are the only thing that
> exercises the seeded DB, CI deselected them, and the one place they could run — the host —
> was broken by default. So they rotted, silently, for ~6 weeks.**

- [x] **Commit 1** (`ac0b9d29`) — **D-36**: `_host_env.py` no longer seeds the DB identity from `env.local`. Root cause is sharper than the row said: env.local's `POSTGRES_*` are dead **by construction** — every compose service hardcodes `POSTGRES_DB`/`USER`/`PASSWORD` in its own `environment:` block, so no container reads them. Host pytest was the only reader ⇒ the only thing that could disagree with the DB it talks to. `.env.example` already had the right values.
- [x] `tests/ticketing/test_host_env.py` — 4 tests, **verified red** (2 fail pre-fix; the 2 that pass are the guards: env.local still supplies non-DB keys, host port stays 5433 not env.local's in-network 5432).
- [x] **Result: bare `pytest tests/ticketing` = 110 failed / 256 passed / 234 errors (132s) → 567 passed / 5 skipped / 0 failed (14s)**, no `POSTGRES_*=...` prefix. The workaround D-36 documented is retired.
- [x] **Commit 2** (`9e74e457`) — **D-44/D-45**: 3 assertions asserted a load-decided identity; now assert `PROVINCE_L1_POOL` from `ticketing.constants.demo_officers` (single source of truth, so the next roster change updates the expectation instead of rotting it).
- [x] Not a weakening — **mutation-verified**: emptying the candidate pool still turns the test red. Passes on a pristine seed **and** on a drifted dev DB.
- [x] **Commit 3** (`d815ddf1`) — **D-37 + the followup**: `-m "not integration"` removed, `tests/backend` added. **364 passed / 3 skipped / 390 deselected (3m16s) → 897 passed / 7 skipped / 0 deselected (3m32s)** — measured with the exact command, on a clean migrate+seed matching CI's steps, stable over 3 runs.
- [x] Closes [`integration-seed-reconciliation`](../../2026-07_schema_and_legacy_cleanup/followups/integration-seed-reconciliation.md). **Every acceptance item except one was already fixed** by `4f0140dd`; the quarantine simply outlived its reason, and its comment's "195/0" was stale by 169 tests. The one real remaining bug was D-45's.
- [x] `pytest.ini` + `ci.yml` now state the contract: **`integration` marks a dependency, not a quarantine.** Both say plainly not to re-add the filter to silence a red build.
- [x] Verified **non-destructively**: all measurement ran against a scratch `app_db_hygiene` built with the same migrations + seed CI uses. The dev DB was never reset. Scratch DB dropped afterwards.
- [x] Order-randomisation risk ruled out: `pytest-randomly` is not installed or pinned, so `-p no:randomly` was a no-op and CI order is deterministic.

### T3-02 p2 — Characterization tests ✅ DONE (`a6519a6b` · `eccbfeb5`)

- [x] `status_check_form` interior characterized (5 tests) — the branch p3 splits. Pins both arms of its natural seam: intent routing (follow-up ⇒ `done`; modify ⇒ `modify_grievance_menu`; unrecognized ⇒ re-show the choices, the `:1629` never-empty convention) and form-running (`form_otp` runs and holds state).
- [x] `add_more_info_flow` / `add_missing_info_otp_flow` / `add_missing_info_flow` characterized (7 tests) — **202 lines that had zero state references in any test.** The most valuable pin: **OTP + `/skip` lands on `modify_grievance_menu`, not the `add_missing_info_flow` that `:2086` assigns** — the OTP branch hydrates the contact form and it completes in the *same* turn (`:2092`). That is the single most surprising transition in these lines and the one a handler table is most likely to break.
- [x] Driven over HTTP (`post_turn` → `/message`), session seeded first (the `test_introduce_after_done.py` precedent). `test_modify_grievance_flow.py` is the only direct-call precedent and **both of its tests are `xfail` aspirations, not records of behaviour** — so it was not followed.
- [x] **Every assertion written from a probe of the real code, not from reading it.** That is what surfaced D-51 and D-52; reading tells you what someone *meant*.
- [x] Bugs **recorded, not fixed** → **D-51** + **D-52**, each with a followup **and** a TODO row **and** in the same commit. Both pinned by tests whose docstrings say: *if this fails, the bug was fixed — delete the test, don't relax it.*
- [x] **Coverage gap recorded rather than papered over:** `status_check_form`'s OTP arm (`:1600-1616`) is **not** covered — the demo grievance has no phone, so `follow_up_needs_otp_verification` short-circuits before OTP is considered, and both `otp_status=verified` and the unverified phone-route case land on the same `done`. Stated in the test so p3 could not mistake it for full branch coverage.
- [x] Suites: **203/1** then **208/1** (12 new tests total).

### T3-05 — Settings extraction
- [x] `friendlyError` moved first (34 call sites) — `977375f0`, its own commit, to `components/settings/lib/friendlyError.ts`. Grepped first per the spec: no existing util covered it.
- [x] 6 clusters extracted, **one commit each**, ascending risk: AdminAccess (`94e0ab13`) → Locations (`c2f978df`) → SystemConfig (`0976ad58`) → Roles (`e31d472a`) → Workflows (`ba2fe75d`) → Projects (`4228d72e`)
- [x] The 2 cross-cluster imports wired (`RoleCreateModal` → workflows `StepForm`; `ProjectWorkflowsEditor` → projects `ProjectEditor`) — both resolved as ordinary imports exactly as the spec predicted; no refactor needed.
- [x] Code moved **verbatim** — no renames/restyles/re-types in a move commit. Bodies were `sed`-extracted by line range, never retyped; only the leading `function X(` → `export function X(` and the new import block differ. Pre-existing dead imports/locals left as found (D-47).
- [x] `page.tsx` down to ~250 lines → **301** (shell + `MAIN_TABS` + `SettingsSubTabs` + `ComingSoon`), from 4,372. The ~50-line gap over target is the import block + the 4 pre-existing dead imports the spec's "don't fix the 141 warnings" rule requires leaving (D-47).
- [x] `tsc` clean / `eslint` 0 errors + warnings **not increased** / `build` green / vitest green — at **every** commit. Run in Docker via the `builder` stage (D-45).
- [x] Per-tab smoke tests added (or deferral logged) — **partial + deferral logged.** 27 unit tests on the extracted helpers (first coverage under `components/settings/`, mutation-checked); per-tab *render* tests deferred — **no DOM harness exists at all** (D-48) → [`followups/settings-tab-render-tests.md`](followups/settings-tab-render-tests.md) + TODO row.
- [x] **Bundle size measured** before/after — reported honestly even if it didn't move. **It did not move: 1,461,565 → 1,461,373 B (−192 B, −0.01%); settings server chunk 32,853 B unchanged; 38 chunks both sides.** Method changed — Next 16 no longer prints per-route sizes (D-46).
- [ ] Manual: every tab at every role tier; gating unchanged — **pending-human** (D-49). No browser in the build env; joins the H2-02/H2-06 + D-24/D-17/D-33 sweeps.

---

## Deviations

> Log every deviation from the spec, every adjacent bug found and **not** fixed, and every deferral. Standing rule: [`../README.md`](../../README.md) — a deferral not logged here **and** in `docs/TODO.md` 🔵 TECH DEBT **and** in `followups/` is a defect, not a deferral.

### Opened at sprint planning (2026-07-15)

| # | Finding | Disposition |
|---|---|---|
| D-01 | **The source review's Tier-3 table is wrong in 4 of 5 rows.** Full evidence in [00-reassessment.md](00-reassessment.md). | Corrections owed to `devils_advocate_codebase.md` at close-out (listed in 00-reassessment.md §Consequences). Sprint tickets follow the **corrected** findings. |
| D-02 | `file_name` derived from module name (`base_mixins.py:63`) — affects **~200** sites; the *real* refactorability blocker T3-01 was nominally aimed at. M+. | **Deferred** → [`followups/utterance-file-name-derivation.md`](followups/utterance-file-name-derivation.md) + TODO.md |
| D-03 | ~~Ticketing reads `public.grievances` + `public.file_attachments` **directly via its own session** — violates data rules #1/#5.~~ → **SUPERSEDED 2026-07-15 by D-19.** The framing was wrong on three counts: the surface is **11 statements / 5 tables / 3 writes**, not 3 reads; the rule violated protects a goal both sides of the codebase abandoned; and the prescribed fix (route via the API) is a **security downgrade**. | **Reclassified, not deferred.** Rule dropped per the §6 DECISION → **T3-07**. The reads are **legitimized as-built**. **T3-04 must still not claim the boundary is fully unified** — that part stands. |
| D-04 | `run_flow_turn` CC measured at **189**, not the reviewed ~120. Line counts (1,485 / 2,303) exact. | Recorded; correction owed to the review at close-out. |
| D-05 | `check_form_function_name` (`base_mixins.py:313`) is defined and **never called** — a guard built for exactly the T3-01 bug class, never wired up. | **In scope for T3-01** (wire up or delete). |
| D-06 | ~~`get_buttons` (`base_classes.py:126`) has the identical introspection flaw~~ → **CORRECTED 2026-07-15: it is DEAD CODE.** AST+MRO shows 0 of 200 call sites reach it; all 76 `get_buttons` calls go through `ActionHelpersMixin`'s explicit path. The `get_buttons` calls in the same files live in sibling `Ask*` classes. | **In scope for T3-01 — but DELETE, not fix.** Free win. |
| D-13 | **T3-01 reachability is UNTRACED.** The 3 keys provably don't resolve, but whether their branches can be hit at runtime is unknown — they may be dead or swallowed. Surfaced by a second independent assessment (the H2-08 agent), which correctly refused to assert reachability it hadn't traced. | **GATES T3-01's phase** (Phase 1 bug fix vs Phase 3 refactor). ~10 min. **Do first.** Deliverable unchanged either way. Record the verdict here before starting T3-01. |
| D-14 | Call-site count disputed between two assessments: 4/196 (this pass) vs 6/180 (H2-08 pass). Settled by AST+MRO: **4 introspection / 196 explicit / 0 unresolved**. Not load-bearing — both concluded *below M*. | Recorded. **Resolve by MRO, never by grep** (`ValidateFormOtp` has multiple bases). |
| D-15 | **Spec error, self-inflicted:** `02-webchat-voice-spec.md:82` credited the FormData-rebuild-on-retry precedent to **H2-08** (the SEAH mixin/Nepali sprint). The actual precedent is **H2-01 + the `apifetch-refresh-non-json-sites` follow-up**. | ✅ **Fixed 2026-07-15** in the spec. Recorded because it would have sent an agent to read the wrong sprint. |
| D-07 | `isIgnorableLateChunkError` swallows `"out of order"` unconditionally (`voiceNote.js:32-34`) → **silently truncated voice notes**; review never mentioned it. | **In scope for T3-03** (serializing kills the class). |
| D-08 | `run_flow_turn` has **no terminal `else`** → unrecognized state returns zero messages (dead air). | **In scope for T3-02 p1.** |
| D-09 | **No test imports `pii_vault`**; `test_ticket_access_matrix.py:68` asserts against the `_backend_unavailable` branch and would pass whether decryption works or is deleted. | **In scope for T3-04 commit 1** (the test net is the first commit). |
| D-10 | `CLAUDE.md:121` — "Grievance API … handles PII decryption" is **factually false today**. | Fixed by T3-04 step 2; doc corrected in T3-04 step 4. |
| D-11 | No JS test suite exists for `channels/REST_webchat/` at all. | ✅ **CLOSED by T3-03 (2026-07-15).** Path **(a)** taken: vitest harness seeded at `channels/REST_webchat/` + `webchat-checks` CI job, both in the same commit. No followup owed — the CI wiring the spec allowed deferring was completed. The webchat now has a test suite where it had none. |
| D-12 | `app/settings/page.tsx` has **zero** direct test coverage (portal vitest = 61 tests, none on settings). | T3-05 adds smoke tests, or logs the deferral. |

### During execution

*(append here — one row per deviation, with commit ref)*

| # | Finding | Ticket | Disposition |
|---|---|---|---|
| D-25 | **§0 PHASE GATE ANSWERED — `:5001` is NOT internet-open. Not an incident.** Read live from the AWS account (`ap-southeast-1`), not inferred: SG `sg-01739221c73ab3eb1` — attached to `chatbot-main` **and** `chatbot-rest` — admits **80, 443, 22 (158.62.26.255/32), 8000, 3000, and tcp/0 (103.41.172.170/32)**. **No rule matches 5001.** Corroborates the repo-side reasoning (no `location /api/grievance` in any nginx conf). All 7 instances in the account are currently **stopped**. | **T3-06** | **Phase 2 CONFIRMED** — D-20's gate resolved. The unauthenticated endpoints were reachable only from the host/Docker network, never the internet. TODO row updated from "⚠️ UNVERIFIED" to the measured answer. |
| D-26 | **Prod (DOR) firewall is NOT verifiable from here.** Production is `grm-chatbot.dor.gov.np` on **DOR infra**, not this AWS account (CLAUDE.md §Superseded), so D-25 clears **facets/staging only**. No repo artifact can answer it. | T3-06 | **Open — carried to TODO.md** as a question for whoever operates DOR infra (`:5001` and `:8000` inbound). Mitigated but not closed by the authn now shipping: an open port is no longer an *anonymous* read. |
| D-27 | **Read audit: structured log, not a `public.*` table** — the spec's own recommendation, taken. Own logger name (`audit.grievance_read`) so the trail is filterable/shippable and promotable to a table later without touching call sites. JSON payload; **INFO** (not `.debug`, which is invisible at the deployed `LOG_LEVEL=INFO` — the exact way `grievance_manager.py:172` already fails to be a trail). | T3-06 | **Decision recorded per the spec.** A log line is reversible; a `public.*` table is a migration-stream commitment (CLAUDE.md §Migration traceability). Revisit if the trail must be queryable/retained. |
| D-28 | **The spec's drift test ("model's fields exist in `public.grievances`") is under-specified — implemented stronger.** `get_grievance_by_id` is **not** just `SELECT g.*`: it **joins `complainants` + `grievance_parties`**, so of the **51** response fields only **23** are grievances columns (26 complainants, 2 parties). Checking all 51 against `grievances` alone would fail on 28 correct fields. Each field is checked against its **owning** table, plus the reverse direction (a new `grievances` column must be declared). Reads the **CL-01 canonical baseline** (`expected_public_schema.sql`) that CI already pins ⇒ no live DB needed. | T3-06 | **Deviation taken deliberately — better than the spec.** Both directions mutation-verified (a fabricated new column and a typo'd field each fail the gate), so it is not vacuous. |
| D-29 | **Spec's "commit 3" split into two commits (`e5fdc4c4` then `28c73f13`).** The spec makes it binding that *"every caller [is] confirmed sending the key **before** this commit lands"* — but the GET and POST clients **sent no key at all**, so a single commit could not satisfy its own precondition. Client-sends-key landed first (inert; the backend ignored it), enforcement second. | T3-06 | **Deviation taken deliberately — implements the spec's binding requirement literally.** Also makes the breaking change independently revertible without stranding callers. |
| D-30 | **Verified-red evidence is stronger than the spec's phrasing.** The spec says the 401 test should fail "because today it returns 200". With a non-existent id it returns **404** — which only shows the request was *served*, not that data leaked. The test therefore mocks a **real** grievance: pre-fix it returns **200 carrying `grievance_description`** to an anonymous caller. Removing just the two `Depends` fails **6 of 13** tests. | T3-06 | **Recorded.** The disclosure — not the routing — is the finding, and the test now pins it. |
| D-31 | **Rate limit dropped — measured, not assumed.** nginx `limit_req_zone` **does** exist (`webchat_rest_compose_prod.tls.conf:8-10`) but **cannot reach these endpoints**: there is no `location /api/grievance` block, so `:5001` bypasses nginx entirely. No Python limiter is installed. The in-code `rate_limit` hits are outbound geocode throttles / report quotas. | T3-06 | **Deferral logged** ([`followups/grievance-api-rate-limiting.md`](followups/grievance-api-rate-limiting.md) + TODO row), per the spec: §6's chokepoint argument cites rate-limiting, so its absence stays visible. **§6's "loggable, authorizable, rate-limitable" is now 2/3 built, not 3/3.** Not solved by network posture: `_ticketing_auth_check` failures are unthrottled ⇒ the key is brute-forceable. |
| D-32 | **`curl` GET without `x-api-key` ⇒ 401 — verified against the running container**, not only via TestClient. Rebuilt the `backend` image and ran it in the deployed posture (key set, `APP_ENV=production`): no key ⇒ **401**, wrong key ⇒ **401**, valid key ⇒ **200 with all 51 fields and `grievance_description` intact**, `POST /status` no key ⇒ **401**. Separately confirmed the shipped dev config (`env.local`: empty key + `AUTH_MODE=bypass`) still returns **200** ⇒ **the local loop is not broken**. Audit verified under the **real** logging config (not `caplog`): emits at INFO with `principal=ticketing`, client IP and outcome, and the secret appears **0 times** in the logs. | T3-06 | Done — see the T3-06 checklist. |
| D-39 | **A rejected (401) request emits no audit record.** Verified against the container: `_ticketing_auth_check` raises before the handler, so `_audit_grievance_read` never runs. Defensible by design — the spec scopes step 2 as a **read** audit, and a 401 reads nothing; uvicorn's access log still shows the bare 401, but with no principal and outside the audit stream. **Consequence: a caller probing keys leaves no trace in the audit trail.** | T3-06 | **Logged, not built** — outside step 2's scope, and it pairs naturally with the throttle deferred in D-31 (unthrottled **and** unaudited is what makes a brute-force cheap *and* invisible). Folded into [`followups/grievance-api-rate-limiting.md`](followups/grievance-api-rate-limiting.md) DoD item 5 so the two are fixed together rather than separately. |
| D-33 | T3-06's manual sweep not executed **through a browser** — none in the build env. | T3-06 | **Pending-human, but largely pre-empted — the integration itself IS verified.** The spec's manual items exist to prove the authn change didn't break real callers; that was driven directly, without a browser. Both `backend` and `ticketing_api` images were rebuilt, a backend was run in the **deployed posture** (key set, `APP_ENV=production`, no bypass) on the compose network, and **ticketing's real client** was pointed at it: `get_grievance_detail` ⇒ **200, 51 fields, description intact** with the key / **401** without; `update_grievance_status('UNDER_EVALUATION')` ⇒ **200 SUCCESS with the `grievance_status_history` row actually written** (`created_by=t3-06-verify`) with the key / **401** without. That is the portal's ticket-detail (`pii.py:55`) and acknowledge/resolve (`ticket_actions.py:375`, `archiving.py:247,312`) paths end-to-end. What remains genuinely browser-only: the rendered UI. Joins D-24 / D-17 — one session covers all three. **Note:** this left demo grievance `B-GR-20260715-KOJH-2441` at `UNDER_EVALUATION` in the local dev DB (append-only history row; local demo data only). |
| D-40 | **Pre-existing wart, noticed while verifying — not fixed.** `POST /api/grievance/{id}/status` returns **500 "Failed to update status"** for an *invalid* `status_code`, because `update_grievance_status` validates the code against `grievance_statuses` and returns `False`, which the handler maps to 500 (`grievance.py`). A client error should be **422**, as the sibling `PATCH /classification` already does for a bad status (`:297`). Found by passing a status code that does not exist (`UNDER_REVIEW`; the valid set is `SUBMITTED / UNDER_EVALUATION / ESCALATED / RESOLVED / DENIED / DISPUTED / CLOSED / archived`). | — | **Logged, not fixed** — out of T3-06's scope (authn/audit/contract), and changing a status code is an API behaviour change for existing callers. Recorded so the next person on this endpoint does not mistake it for a regression from this ticket, as it briefly appeared to be during verification. |
| D-34 | **`5001:5001` not dropped — the spec's premise is off.** It says to drop it "from the **prod** overlay"; the mapping is in **`docker-compose.grm.yml`** (shared by dev and both deploys), and compose **merges `ports` by concatenation**, so `docker-compose.aws.yml` cannot override it (you'd get both binds). Removing it outright breaks the documented host-browser dev loop. | T3-06 | **Necessity recorded + deferred** ([`followups/grievance-api-host-port-binding.md`](followups/grievance-api-host-port-binding.md) + TODO row). Defence-in-depth only — D-25 shows the firewall already closes it. Suggested shape: `"${BACKEND_HOST_BIND:-0.0.0.0}:5001:5001"`. |
| D-35 | **NEW SECURITY FINDING, surfaced by D-25's SG check — out of T3-06's scope.** Orchestrator **`:8000` is open to `0.0.0.0/0`** (SG `sg-01739221c73ab3eb1`), **host-mapped** by `docker-compose.grm.yml:122` (the overlay the staging deploy composes), and **`POST /message` has no auth** (`backend/orchestrator/main.py:102`; no `Depends`/`Header` in the module). **Honest severity: lower than it reads** — `/message` is the *public* chatbot entry point, so it discloses nothing the web chat doesn't. What it bypasses is nginx: **no TLS** (plaintext `session_id`) and **no rate limit** (the `public` 30r/m zone is nginx-level) on a path that triggers **LLM spend**. Also `tcp/3000 0.0.0.0/0` appears **vestigial** — no compose file maps 3000. | — | **Logged, not fixed** ([`followups/orchestrator-port-8000-open.md`](followups/orchestrator-port-8000-open.md) + TODO row). **Worth reading as a pair with D-25:** T3-06 spent 4 commits authenticating a port the firewall already **closed**, while the port it leaves **open** has no auth at all. Repo-level reasoning and infrastructure reality pointed opposite ways; only checking both surfaced it. |
| D-38 | **⚠️ THE TICKET'S TITLE PROMISES `authz`; ITS STEPS DO NOT CONTAIN ONE — AND IT IS NOT IMPLEMENTABLE AS SPEC'D.** Title: *"authn + **authz** + read audit + contract"*. The binding Order block lists only `CONTRACT → AUDIT → AUTHN → RATE LIMIT`, and steps 1-4 match. **No step defines authz, so none was built.** This is not an oversight in execution — it is not derivable from the current scheme: `x-api-key` is a **service** credential (`TICKETING_SECRET_KEY` = "this is ticketing"), carrying **no user identity**. The authz the §6 table compares against — `assert_ticket_visibility`, i.e. **SEAH + jurisdiction gating** (`crud.py:446`) — is a **per-officer** decision. A service key cannot express it: after T3-06, *any* holder of the ticketing key can read *any* grievance, incl. SEAH cases, exactly as ticketing's own process can. Real authz here needs the **officer's JWT propagated through ticketing to the backend**, which is a design change (token exchange / on-behalf-of), not a `Depends`. | **T3-06 / T3-07** | **Recorded as an open gap — do not read T3-06 as having closed the §6 authz row.** Post-T3-06 the §6 comparison stands at: **Authn ✅ · Audit ✅ (log) · Contract ✅ · Authz ❌ · Rate-limit ❌ (D-31).** ⇒ **"Route reads through the grievance API" is still NOT equivalent to the direct read**, which keeps its JWT + jurisdiction gate. The §6 DECISION (legitimize the direct reads as-built) therefore **survives T3-06 unchanged and is, if anything, better supported** — the API is now *safer*, but it is still not an authorization chokepoint. **Whoever revisits the boundary must not cite T3-06 as having made the API a credible authz boundary; it made it a credible *authn + audit* one.** |
| D-36 | **Host `pytest tests/ticketing` is broken by stale `env.local` DB credentials** — `108 failed / 234 errors`, all `password authentication failed for user "nepal_grievance_admin"`. `_host_env.py:44-55` prefers `env.local` over its own correct `setdefault("user"/"password"/"app_db")` fallbacks; compose actually runs `user`/`app_db` (verified against the `db` container). **This is the "dead config" `00-reassessment.md` §6 already named** (`grievance_db` "is dead config, overridden by every compose `environment:` block") — but it is not merely unused, it **actively breaks the host test loop** and reads as a code regression. | — | **Logged, not fixed** (TODO row) — out of T3-06's scope. Workaround used for this ticket's baselines: `POSTGRES_USER=user POSTGRES_PASSWORD=password POSTGRES_DB=app_db POSTGRES_PORT=5433 pytest` (the `:54` guard yields to `os.environ`). With it, host and container agree exactly (544/1/5). **✅ FIXED by T3-08 (`ac0b9d29`)** — by making `_host_env.py` stop reading DB identity from env.local, for a stronger reason than this row gives: those values are dead **by construction** (every compose service hardcodes `POSTGRES_DB`/`USER`/`PASSWORD`), so host pytest was their only reader — i.e. the only thing that could disagree with the DB it talks to. **Bare `pytest tests/ticketing`: 110 failed / 234 errors (132s) → 567 passed / 0 failed (14s).** The workaround above is retired; pinned by `tests/ticketing/test_host_env.py`. |
| D-37 | **`tests/backend` is not in CI.** The `backend-tests` job runs `pytest tests/ticketing tests/orchestrator tests/actions` (`.github/workflows/ci.yml`) — **`tests/backend` is absent**, so all 33 tests added by T3-06 (and the 107 pre-existing) are **ungated**. Found while checking whether the new auth tests would pass under CI's env. | — | **Logged, not fixed** — adding a suite to CI is out of a `backend/` security ticket's scope, and `tests/backend` needs a live DB for some tests (`test_get_grievance_statuses`), so it is not a one-line addition. **Mitigated meanwhile:** every T3-06 test was verified green under **four** env configurations incl. CI's exact vars (`TICKETING_SECRET_KEY=ci-test-secret APP_ENV=dev AUTH_MODE=bypass`). ~~Worth a row in the next CI pass.~~ **✅ FIXED by T3-08 (`d815ddf1`).** The "needs a live DB" blocker was moot — the job already has a migrated, seeded Postgres; the suite costs **1.9s**. Fixed together with the `-m "not integration"` quarantine because they are one hole: **CI seeded the DB specifically for the integration tests, then deselected them.** **364 passed / 390 deselected → 897 passed / 0 deselected (+533 tests, +16s).** **Against this sprint's own standing rule, this deferral never got its `docs/TODO.md` row** — it said "worth a row" and stopped. PROGRESS-only logging is how a deferral goes invisible. |
| **D-13 ✅ ANSWERED** | **§0 reachability trace done (2026-07-15) — T3-01 CONFIRMED Phase 1, a live bug fix.** Verdict per site, established statically (AST+MRO, importer graph, `_FORMS` registry, compose services) **and empirically** (each validator invoked directly): **Site 2** `form_grievance_complainant_review:495` — **REACHABLE**: `grievance_cat_modify` is a required slot (`:281`) on the mainline category-review path, and the branch is the *"user is happy with the selection"* case, not an error case. Probe returns `{'grievance_categories_status': 'LLM_generated', 'grievance_cat_modify': 'slot_skipped'}` with **zero messages dispatched** — the `ValueError` is laundered into a `SKIP_VALUE` exactly as predicted. **Silent.** · **Site 3** `form_seah_1:199` — **REACHABLE**: `sensitive_issues_follow_up` is a required slot (`:70`) for `role == "victim_survivor"`; **no `try/except`** (was unchecked). Probe raises `ValueError: Error getting utterance: 'validate_sensitive_issues_follow_up'`. It propagates through `form_loop.py:231` (bare `getattr` call, no guard) to `main.py:127-135` ⇒ **HTTP 500 on the SEAH intake flow.** · **Site 4** `form_story_main_route_step:33` — **DEAD** (see D-22). | T3-01 | **Phase confirmed: 1.** Two live bugs, one dead site. **Severity is worse than the reassessment estimated** — it framed these as "error/re-prompt branches only, which is why nobody noticed". Site 2 is a *mainline* branch, and site 3 is a **500**, not a silent skip. C-2's open "if" is now closed: reachable **and** swallowed. |
| D-22 | **`backend/actions/forms/form_story_main_route_step.py` is entirely dead code** (both `ValidateMenuForm` and `ValidateFormStoryStep`, 79 lines). Proof: repo-wide grep for the module name returns **no importer**; `ValidateMenuForm` appears nowhere but its own `class` statement; it is **not** in `form_loop.py`'s `_FORMS` registry (the only thing that instantiates form classes); and **no Rasa action server exists** to auto-discover it by class scan — the sole `action_endpoint` is under `config/source/legacy_rasa_config/`, there is no `rasa run actions` anywhere, and the compose stack has no such service. `backend/orchestrator/config/domain.yml:1054` still declares `validate_form_story_main`, which is what makes it *look* live. | T3-01 | **Deferred — spec §Out of scope: "dead branches … log, don't fix".** → [`followups/dead-form-story-main-route-step.md`](followups/dead-form-story-main-route-step.md) + TODO.md. T3-01 still gives site 4 an explicit key + a mapping entry so the "every call-site key resolves" invariant is **total** with no exclusion list; module + entry get deleted together by the followup. |
| D-23 | **Spec step 4 said "author the 3 missing mapping entries". Only 1 was needed — and authoring 3 would have been the wrong fix.** For sites 2 and 3 the copy **already existed**, translated and reviewed, written *for those exact branches*; the introspection was simply looking it up under the wrong key. **Site 2** → `validate_form_grievance_complainant_review` idx 1 = *"No category selected. skipping this step."* — this is precisely the key `self.name()` yields, i.e. the convention `ActionHelpersMixin` uses for the other 196 sites. **That is the root cause in one line: the copy was authored for the `self.name()` convention and the `f_back` override looked it up by calling-function name.** **Site 3** → `action_ask_form_seah_1_sensitive_issues_follow_up` idx 2 = *"Please choose anonymous grievance or grievance with contact details."* — the ask block's index 2, which **nothing else uses**; it was written for this re-prompt. (`validate_form_seah_1` does not exist, so the `self.name()` convention could not help there.) | T3-01 | **Deviation taken deliberately — better than the spec.** Authoring new copy for 2 and 3 would have **duplicated existing strings under second keys**, re-introducing the exact EN/NE drift class H2-08 spent a sprint merging away — and would have added 2 unnecessary AI translations to live user-facing paths. The spec permits this: step 4 says *"either add it or point the call at the correct existing key"*. Only the dead site 4 got new copy (1 CSV row, `needs_translator=0`). |
| D-24 | T3-01's manual EN/NE browser sweep not executed — no browser in the build env. | T3-01 | **Pending-human.** Overlaps H2-08's pending SEAH EN/NE walk-through and T3-03's Slow-3G sweep — one browser session covers all three. **Largely pre-empted**: the §0 probe drove both live sites in EN *and* NE and captured the rendered strings (see the T3-01 checklist), so the sweep is confirmation rather than discovery. |
| D-16 | **Voice-chunk upload sessions live in a process-local dict** (`file_server_core.py:29-31`), and the `.part` file is a local path. The protocol is stateful across requests, so every chunk must hit the same process. Works today — `docker-compose.yml:48` runs uvicorn with **no `--workers`** (verified) — but adding workers/replicas silently breaks voice notes: chunk 1 gets a 404, which the client **swallows** (`voiceNote.js:31` matches `"not found"`), so it reads as a flaky feature, not a misconfiguration. Same "invisible on dev, deterministic in prod" shape as T3-03 itself. | T3-03 | **Deferred — spec §Out of scope says log, don't fix** (`file_server_core.py` session lifecycle). → [`followups/voice-chunk-session-store-is-process-local.md`](followups/voice-chunk-session-store-is-process-local.md) + TODO.md. **Not a live bug** — do not report it as one. |
| D-17 | T3-03's manual Slow-3G browser sweep not executed — no browser in the build env. | T3-03 | **Pending-human.** Overlaps HR-07's webchat sweep — do both in one session. Automated coverage is the mitigation: the race is now guarded by a test that is verified red pre-fix, which is stronger than the manual check it substitutes for. |
| D-18 | **Scope note — the spec's change 2 was insufficient as written.** It prescribed moving `FormData` construction inside the retry closure. That alone would **not** have healed the retry: `uploadId` was a *parameter*, captured at call time, so a rebuilt body would re-read the same stale `null`. The fix required changing the param to a `getUploadId` resolver. | T3-03 | **Fixed in scope** (single caller, contained). Recorded because the spec's own acceptance test ("retry re-reads a now-populated `uploadId`") could not have passed against the change as literally specified. |
| D-19 | **The `ticketing.*` ↔ `public.*` boundary is vestigial, and D-03's framing of it was wrong.** Measured: **11 statements / 5 tables / 3 writes** ticketing→`public.*` (incl. `DELETE FROM public.grievance_classification_taxonomy`, unqualified), **~12 sites** backend→`ticketing.*` (chatbot intake location validation reads `ticketing.locations` via its own `psycopg2.connect`), **one DB, one role**, zero enforcement. Both goals the March-2026 rule was written to serve are already dead. `public.complainants` — the one table marked *"never touch"* — is joined by a Celery beat job **every 2 min** (`grievance_sync.py:178`, non-PII column only). Archaeology: rules #1/#2 are **genuine architecture** (2026-03-11, pre-dating any AI split, rationale deleted by doc reorg `21631051`); rules #3/#4/#5 have **no recorded rationale ever** and trace to a `.claudeignore` that forbade Claude from reading `backend/services/` plus a hand-written context doc that **falsely** claimed the API decrypts PII — `pii_vault.py` is the workaround that fell out of it. Full evidence: [00-reassessment.md](00-reassessment.md) §6. | T3-07 | **DECIDED 2026-07-15 by the project owner** (§6 → DECISION): drop rule #1, keep + **pin** no-FK and no-PII-columns, amend the docs to as-built. Supersedes D-03. Closes the followup's DoD item 1. |
| D-20 | **`GET /api/grievance/{id}` and `POST /api/grievance/{id}/status` are unauthenticated** (`backend/api/routers/grievance.py:249`, `:202`) — no `Depends`, while the two `PATCH`es beside them have `Depends(_ticketing_auth_check)`. The GET returns the full record incl. `grievance_description`; the POST **mutates state and fires SMS + email to the complainant** (`:227`). No read audit exists in `backend/` at all. Both bound to the host via `docker-compose.grm.yml:117` (`5001:5001`); **not** proxied by prod nginx. | **T3-06** | **New ticket** → [`05-grievance-api-hardening-spec.md`](05-grievance-api-hardening-spec.md). **⚠️ Phase gate: EC2 :5001 inbound is UNVERIFIED from the repo.** If open ⇒ Phase 1 / incident. **Check the security groups first.** |
| D-40 | **`scripts/ops/create_scoped_roles.sql` has never been runnable — a defect nobody could have found by reading it.** The spec expected a comment fix ("the script is opt-in and inactive, so this is a doc fix, not an outage — but adopting it as written would break 4 paths"). **Adopting it as written was already impossible:** all three `CREATE ROLE … PASSWORD :'…_pw'` calls sit inside `DO $$ … $$` blocks, and **psql does not interpolate `:'var'` inside a dollar-quoted string** — it errors `syntax error at or near ":"` on the *first* role, before granting anything. Verified against the running PG container, and verified byte-identical at `HEAD` ⇒ **pre-existing, not introduced here.** So the spec's "would break 4 paths" concern was moot: it breaks at line 26. | T3-07 | **Fixed in scope, deliberately.** Leaving it would have made the grants fix worthless — the next person to adopt least-privilege hits a syntax error on line 26 and reasonably concludes the script is abandoned. Rewritten to the idiomatic psql `SELECT format(…) WHERE NOT EXISTS … \gexec` pattern (interpolation works outside `$$`). **Validated end-to-end**: whole script run against the dev DB inside `BEGIN … ROLLBACK` — 3 roles created, all grants applied, `information_schema` shows exactly the intended privileges, rollback left only the pre-existing `ops_app`. **This is the one place T3-07 touched a non-doc/non-test file**; the spec puts the script in step 3's scope explicitly. |
| D-41 | **The spec's "prove the guard guards" test is inapplicable as written — done stronger.** It asks to *"verify it goes red if you rename a column in a scratch DB"*. **That cannot work by construction:** the guard reads the committed `expected_public_schema.sql` baseline *file*, so a scratch-DB rename would not move it. The chain is `real schema ──(CL-01 gate, in CI)── baseline ──(this guard)── ticketing's contract`; CL-01 already owns the DB↔baseline link, and duplicating it would need a live DB in a test that is deliberately DB-free. | T3-07 | **Mutation applied where the guard actually reads** — the T3-06/D-28 standard. Stronger than the spec in coverage: **all 5 guards** proven to fire against the real tree (not just the drift one), then restored green. Two mutation tests are also committed *in* the suite so the proof does not decay into a one-off I ran once. |
| D-42 | **Method finding — a text-based SQL scanner cannot do this job, and the failure is invisible.** The guard first collected every string constant naming a `public.*` table, anchored on clause position (`FROM|JOIN|INTO|UPDATE public.x`) to exclude prose. **It still found 13 "statements" where 11 execute** — because English prose *is* clause-shaped here: `grievance_content.py:2` says *"merge non-PII grievance fields **from public.grievances**"* and `ticket_intake.py` says *"TicketCreate **from public.grievances**"*. A docstring was being parsed for columns. Fixed by scoping the collector to **string arguments of `text()` calls** via the AST — "what executes", not "what mentions a table". | T3-07 | **Recorded because the failure mode is silent**: had the prose been counted, the census would have been wrong by 2 and the contract would have listed `is_temporary` (named only in a docstring) as a live dependency. Same family as D-14's *"resolve by MRO, never by grep"* and §1's `awk`/`\b` trap: **on this codebase, structural questions must be answered structurally.** Third instance this sprint. |
| D-43 | **`ticketing.tickets.complainant_reply_owner_id` trips a naive PII-name check — and it is not PII.** The `complainant_*` namespace fence (broader than the spec's four exact names, to catch a future `complainant_mobile`) flagged it. Despite the prefix it parses as **`(complainant_reply)_owner_id`**: the user_id of the **officer** holding the "reply to complainant" duty (`models/ticket.py:100-103`). | T3-07 | **Declared in the fence's allowlist with the reasoning inline**, not by weakening the test. Recorded because the next reader will hit the same false alarm — and because it is exactly the trap the spec's own narrower "four exact names" check would have silently passed over in the other direction. |
| D-44 | ~~**Pre-existing test failure, not T3-07's:** … **Environmental, not code:** `l1-officer-3` appears in **neither the seed code nor the test** — the local dev DB has 4 L1 officers, so load-balancing picks one the assertion doesn't list.~~ → **PARTLY WRONG. CORRECTED 2026-07-15 by T3-08 — see D-45.** The *verdict* (pre-existing, not T3-07's, DB-state-sensitive) holds. The *mechanism* was wrong on its central claim: **`l1-officer-3` IS in the code** — `ticketing/constants/demo_officers.py:22`, scoped by `ticketing/seed/mock_tickets.py:268` since `b8cab274`. It is a legitimate seeded province-pool officer, not DB residue. The grep behind "appears in neither" covered `ticketing/seed/` and the test file but **not `ticketing/constants/`**. | — | **Superseded by D-45.** Fixed in T3-08, not deferred. |
| D-45 | **D-44 re-diagnosed, and the real finding is upstream of it.** Measured, not inferred: the province-fallback pool is **4** seeded L1s (Morang `l1-officer`+`l1-officer-4`, Jhapa `l1-officer-2`, Sunsari `l1-officer-3`); `auto_assign_officer` ranks by `(territory-covering, active ticket count, user_id)`; `territory_covering_user_ids` is inert in the demo seed (it keys off `Organization.territory_location_code`, which the seed never sets) ⇒ **ranking is purely least-loaded, uid-tiebreak**. The seed gives officers 3/4 scopes but **no tickets**, so on a pristine DB all of `-2`/`-3`/`-4` sit at load 0 and `l1-officer-2` wins **by `user_id` sort order alone** — the assertion passed by luck. On the dev DB (`l1-officer-2` = 6 active, `-3`/`-4` = 0) `l1-officer-3` wins and it fails. **So the assertion pinned an identity decided by live data while the test's stated property is a scope.** **Why it rotted:** `b8cab274` grew the roster 2→4 and nothing pointed at the roster. **Why nobody noticed: CI never ran it** — `-m "not integration"` (D-37's neighbour), even though the same job seeds the DB *specifically for these tests*. | **T3-08** | **Fixed** (`9e74e457`): asserts `PROVINCE_L1_POOL` sourced from `demo_officers`. Not a weakening — verified it still goes red when the fallback is broken, still passes on a pristine seed, and now passes on a drifted dev DB. ⇒ bare `pytest tests/ticketing` = **567/5/0**, stable across runs. |
| D-45 | **Portal checks have no Docker path as shipped — built one rather than going native.** CLAUDE.md forbids native `npm run build`; but `grm_ui`'s image is the Dockerfile's **`runner`** stage (standalone output) and carries **no dev deps at all** — no `next` bin, no `tsc`, no `app/` — so `tsc`/`eslint`/`vitest`/`build` cannot run in it. Resolved by building the Dockerfile's existing **`builder`** stage (`docker build --target builder`, layers already cached from `make wsl-up`) and running each check in it with the source bind-mounted and the image's `node_modules` preserved via an anonymous volume. Node in-harness is **20.20.2**, matching the image (host is 20.19.2 — so this is also *more* faithful than the host). | T3-05 | **Recorded as method.** Every gate in T3-05's 8 commits ran this way; nothing was built or served natively. Reusable: `--target builder` + bind-mount is the general answer for portal dev checks under the Docker rule. |
| D-46 | **The spec's bundle-measurement method does not exist in this Next version — and the benefit it was measuring is not real.** §Tests says *"record the settings route's build size (`next build` output)"*. **Next 16.2.6 no longer prints the Size / First Load JS columns** — the route table is routes-only — and under Turbopack there is **no `app-build-manifest.json`** and no per-route chunk mapping (`.next/static/chunks` is flat + hashed), so per-route attribution is unavailable without adding a bundle analyzer. Substituted a reproducible whole-app metric (total client JS bytes + the settings server chunk). **Result: 1,461,565 → 1,461,373 B (−192 B, −0.01%), 38 chunks both sides, settings server chunk 32,853 B byte-identical.** | T3-05 | **Measured and reported honestly, per the spec's own instruction ("if it doesn't move, say so").** The −192 B is the pruned dead imports, i.e. noise. **This is a structural result, not a measurement artifact: moving code between modules that are all still statically imported by the same page cannot shrink the bundle — the module graph is unchanged.** Only per-tab `dynamic()` would, and the spec explicitly forbids that ("move as-is"). ⇒ **The review's "cuts the settings bundle" claim is now the *third* dead claim on this item** (joining "kills the hooks-crash class" and "L effort"). **Correction owed to `devils_advocate_codebase.md` at close-out; the sprint summary must claim reviewability only.** |
| D-47 | **`ProjectWorkflowSelect` is dead code — and extracting it made the signal *quieter*.** Zero callers repo-wide (only its own definition); superseded by `ProjectWorkflowsEditor`. Already dead pre-sprint — it is one of the 141 baseline eslint warnings. Moved verbatim per "move, don't fix". **Side effect: the portal warning count fell 141 → 140, because `no-unused-vars` does not flag an *exported* symbol** and every component in the cluster is exported by convention. | T3-05 | **Logged, not fixed** ([`followups/dead-project-workflow-select.md`](followups/dead-project-workflow-select.md) + TODO row). **The −1 is a lost signal, not lint progress — do not bank it as an improvement.** Same reason pre-existing dead imports/unused locals (`listScopes`, `ComingSoon`, `canManageStructure`, `wfSaving`…) were left exactly as found: the spec's *"do not fix the 141 warnings; do not let the count grow"* rule. |
| D-48 | **Per-tab render smoke tests deferred — the portal has no DOM test harness at all.** Not a skipped test, a missing capability: no `@testing-library/react`, no `jsdom`, no `@vitejs/plugin-react`; `vitest.config.ts` is `environment: "node"` with `include: ["**/*.test.ts"]` — **`.tsx` is never collected** — and its header states the intent deliberately. Building one = 3 dev deps + lockfile + `grm_ui` rebuild + switching the **shared** config to `jsdom` under all existing tests: a dependency decision, not a verbatim move. **Delivered instead: 27 unit tests on the extracted leaf helpers** (`workflowHelpers`, `roleEntry`, `friendlyError`) — the **first tests under `components/settings/`**, fitting the existing node harness with zero new deps. **Both suites mutation-checked** (drop `publishedWorkflowOptions`' `selectedId` guard ⇒ red; swap `mapGrmRoleToEntry`'s `??` for `||` ⇒ red) so they are not vacuous. Vitest 61 → 88. | T3-05 | **Deferral logged** ([`followups/settings-tab-render-tests.md`](followups/settings-tab-render-tests.md) + TODO row), per the spec. **Closes half of D-12** — settings is no longer at zero coverage. The extraction is what makes the tabs individually mountable, so the remaining work is now S/M rather than blocked. |
| D-49 | T3-05's manual sweep (every tab at every role tier; gating unchanged) not executed **through a browser** — none in the build env. | T3-05 | **Pending-human.** Weaker cover than D-33's: T3-05 has no equivalent of driving the integration directly — the deliverable **is** the rendered UI, and `page.tsx`'s role-gating (`isSuperAdmin`/`isCountryAdmin`/`isProjectAdmin`) was not exercised. What *is* banked: `tsc` clean at every commit, `next build` green, bodies moved by line-range extraction rather than retyped, and the gating code in the shell was **not touched** (only imports changed). **Joins H2-02/H2-06 + D-24/D-17/D-33 — one browser session covers all of them.** |
| D-50 | **Method finding: dependency resolution by eye is unreliable at this scale — scan, don't guess.** The first extraction (`SystemConfigTab`) was import-scanned by hand-listing the symbols I expected and missed **`getOrgRoles`/`setOrgRoles`**, caught only by `tsc`. Switched to resolving each extracted body against `lib/api`'s parsed export list programmatically; the Workflows (8 files) and Projects (5 files) clusters then landed with **no missing api imports**. A second, related miss: the hook scan used `\buseRef\(` and missed the generic call form `useRef<...>()`. | T3-05 | **Recorded as method.** Same lesson the sprint learned twice elsewhere — [`00-reassessment.md`](00-reassessment.md) §4 settles the call-site count by **AST+MRO, never grep** (D-14), and D-42 scopes the SQL collector to `text()` args. **Static-symbol questions get resolved by parsing, not pattern-matching.** `tsc` is the backstop, but it only fires after the fact and only for *missing* symbols — never for *stale* ones. |
| D-21 | **The followup doc's load-bearing justification is inverted.** [`followups/ticketing-cross-schema-direct-reads.md:50`](followups/ticketing-cross-schema-direct-reads.md) asserts reads via `GET /api/grievance/{id}` *"**are** loggable, authorizable, and rate-limitable at one place."* **None of the three is implemented** (D-20) — the verb should be *"could be"*. The direct SQL it condemns sits behind Keycloak JWT + a jurisdiction gate + an audit model; the API has none. Its DoD item 2 ("extend the API to serve what `grievance_content.py` needs") is **already satisfied** — 9/9 fields ship via `SELECT g.*`, and `resolved_summary_builder.py:139-150` already reads `grievance_description` over HTTP. Its unpriced cost: `grievance_sync.py` pages 500 rows/2 min and the API has **no bulk endpoint**. | T3-07 | **Doc corrected in T3-07 step 3**; the followup is closed with the decision rather than executed. Recorded because it was 1 sprint away from being implemented as written. |
| D-51 | **⚠️ BUG FOUND WHILE CHARACTERIZING — pinned, not fixed.** `add_more_info_flow`'s completion path (`state_machine.py:1916-1932`) invokes `action_ask_story_step`, whose **entire body** is guarded by `if story_route in [route_status_check_grievance_id, route_status_check_phone]` (`action_ask_commons.py:29`) and otherwise dispatches nothing and returns `[]`. The branch extends `dispatcher.messages` with an empty list ⇒ the turn returns **zero messages**. The user submits the detail they just typed and the chatbot says nothing. Measured on identical sessions: `story_route` unset ⇒ **0 messages**, set ⇒ 1. | T3-02 p2 | **Logged, not fixed** — spec: *"characterization records current behavior, including bugs… do not fix it in this ticket; you would be changing behavior under a test net you just wrote"*. → [`followups/add-more-info-silent-turn.md`](followups/add-more-info-silent-turn.md) + TODO row + `a6519a6b`, same commit. **Reachability deliberately not guessed** (the real status-check flow sets `story_route` first) — D-13 is the precedent for reachability guesses being wrong here. The wrong behaviour is pinned by a test whose docstring says: **if it fails, the bug was fixed — delete it, don't relax it.** |
| D-52 | **⚠️ SECOND BUG, same class.** `status_check_form`'s form-selection chain (`state_machine.py:1674-1683`) ends in a bare `else: form = _get_status_form_1()`. An unrecognized `active_loop` therefore silently runs status form 1, which on an already-filled session dispatches nothing ⇒ **0 messages** — and nothing clears the bogus `active_loop`, so **every subsequent turn repeats it**: the session is wedged, silently. | T3-02 p2 | **Logged, not fixed** → [`followups/unknown-active-loop-silent-fallback.md`](followups/unknown-active-loop-silent-fallback.md) + TODO row + `eccbfeb5`. Preserved verbatim through p3 — moving code and fixing behaviour in one commit is how a refactor hides a bug. |
| **D-51+D-52 ⇒ the pattern is the finding** | **Three instances of one class this sprint, and only the first is fixed.** D-08: unrecognized **state** ⇒ caught by p1's terminal `else`. D-51: recognized state, action no-ops. D-52: recognized state, unrecognized **loop**. **Neither p1's `else` nor p4's table can see D-51/D-52 — the state is recognized, so the dict lookup succeeds and the default never fires.** *"Never return empty messages"* is a convention this file **states** at `:1629` and honours in some places, but **enforces nowhere**, so each site fails separately and gets found separately. | T3-02 | **Recommendation recorded in both followups:** a single `run_flow_turn` **postcondition** — no turn returns zero messages; log state + active_loop at error — subsumes D-08, D-51, D-52 and any future sibling. **Cheaper than p4 and worth more.** If only one of the two gets done, do the postcondition. (Check `attachment_ids_sync` for legitimately-silent turns first.) |
| D-53 | **T3-02 p4's estimate does not survive measurement.** The spec sizes the handler table as *"the cheap, safe part — 22 one-line entries against a proven signature"*. That holds only if every branch **has** the signature. p3 extracted **one** (`status_check_form` — correctly; it is where the risk lives), leaving **20 arms / ~1,058 lines of branch bodies still inline**, ten of them > 40 lines (`done` 120, `modify_grievance_menu` 107, `contact_form` 101, `map_location` 94, `main_menu` 92…). ⇒ p4 is **"extract 19 more branches, then write a table"** — an **M**, not an S; the table is its last 5%. | T3-02 p3/p4 | **Not started (STRETCH), logged in all three places** (`4c454c5a`) → [`followups/run-flow-turn-handler-table.md`](followups/run-flow-turn-handler-table.md) + TODO row. Not an argument against p4 — an argument against doing it in the hour the "S" implies. **Banked meanwhile: `run_flow_turn` 1,485 → 1,175 (−21%)** and the convention proven against the hardest branch in the file. |
| D-54 | **T3-04's spec: the parametrized net cannot survive its own steps 2-4 as described.** It says to parametrize the mocked payload over ciphertext and plaintext *"so the same test proves both … unchanged"*. It cannot: **today ciphertext is decrypted to plaintext by the vault workaround; once step 3 deletes that workaround the same ciphertext is masked to `None`.** No single assertion is true at both ends. | T3-04 | **Deviation taken deliberately.** Pinned the property that IS true at every commit, and is the one officers care about: *plaintext in ⇒ plaintext on the card* (the regression guard proper) and *ciphertext in ⇒ **never ciphertext on the card*** (by decryption today, by fail-closed masking after step 3). Recorded because the spec's own acceptance wording could not have held. Same family as D-18 (T3-03's change 2 was insufficient as literally written). |
| D-55 | **T3-04's step 4 is not implementable as written, and the alternative is stronger.** It says remove `DB_ENCRYPTION_KEY` *"from the ticketing service's env in the compose stacks + env.local"*. There is nothing to remove: the var appears in **no compose file**, and every service — `backend` included — loads the **same shared `env_file`**, so deleting it from `env.local` strips it from `backend`, which legitimately owns it, and breaks encryption at intake. | T3-04 | **Deleted the settings *field* instead** (`extra="ignore"` makes the env var inert for ticketing). Stronger than the spec's own check: ticketing's code has **no accessor at all**, so "ticketing holds no PII key" is a structural fact in the repo rather than a per-environment convention. **Demonstrated in the rebuilt container: `DB_ENCRYPTION_KEY` IS present in ticketing_api's env (unavoidably — shared env_file) and `hasattr(settings, "db_encryption_key")` is `False`.** Guards mutation-verified. |
| D-56 | T3-04's manual sweep not executed **through a browser** — none in the build env. | T3-04 | **Pending-human, but the check the ticket exists for IS verified** — the D-33 standard. Both images rebuilt; a grievance seeded with **real pgcrypto ciphertext at rest** (`c30d0407…`); then: **real HTTP API ⇒ plaintext on all four fields**; **ticketing's real client + real `pii_vault` ⇒ standard card shows `Sita Rai` / `+9779812345678` / email / address, SEAH card all `None`**; `ticketing_api` boots with no key accessor. Seeded rows deleted afterwards (verified 0 leftover) — unlike D-33, this left **no** mutated demo data. What remains genuinely browser-only: the rendered UI. Joins D-24 / D-17 / D-33 / D-49 — one session covers all of them. |
| **D-57** | **⚠️ THE DEVIL'S-ADVOCATE PASS FOUND THE BOOKKEEPING WORSE THAN THE ENGINEERING.** Three independent auditors re-verified every "done" claim against the tree. **The engineering held: 14/14 T3-01+T3-03 claims survived refutation, T3-07's 11/5/3 census reproduced exactly under an independently-written AST collector, T3-05's "verbatim extraction" survived a multiset line-diff of all 7 move commits, and D-48's tests were confirmed non-vacuous by re-running both named mutations.** What did **not** hold was the record of it: **(a)** every commit hash cited for T3-05/06/07 — **15 of them** — was **unreachable from HEAD**, surviving only on a local-only `pre-split-tip` branch (a rebase; `git patch-id --stable` matched all 15 1:1, so no work was lost, but `git show 25223408` fails for anyone auditing the branch, and the trail evaporates if that ref is deleted); **(b)** two T3-05 followup docs were **untracked** — linked from PROGRESS **and** TODO.md, present on disk, in **no commit** — which broke the `docs-links` CI gate on HEAD (5 broken links) and made the standing rule satisfied *in appearance only*; **(c)** `PROVINCE_L1_POOL` "sourced from `demo_officers`" was **false** — a hand-typed frozenset, so its stated benefit (roster change updates the expectation) did not exist and it was one roster change from rotting by the exact mechanism D-45 diagnoses; **(d)** CLAUDE.md rule 1's *"Pinned by `test_boundary_policy.py`, which fails when the code and this list disagree"* was **false** — nothing opened CLAUDE.md; the test pinned code ↔ a dict inside itself; **(e)** the "Inherited pending-human debt" section still listed the `-m "not integration"` quarantine as open, **contradicting four other places in this same document**. | **all** | **All five fixed, and the three that could be made *true* were, rather than softened** — the option this sprint took for `CLAUDE.md:121` in T3-04: (a) 33 hash references rewritten to their on-branch twins, each verified `merge-base --is-ancestor HEAD`; (b) both followups committed (`b4880ee5`) + a link-checker run over the sprint docs ⇒ all resolve **and** are tracked; (c) `PROVINCE_L1_POOL` now **derived** from `DEMO_OFFICER_SPECS` (`c437d7b3`) — reproduces the old set exactly, plus a guard that it is neither empty nor over-broad, mutation-verified; (d) **CLAUDE.md's table is now genuinely pinned** (`4bc1fd19`) — two guards parse it and gate the table set **and** which tables are written, both mutation-verified; (e) rows struck through with the contradiction recorded rather than deleted. **The lesson is the sprint's own, turned on itself:** §6 warned that *"a rule without its reason decays into cargo cult"*; the sibling is that **a claim that only *asserts* enforcement decays the same way but invisibly, because everyone believes a test has their back.** Four of the five were claims *about* verification, not verification — and every one was written by the same passes that did the (real, verified) engineering. |
| D-58 | **Unresolved published discrepancy: `tests/ticketing` is documented as both 563 and 567.** 567 in PROGRESS/D-45/T3-08's row (host); 563 in `ci.yml`'s comment + the closed followup (a CI-shaped scratch DB). Identical skip count (5), delta exactly **4**. **The obvious hypothesis is disproven**, not merely unchecked: it is not `test_host_env.py`'s 4 tests — that file was added by T3-08's **commit 1** (`ac0b9d29`), so it existed at commit 3, and it passes **in-container** too. No DB-dependent parametrization exists that would collect fewer cases on a fresh seed. | — | **Recorded, NOT explained — deliberately.** Settling it needs a run in CI's environment, which is not reproducible from here; inventing a cause is how D-44 got published with a wrong mechanism and needed D-45 to correct it. Both figures are real measurements of differently-scoped runs; **neither doc says which environment it measured**, which is the actual defect. **Whoever next reads a green CI run: record the environment alongside the number, and close this.** Today's host figure is **605 passed / 5 skipped / 610 collected** (post-T3-04 + the new guards). |

---

## Inherited pending-human debt (NOT cleared by this sprint)

Carried from Tier 1 / Tier 2 — listed so they aren't forgotten, but they are **not** in this sprint's DoD:

- HR-07 webchat manual browser sweep (**overlaps T3-03's manual sweep — do them together**)
- HR-05 live deliberate-failure + branch-protection checks
- H2-01 Keycloak token-expiry sweep; H2-02 / H2-06 UI click-through parity (**overlaps T3-05's manual sweep**)
- H2-08 SEAH EN/NE webchat walk-through (**overlaps T3-01's manual sweep**)
- ~~CI-on-integration~~ ✅ **CLOSED by T3-08** (`d815ddf1`) — CI runs the integration tests; `tests/backend` gated too. The **merge from `dev/tier2-quality`** is the half of this row that remains open.
- ~~`@integration` backend tests quarantined (`-m "not integration"`)~~ ✅ **CLOSED by T3-08** (`d815ddf1`). **This row was flatly false from the moment T3-08 landed** and contradicted four other places in this same document (the T3-08 row, the baselines caveat, D-37, D-45). Kept struck-through rather than deleted: a sprint that spent T3-08 proving the quarantine had "outlived its reason" should not lose the evidence that its own doc went on asserting the quarantine for the rest of the day. **Found by the sprint's own devil's-advocate pass, not by anyone reading the file.**
- SEAH translation fact-check (`docs/seah/translations_seah_review.csv`)
