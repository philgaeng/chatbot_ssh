# GRM Ticketing — TODO / Backlog

> This file tracks open gaps, pending tasks, and future features.
> Updated alongside `PROGRESS.md`. Read both before picking up work.
> Last reviewed: 2026-07-04 — moved from `docs/sprints/claude-tickets/` to `docs/`; obsolete Cognito rows removed (auth is Keycloak as-built). Entry dates below reflect when items were logged.

---

## 🔵 Author-defined slots — BUILT 2026-08-04 (§9 build order complete)

**Entry point: [`sprints/2026-07_org_chart_positions/HANDOVER-author-defined-slots-build.md`](sprints/2026-07_org_chart_positions/HANDOVER-author-defined-slots-build.md)** §7 — what the closing slice built, and the traps that cost time. Plan: `DECISION-author-defined-slots.md`.

The project type is the template: it names the workflows a project runs and the organizations
it must have, in the author's own words. Creating a project is **organization → its types →
name it**. Built across two sessions: the project console + author-named jobs
(`tier_labels`/`required_tiers`, `j6l8n0p2`) + binary go-live + type owner and freeze-when-bound
(`l8n0p2r4`) + back-filled types (`n0p2r4t6`), then the type authoring UI, the project reading
its type's catalog, the creation flow, and **A3/A5 → B1** (a blocker that names the gap with the
author's word).

**Amended the same day — [`DECISION-organization-membership`](sprints/2026-07_org_chart_positions/DECISION-organization-membership.md).** The `routing_org_role` anchor is **retired**: an organization's grievances are the ones on **its projects** (and its lots), widened down the org tree — so a donor and a ministry on the same project both see the same grievances, where the old single stamp gave them to exactly one. `services/org_reach.py` is the rule; the export column is now **"Organizations"**; GRC convening resolves members from the project's staffing.

**Staffing (2026-08-04):** a level now declares whether it is staffed per lot or once for the
project (`workflow_steps.staff_per_package`, `p2r4t6v8`); the staffing screen is one place, levels
last→first, per-lot levels asking lot by lot; the go-live gate reads the flag instead of guessing.
*Known cost:* the screen mounts one `CastStaffing` per level (and per lot for per-lot levels), each
fetching the workflow + casts — fine at 4 levels × 3 lots, worth lifting the fetch if a workflow
grows.

**Test hygiene (2026-08-04):** `test_admin_scope_org_staffing` cleaned up by the id it *asked*
for, but `create_organization` upper-cases ids (`ascii_alnum(raw.upper())`), so the lookup found
nothing and every suite run left two contractor organizations behind — 69 of them by the time the
picker on a lot row made it visible. Cleanup now uses the **returned** id. Worth copying that
habit: after any create-through-the-API test, delete what the API says it made, not what you sent.

**Left, deliberately:** the legacy organization store is dead but not dropped —
`project_actor_roles`, `implementing_agency_org_id`, `project_donors` are read-only fallbacks
for projects created before types existed — plus `project_types.routing_org_role`, now unused. One cleanup sweep once none exist; touch list in
[`followups/actor-role-catalog-not-dropped.md`](sprints/2026-07_org_chart_positions/followups/actor-role-catalog-not-dropped.md).
Also open: the position-first staffing picker (13 §5A.2), the `is_sensitive` rename below, and
the ui/04 wireframe still mocking "Implementing agency + Donors".

---

## 🔵 One role key per slot — BUILT 2026-08-09 (`followups/seed-synthetic-slot-keys.md`)

`officer_scopes` identifies a cast assignment by `role_key` alone — no step, no tier — so two
slots sharing a key were indistinguishable in the data. `adb_hq_safeguards` backed both
L3-supervisor and L4-actor, and 18 assignments made at Level 3 all surfaced at Level 4; worse,
assignment and go-live resolve by `role_key`, so "kept informed" at one level made someone a
candidate **Actor** at another. Five keys collided, `dor_dpd_adb` across four slots.

Migration `x0z2b4d6` gives every slot its own synthetic key, the seeds author them, and publish
refuses a workflow that reuses one. A fresh migrate + seed reports zero collisions.

| Outstanding | Why |
|---|---|
| **Scopes left where the key backed 2 slots in one workflow** | Which slot a row meant is unknowable; guessing could promote an observer to actor. They are inert (no slot uses those keys) — re-assign those officers on screen |

## 🔵 Packages are the only coverage — BUILT 2026-08-08 (`followups/drop-project-locations.md`)

Decided by Philippe 2026-08-08: a package is **code + name + description + locations, and that
is it**; every project has at least one; coverage is declared nowhere else. Locations folded into
Packages, per-package organizations moved to Organizations, and **"lot" is retired** — one
object, one word ([ui/05 §4.1](ticketing_system/ui/05_ui_copy_style.md)).

| Outstanding | Why it is not done |
|---|---|
| **Drop `ticketing.project_locations`** | Left in place, unread. Dropping a populated table is irreversible and belongs in its own migration — see [`followups/drop-project-locations.md`](sprints/followups/drop-project-locations.md) |
| **Staffing screen wording for overlapping packages** | Packages may overlap (a bridge contract covers several road packages), so a district can have *plural* responsible officers. The engine already unions them; the Staffing screen has not been checked for copy that implies one |
| **`ui/04` mockup is stale** | The HTML mockup still shows Locations and Packages as separate sections, and organizations inside the package card |

## 🟡 Tests coupled to live demo config (2026-08-07, `followups/tests-coupled-to-live-demo-config.md`)

- [ ] **10 ticket tests fail whenever an admin closes KL Road's intake.** `test_ticket_uniqueness.py`
      and `test_grievance_sync.py` create tickets on the shared `KL_ROAD` project, so they inherit
      its go-live gate — ticking "Staffed for each lot" on Level 1 with unstaffed lots reds them
      all. Give them a disposable project fixture; leave the intake gate alone, it is correct.
- [x] The three staffing/go-live tests that asserted the same mutable config as a standing fact
      now set their own precondition (2026-08-07).

## 🟠 Sensitive workflows — access slice SHIPPED 2026-08-02; rename outstanding (`DECISION-sensitive-workflows.md`)

SEAH stops being a `workflow_type` track and becomes **an ordinary optional workflow with `is_sensitive`**. Specs updated (09/11/12/13, ui/04, ui/06).

**✅ Built (660 passed, 5 skipped):** case access is **cast-only** — `can_see_seah_extended()` lost its
`super_admin`, `adb_hq_exec` and `workflow_track=='seah'` branches; the configure side moved to a new
`can_configure_sensitive_workflows()` so admins still author/bind sensitive workflows while seeing no
case. Both PII disclosure endpoints gate on cast membership — **this closed a real hole**: the reveal
had no policy check at all (`clients/grievance_api.py` is a proto fallback that always returns
`granted: true`; the backend's `POST /api/grievance/{id}/reveal` was never built), so any admin who
could see a sensitive case could reveal its PII. Also: sensitive-as-default rejected (422), go-live A2
deleted, portal takes the server's answer instead of deriving access from role keys. Pinned by
`tests/ticketing/test_sensitive_workflow_access.py`.

**Outstanding** (DECISION §7): the **rename** (`workflow_type`/`is_seah`/`workflow_track` →
`is_sensitive` + a configure capability, with migration), `include_seah` → `include_sensitive` in
reports, `archiving_policy` + `tasks/notifications` track keys, retiring the `SEAH_ROLES` fast-path,
the **Sensitive** checkbox in the workflow editor (mocked in ui/06, not built), the real backend
reveal-policy endpoint, and docs/seah/.

---

## Grievance-workflows screen follow-ups (2026-08-02, `followups/workflow-stream-vocabulary-and-intake-route-labels.md`)

- **[Copy] `INTAKE_ROUTE_CATALOG` labels carry jargon.** "File a grievance (safeguards GRM)" /
  "Report a road hazard (fast path)" surface in the project editor's **Chatbot menu** picker, so
  they are governed by [ui/05](ticketing_system/ui/05_ui_copy_style.md). Rename to plain wording —
  ideally mirroring the chatbot's own `story_main` menu text. (`ticketing/constants/workflow_routing.py`)
- ~~**[Go-live] Delete the "Classification coverage" check.**~~ ✅ **done 2026-08-02** — check and
  `workflow_routing.uncovered_classifications()` removed; the `A4` ID collision with doc 13 §7 is
  gone. Go-live copy de-jargoned in the same pass (no "binding"/"catch-all"/"actors"/`L1`; the
  panel says "Accepting grievances", not "Tickets OK").
- ~~**[Go-live] Binary severity is spec-only.**~~ ✅ **done 2026-08-04** — A1 (default workflow), D1
  (locations), E1 (name + code) and C4 (sensitive staffing) promoted to blockers; everything else is
  `severity="info"` and never blocks. **Behaviour change:** a project with no default workflow or no
  linked locations can no longer be activated. Projects already active stay active until deactivated.

---

## Cast-model follow-ups (2026-07-23, `DESIGN-cast-model-and-package-staffing.md`)

- **[Participants DECISION — DECIDED 2026-07-30: keep (B)] Actor-role catalog is deprecated back-compat, not dropped.**
  New model (`implementing_agency_org_id` + `project_donors` + the staffing go-live gate) is primary; the legacy
  tables/service are kept as a dormant fallback (still seeded on create). **No cleanup planned.** Docs 02/03/04/10/11/13/14
  reflect "deprecated, present". (`sprints/2026-07_org_chart_positions/followups/actor-role-catalog-not-dropped.md`)
- **[Phase 2] Owning-level re-scope has no UI.** Position owner is server-stamped; the modal
  dropped the picker. `PATCH /position-types/{id}` still accepts `owner_organization_id`, so
  re-homing a title is API-only. Add an "advanced" edit affordance if needed.
  (`followups/phase2-positions-followups.md`)
- **[Phase 2] `assign_officer_position` default-role fallback.** Assign requires an explicit
  `role_key` for null-default positions; legacy non-null defaults still pre-fill. Drop the
  `or pt.default_role_key` fallback once the seed drops position defaults (Phase 3/§8).
- **[Phase 3e] Cast matrix screen — BUILT** (`ProjectCastSection`, in Projects & packages).
  Remaining §3.6 *niceties*: Copy-from-package, explicit per-cell Override/revert, inline
  "Add officer" (invite + position) in the picker, contractor-org picker (today org = implementing
  agency). (`followups/phase3-cast-ui-followup.md`)
- **[Phase 3e] Seed refresh (§8) optional.** Demo seeds still use named role keys (coexist with
  synthetic keys, §6) — refreshing to the staffing model is fidelity, not correctness.
- **[Phase 5 remainder] GRC per-step `is_grc` flag** — retire the `grc_committee` archetype;
  gate `GRC_CONVENE` on the flag; derive GRC members from the step cast. Working today via the
  role model; deferred (cross-file test + seed coupling). (`followups/phase5-remainder-followup.md`)
- **[Phase 5 remainder] Remove archetype apparatus + user-facing `/roles` CRUD** — already dead at
  runtime (Phase 1); coupled to invariant `test_authz_matrix_extended` + `test_roles_crud`, so
  remove endpoints **with** those test rewrites. Keep `GET /roles`. Completes "stop writing
  per-role permissions". (`followups/phase5-remainder-followup.md`)
- **[Phase 1] Per-role permission writes retire in Phase 5.** Tier is the source of truth now;
  `create_role`/`update_role` still write `roles.permissions` until the archetype apparatus is
  removed (Phase 5). (`followups/tier-permissions-reconciliation.md`)
- **[Perf §6/§3.6] Supervisor-tab hot path — verified, no work needed.** Supervisor membership is
  already materialized at write-time into `ticket_viewers` (`escalation._apply_step_tier_roles`);
  no MV/tier column. (`followups/phase5-remainder-followup.md`)

---

## ✅ Admin-scope org-staffing (2026-07-25, `followups/admin-scope-org-staffing-gaps-followup.md`)

> Surfaced auditing the Admin-access org-scoping change (org_admin now scoped to an `organization_id`
> node, not a country code). All three audited capabilities (child-org create / add donor officer /
> create contractor) already worked; the two adjacent guard gaps below were **logged as debt
> 2026-07-24, then built 2026-07-25** (migration `h4j6l8n0`; ticketing suite 649 passed / 0 failed).

- **✅ [Gap A] Contractor authorship.** `owner_organization_id` on `ticketing.organizations` (self-FK;
  `parent`/`children` relationships now pin `foreign_keys`). `create_organization` stamps the
  creator's org node on a non-super org_admin's `third_party`; `can_admin_org_or_owned` (super OR
  `owner ∈ caller subtree`) gates `update`/`delete` → maintainable by creator + ancestor-org admins.
- **✅ [Gap B] Project-staffing scope.** `require_org_admin_project_scope` constrains the org_admin
  tier to projects its subtree manages (project IA ∈ subtree; unanchored → open); actor orgs stay
  unconstrained; other tiers unchanged. Wired into `staff_cast_slot`, `add/update/remove_project_organization`,
  `add/remove_package_organization`, and `_require_project_scope` (donors).

---

## ✅ WEEK 2 — Frontend (complete as of 2026-04-27)

All screens confirmed built and running on port 3001 (`NEXT_PUBLIC_BYPASS_AUTH=true` for local):
- AppShell (sidebar, nav, badge count, SEAH indicator, OIDC + bypass auth — Keycloak as-built)
- Queue page (tabs, summary tiles, ticket rows, SLA countdown, SEAH red border/badge)
- All Tickets page (list + search), Escalated page (focused list)
- Ticket detail (grievance card, workflow stepper, SLA bar, event timeline, complainant PII + phone reveal)
- File attachments (complainant read + officer upload), assign + reassign-to-teammate panels
- Action panel (ACKNOWLEDGE / ESCALATE / RESOLVE / CLOSE / NOTE / GRC_CONVENE / GRC_DECIDE / REPLY)
- Settings page (full admin panel), Badge + SlaCountdown components

---

## 🔴 WEEK 3 — Must fix before demo (May 10)

### ~~1. Auto-assign officer on escalation~~ ✅ DONE (`d4e2f1a`)
**File:** `ticketing/engine/escalation.py` — `escalate_ticket()`  
**Problem:** After escalation, `assigned_to_user_id` stays as the previous officer.
The next-level officer is never assigned automatically — ticket sits in limbo until a
super-admin manually reassigns via `PATCH /tickets/{id}`.  
**Fix:** Call `auto_assign_officer(ticket, db)` inside `escalate_ticket()` immediately
after advancing `current_step_id`. Set `ticket.assigned_to_user_id` from the result.
Also: the unseen notification event at line 130 of `escalation.py` currently fires
`notify_user_id=ticket.assigned_to_user_id` *before* the reassign — move it after
so the notification goes to the new officer, not the old one.  
**Demo impact:** Pre-seeded tickets have hardcoded `assigned_to` so demo is safe.
New tickets created via live API or chatbot webhook will have no assigned officer.

### ~~2. Automatic complainant notification on RESOLVE / ESCALATE~~ ✅ DONE (`d4e2f1a`)
**File:** `ticketing/api/routers/tickets.py` — `perform_action()`  
**Problem:** Action handler commits and returns — no notification fires automatically.
Demo scenario 1 ends with "complainant notified via chatbot" but that requires the
officer to manually hit `POST /reply`. The plumbing exists; it just isn't wired.  
**Fix:** Add `notify_complainant.delay(ticket.ticket_id)` at the end of the `RESOLVE`
and `ESCALATE` branches (after commit). The task is already scaffolded in
`ticketing/tasks/notifications.py`.  
**Demo impact:** Low — presenter manually clicks Reply during demo. Fix before week 3 integration.

### 3. Test public.grievances integration and PII fetching
**What to verify:**
- ~~`ticketing/tasks/grievance_sync.py` — Celery Beat task polls `public.grievances`
  every 2 min. Column names are hardcoded in a raw SQL query. Must be validated against
  the actual schema after the public Alembic migration is finalised.~~
  ✅ **Validated automatically since T3-07 (2026-07-15):** the column list is declared in
  `tests/ticketing/test_boundary_policy.py`'s `CONTRACT` and gated against the CL-01
  `public.*` baseline in both directions. No manual verification needed.
- `ticketing/clients/grievance_api.py` — `GET /api/grievance/{grievance_id}` PII fetch
  called from the ticket detail view. Test with a real grievance ID against the running
  backend on port 5001. Verify: name, phone, province/district/municipality/ward fields
  all returned and mapped correctly in the ticket detail response.
- ~~`public.complainants` — confirm column names match what `grievance_sync.py` expects
  (location fields: `province`, `district`, `municipality`, `ward`, `village`).~~
  ⚠️ **This row was factually wrong and is retired (T3-07).** `grievance_sync.py` takes
  **exactly one** column from `public.complainants` — `location_code` — via a `LEFT JOIN`,
  plus `complainant_id` to join on. It reads none of those five fields. The two it does read
  are gated by `tests/ticketing/test_boundary_policy.py`, which **fails if ticketing ever
  selects complainant PII** (CLAUDE.md §Data rules rule 3).
- End-to-end smoke test: submit a grievance via chatbot → confirm ticket auto-created
  in `ticketing.tickets` within 2 min, with correct `grievance_location`, `priority`,
  `grievance_summary`, `session_id`.
**Dependency:** User is currently rewriting `public.*` tables via Alembic migration.
  Do not test until that migration lands and DB is re-seeded.

### ~~4. Wire OfficerScope seed rows so auto-assign works for live API tickets~~ ✅ DONE
`seed_mock_officer_scopes()` in `mock_tickets.py` — already seeded 9 rows, auto-assign works.

### 5. Visual test + polish pass (backend verified 2026-05-06)
**Backend smoke test results (all passing):**
- All 6 demo tickets seeded and visible correctly per role (ACKNOWLEDGE action ✓, GRC_CONVENE+GRC_DECIDE ✓, RESOLVE ✓)
- SEAH access gate: 403 for non-SEAH roles, 200 for seah_national_officer + super_admin ✓
- Badge counts: Site L1=3, PIU L2=2, GRC Chair=1, SEAH National=1, ADB observer=0 ✓
- SLA data: GRV-2025-005 breached (-24h, urgency=overdue), GRV-2025-001 warning (48h) ✓
- XLSX report export: generates valid file ✓
- My Queue counts: Site L1=2, PIU L2=1, GRC Chair=1, SEAH National=1, ADB observer=0 ✓
- Escalated tab: GRV-2025-004 now in ESCALATED status, visible to all in-scope roles ✓

**What remains:** Browser click-through (UI rendering, mobile shell, Settings panel)
- Open http://localhost:3001, switch roles via MockRoleSwitcher, verify each screen
- GRC convene → decide flow visual check (date picker + purple button)
- Mobile shell (/m/ routes) quick pass on narrow viewport
- BUG NOTE (2026-06-02): In mobile chat view (`grm-auth.nepal-gms-chatbot.facets-ai.com`),
  the message textbox intermittently freezes (cannot type/send). Repro and fix in next UI
  polish pass; likely input/focus or resize event handling regression.
- Reports — Overview / **Summary** / Pivot / Quarterly ✅; run `alembic upgrade head` + `python -m ticketing.seed.backfill_overdue_episodes` on existing DBs

### 6. Staging deploy to grm.stage.facets-ai.com
**What:** Docker deploy to staging EC2, Nginx config, SSL.  
**How:** Same EC2 as chatbot, add a new Nginx `location` block for port 3001 (UI)
and 5002 (API). Run `docker compose -f docker-compose.yml -f docker-compose.grm.yml up -d`
on the EC2 after pulling the branch.  
**NEXT_PUBLIC_API_URL** must be set to the public staging URL (not localhost) for the
UI to talk to the API from the browser.  
**Dependency:** Visual test (above) should pass first.

---

## 🟡 POST-DEMO FEATURES (prioritised)

### ~~7. LLM: Multilingual notes + "Findings" summary~~ ✅ DONE (`edfa942`) ⭐ BIG FEATURE
**Rationale:** Field officers (L1/L2, DOR) write notes in Nepali or mixed Nepali-English.
Supervisors and observers (GRC chair, ADB roles) read only English. Without translation
the case timeline is opaque to half the audience. The "Findings" panel gives supervisors
a one-glance digest of a case without reading every raw note.

#### 7a — Per-note translation
- When a `NOTE_ADDED` event is created, fire a Celery task `translate_note.delay(event_id)`.
- Task calls LLM: *"Translate the following to English. If already in English, return as-is.
  Preserve technical/legal terms. Output only the translated text."*
- Store result in `TicketEvent.payload["translation_en"]`.
- In the ticket detail timeline, if `translation_en` exists show it below the original
  with a small `🌐 Translated` chip. Original is always preserved and visible.
- Non-English users see only the original (toggle if needed post-demo).

#### 7b — "Findings" summary panel
- A dedicated **Findings** card on the ticket detail (right column, below complainant card).
- Content: one LLM call over all `NOTE_ADDED` events + key status events (ESCALATED,
  GRC_CONVENED, GRC_DECIDED, RESOLVED) for the ticket.
- Prompt: *"You are a grievance officer. Summarise the following case notes into a brief
  Findings report: key facts, actions taken, outstanding issues, and recommended next step.
  Write in formal English. Max 150 words."*
- Cached in `ticketing.tickets.ai_summary_en` (new column, Alembic migration needed).
- Regenerate button visible to admin/supervisor roles — fires `generate_findings.delay(ticket_id)`.
- Shown to: `grc_chair`, `adb_hq_safeguards`, `adb_hq_exec`, `adb_national_project_director`,
  `super_admin`. Hidden from L1/L2 field officers (they write the notes, don't need the digest).

#### Implementation plan
- **LLM provider confirmed: OpenAI** (`gpt-4` for translation, `gpt-3.5-turbo` for
  classification). Client lives in `backend/services/LLM_services.py`.
  `translate_grievance_to_english_LLM()` already exists — we reuse the same pattern.
  Init: `OpenAI(api_key=os.getenv("OPENAI_API_KEY"))`. Key is already in `env.local`.
- **New file `ticketing/tasks/llm.py`** with two Celery tasks on `grm_ticketing` queue:
  - `translate_note(event_id)` — fetches event note, calls OpenAI `gpt-4`, stores
    result in `TicketEvent.payload["translation_en"]`
  - `generate_findings(ticket_id)` — fetches all NOTE_ADDED + key status events,
    calls OpenAI `gpt-4`, stores result in `Ticket.ai_summary_en`
- **New file `ticketing/clients/llm_client.py`** — thin wrapper around `OpenAI` client
  (same init pattern as `LLM_services.py`). Keeps ticketing independent of backend/
  (DO NOT import from `backend/services/` — reuse the pattern, not the code).
- **DB migration:** Add `ai_summary_en TEXT` + `ai_summary_updated_at TIMESTAMPTZ`
  to `ticketing.tickets`. (`translation_en` goes into existing JSONB payload — no migration.)
- **API:** Add `POST /api/v1/tickets/{id}/findings` (trigger regenerate, admin only).
  `GET /api/v1/tickets/{id}` already returns `ai_summary_en` once added to `TicketDetail`.
- **Frontend:** `FindingsCard` component in ticket detail right column. Translated note
  shown inline in `EventTimeline` when `payload.translation_en` present.

#### 7c — Hook: fire translation automatically on NOTE_ADDED
- In `tickets.py` `perform_action()`, after commit for `NOTE` action:
  `translate_note.delay(event.event_id)`
- Same pattern as `notify_complainant.delay()` already wired for RESOLVE/ESCALATE.

**Dependencies:** `OPENAI_API_KEY` already in `env.local` (used by chatbot). No new
credentials needed.

### 8. User language preference (per organisation)  *(depends on #7)*
**Rationale:** Required to correctly present translated content and future
UI localisation. Officers from DOR likely prefer Nepali; ADB officers prefer English.  
**Scope:**
- Add `preferred_language` column to `ticketing.user_roles` (or a new
  `ticketing.user_preferences` table). Default: `"ne"` for DOR org, `"en"` for ADB.
- Organisation-level default in `ticketing.organizations.default_language` (new column).
  Individual officer preference overrides org default.
- Expose `GET /api/v1/users/me/preferences` and `PATCH` endpoint.
- Frontend: Settings → My Profile → Language (dropdown: English / नेपाली).
- Used by: LLM translation feature (above), future UI i18n.
**Alembic:** One migration — add column to `ticketing.organizations` + new
  `ticketing.user_preferences` table.

### 9. Mobile-first ticket management UI (officer field app)
**Rationale:** Field officers (L1 site focal persons, contractor officers) work primarily
on phones. The full Next.js settings-heavy UI is not suitable for mobile field use.  
**Scope — what's IN:**
- View assigned tickets + SLA countdown
- Acknowledge / add note / escalate / resolve actions
- View case timeline (events + notes)
- Reply to complainant
- Photo/file upload (camera roll)
- SEAH badge (read-only visual, same access control)
- Push notification support (post-proto: Firebase or PWA push)
**Scope — what's OUT (explicitly):**
- No Settings tab (no workflow editor, no user management, no org/location config)
- No Reports tab
- No All Tickets view (officers see only their queue)
- No GRC convene/decide (desktop-only action for GRC chair)
**Stack options (decide before building):**
  - Option A: PWA skin of the existing Next.js app — same repo, responsive breakpoints,
    hide Settings/Reports nav on mobile viewport. Lowest effort.
  - Option B: Separate React Native / Expo app — true native push, camera API, offline.
    Higher effort, separate repo.
  - Option C: Separate Next.js route group `/mobile/*` with a stripped layout.
    Middle ground — same API, different shell.
**Recommendation:** Start with Option A (PWA) — add `mobile:` Tailwind breakpoint
overrides to the existing ticketing-ui, hide desktop-only nav items, add a
`manifest.json` for PWA install. Revisit native if offline or camera is needed.
**Dependencies:** Week 2 desktop UI complete ✅. Consider adding note translation (#7a) to the mobile UI once that feature lands.

---

## 🟠 CHATBOT-SIDE INTEGRATION NOTE — PII scrubbing in grievance intake

> **For whoever works on `backend/actions/` or `rasa_chatbot/` intake flow.**
> This is a ticketing-system dependency — the findings pipeline reads `grievance_summary`
> and officer notes, and those go to OpenAI. We need PII stripped before storage.

### Problem
When a complainant submits a grievance, the free-text narrative often contains PII
(full name, phone number, neighbours' names, contractor names, home address details).
The chatbot LLM call already summarises + categorises the grievance — we need to add
PII scrubbing to that same call so the stored `grievance_summary` is clean.

`ticketing.tickets.grievance_summary` is cached at ticket creation from this field
and flows directly into the AI findings pipeline (OpenAI call). If the summary
contains PII, it leaves the system.

### Fix — extend the existing summarisation prompt
In the LLM call that generates `grievance_summary` (wherever the chatbot assembles
the grievance summary before storing it), add the following instruction:

```
Replace any personal identifiers with role descriptors:
- Person names → "the complainant", "a neighbour", "the contractor", "the officer"
- Phone numbers → [phone redacted]
- Email addresses → [email redacted]
- Specific street addresses → [address redacted]  (keep district/municipality level)
- ID card / passport numbers → [ID redacted]
Keep: location at district/municipality level, dates, nature of the grievance,
project name, road name.
```

### Storage convention (two fields)
- `grievance_summary` (existing) — **scrubbed** summary → safe for AI pipeline, ticketing
- `original_statement` or `raw_narrative` (vault, existing) — original unredacted text
  → only accessible via the reveal session (`POST /tickets/{id}/reveal`)

If the chatbot currently stores only one field, the scrubbed version should replace it
and the raw text should be kept in an encrypted/restricted column.

### Why this boundary matters
The ticketing `context_builder.py` (see `ticketing/engine/context_builder.py`) only
reads `ticket.grievance_summary` — if that field is clean, the entire AI pipeline
downstream is structurally PII-free. Officer notes may also contain incidental PII
(e.g. officer writes "called Ram at 9841…") — the translation + findings prompts
should also include the redaction instruction as a safety net (already done in
`ticketing/clients/llm_client.py` `_FINDINGS_SYSTEM` prompt).

### Officer notes
Officer notes (`NOTE_ADDED` events) can also contain incidental PII. The findings
prompt in `ticketing/clients/llm_client.py` already instructs the LLM never to
include names/phones/addresses in output — this is a safety net, not a substitute
for source-level scrubbing.

---

## 🟡 LLM: Structured context cache + findings pipeline (Layer 1 + 2)

**Implemented:** `ticketing/engine/context_builder.py`, `ticketing/models/ticket_context_cache.py`,
migration `i6j8l0n2p4`, updated `ticketing/clients/llm_client.py` + `ticketing/tasks/llm.py`.

**Layer 1 — `ticketing.ticket_context_cache`**
One row per ticket. Rebuilt whenever an event with `summary_regen_required=True` is committed.
`context_builder.build_and_store()` is the single, auditable place that assembles events into
a PII-clean JSON document. Never includes `created_by_user_id` — only `actor_role`.
`findings_json` stores the structured LLM output alongside the input context.

**Layer 2 — Structured JSON output from LLM**
`llm_client.generate_case_findings(context, is_seah)` now:
- Takes the pre-assembled context dict (not raw text)
- Uses `gpt-4o-mini` for standard tickets, `gpt-4o` for SEAH
- `temperature=0.0` for consistency
- Returns structured JSON: `{summary_en, key_findings[], recommended_action, urgency, languages_detected[]}`
- `Ticket.ai_summary_en` still populated from `summary_en` (frontend backward compat)
- Full structured output stored in `ticket_context_cache.findings_json`
- System prompt explicitly instructs: never include names/phones/addresses in output

---

## 🔴 TP-14 — Classification status + portal sync (P1, spec locked 2026-06-03)

**Specs:** [`docs/sprints/archive/June5/04-classification-status-spec.md`](sprints/archive/June5/04-classification-status-spec.md) · [`03-portal-p1-spec.md`](sprints/archive/June5/03-portal-p1-spec.md) § TP-14

**Classification codes:** `pending` (default) → `LLM_generated` | `LLM_failed` | `LLM_skipped` → `complainant_confirmed` or `officer_confirmed`. Officer gate for `LLM_*` (not complainant confirmed). Retire `is_temporary`, `slot_skipped`, `LLM_error` in DB.

**Implementation checklist:**

- [ ] Chatbot: persist `grievance_classification_status` on review + submit; `LLM_skipped`; LLM task sets `LLM_generated`/`LLM_failed`
- [ ] `ticketing_dispatch.py` — DB fallback for summary/categories at submit
- [ ] `grievance_sync.py` — no `is_temporary` filter; UPDATE existing ticket cache
- [ ] `GET /tickets/{id}` — merge grievance; expose classification status
- [ ] Portal: badges, amber panel, block Ack until `officer_confirmed` when required
- [ ] Officer validate API → `officer_confirmed` + audit event
- [ ] AWS backfill for empty cache rows
- [ ] Seed/migration: `LLM_skipped` in `grievance_classification_statuses`; normalize `slot_skipped`

---

## 🔴 ACTIVE SPRINT — Tier-1 Hardening (July 2026)

HR-01…07 from the adversarial codebase review (fail-closed auth, ticket-access gates, unique ticket index, escalation locking, CI, portal + webchat robustness). Specs, tracker, and agent runbooks: `docs/sprints/2026-07_hardening/` · Source: `docs/reviews/devils_advocate_codebase.md` §3 Tier 1.

## 🟠 QUEUED SPRINT — Tier-2 Quality & Performance (Aug 2026, after Tier-1)

H2-01…08: OIDC refresh-grant, `tickets.py` split + engine extraction, authz/escalation test extensions, grievance-sync watermark, auth-dependency cache, shared `useTicketThread` hook, SEAH form mixin + Nepali copy repair. Specs, tracker, runbooks: `docs/sprints/archive/2026-08_tier2_quality/` · Source: `docs/reviews/devils_advocate_codebase.md` §3 Tier 2.

---

## 🔴 SECURITY — open (active ticket, not deferred)

| Item | File | Notes |
|------|------|-------|
| 🟡 **The officer's category correction overwrites the AI's answer with no record of what it proposed** | `ticketing/api/routers/tickets/crud.py:523` · `backend/services/database_services/grievance_manager.py:83` | Logged 2026-08-25 as **D-57**, owned by [DPG-46](sprints/2026-08-llm/05-prompt-engineering-spec.md#dpg-46). ⭐ It matters because of what [Q-27](sprints/2026-08-llm/DECISIONS.md#q-27) decided: the classifier is a **baseline the officer corrects**, so the correction rate *is* its real-world score — real Nepali grievances, no authoring cost. The tracking variant (`update_grievance_with_tracking`, `:383`) already exists and writes old→new to `grievance_status_history.field_changes`; this path just doesn't use it. ⚠ Aggregate rate only — a corpus from real grievances is a separate privacy decision, and an officer edit is **agreement**, not accuracy |
| 🟡 **The SEAH detector's confidence is computed on every call and thrown away** | `backend/services/LLM_services.py:633` · `backend/actions/grievance_intake/sensitive.py:103` · `ticketing/models/ticket.py:94` | Logged 2026-08-25 as **D-56**, owned by [DPG-40](sprints/2026-08-llm/05-prompt-engineering-spec.md#dpg-40). The model already answers `level: high|medium|low`; it is never persisted (no column — and `sensitive.py:103` reads it anyway, always getting its `"low"` default), never reaches the ticket (`is_seah` is boolean), so the SEAH queue — which **by design** contains deliberate false positives — has no ordering signal. ⚠ Fix is plumbing, not a prompt change. `is_seah` must stay boolean and stay the access control |
| ⏳ **The classification benchmark has not been re-run since the D-51 storage guard** | `scripts/ops/llm_benchmark.py` · [`docs/dpg/model-benchmarks.md`](dpg/model-benchmarks.md) §3.1 | Logged 2026-08-25 closing D-51. Repaired categories now reach the scorer, so **set-level precision should rise from 0.773** and the §2 cells dated 08-21 are stale in that one direction. ⚠ The invention row must **stay 4/105** — it measures the model, and `InventionMeter` reads it from the resolution log for exactly that reason; if a re-run shows 0/105 the meter has broken, not the model improved. Costs one funded run against `gpt-5-nano` ⏳ Sprint 4 ([DPG-42](sprints/2026-08-llm/05-prompt-engineering-spec.md#dpg-42)) needs this baseline before it can A/B a prompt against it |
| ~~🔵 **Roughly a dozen `LLM_services.py:NNN` citations in the docs point at the wrong line**~~ ✅ **SWEPT 2026-08-27** | `docs/dpg/privacy-assessment.md` · `docs/sprints/2026-08-llm/*.md` | Logged 2026-08-25, closed by a read-and-verify pass over **every** `*.py:NNN` citation in the sprint folder and the privacy assessment — each one opened, not pattern-matched. ⚠ **Two were claims, not numbers, and both are now corrected in place:** P-10 / privacy-assessment **F-6** said `parse_llm_response` logs the raw model response (it logs the **length**, since DPG-13) and that the translation paths interpolate the whole input dict (`_grievance_ref()` has bounded them to the id plus three words since DPG-19.3). ⭐ **And the sweep found a third, worse one:** privacy-assessment leg **L4** claims *"two reachable call sites"* and cited one live site and one **parked** one (`:535`, translation) — a distinction no line number carries. Now `:312` and `:618`. **Historical tables were scoped, not re-pointed** — PROGRESS's `P-##` register and the README premise table say in place that they describe planning-time code, because re-pointing a finding falsifies it |
| ✅ **FIXED 2026-08-24 — the ops monitor was blind, and the report had never worked** | `ops/config.py:107`, `docker-compose.grm.yml:278` | Found 2026-08-24 by running the daily-report queries rather than reading them. `ops` falls back to `POSTGRES_PASSWORD` when `OPS_DB_PASSWORD` is unset — it is unset everywhere — so it authenticates as **`ops_app` using the `user` role's password**. The 2026-08-21 `POSTGRES_PASSWORD` rotation did not include `ops_app`, so every ops query has failed since. **Measured: last successful write 2026-08-18, 253 auth failures, every report row `n/a`.** ⚠ **Three masks hid it** — `_safe_scalar` degrades to `n/a` instead of raising, the container reports `healthy` because its healthcheck never touches the DB, and nothing monitors the monitor. ✅ **Fixed:** `ops_app` has its own `OPS_DB_PASSWORD` in `secrets.enc.env`. ⭐ **And the repair found the report had never worked on any deployment** — a missing `db.rollback()` let one failed query blank every row after it, four queries named columns that do not exist (`grievances.created_at`, `tickets.status`, two more), and `ops_app` lacked SELECT on five tables including the whole `keycloak` schema (`ops001`'s `to_regclass` guard skips silently). Fixed by `ops003_report_grants` + query corrections + a healthcheck that probes the DB — red-tested by re-breaking the credential. All eleven report rows now return real numbers. ⚠ **Local stack only — `ops` runs on neither server.** [`sprints/2026-08-llm/followups/ops-cannot-authenticate-since-rotation.md`](sprints/2026-08-llm/followups/ops-cannot-authenticate-since-rotation.md) |
| **Keycloak event storage is OFF on staging and DOR production** | `ticketing/auth/keycloak_setup.py` (`setup_realm_event_logging`) | ✅ **Found and fixed on the local realm 2026-08-24** while writing [`deployment/19_incident_response.md`](deployment/19_incident_response.md). Keycloak persists **no** login, login-failure or admin events unless the realm asks for it, and both default to off — verified `events_enabled=f`, `admin_events_enabled=f`, `keycloak.event_entity` **0 rows**. ⚠ **It survived every review because `ops/reports.py:45,55` queried that empty table and reported `0` officer logins and `0` failed logins — indistinguishable from a quiet day.** The bootstrap now enables both with a 90-day expiration (verified: a failed login writes `LOGIN_ERROR`). **Outstanding:** the change ships with the next image build, then `make keycloak-setup` must be re-run on **staging and DOR prod** — until then neither server records any authentication evidence, and nothing before 2026-08-24 is recoverable anywhere. Consider an ops health check so a recreated realm cannot regress it silently. [`sprints/2026-08-llm/followups/keycloak-recorded-no-login-events.md`](sprints/2026-08-llm/followups/keycloak-recorded-no-login-events.md) |
| **Breach procedure: B1/B2/B3 have no DOR owner** | [`deployment/19_incident_response.md`](deployment/19_incident_response.md) §0 | The runbook is written (2026-08-24) and `SECURITY.md` points at it. **Three decisions are blank and are not engineering's:** who declares a personal-data breach and within what time; who is notified on what clock (DOR, MoPIT, ADB, data subjects, any authority under the Individual Privacy Act 2018); and whether/how/by whom a survivor is told when a SEAH case is exposed — a safeguarding decision **nobody currently holds**. **Philippe holds B1 and B2 interim**; B3 is explicitly unheld. Filling them is a document edit, not a project. Privacy assessment **F-8**. |
| ~~`run_flow_turn` **handler table** (T3-02 p4)~~ ✅ **DONE 2026-07-15** | `backend/orchestrator/state_machine.py` | Shipped. **run_flow_turn 1,485 → 150 lines (−90%)**; the 20-arm chain replaced by a dict lookup + p1's recovery as the default. **D-53's re-sizing settled by doing it: an M, not the spec's S** — 19 arms / ~1,058 lines had to come out before the "22 one-line entries" existed to write. p2's characterization net passed **unchanged** (214/1 both sides) and the extraction is **proven verbatim** (1,025/1,025 moved lines). The win is per-handler signatures — each declares exactly what its body reads, which the chain could not record — **not** the table itself, and **not** correctness: the table is blind to D-51/D-52/D-59 (a recognized state that dispatches nothing passes the lookup), which is why the postcondition landed first. |
| ~~`status_check_form` **silently** treats an unknown `active_loop` as status form 1~~ ✅ **FIXED 2026-07-15** | `backend/orchestrator/state_machine.py` (postcondition + the form-selection `else`) | Closed with **both** halves, because they fix different things: the **postcondition** catches the symptom and — via `_recover_from_unknown_state` — clears the bogus `active_loop`, which is what **un-wedges** the session (a message alone would have left it to re-silence every later turn); the **point fix** logs the offending loop at error, which is the actionable half since `status_check_form` is a *valid* state and the postcondition's log alone would send an operator to the wrong place. Trap recorded for the next person: `story_route` is the hinge and **D-51/D-52 need opposite values of it** — that made two new tests pass pre-fix for the wrong reason, caught only by red-verification. |
| ~~**`GET /api/grievance/{id}` and `POST /api/grievance/{id}/status` are unauthenticated**~~ | — | ✅ **RESOLVED 2026-07-15 by T3-06** (4 commits: contract → audit → client key → authn). Both now `Depends(_ticketing_auth_check)`; the GET gained a `response_model` (51 fields, `extra="allow"` so no column is silently dropped) and a read audit at INFO carrying the caller principal. Verified red pre-fix: unauthenticated, the GET returned **200 with `grievance_description`**. Dev bypass + HR-01 fail-closed 503 preserved and pinned. **⚠️ The exposure check is now ANSWERED: `:5001` is NOT admitted** — SG `sg-01739221c73ab3eb1` (`chatbot-main`/`chatbot-rest`, `ap-southeast-1`) allows only 80/443/22(restricted)/8000/3000. **Not an incident; Phase 2 confirmed.** Two caveats carried below: DOR prod is on non-AWS infra and remains unverified, and `tcp/8000 0.0.0.0/0` **is** open to an unauthenticated orchestrator. See [`sprints/archive/2026-08_tier3_structural/05-grievance-api-hardening-spec.md`](sprints/archive/2026-08_tier3_structural/05-grievance-api-hardening-spec.md). |
| **DOR prod firewall for `:5001` is UNVERIFIED** | `grm-chatbot.dor.gov.np` | T3-06 cleared `:5001` on the **facets/AWS** staging hosts by reading the live security group. **Production runs on DOR infra, not this AWS account** (CLAUDE.md §Superseded), so its firewall could not be checked from here and **no repo artifact can answer it**. The endpoints are now authenticated either way, so an open port is no longer an anonymous-read hole — but it should still be closed. **Ask whoever operates DOR infra whether inbound `:5001`/`:8000` are admitted.** |
| **Orchestrator `:8000` is open to `0.0.0.0/0` and has no auth** | `backend/orchestrator/main.py:102`, `docker-compose.grm.yml:122` | Surfaced by T3-06's exposure check (out of its scope). `POST /message` has no `Depends`; the port is host-mapped by the overlay the staging deploy composes, and SG `sg-01739221c73ab3eb1` admits `tcp/8000` from anywhere. **Less severe than it reads** — `/message` is the public chatbot entry point, so it discloses nothing the web chat doesn't. What it bypasses is nginx: **no TLS** (plaintext `session_id`) and **no rate limit** (the `public` 30r/m zone is nginx-level) on a path that triggers **LLM spend**. Note the irony worth reading: T3-06 spent 4 commits authenticating a port the firewall already closed, while the open port has no auth at all. Tracked: [`sprints/archive/2026-08_tier3_structural/followups/orchestrator-port-8000-open.md`](sprints/archive/2026-08_tier3_structural/followups/orchestrator-port-8000-open.md) |

---

## 🔵 TECH DEBT (low urgency)

| Item | File | Notes |
|------|------|-------|
| 🔵 **Encrypt the original narrative and work from the redacted one — reveal by case type, not by frequency** | [`docs/sprints/2026-08-llm/04-pii-redaction-spec.md`](sprints/2026-08-llm/04-pii-redaction-spec.md) · `backend/services/pii_service.py` (Sprint 3) | Proposed 2026-08-27 by the owner, out of the privacy assessment §5.4 conversation. **A sprint after Sprint 3** — it needs a redacted derivative to exist first. Revisits **Q-12b** with a third option: the rejected one was *"the original is gone"*; this keeps it encrypted behind the audited reveal that `begin_reveal`/`close_reveal` already implements for contact details. ⭐ **Reveal is scoped by case type, not rationed by frequency** — for a misconduct or SEAH case the accused's identity *is* the grievance, so the assigned officer gets it by default; the control targets **lateral spread** (queue, search, XLSX reports, backups, the border), not the officer. ⚠ **Decryption must stay in `backend`** — ticketing holds no key and is pinned that way. ⚠ **Two parts constrain Sprint 3 and are already written into the spec**: §31.4 (redact model *output*, both summary prompts) and DPG-33 step 2 (the stored summary has three consumers with different needs — deciding it wrong means building the reverse). Tracked: [`sprints/2026-08-llm/followups/encrypt-the-original-work-from-the-redacted.md`](sprints/2026-08-llm/followups/encrypt-the-original-work-from-the-redacted.md) |
| 🟡 **The grievance taxonomy is authored in two files — and the copy that seeds every database is not the one anybody edits** | [`ticketing/constants/grievance_categories_default.json`](../ticketing/constants/grievance_categories_default.json) · [`backend/dev-resources/grievances_categorization_v1.1.csv`](../backend/dev-resources/grievances_categorization_v1.1.csv) | Found 2026-08-27 while diagnosing four days of red `backend-tests` on `dpg/sprint2-open-models`. The two copies had drifted to **64 disagreeing fields**: D-49 repaired `high_priority` and D-51 rewrote the six Road Hazard categories, both **in the CSV only** — while `mock_tickets` seeds the taxonomy by DELETE+INSERT **from the JSON**. So the dust complaint and the SEAH route were still `high_priority=False` in every seeded database, and one category's `description` was the literal string `"True"`. ✅ **Drift repaired and pinned** — `scripts/ops/sync_category_catalog_json.py` regenerates the JSON from the CSV, `tests/repo/test_category_catalog_sources.py` compares the two authored files so drift fails in the repo before any DB exists. ⚠ **Still owed:** one source, not two in sync — needs an `intake_route` column in the CSV and an owner for a file both the chatbot and ticketing read. ⚠ **Deployed environments keep the stale rows until re-seeded** — same re-seed the D-51 row above already calls for. Tracked: [`sprints/2026-08-llm/followups/the-taxonomy-is-authored-twice.md`](sprints/2026-08-llm/followups/the-taxonomy-is-authored-twice.md) |
| 🔴 **A shortlisted open model refuses grievances about harm to children — and the filter may be the provider's, not the model's** | `scripts/ops/llm_candidates.json` · `docs/dpg/open-model-configuration.md` | Measured 2026-08-20 by DPG-21. `swiss-ai/Apertus-70B-Instruct-2509` replied **`Your request was blocked.`** in 0.42 s to the benchmark's flagship prompt (construction dust, children ill); every other candidate answered. ⭐ **This system's entire input distribution is human harm** — dust and sick children is the mildest end — so a filter like this refuses hardest on the reports that matter most, and ⚠ **the SEAH path fails open**, so a blocked harassment report is silently handled as an ordinary complaint. A genuine loss: Apertus was the strongest DPG story in the shortlist (open weights **and** open training data). ⚠ **Not yet concluded** — one prompt, and *"Your request was blocked"* reads as infrastructure phrasing, so it may be the **serving provider's** filter; another route to the same weights may be clean. Next: 5–10 prompts across the benchmark's range, the SEAH detection prompt **specifically**, and the same weights via another provider. ⭐ Whatever the answer, *"does this model refuse our actual input distribution?"* belongs in the standard capability probe alongside `json_schema`. Tracked: [`sprints/2026-08-llm/followups/apertus-content-filter-blocks-a-grievance.md`](sprints/2026-08-llm/followups/apertus-content-filter-blocks-a-grievance.md) · PROGRESS **D-54** |
| ⏸ **The end-to-end test against a live vLLM endpoint is deferred — it is the step that turns "documented" into "demonstrated"** | `docs/dpg/vllm-deployment.md` | Logged 2026-08-20 with DPG-25, which requires the deferral to be explicit. The claim *"same code, different `LLM_BASE_URL`"* is well-founded — one registry, both surfaces moved by one env change, both pinned by tests, and the open configuration measured against a hosted router — but **nobody has pointed this code at a vLLM instance**. ⭐ **The interesting half is audio:** the hosted router does **not** expose `/v1/audio/transcriptions` (D-53), so T2 is currently the *only* configuration that could serve ASR at all, and that is exactly the untested claim. ⚠ **The cheap version is worth doing while T2 stays parked**: a spot GPU for an afternoon converts the strongest untested claim in the DPG submission into a measured one, and costs nothing like the running instance the parking decision was about — **parking the deployment is not the same as parking the proof**. Until then the wording stays *documented and costed*, never *supported* or *verified*. Tracked: [`sprints/2026-08-llm/followups/vllm-end-to-end-test-deferred.md`](sprints/2026-08-llm/followups/vllm-end-to-end-test-deferred.md) |
| 🔵 **A Nepali ASR fine-tune on OpenSLR SLR54 would be a genuine DPG *contribution*, not just a dependency** | — (research task, no code) | Logged 2026-08-20 out of DPG-22, whose acceptance requires it. **An opportunity, not a defect** — nothing is broken. SLR54 is ~165 openly-licensed hours of Nepali speech; the 2026 comparative study reached **14.76% WER** (Whisper-Large-v3-Turbo) and **14.89%** (IndicWav2Vec) on it, statistically tied, both permissive. The fine-tune is a single-GPU job. ⭐ **Why it is worth more than a better WER:** indicator 4 is about not depending on a proprietary component, and this project currently only *consumes* open models — publishing a Nepali ASR fine-tune would make it one that gives something back, which is a materially stronger submission position than compliance alone. Permissively-licensed Nepali speech tooling barely exists: `ai4bharat/indicwav2vec_v1_nepali` has **0 downloads**. ⚠ Two prerequisites, in order: a **working ASR endpoint** (D-53 — `.env.open`'s returns 404) and an **evaluation set of this project's own audio** (SLR54 is *read speech*; our voice notes are road noise, code-switching and a self-identification preamble). ⚠ And a named maintainer, or an explicit "released as-is, unmaintained" — an unmaintained model on a hub acquires citations and then rots, which is Q-05's unowned-GPU failure in a different costume. Tracked: [`sprints/2026-08-llm/followups/slr54-nepali-asr-finetune-as-a-dpg-contribution.md`](sprints/2026-08-llm/followups/slr54-nepali-asr-finetune-as-a-dpg-contribution.md) |
| 🔴 **`.env.open`'s ASR endpoint does not exist — the committed open configuration cannot transcribe** | [`.env.open`](../.env.open) · `docs/dpg/open-model-configuration.md` | Verified 2026-08-20 by DPG-22. `POST https://router.huggingface.co/v1/audio/transcriptions` returns **404** for `openai/whisper-large-v3`, the turbo variant, and a provider-pinned form. **Not auth, not billing** — the same route **without** a token returns **401** (so it exists and authenticates), `GET /v1/models` and `POST /v1/chat/completions` both return 200 with that token, and the router's catalogue holds **132 models, none of them audio**. Hugging Face serves ASR, just not through the OpenAI-compatible `/v1/audio/*` surface this code calls. ⚠ **The indicator-4 claim is unaffected for text** (nine of ten call sites, all the live ones); what is affected is the sentence *"the open configuration runs the whole system"*. Nothing breaks today because voice is off — this is a documentation defect that **would become a runtime defect** the day voice is switched on with `.env.open` loaded. Cheapest fix: point `ASR_BASE_URL` at a provider that does serve OpenAI-compatible audio — DPG-17 already keeps the ASR endpoint and key **separate** from chat's, precisely so that is a two-line change. ⚠ Do **not** close it by deleting the ASR variables. Tracked: [`sprints/2026-08-llm/followups/the-open-config-has-no-working-asr-endpoint.md`](sprints/2026-08-llm/followups/the-open-config-has-no-working-asr-endpoint.md) · PROGRESS **D-53** |
| ~~🔴 **The classifier invents categories on 17% of grievances, and they are stored**~~ ✅ **CLOSED 2026-08-25** — six `Road Hazard - *` categories added; invention fell 18/105 → 4/105, of which only **one** is a genuinely new concept. ⚠ Staging + prod need a re-seed (30 categories now, not 24) | `backend/services/category_resolution.py` · `backend/services/LLM_services.py:416` · `backend/actions/utils/ticketing_dispatch.py:117` | Measured 2026-08-20 by DPG-23's closed baseline over all 105 benchmark items against **`gpt-5-nano`, the model production runs**. 18 of 105 got a category that exists nowhere in the taxonomy — a coherent fictional family: `Road Hazard - Dust` ×7, `- Accident` ×5, `- Animal On Road` ×3, `- Potholes`, `- Flood And Landslide`, `- Others`, plus a malformed `Wildlife Passage`. **21 of the run's 32 false positives are invented**; precision would be ≈0.88 rather than the measured 0.740 without them. The prompt already says *"Do not create new categories"*. `_warn_about_unlisted_categories` logs and never rejects — deliberately, and rightly for a *near-miss name*; `Road Hazard - Dust` is not one. It is stored, shown to the complainant as the system's understanding of their own complaint, synced to ticketing, **invisible to every category filter and report**, and **not found** by the high-priority lookup. ⭐ **Compounds D-49**: `Air Pollution` already loses its priority flag to the CSV parse defect, and on 7 items the model *also* offered `Road Hazard - Dust` — so a dust complaint can arrive with **no priority signal from either direction**. ⚠ Fix is probably **both** a parse-time guard and a prompt repair; and seven items independently wanting a road-hazard category is a **taxonomy question for the owner**, not only a model defect. Tracked: [`sprints/2026-08-llm/followups/the-model-invents-categories-and-they-are-stored.md`](sprints/2026-08-llm/followups/the-model-invents-categories-and-they-are-stored.md) · PROGRESS **D-51** ✅ **Storage guard landed 2026-08-25 and closes this.** The four survivors were all a **correct leaf under a wrong or missing parent**, and leaves are unique across the taxonomy, so they repair by lookup: the catalogue now goes to the provider as a per-call **JSON-Schema enum**, an unmatched value is folded and resolved by leaf, and anything left is **dropped, never stored** (with the model's top alternative promoted if that would leave the item bare). ⛔ No fuzzy matching, and no repair prompt — it would have buried the taxonomy signal that produced the 08-21 change. `grievance_categories_alternative`, which the complainant **picks from**, was never validated at all and now is. ⏳ Benchmark re-run still owed (precision should rise from 0.773; the invention row should stay 4/105 — it measures the model, not storage, and `InventionMeter` keeps it measurable now that the harness calls the repaired function) |
| 🟡 **The SEAH detector flags gendered complaints that are not harassment — ⭐ REFRAMED 2026-08-24: this is intentional, and the gap is the return path** | `backend/services/LLM_services.py:625` (the detection prompt) | Measured 2026-08-20 by DPG-23. Over 105 items, **none a harassment report**, 7 were flagged (false-alarm rate 6.7%) — and **5 of the 8 items authored specifically as the hard case**: no separate toilet for women workers · an unlit route home from school · **unequal pay for the same work** · one tap for the whole labour camp · **refused work for being a woman**. ⭐ **REFRAMED 2026-08-24 by the project owner: the over-flagging is deliberate, not a defect.** The detector is tuned **recall-first** because the errors are asymmetric — a **miss** leaves a harassment report in the ordinary queue and is unrecoverable, while a **false alarm** costs a SEAH officer a review and carries **no risk to the complainant**, since a SEAH officer reading a dust complaint discloses nothing. ⛔ **So the prompt change originally proposed here is NOT the recommendation.** ⚠ **The real gap is the return path**: over-flagging is only cheap while a cleared case can go back, and today that works only as a side effect of a category edit — no explicit action, no de-flag event, no test. Tracked separately: [`sprints/2026-08-llm/followups/seah-officers-cannot-explicitly-return-a-cleared-case.md`](sprints/2026-08-llm/followups/seah-officers-cannot-explicitly-return-a-cleared-case.md). The residual that survives: these are **women's access and discrimination grievances**, and they are **delayed** by a round trip nobody measures. ⚠ There is a **third signal**: classification returned `Gender - Gender Discrimination And Harrassment` as a false positive 3× in the same run and the review step keeps any `"gender"` category, so what a complainant experiences is the **union**. ⚠⚠ **DO NOT TIGHTEN WITHOUT MEASURING RECALL** — the committed set has no positives, so a prompt change could remove real detections and nothing here would notice: false alarms drop, the benchmark goes green, and an oblique report stops being flagged. Gated on a paired re-run against the owner's held-out set (`--seah-set`). **There is currently no baseline recall figure at all.** ⏳ **Now owned by [DPG-44](sprints/2026-08-llm/05-prompt-engineering-spec.md#dpg-44), written and ⛔ blocked**, with the held-out set as [Q-25](sprints/2026-08-llm/QUESTIONS.md#q-25) — the ticket exists so nobody edits the prompt by accident. Tracked: [`sprints/2026-08-llm/followups/seah-detector-flags-gendered-non-harassment-complaints.md`](sprints/2026-08-llm/followups/seah-detector-flags-gendered-non-harassment-complaints.md) · PROGRESS **D-52** |
| ⏸ **The Hugging Face account has no inference credit — the measurement half of Sprint 2 is blocked on a purchase, not on code** | `env.local` (`HG_TOKEN`) · `scripts/ops/llm_smoke.py` | Hit 2026-08-20 on DPG-21's first probe run. The token authenticates and the first requests succeeded — `gpt-oss-20b` completed all six capability probes, served by Groq — then every subsequent request returned **HTTP 402 *"You have depleted your monthly included credits"***. ⚠ **Not Q-19 returning:** the budget was answered the same day (a few hundred USD, owner-funded). It is the narrower problem that **the funded budget is not on the account the code points at.** Blocks DPG-21's remaining 5 candidates, all of DPG-23's numbers, and DPG-22's (already blocked independently for want of audio). Everything buildable ships regardless: probe script, shortlist, licence verification, harness, and every unmeasured cell marked `⚠ Not measured` rather than left blank. ⭐ **The incident's real value:** the probe's first version rendered those 402s as ❌ beside real 400s, so *"credits depleted"* read as *"this model does not support temperature"* — and the script prints those cells as a `ModelProfile` tuple **to paste into the running code path**. Fixed same day and pinned by 16 tests. ⚠ **Set the hard token cap when topping up, not after** — DPG-24 requires it before the job's first run, and that job is the only recurring cost in the plan. Tracked: [`sprints/2026-08-llm/followups/hf-router-rate-limit-reads-as-credit-exhaustion.md`](sprints/2026-08-llm/followups/hf-router-rate-limit-reads-as-credit-exhaustion.md) · PROGRESS **D-50** |
| ~~🔴 **Four grievance categories silently lost their high-priority flag — including the dust complaint and the SEAH category**~~ ✅ **FIXED 2026-08-21** (CSV repaired at source; ⚠ **staging + prod still need a re-seed** — `python dev-scripts/seed_reference_data.py` — and it shifts SLA clocks for four categories, so use a known window) | `backend/dev-resources/grievances_categorization_v1.1.csv` · `backend/config/constants.py:646` | Found 2026-08-20 by DPG-20, which had to read the taxonomy to label benchmark severity and found it disagreeing with itself. Five of the CSV's 24 rows carry a field count the 13-column header does not declare — a third follow-up-question pair — so `csv.DictReader` files the surplus under the `None` key and `row["high_priority"]` receives **a question string** instead of `True`. `'"…scale from 1 to 5?"'.lower() == "true"` is `False`. Net effect: **`Environmental - Air Pollution`** (the dust complaint — demo scenario 1, the highest-volume category on a road project), **`Gender - Gender Discrimination And Harrassment`** (the category the SEAH review step keys on), `Environmental, Social - Cutting Of Trees` and `Wildlife, Environmental - Wildlife Destruction` are all high-priority in the authored data and **normal priority in the running system**. Ticket priority reads this flag directly (`backend/actions/utils/ticketing_dispatch.py:117`), so SLA clocks and queue badges follow it. ⚠ **It is in the seeded database too** — the DB loader is preferred over the CSV, so re-seeding re-applies the defect rather than clearing it. ⚠ **Not fixed in a benchmark commit on purpose:** flipping four categories changes SLA timing, escalation and demo seed data — a product decision with a blast radius. The benchmark records the **authored** value so it is already correct when this lands, and a canary test fails when it does. Tracked: [`sprints/2026-08-llm/followups/taxonomy-csv-column-drift-silently-clears-high-priority.md`](sprints/2026-08-llm/followups/taxonomy-csv-column-drift-silently-clears-high-priority.md) · PROGRESS **D-49** |
| 🟠 **The stored grievance summary carries no names** ✅ **DECIDED by the owner 2026-08-27** | `ticketing/models/ticket.py:59` · `ticketing/api/routers/tickets/crud.py:341` · `ticketing/services/report_rows.py:459` · Sprint 3 [`04-pii-redaction-spec.md`](sprints/2026-08-llm/04-pii-redaction-spec.md#dpg-33) | **Store the pseudonymised summary; the officer keeps names via the live narrative read.** DPG-30 established this costs the officer nothing: `merge_grievance_into_ticket` reads `grievance_description` **live** from `public.grievances` (`grievance_content.py:69`, `:74`), so Q-12b's *"the officer needs to know which engineer was named"* is already satisfied by another path. **Cleans:** officer free-text search (`crud.py:341` does `grievance_summary.ilike`, so a named third party is searchable by every officer with queue access), the XLSX to external ADB/DOR roles (summary only — **no report-code change needed**), the ticketing backup, the queue view. ⚠ **Say "carries no names", never "anonymous"** — ward-level location and circumstance remain. ⭐ **Generate downstream of the pseudonymiser (§31.4), not at storage time**, or the five chatbot-side log sites in `form_grievance_complainant_review.py` still hold the original. Needs DPG-31 first |
| ~~🟠 **The admin recap email sends a pseudonymised summary and a ticket link, not the record**~~ ✅ **BUILT 2026-09-03** | `backend/actions/services/messaging/recap_email.py` · `backend/config/constants.py` · `backend/actions/action_outro.py` · `backend/actions/forms/form_status_check.py` · `tests/backend/test_admin_email_boundary.py` · `tests/mutations/test_admin_email_boundary.yml` | **Built at the boundary — one function, not per template.** ⭐ **The cause was one line:** there was no admin body; `GRIEVANCE_RECAP_ADMIN_BODY` was ASSIGNED from `GRIEVANCE_RECAP_COMPLAINANT_BODY`, so the admin list got the complainant's own receipt. ⭐ **Two templates, not one** — the status-check follow-up was purpose-written for an admin and carried the same fields. 🔴 **And it was ungated for sensitive cases**, so a SEAH grievance mailed a survivor's narrative and contact details to a list not cast on the workflow — an isolation bypass the original finding never recorded. **Fix:** allow-list projected before formatting; a sensitivity gate failing closed on an unknown flag (a sentinel, not `.get(k, False)`); a template naming a non-safe field REFUSES to send rather than retrying with the full dict; both call sites read the flag from the stored row, not the tracker slot (D-64). The old full-PII template is DELETED, not shadowed. **19 assertions, 7 mutations as recorded**, one an explicit `expect: survives` with its reason. ⚠ Residual: the detector can raise the flag after the send — the allow-list is what bounds that. ⏭ **Not deployed.** |
| ~~🟠 **Prune PII from logs to a findable prefix**~~ ✅ **BUILT 2026-08-27** | `backend/services/db_debug_log.py` · `backend/actions/services/contact/phone.py` · `form_otp.py` · `form_status_check.py` · `action_outro.py` · `form_grievance*.py` · `tests/backend/test_log_pii_pruning.py` | Owner's rule, per field type: **free text → first 8 chars**, **phone → last 4**, **OTP → never logged**, **whole dict → ids and lengths**. ⭐ **The convention already existed and stopped at the wrong layer** — `db_debug_log` has had `mask_phone_for_log` and `grievance_row_summary` all along and `backend/services/` uses them consistently; **every raw site was in `backend/actions/`**, which never adopted them. Only `text_prefix_for_log` had to be written. **11 sites fixed, 14 assertions, 4 mutations checked red.** ⚠ **The first test file was decorative and a mutation proved it**: reverting the masking at both phone call sites left all 10 green, because it tested the helper and not the call site. A call-site AST pin was added — and **immediately found three more sites the hand inventory had missed** (`form_status_check.py:223`, `:242`, `:244`). ⏭ **Still open:** the repo-wide sweep — the pin is scoped to four files on purpose |
| ~~🟠 **OTP hardening — a validity window, and erase the code once verified**~~ ✅ **BUILT 2026-08-27** | `backend/config/constants.py` (`OTP_VALIDITY_SECONDS`) · `backend/actions/services/otp/verification.py` · `backend/actions/forms/form_otp.py` · `backend/orchestrator/config/domain.yml` · `tests/backend/actions/test_otp_verification.py` | **10-minute window** (owner) + **the code is erased on successful verification**. `is_expired()` **fails closed** — a missing or unparseable stamp counts as expired, because failing open turns any parse bug (or an OTP issued before this shipped) into a code that never expires. Expiry is checked **before** the match, so a correct-but-stale code does not verify; **pinned by an ordering test**. New `otp_issued_at` slot; `otp_status` gains `expired`. **17 assertions, 4 mutations checked red.** ⚠ **The expired path reuses the existing *"Invalid code… type 'resend'"* message** rather than adding an "expired" one: utterances are bilingual and inventing Nepali for a live complainant-facing flow is not something this change should do. Behaviour is right, the *reason* is imprecise — see the copy row below. ⭐ Found while doing it: `otp_status: invalid_format` had been set by code and **never declared** in `domain.yml`; declared now |
| 🔵 **The OTP-expired message says "invalid code" — accurate in effect, imprecise about why** | `backend/actions/utils/utterance_mapping_rasa.py` (`action_ask_otp_input` utterance 6) · `backend/actions/forms/form_otp.py:390` | Logged 2026-08-27 with the OTP window. The complainant is told *"Invalid code. Please try again or type 'resend'"* when the real reason is that ten minutes elapsed. **Deliberately not guessed at:** a new utterance needs Nepali, and machine-translating complainant-facing copy is the wrong way to close a wording gap. Route a proper *"your code has expired, here is a new one"* string through the translation workflow (`utterance_mapping_translation_checks/`). ⚠ It also needs the census pin in `tests/actions/test_utterance_key_integrity.py` updated with it |
| 🟡 **A credential is written to the application log — and the OTP has no expiry at all** | `backend/actions/forms/form_otp.py:312`, `:343`, `:121`, `:352-364` · `backend/actions/services/otp/verification.py` · `form_status_check.py:76` | Found 2026-08-27 by DPG-30, **and downgraded from 🔴 the same day: the original claim of a completed status-check impersonation was wrong.** The OTP is verified against `tracker.get_slot("otp_number")` — per-session state — so a leaked OTP string authenticates no other session. **What stands:** the OTP is logged at INFO (two lines, `len()` would do); ⭐ **the OTP has NO expiry anywhere** — no timestamp, no TTL, the bound is the conversation slot, so *"it only works in a time window"* is not true here; ⭐ `otp_number` is **not cleared on success**. ⚠ **Open, needs tracing:** on SMS failure `:121` prints the OTP into the chat, which on the **status-check** path would defeat the control — and DOIT has no fallback transport since the SNS deletion. ⭐ Separately: `form_status_check.py:76`'s `[:20]` truncation **never fires** on a 10-digit number. [`followups/otp-and-phone-logged-at-info.md`](sprints/2026-08-llm/followups/otp-and-phone-logged-at-info.md) |
| ~~🔴 **Redis persists grievance text to disk**~~ ✅ **CAUSE REMOVED 2026-08-27** | `backend/actions/grievance_intake/classification.py` · `backend/task_queue/registered_tasks.py` · `tests/backend/test_celery_payload_carries_no_narrative.py` | **Measured** (no volume, but RDB on, `dump.rdb` 47 KB, a key survived `docker restart`) and **all four documents corrected**. ⭐ **Then resolved by removing the cause rather than taking the trade**: instead of `--save "" --appendonly no` (which buys PII-at-rest by paying in lost tasks), DPG-34 step 3 shipped — the payload carries `grievance_id` and the task reads the narrative from Postgres. **Redis no longer holds grievance text on this path, and the reliability is kept.** ⚠ **The reliability question is what redirected the fix, and the answer was not obvious:** Celery retry does **not** cover a lost message — it fires when a task *runs and raises*. The chatbot app has **no beat schedule at all** and `grievance_sync` never touches classification, so a lost classification would sit at `pending` **forever**. 6 assertions, 4 mutations red, verified in-container against a real grievance |
| ~~🟠 **SEAH detection sends the narrative through the broker**~~ ✅ **FIXED 2026-08-27** | `backend/actions/grievance_intake/sensitive.py:35` · `backend/task_queue/registered_tasks.py:895` · `tests/backend/test_celery_payload_carries_no_narrative.py` | `text=` is no longer sent; the task reads the narrative from Postgres by `grievance_id`. ⚠ **Handled differently from classification, because the risk is different**: SEAH's pre-dispatch write is **best-effort** (early return without ids, swallowed exception) where classification's is hard, so a missing row is **reachable**. The task therefore **retries** (the row may still be landing — the write and the dispatch race across a thread hand-off) and then **fails terminally at ERROR**, naming the grievance and stating that the deterministic keyword detector still applied. Silence was the one outcome not acceptable: a SEAH detection that never ran writes no flag and shows no gap anywhere else. ⭐ **Two more leaks fixed with it**, both at INFO on the sensitive path: 120 chars of the narrative (`text_snippet`), and — worse — `message_snippet`, **the excerpt the model selected *because* it is the harassment disclosure**. The narrative now takes the 8-char rule; **`message` is never logged at any length**, because a prefix of a model-chosen sensitive excerpt is not the same class of thing as a prefix of a narrative. 11 assertions, 4 mutations red, verified in-container |
| ~~🔴 **The final submit erased the LLM's SEAH detection**~~ ✅ **FIXED 2026-08-27** | `backend/services/database_services/grievance_manager.py` · `backend/actions/services/submit/collect.py:48` · `tests/backend/test_sensitive_flag_only_escalates.py` | **D-64.** The LLM SEAH task writes `grievance_sensitive_issue=True` during the contact/OTP window; the final submit collected the stale **keyword** slot and wrote **False over it**. It fired exactly when keywords miss and the model catches (D-52), and the ticket's `is_seah` reads that column via the 2-minute sync — so a harassment report was silently not routed to the SEAH workflow. **Fix:** the flag only ever escalates — `= (COALESCE(col, false) OR %s)`, in **both** writers, **in SQL** so it cannot race the task (read-modify-write would still lose it). 7 assertions, 3 mutations red, and the full scenario driven against the live DB: False → LLM True → stale False submit → **stays True**. ⚠ Recall-first by decision (owner): over-flagging is reviewed-and-returned; clearing a false positive is a ticketing-side action on a reviewed case, never a rewrite of this column |
| 🔴 **A third email leg mails the whole grievance record to an office list derived from MUNICIPALITY, not from the case's cast** | `backend/api/routers/grievance.py:296`, `:323` · `backend/services/database_services/grievance_manager.py:1015` · `backend/config/constants.py` (`GRIEVANCE_STATUS_UPDATE_BODY`) | Found 2026-09-03 while fixing the admin recap email. `GRIEVANCE_STATUS_UPDATE_BODY` carries the **full narrative, complainant name, phone, municipality, village and address** on every status change, and `get_office_emails_for_grievance` resolves the recipients from the grievance's **municipality** — the PD office plus whichever office covers that location — **not from the case's assigned cast**. So a status update on a SEAH case mails a survivor's record to a location-derived office list. ⛔ **Worse than the leg it was found by**: that one had a single configured address, this recipient set **grows with deployment**. ⏭ Fix is the same shape and the helper now exists — route it through `send_recap_email_to_admin`'s boundary, or give office notifications their own allow-list. **Deliberately not folded into the F-19 commit**: different subsystem (`backend/api/`, stable shared service), safeguarding path, deserves its own decision. ⭐ **The pattern, not the leg, is the finding: three email paths each carried the whole record, each written by someone solving a different problem, none wrong-looking at its own call site. Grep for the TEMPLATES, not the senders — and assume a fourth.** [`docs/dpg/privacy-assessment.md`](dpg/privacy-assessment.md) F-22 · [`pii-egress-inventory.md`](dpg/pii-egress-inventory.md) E13 |
| 🟠 **Nothing re-drives a classification that never ran — grievances can sit at `pending` forever** | `backend/task_queue/celery_app.py` (no `beat_schedule`) · `backend/config/database_tables.py:41` | Found 2026-08-27 while weighing the Redis persistence trade. Celery **retry** only covers a task that ran and raised; a message lost from the broker leaves no retry state, and **no periodic task sweeps for the gap** — the chatbot Celery app has no beat schedule, and ticketing's `grievance_sync` creates tickets without touching classification. The ticket is still created, so the officer sees an **uncategorised** case (and `high_priority`, computed from categories, is not set). ⚠ **Independent of the Redis fix** — that removed one way to lose the message, not the absence of recovery. A sweeper over `grievance_classification_status='pending'` older than N minutes would close it |
| ~~🟠 **95 mutation checks are recorded as prose, so none of them can be re-run**~~ ✅ **30 ARE NOW DATA 2026-09-03** | [`followups/mutation-records-are-prose-not-data.md`](sprints/2026-08-llm/followups/mutation-records-are-prose-not-data.md) · `tests/mutations/*.yml` · `scripts/ops/run_mutations.py` · [`engineering/04_testing.md`](engineering/04_testing.md) §5a | **31 records across the six Sprint 3 modules**, and **every one verified to reproduce its recorded outcome** — not transcribed and trusted. ⭐ **It caught a live regression within an hour of existing**: commit `7dddf5e8` (landed mid-build) tightened the roman address pattern, which quietly made T-31-d's recorded mutation INERT — the test still passes and the commit is a genuine improvement, but the ledger row would have gone on claiming a check that no longer happens. Handled as two records (a working one + the loss kept **executable** as a documented regression), never by adjusting the old one. `run_mutations.py` applies, runs, restores and compares against `expect`; the whole set is **~20 s**. ⭐ `expect: survives` is a **first-class outcome** and is rejected without a `why`: `T-31-a` is a documented non-kill, because `find_pii` normalises centrally so the dual-script digit class is redundant — removing the *normalisation* is what kills (3 red). ⚠ **Two records did not reproduce first time, and both were caught by the runner's own guards rather than by review**: an anchor matching boilerplate in three tasks (the exactly-once assertion refused it), and a single-quoted YAML scalar that shipped a `SyntaxError` (pytest exits 2, not 1, so it was reported as `broken` rather than miscounted as a kill). ⚠ **The clean-tree refusal earned its keep on the first real run** — it stopped a `git checkout --` over another session's uncommitted work on `pii_service.py`. **NOT in CI, deliberately** (D-26: a slow gate is the gate nobody watches), and a third of the records need a live Compose stack anyway. ⏭ **Still open:** ~65 ledger entries remain prose — back-fill when next touching their module; the transcription is not mechanical |
| 🔵 **The live LLM spec never says which of the nine call sites actually run** | [`docs/services/06_llm_service.md`](services/06_llm_service.md) | Found 2026-08-27 reconciling `02-llm-agnostic-spec.md`: DPG-18's acceptance said §18.0's live/parked table would be reproduced there, and it was not — the word *parked* does not appear in the file. So the document an engineer opens still describes nine capabilities with no sign that **four are switched off**, which is precisely what DPG-19b spent a ticket undoing. XS. [`followups/the-live-spec-never-says-which-call-sites-run.md`](sprints/2026-08-llm/followups/the-live-spec-never-says-which-call-sites-run.md) |
| 🔵 **The two-minute back-fill is what makes the classification budget survivable, and the ticketing spec does not mention it** | [`docs/deployment/11_llm_pipeline_policy.md`](deployment/11_llm_pipeline_policy.md) | Found 2026-08-27, same pass. DPG-15b's acceptance asked for it in `11_llm_pipeline_policy.md`; it went into `02_flow_spec.md` instead — a fine home, but not the one a **ticketing** engineer opens to ask *"the complainant gave up waiting; does the officer still get the categories?"*. ⚠ The *"verified with a real late arrival"* limb is also outstanding, including whether a late sync can overwrite the complainant's own review edits. [`followups/the-two-minute-backfill-is-undocumented-for-ticketing.md`](sprints/2026-08-llm/followups/the-two-minute-backfill-is-undocumented-for-ticketing.md) |
| 🟠 **Nobody has measured the one path the 30 s classification budget exists for** | `backend/actions/forms/form_otp.py:181-183` · [`deployment/17_manual_browser_sweep.md`](deployment/17_manual_browser_sweep.md) | **D-41**, raised 2026-08-19, written up 2026-08-27. `complainant_consent is False` empties `form_otp.required_slots()`, the SMS round-trip disappears, and the gap to the review step collapses to seconds — so **the fast path through the flow is the privacy-conscious path**, and the complainant who declines contact details is the one shown *not ready yet*. Risk mitigated by DPG-15b; the number is unmeasured. Fold into the next browser sweep. [`followups/no-contact-path-latency-unmeasured.md`](sprints/2026-08-llm/followups/no-contact-path-latency-unmeasured.md) |
| 🟠 **The benchmark set has no audio, so DPG-22 cannot measure word error rate** | `tests/data/benchmark/` | Logged 2026-08-20 with DPG-20. The set's six `voice_origin` items are transcript-*shaped* text — no field boundaries, self-identification in the opening sentence, fillers — which is what **classification** and **extraction** consume. WER needs recorded audio with verbatim transcripts, and nothing in this repository can author a recording. ⚠ TTS would not close it either: synthetic speech is cleaner than a person on a rural mobile connection, so the WER would flatter every candidate. DPG-22's other acceptance items remain reachable (licence per candidate, p95 latency, cost per 1,000 minutes, and the honestly-empty baseline column — voice has never been live, Q-13.2); the number the ticket is named after is not. **Blocks a measurement, not a feature** — the voice flow is declared `PARKED`. Two routes out: record 6–20 clips from the existing authored scripts (which also buys an end-to-end transcribe-then-classify score), or evaluate on a held-out slice of **OpenSLR SLR54** for a citable number — a complement, not a substitute, since SLR54 is read speech with no road noise and no code-switching. Tracked: [`sprints/2026-08-llm/followups/no-audio-subset-for-asr-benchmark.md`](sprints/2026-08-llm/followups/no-audio-subset-for-asr-benchmark.md) |
| ~~🟠 **No Celery task has ever had its retry configuration applied — a key-name mismatch**~~ ✅ **FIXED 2026-08-19** | `backend/task_queue/task_manager.py` (`TASK_CONFIG` … `'retries'` vs `config.get('retry', {})`) | Found 2026-08-19 by DPG-15b while making `LLM_failed` reachable: `self.retry()` reported *"Retry in 180s"* against a configured 2 s. `TASK_CONFIG` declares the settings under `retries`, the decorator reads `retry`, so `retry_config` is always `{}` and **every task runs on Celery defaults** — including **no `autoretry_for` at all**, which is why nothing in the queue has ever retried automatically. It also half-explains D-34: the retry counter the terminal-status guard waited on could never move. ⚠ **Not fixed in DPG-15b**: correcting the key switches on `autoretry_for=(Exception,)` for *every* task type at once (file upload, messaging, database), because `getattr(__builtins__, 'RateLimitError', Exception)` resolves unknown names to `Exception`. That is a queue-wide change needing its own tests. DPG-15b passes an explicit countdown on the one task it owns. Tracked: [`sprints/2026-08-llm/followups/task-retry-config-has-never-been-applied.md`](sprints/2026-08-llm/followups/task-retry-config-has-never-been-applied.md) · PROGRESS **D-45** |
| ✅ **DONE 2026-08-19 — two degraded-mode terminal states that no code path reached** — `LLM_failed` after a model outage, and the closure document after an LLM failure | `backend/task_queue/registered_tasks.py` (`_persist_classification_failed_if_final`) · `ticketing/tasks/llm.py` (`generate_resolved_case_summary`) | Found 2026-08-18 by DPG-15's dead-port audit, which fixed the two *active* faults beside them (a failed classification recorded as `LLM_generated`; a complainant's stored phone number overwritten with `""`). These two are **absences**: `LLM_failed` is written only once Celery retries are exhausted, but the task returns a FAILED dict instead of re-raising, so nothing ever retries and the row sits at `pending` — where the retrieve step waits the **full 20 s** before giving up (the same 20 s D-30 shows we already brush against on a good day). And a closure document that fails to generate is marked `llm_failed` and never rebuilt: the complainant is told their case is resolved but never receives the outcome narrative. Both change the retry topology of live paths, so each needs its own tests and its own dead-port run. Tracked: [`sprints/2026-08-llm/followups/llm-failed-is-unreachable-and-the-closure-doc-never-retries.md`](sprints/2026-08-llm/followups/llm-failed-is-unreachable-and-the-closure-doc-never-retries.md) · PROGRESS **D-34**, **D-36** · ✅ **Both closed.** **D-34** (DPG-15b) — the terminal write now happens *before* the retry decision, because `self.retry(exc=…)` re-raises the original exception, not `MaxRetriesExceededError`; verified against the database, the row reaches `LLM_failed` and the poll stops in 0.19 s. It surfaced **D-45** (no task had ever had its retry config applied — the loader read a `retry` key that the config spells `retries`), also fixed. **D-36** — the public closure routes now gate on *is there something readable* rather than *did the AI succeed*, and the task retries twice before logging that the complainant was not notified |
| 🟠 **Classification can take longer than the chatbot waits for it — the complainant sees "no classification" on a *successful* call** | `backend/actions/grievance_intake/classification.py` (`CLASSIFICATION_POLL_MAX_SECONDS = 20.0`) · `backend/services/LLM_services.py` (`classify_and_summarize_grievance`) | Measured 2026-08-18 (DPG-14.1, three live `gpt-5-nano` calls): **20.5 s / 15.2 s / 14.0 s** of server processing against a **20 s** poll deadline — and that excludes Celery queue wait and network. `finish_reason` was `stop` every time, so this is **not** truncation and not a model failure; it is the success path missing the window. 92% of one run's completion budget was reasoning tokens (2,880 of 3,087), and the prompt is 20,725 chars because the category catalogue is injected twice in two shapes. Fix needs a distribution (≥20 calls, p50/p95), not three samples, and touching reasoning effort is a cost/quality change against the baseline DPG-23 benchmarks — and against an unanswered budget (Q-19). Tracked: [`sprints/2026-08-llm/followups/classification-latency-exceeds-the-poll-deadline.md`](sprints/2026-08-llm/followups/classification-latency-exceeds-the-poll-deadline.md) · PROGRESS **D-30** |
| 🟠 **The translation error path raises `UnboundLocalError`, and the one-line fix would leak the narrative into the logs** | `backend/services/LLM_services.py` · `translate_grievance_to_english_LLM` (⚠ a line number was here and rotted twice — the function name does not) | Found 2026-08-18 by DPG-10's characterization net — the test that pinned the failure contract discovered the contract was not the one the code has. `translate_grievance_to_english_LLM` interpolates `result` into its `ValueError` message, but `result` is bound *after* the API call, so **every pre-call failure** (provider down, timeout, bad model name, missing district key) raises `UnboundLocalError` instead. Callers catching `ValueError` do not catch it. ⚠ **Why it is not simply fixed:** binding `result` early makes the declared `ValueError` reachable — and that message interpolates **`input_data` in full**, grievance narrative included, into the Celery logs. The correct fix is the binding **plus** dropping the interpolation, which is Sprint 3's **T-34-b**. Half of it is a bug fix that leaks. Second instance of one pattern (see D-28, same module) — the followup's definition of done includes sweeping for a third. Tracked: [`sprints/2026-08-llm/followups/translate-error-path-unbound-local.md`](sprints/2026-08-llm/followups/translate-error-path-unbound-local.md) · PROGRESS **D-29** |
| ✅ **DONE 2026-08-19 — CI was red on `integration/stage` for 10 consecutive runs (2026-08-08 → 08-17) and gated nothing** | `.github/workflows/ci.yml` · `channels/ticketing-ui/lib/useTicketThread.ts` · `tests/ticketing/test_donor_guardrail.py`, `test_grievance_sync.py`, `test_project_type_authoring.py`, `test_roles_crud.py` | Found 2026-08-18 by pushing `dpg/sprint0-licensing` and reading the result. **Two failures, both pre-existing, identical in shape on both branches** (verified step by step): **(1) `ui-checks` → Lint**, `✖ 166 problems (2 errors, 164 warnings)` — the 2 errors are `react-hooks/set-state-in-effect`; `--max-warnings=-1` means only errors fail it. ⚠ **Lint failing skips the Build step**, and Build is the `next build` gate D-02b named for the SPDX-above-`"use client"` question — so **that gate has never run in CI**. ⚠ Also: the portal-lint row below says "143 warnings, 0 errors"; it is now 164 and 2. **(2) `backend-tests` → pytest**, 18 failed / 1143 passed **as measured that day** (the passing count is now 1166 — `tests/repo` grew; **the failure count, 18, is the number that matters and it has not moved**), clustered in project-type / back-fill / grievance-sync tests — the row below says 10, so **8 are unaccounted for**. ✅ `tests/repo` (the DPG compliance pins) passes. **The point is not the 20 fixes — a permanently red build is indistinguishable from one nobody watches**, which is the same root as the 27 uncollected test files and the missing `dpg/**` push trigger. Get to green once, then red means something. Tracked: [`sprints/2026-08-llm/followups/ci-has-been-red-for-ten-days.md`](sprints/2026-08-llm/followups/ci-has-been-red-for-ten-days.md) · PROGRESS **D-26** · ✅ **Green.** `backend-tests` **18 → 0 failed** (1166 → 1388 passed), `ui-checks` **2 → 0 errors**, verified by rebuilding the database exactly as CI does rather than inferring it. The eighteen were **one root cause said four ways**: something reads data the seeder writes afterwards, so on a *fresh* database — and only there — a back-fill finds nothing, raises nothing, and leaves a table empty whose symptom arrives much later looking unrelated. The lint errors were a ref assigned during render to bridge two callbacks declared in the wrong order; there was no cycle to break. ⚠ **Build now runs in CI for the first time** — lint failing had been skipping it, which is the D-02b gate this row named. ⚠ **The 8 "unaccounted for" failures are accounted for**: the row below counted 10 and the real number was 18. ✅ **And the thing this row was actually about is done too:** `dpg/sprint1-llm-agnostic` fast-forwarded into `integration/stage` (48 commits, 0 behind) on 2026-08-19, run **32266109536 — all four jobs green**. That is the branch's first green build since 2026-08-08, so **red means something again**. ⚠ Deployment is a separate step and has not happened; `scripts/database/rehash_search_tokens.py` (D-19/F-3) must run on that box or phone and email lookup silently return nothing |
| ✅ **DONE 2026-08-19 — a tracked file was rewritten on import** | `backend/task_queue/config.py` → `backend/scripts/task_queue/config.sh` | Found 2026-08-19 while fixing D-26. `update_shell_config()` runs at module scope and writes a **git-tracked** file from the importing process's environment — `REDIS_HOST="redis"` in the stack, `"localhost"` on the host. The working tree goes dirty by itself after every containerised test run, and the committed value records whichever environment imported last. It was swept into the D-26 commit by `git add -A` and pulled back out. **Recommendation:** generate to an untracked `config.generated.sh`, gitignore it, point the consumers at it. ⚠ Check what sources the file first — the deploy path does, and a missing one breaks worker startup. Tracked: [`sprints/2026-08-llm/followups/a-tracked-file-is-rewritten-on-import.md`](sprints/2026-08-llm/followups/a-tracked-file-is-rewritten-on-import.md) · PROGRESS **D-47** · ✅ **Deleted.** Nothing sourced it — its consumers went with the legacy shell runtime in `28508aec` — so the fix was removing the generator and the artefact, not redirecting them. ⚠ **This row's own recommendation was wrong**: "the deploy path sources this file" came from a grep hit belonging to a different `config.sh`. Pinned by `tests/backend/test_import_side_effects.py` on the property (importing writes nothing to the source tree), not on the deletion |
| 🟡 **112 backticked paths in `docs/` point at files that do not exist** | 39 distinct targets · worst: `ticketing/api/routers/tickets.py` ×22 (module → package, prose never chased) | Measured 2026-08-19 while widening the `docs-links` gate. **Not gated on purpose:** a backticked path in prose is not always a citation — a post-mortem names what it deleted, a spec names what it will create — so enabling it across `docs/` needs a convention for those two cases first, or it buys an ignore list, which is how a gate stops gating. The gate now covers backticked paths in the **root** `*.md` (0 broken) and markdown links everywhere. Tracked: [`sprints/2026-08-llm/followups/backticked-paths-in-docs-are-unchecked.md`](sprints/2026-08-llm/followups/backticked-paths-in-docs-are-unchecked.md) · PROGRESS **D-48** |
| 🟠 **`ops` writes no findings at all when `OPS_DB_PASSWORD` is unset** | `ops/config.py:104-111` · `ops/migrations/versions/ops001_init.py:54-64` · `env.local` | Found 2026-08-18 running `licence_scan()` for real. `database_url` does `user = ops_db_user or postgres_user`, but `ops_db_user` **defaults to the non-empty `"ops_app"`** so the fallback can never fire — while the password *does* fall back, to the admin's. Login is then `ops_app` + admin password; and the migration creates the role **with no password** when the env var is unset, so nothing authenticates. **This disables every ops finding — `pip-audit` CVEs and health checks too, not just licences.** The failure is silent: the check returns normally, logs `Failed to record check`, and tries to alert (which also fails if `HEALTH_ALERT_EMAIL` is unset). Documented in the ops001 comment as an operator step, so it is a **configuration precondition, not a code defect** — but its omission is undetectable, which is the same *manufactured confidence* the licence-classifier bug had. Suggested fix: default `ops_db_user` to `""` so the documented fallback works, and fail loudly when neither credential set resolves. PROGRESS **D-25** |
| ✅ **DONE 2026-08-19 — three privacy defects in the storage layer** | `backend/services/database_services/base_manager.py:266-297`, `:559-573` · `scripts/ops/backup_db.sh:45-60` | Found by DPG-04 while building the data-flow diagram **from the code** rather than from the existing privacy specs — which is why no spec had any of them: every privacy doc describes the *architecture*, none describes what the storage layer does when something goes wrong. **(F-2) Encryption at rest fails open** — `_encrypt_field` returns the plaintext value unchanged when `DB_ENCRYPTION_KEY` is unset *and* on any pgcrypto exception; the error is logged and the write proceeds, and `_decrypt_field` mirrors it, so reads look correct and nothing downstream can tell. **(F-3) `*_hash` search tokens are unsalted SHA-256** of phone/email/name/address — Nepal's mobile space is exhaustible in seconds, so the phone hash is a reversible encoding, not a pseudonym; fix is almost certainly HMAC with a server-side pepper, which preserves the equality lookup. **(F-4) Backups unencrypted by default** — contact columns stay ciphertext inside the dump, but the narrative, every officer note and **every voice recording and photograph** do not; encryption only runs if an env var is set, and the off-box destination is nowhere in the repo. **⭐ The clock:** no genuine grievance has been processed yet (all records are AI-generated or demo dummies), so all three are ordinary engineering tasks **today** and become a breach assessment after go-live. Tracked: [`sprints/2026-08-llm/followups/storage-layer-privacy-defects.md`](sprints/2026-08-llm/followups/storage-layer-privacy-defects.md) · PROGRESS **D-19** · assessment §6 · ✅ **All three fixed** (branch `dpg/sprint1-llm-agnostic`): encryption fails closed, tokens are HMAC-SHA256 with `SEARCH_TOKEN_PEPPER`, backups discard an unencryptable dump and uploads tar unless `BACKUP_ALLOW_UNENCRYPTED=1`. ⚠ **One operational step remains:** run `scripts/database/rehash_search_tokens.py` on each deployment, or phone/email lookup breaks silently against the old tokens |
| 🟠 **27 test files have never run in CI** — 21 sitting directly in `tests/` plus 6 in `tests/shared/`, including SEAH routing, complainant functions, sensitive-content detection and QR-token integration | `.github/workflows/ci.yml:178` | `ci.yml`'s pytest step names its paths explicitly, so an unnamed directory never runs. Found while deciding where DPG-01's SPDX pin should live — which is **why `tests/repo/` exists** as a separately-named directory rather than a file dropped into `tests/`. **Not fixed in a licence commit**: nobody knows whether these pass, and finding out is unbounded work that would make both changes unreviewable. Same failure mode T3-08 dismantled once (`-m "not integration"`), wearing a different hat. Endgame: `ci.yml` should name **no** paths — collect `tests/` wholesale and let markers exclude. Tracked: [`sprints/2026-08-llm/followups/ci-untested-root-test-files.md`](sprints/2026-08-llm/followups/ci-untested-root-test-files.md) · PROGRESS **D-01** |
| 🟠 **NEW FEATURE — a SEAH officer cannot explicitly return a cleared case to the standard queue** | `ticketing/engine/ticket_actions.py:561` (no action) · `ticketing/api/routers/tickets/crud.py:523` + `ticketing/services/ticket_workflow_reroute.py:28` (the mechanism that does exist) | Raised by the project owner 2026-08-24. **The safeguarding detector is deliberately tuned recall-first** — a miss is unrecoverable, a false alarm costs a SEAH officer a review and carries no risk to the complainant — so over-flagging is intentional and it flags **5 of 8** of the hardest benchmark items. ⭐ **That trade is only sound while the return path works.** It half-works today: correcting a ticket's classification re-resolves the workflow with `legacy_is_seah=False` and clears `is_seah`, restarting the case at L1 of the standard workflow — but **only as a side effect of a category edit**. No explicit action, no de-flag audit event (it records `CLASSIFICATION_VALIDATED`), **no test pinning the flag-clearing**, and no measurement of the round-trip latency. ⚠ A grievance the *complainant* routed to SEAH stays there by design (`workflow_routing.py:78`) — do not make that reversible. Tracked: [`sprints/2026-08-llm/followups/seah-officers-cannot-explicitly-return-a-cleared-case.md`](sprints/2026-08-llm/followups/seah-officers-cannot-explicitly-return-a-cleared-case.md) |
| 🟠 **Two AWS credentials remain in `secrets.enc.env` with no consumer, and the licence audit is stale** | `secrets.enc.env` (`AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`) · `docs/dpg/dependency-licenses.md` | Fallout from deleting the AWS SNS SMS path 2026-08-24 (privacy assessment **F-12**), on the owner's call that SMS providers serving Nepal must be in-country. The code, config and `boto3` are gone; **three cleanup steps need a human**: (1) drop both keys with `make secrets-edit` — an unused credential is pure liability, and `13_security.md` row 24 notes they are *distinct* from row 12, so **revoke the IAM key rather than rotating it**; (2) re-run the generated licence scan against a rebuilt image — the report's counts are ~4 high and five rows describe packages no longer in the tree (⚠ **re-run it, do not hand-edit the rows**); (3) confirm no deployed host still sets `AWS_*`/`PINPOINT_APPLICATION_ID` in `env.local.extra`. ✅ Already done: `SnsSmsClient`, the `aws_sns` provider, `format_philippines_e164`, `boto3`, `AWS_REGION`, `PINPOINT_APPLICATION_ID`, and **two real Philippine mobile numbers that were hardcoded in `constants.py`**. ⭐ Two latent defects closed on the way: the resolver used to default to `aws_sns` when no DOIT token was set (a misconfigured host sent SMS abroad silently — now fails closed to `disabled`), and `SMS_WHITELIST_ONLY=true` on DOIT raised an uncaught `ValueError` because the whitelist held PH numbers |
| ⏸ **Governance model + release/versioning policy deferred** (DPG indicator 8) | repo root | DPG-05 shipped `SECURITY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` and the issue/PR templates — all of which describe what is **already true**. A governance model and a supported-version window would describe **commitments nobody has agreed to**, on a project whose IP ownership is still an open question with ADB OGC (DPG-03/Q-01). Whether the DPGA requires either is itself open with the consultant (Q-08-01). Indicator 8 already reads 🟢 mostly compliant without them. Revisit when Q-10 answers **and** DPG-03 resolves. Tracked: [`sprints/2026-08-llm/followups/governance-and-versioning-policy.md`](sprints/2026-08-llm/followups/governance-and-versioning-policy.md) |
| 🟡 **Demo officer switcher one-way door — ROOT FIX LANDED 2026-07-16; C4 unblocked. Optional portal hardening remains.** | `ticketing/api/dependencies.py` (`require_admin_or_bypass`) · `ticketing/api/routers/users.py` (roster routes) | Root cause: `GET /users/roster` was `require_admin`, so switching to a non-admin officer 403'd the roster the switcher needs to switch back. **Fixed:** roster now `require_admin_or_bypass` — admin-only under Keycloak, any authenticated identity in dev bypass. Verified live (officer 403→200), pinned by `tests/ticketing/test_demo_switcher_roster.py`, full suite 607 passed. **Bypass builds only; Keycloak posture unchanged.** Residual (optional, defense-in-depth, now moot for the normal flow): the portal catch branch at `AuthProvider.tsx:264-269` still doesn't clear the cookie on a roster failure, and `fallbackBypassToken` still hardcodes super_admin. Tracked: [`sprints/archive/2026-08_tier3_structural/followups/demo-officer-switcher-one-way-door.md`](sprints/archive/2026-08_tier3_structural/followups/demo-officer-switcher-one-way-door.md) · PROGRESS **D-65** |
| ~~`add_more_info_flow` completes into a **silent turn** when `story_route` is unset~~ ✅ **FIXED 2026-07-15** | `backend/orchestrator/state_machine.py` (turn postcondition) | Closed by the **`run_flow_turn` postcondition** — the general guard both followups recommended over point fixes: *no turn returns zero messages*, logged at error with state/intent/active_loop/next_state, then the user is recovered. **One guard closed four instances: D-08, D-51, D-52 and D-59.** Safe to *recover* rather than merely log because the precondition was **measured, not assumed**: instrumenting all three of `run_flow_turn`'s returns across 208 tests found **3 zero-message turns and all 3 were bugs** — `attachment_ids_sync` and the `/introduce` restart never return empty, so there was no legitimate silence to protect and the blast radius is exactly the already-broken turns. That measurement also **found D-59**, a fourth instance nobody knew about. Owned by `tests/orchestrator/test_no_silent_turns.py` (8 tests, verified red). The characterization pin was deleted on the signal its docstring specified. |
| Seed log message still says `PROVINCE_1` | `kl_road_standard.py:319` | Cosmetic — actual stored value is `NP_P1` |
| `_scope_candidates` calls `_location_and_ancestors` twice (branches B + C) | `workflow_engine.py` | Minor perf — combine into one call |
| `OfficerScope` seed creates `UserRole` rows but no `OfficerScope` rows | `mock_tickets.py` | Auto-assign returns `None` for API-created tickets; pre-seeded demo unaffected |
| ~~Cognito user pool not created~~ | — | ✅ Obsolete — Keycloak shipped instead (see `docs/deployment/16_auth_keycloak.md`) |
| ~~`grievance_sync.py` hardcoded column list~~ | — | ✅ **CLOSED by T3-07 (2026-07-15).** The column list is now declared in `tests/ticketing/test_boundary_policy.py`'s `CONTRACT` and gated in both directions against the CL-01 `public.*` baseline: a renamed/dropped column fails in CI **naming the ticketing code that would break**, instead of at 02:00 in a Celery beat job. Mutation-verified (renaming `grievance_summary` in the baseline goes red). The guard covers **all 5** tables ticketing touches, not just this file. `public.complainants` is gated to `location_code`/`complainant_id` — PII selection fails the suite. |
| Utterance `file_name` derived from module name (~200 sites) — move a class to another file and its copy silently breaks | `backend/actions/base_classes/base_mixins.py:63` | Deferred out of T3-01 (which fixes the 4-site `action_name` introspection). **This is the finding the review's "makes the chatbot refactorable" item was actually aiming at**; M+. Already forced one workaround (`services/contact/utterances.py:6` hardcodes `"form_contact"`). Tracked: [`sprints/archive/2026-08_tier3_structural/followups/utterance-file-name-derivation.md`](sprints/archive/2026-08_tier3_structural/followups/utterance-file-name-derivation.md) |
| ~~Ticketing reads `public.grievances` + `public.file_attachments` **directly via its own DB session** — violates locked data rules #1/#5~~ | — | ✅ **NOT DEBT — the rule was retired 2026-07-15.** Reassessment found the surface is **11 statements / 5 tables / 3 writes** (not 3 reads); the rule protects two goals **both already dead in the code** (ticketing can't move DB — it writes to `public.*`; the chatbot can't survive without ticketing — intake location validation reads `ticketing.locations`); there is **no enforcement** (one DB, one role); and routing the reads via `GET /api/grievance/{id}` would be a **security downgrade** (that endpoint has no authn/authz/audit; the direct read sits behind JWT + a jurisdiction gate). **Decision (project owner):** drop "no joins", keep + pin no-FK and no-PII-columns. Evidence: [`sprints/archive/2026-08_tier3_structural/00-reassessment.md`](sprints/archive/2026-08_tier3_structural/00-reassessment.md) §6 → **T3-07** ([`06-boundary-policy-spec.md`](sprints/archive/2026-08_tier3_structural/06-boundary-policy-spec.md)). **✅ T3-07 LANDED 2026-07-15** — CLAUDE.md §Data rules amended to the as-built contract, both surviving rules pinned by `tests/ticketing/test_boundary_policy.py`, downstream docs realigned. The reads are **legitimized as-built; do not re-open them as debt.** |
| `backend/actions/forms/form_story_main_route_step.py` is **entirely dead code** (79 lines, `ValidateMenuForm` + `ValidateFormStoryStep`, zero importers) | `backend/actions/forms/form_story_main_route_step.py` | Deferred out of T3-01 (spec: dead code = log, don't fix). Proven by 4 legs: no importer, absent from `form_loop.py`'s `_FORMS` registry, no name reference, and **no Rasa action server exists** to auto-discover it. **It looks live only because `orchestrator/config/domain.yml:1054` still declares `validate_form_story_main`** — Rasa-era config the hand-rolled state machine no longer honors. T3-01 gave it an explicit key + mapping entry so the "every call-site key resolves" invariant stays total; **delete the module and that entry together.** Tracked: [`sprints/archive/2026-08_tier3_structural/followups/dead-form-story-main-route-step.md`](sprints/archive/2026-08_tier3_structural/followups/dead-form-story-main-route-step.md) |
| The grievance API has **no rate limiting** — `§6`'s "loggable, authorizable, **rate-limitable** at one place" chokepoint argument is now 2/3 true, not 3/3 | `backend/api/routers/grievance.py` | Deferred out of **T3-06 step 4** (spec: *"only if there is an existing mechanism to reuse… if dropped, log the deferral"*). **Measured: nothing reusable reaches these endpoints.** nginx `limit_req_zone` **does** exist (`webchat_rest_compose_prod.tls.conf:8-10`) but there is **no `location /api/grievance` block**, so `:5001` traffic never passes through nginx; no Python limiter is installed (`slowapi` etc. absent from all requirements). Low priority: the port is firewall-closed and both endpoints are now authenticated. **Anyone citing §6's chokepoint argument in a later sprint must know rate-limiting is still unbuilt.** The one piece network posture does *not* solve: auth failures on `_ticketing_auth_check` are unthrottled ⇒ the key is brute-forceable. Tracked: [`sprints/archive/2026-08_tier3_structural/followups/grievance-api-rate-limiting.md`](sprints/archive/2026-08_tier3_structural/followups/grievance-api-rate-limiting.md) |
| `5001:5001` binds the backend API to **all host interfaces** on deployed hosts — a dev convenience applied verbatim in staging | `docker-compose.grm.yml:112-117` | Deferred out of T3-06 §Exposure (*"consider dropping the `5001:5001` host mapping from the **prod** overlay"*). **The mapping is not in a prod overlay** — it is in `grm.yml`, shared by dev and both deploys, and compose **merges** `ports` by concatenation so `docker-compose.aws.yml` **cannot cleanly override it** (you'd get both binds). Removing it outright breaks the documented host-browser dev loop. Defence-in-depth only: the SG already closes `:5001`, so the firewall is the single control holding. Suggested shape: `"${BACKEND_HOST_BIND:-0.0.0.0}:5001:5001"`. Do it together with the `:8000` and vestigial `:3000` SG items — one compose edit, one SG edit. Tracked: [`sprints/archive/2026-08_tier3_structural/followups/grievance-api-host-port-binding.md`](sprints/archive/2026-08_tier3_structural/followups/grievance-api-host-port-binding.md) |
| ~~Host `pytest tests/ticketing` fails on **stale `env.local` DB credentials**~~ | — | ✅ **CLOSED by T3-08 (2026-07-15, `ac0b9d29`).** Root cause was deeper than this row: env.local's `POSTGRES_*` are dead **by construction** — every compose service hardcodes `POSTGRES_DB`/`USER`/`PASSWORD` in its own `environment:` block, so **no container reads them**. `tests/ticketing/_host_env.py` was the only reader, i.e. the only thing that could disagree with the DB it talks to. It no longer reads DB identity from env.local (an explicit shell override still wins). **Bare `pytest tests/ticketing`: 110 failed / 234 errors (132s) → 567 passed / 0 failed (14s).** The `POSTGRES_USER=... pytest` workaround is retired; pinned by `tests/ticketing/test_host_env.py`. `.env.example` already had the correct values — only env.local drifted. |
| ~~`tests/ticketing` is **not reliably green on a developer machine**~~ | — | ✅ **CLOSED by T3-08 (2026-07-15, `9e74e457`).** **This row's diagnosis was wrong** — `l1-officer-3` is not DB residue, it is a seeded officer (`ticketing/constants/demo_officers.py:22`) in the province-fallback pool since `b8cab274`; the original grep missed `ticketing/constants/`. Real cause: 3 assertions named a hardcoded 2-of-4 officer pair, while `auto_assign_officer` ranks by **active ticket load** — so they pinned an identity decided by live data, and passed on a fresh seed only by luck of the `user_id` tie-break. They now assert the province **pool** from `demo_officers`. Mutation-verified still to catch a broken fallback. **Bare `pytest tests/ticketing` = 567/5/0, stable across runs.** The rot went unnoticed because CI never ran these tests — fixed in the same pass (row below). |
| Voice-chunk upload sessions live in a **process-local dict** (+ local `.part` file) — the chunked protocol is stateful across requests, so every chunk must hit the same process | `backend/services/file_server_core.py:29-31`, `docker-compose.yml:48` | Deferred out of T3-03 (spec scopes out `file_server_core` session lifecycle). **Not a live bug** — the API runs a single uvicorn worker (no `--workers`, verified). But adding workers/replicas **silently** breaks voice notes: chunk 1 404s and the client swallows it (`voiceNote.js:31` matches `"not found"`) ⇒ reads as a flaky feature, not a misconfiguration. **Put this on the checklist for any change to the API's worker/replica count.** Tracked: [`sprints/archive/2026-08_tier3_structural/followups/voice-chunk-session-store-is-process-local.md`](sprints/archive/2026-08_tier3_structural/followups/voice-chunk-session-store-is-process-local.md) |
| Portal ESLint debt: 143 warnings, 0 errors (HR-05 downgraded 5 rule families to `warn` to reserve the CI error channel) | `channels/ticketing-ui/` | Tracked ticket: [`sprints/2026-07_hardening/followups/portal-lint-cleanup.md`](sprints/2026-07_hardening/followups/portal-lint-cleanup.md). Endgame: warn → fixed → re-enable as `error`. Includes the 4 unused `eslint-disable` directives (e.g. `AuthProvider.tsx` getUserPreferences effect). **Measured 141 on `dev/tier3-structural` @ `87fbba86` (this row says 143 — one of the two is stale; T3-05 held the measured number flat at 141 across 7 commits).** |
| Settings tabs have **no render smoke tests** — the portal has no DOM test harness at all | `channels/ticketing-ui/vitest.config.ts`, `package.json` | Deferred out of **T3-05** (spec: *"if skipped for scope, log it as a followup + TODO.md row"*). **Not a skipped test — a missing capability:** no `@testing-library/react`, no `jsdom`, no `@vitejs/plugin-react`; vitest is `environment: "node"` and `include: ["**/*.test.ts"]`, so **`.tsx` is never even collected** — the config header says this is deliberate. Standing it up = 3 dev deps + lockfile + `grm_ui` image rebuild + switching the **shared** config to `jsdom` under all 88 existing tests: a dependency decision, not a verbatim move. **T3-05 did close the coverage-from-zero half of D-12** — 27 mutation-checked unit tests now cover the extracted helpers (`workflowHelpers`, `roleEntry`, `friendlyError`), the first tests under `components/settings/`. The extraction is what makes the tabs individually mountable, so the remaining work is now S/M. Tracked: [`sprints/archive/2026-08_tier3_structural/followups/settings-tab-render-tests.md`](sprints/archive/2026-08_tier3_structural/followups/settings-tab-render-tests.md) |
| `ProjectWorkflowSelect` is **dead code** (zero callers; superseded by `ProjectWorkflowsEditor`) | `channels/ticketing-ui/components/settings/workflows/ProjectWorkflowSelect.tsx` | Found while moving in **T3-05**; not fixed (spec: move verbatim, log don't fix). Already dead **before** the sprint — it is one of the 141 baseline eslint warnings. ⚠️ **The extraction made the signal quieter, not louder:** as an *exported* symbol in its own module, `no-unused-vars` no longer flags it, so the portal count went 141 → 140. **That −1 is a lost signal, not an improvement** — do not read it as lint progress. Deletion is XS; natural home is the `portal-lint-cleanup` sweep. Tracked: [`sprints/archive/2026-08_tier3_structural/followups/dead-project-workflow-select.md`](sprints/archive/2026-08_tier3_structural/followups/dead-project-workflow-select.md) |
| ~~Silent OIDC refresh not applied to 4 non-`apiFetch` fetch sites (2 uploads can lose a file selection on token expiry)~~ | — | ✅ **RESOLVED 2026-07-15.** New shared `authedFetch` (proactive refresh + 401→refresh→retry-once) wraps all 4 blob/multipart sites; multipart bodies rebuilt in the thunk so the retry re-sends them; `isSessionExpiredResponse` retired. Vitest 61 (+3 new) / tsc / eslint / next build green. See [`sprints/archive/2026-08_tier2_quality/followups/apifetch-refresh-non-json-sites.md`](sprints/archive/2026-08_tier2_quality/followups/apifetch-refresh-non-json-sites.md). |
| ~~Pre-existing dead imports/local in `routers/tickets.py` (4 unused imports + unused `rerouted` local)~~ | — | ✅ Resolved in H2-02 Pass 4 (package split rewrote the import block — the 4 dead imports weren't carried across; `rerouted` binding dropped, reroute call preserved). Also dropped 3 dead private helpers (`_lookup_workflow`, `_first_step`, `_is_viewer`). Followup doc closed. |
| ~~Authz gaps surfaced by H2-03: (1) 15 unauthenticated reference/config GET endpoints, (2) `DELETE /roles/{id}` no gate on non-system roles, (3) `PATCH /projects/{id}` metadata no gate~~ | — | ✅ **RESOLVED 2026-07-15.** (2) delete gates on `CREATE_OPERATIONAL_ROLE`; (3) patch gates on `MANAGE_PROJECT` (xfail flipped to passing); (1) 10 project-config GET reads locked to `get_authenticated_user`, geography stays public, sweep allowlist trimmed to 5. Full ticketing suite 545 passed / 0 xfailed. Optional later step: per-project scope-gating (not just authenticate). See [`sprints/archive/2026-08_tier2_quality/followups/authz-gaps-h2-03.md`](sprints/archive/2026-08_tier2_quality/followups/authz-gaps-h2-03.md). |
| `StepCast` greys **wrong-track** roles only; the other two "why-excluded" reasons need a backend endpoint, and role labels are EN-only (`ticketing.roles` has no `_ne`) | `channels/ticketing-ui/components/settings/workflows/StepCast.tsx` · `ticketing/api/routers/users.py` · `ticketing/models/user.py` | Deferred from the step-cast parity build (v1 = `frame-04.md` §6 GAP1 recommended subset). "owned by another office" roles are filtered server-side (client never sees them); "bound at L3 only" has no endpoint — both need a picker endpoint returning in+out-of-scope roles flagged with a reason. Role `_ne` is RB-1 i18n. Nothing broken; wrong-track greying + roles-clarity shipped. Tracked: [`sprints/2026-07_org_chart_positions/followups/step-cast-why-excluded-and-role-ne.md`](sprints/2026-07_org_chart_positions/followups/step-cast-why-excluded-and-role-ne.md) |
| ~~🟠 **`redis:7` is a floating tag that now resolves to a non-OSI licence — DPG indicator-2 finding**~~ | `docker-compose.yml:222` · `.github/workflows/ci.yml:42` | ✅ **CLOSED 2026-08-17 — pinned `redis:8.10` in both.** Redis 7.4+ is dual RSALv2/SSPLv1, **not OSI-approved**, and `redis:7` pinned only the major so the tag silently floated onto that line. Redis 8 is **tri-licensed RSALv2 / SSPLv1 / AGPLv3** and we elect **AGPLv3, the OSI-approved option** — that is the indicator-2 answer. Chosen over Valkey (BSD-3-Clause) because it needs **no renaming, no runbook churn, no retraining** (binaries are still `redis-server`/`redis-cli`) and keeps Docker Official Image provenance; four call sites hardcode those binary names (`docker-compose.yml:225,231`, `ci.yml:46`, `scripts/ops/host_watchdog.sh:102`) and ~10 runbooks say `redis-cli`. **Safe:** usage audit found only `PING`, `INFO memory`, `LLEN`, `GET`, `SET … EX`, a redis-py mutex and pub/sub — no modules, no Streams, no cluster, **no persistence volume**, so no dataset to migrate. Image ~55 MB compressed. **Pinned the MINOR deliberately** — the finding's shape was a floating tag, so patches arrive but a relicence is opt-in. **✅ VERIFIED LIVE 2026-08-18** — recreated the service on 8.10 and exercised it in-container: `redis_version:8.10.0`, standalone, auth working; `PING`, `SET … EX`, `GET`, `LLEN`, `INFO memory` all as the usage audit predicted; **pub/sub round-trip delivered (1 receiver)**, which covers the Celery broker and Socket.IO paths; `ticketing_api` stayed healthy across the restart with no redis errors in its logs. No data to migrate, as predicted — the service declares no volume. ⚠ **That last clause is where D-63's inference started, and it is wrong**: no *volume* is true, but RDB snapshotting is on and `/data/dump.rdb` exists (measured 2026-08-27). The row's *conclusion* still holds — ephemeral queue data needs no migration across a version pin — but the premise seeded a false mitigation in three other documents. **Keep the two images in lockstep** or CI tests a different Redis than production. 8.10 drops to security-only when the next minor lands; **Redis 8.2 has security support to 2030-09-01** if a longer window is ever wanted. **Fallback if AGPL is flagged in DPG or ADB/DOR procurement: Valkey.** Tracked: [`dpg/00_compliance_status.md`](dpg/00_compliance_status.md) §2 + Q-02-02 |
| ~~🔴 **`POSTGRES_PASSWORD` is hardcoded as `password` in 11 compose sites; the SOPS-encrypted copy is read by nothing**~~ | `docker-compose.yml`, `docker-compose.grm.yml`, `scripts/ops/security-preflight.sh` | ✅ **CLOSED 2026-08-21.** All 19 literals replaced with `${VAR:?}` interpolation — including **Keycloak's `KC_DB_*`, which the original count missed** — `.env.shared` set to the identity the volumes actually hold (`user`/`app_db`, not the fictional `nepal_grievance_admin`/`grievance_db`), the credential **rotated**, and the literal removed from all six tracked files. The preflight gate now checks what a **container** resolves, not the inert copy, and both of its new checks were mutation-tested red. ⭐ **It also inverted `tests/ticketing/_host_env.py`**, which hardcoded the credential *on purpose* while env.local was dead config — that fence now points the wrong way and would reproduce D-36 against a rotated database. ⚠ **Staging and DOR prod are NOT done and will fail to start on the next deploy** — `${VAR:?}` is loud by design and their databases still answer to the old credential. Coordinated runbook: [`sprints/followups/db-password-hardcoded-in-compose.md`](sprints/followups/db-password-hardcoded-in-compose.md) §Deploying this to staging and DOR prod. Still open there: the role/database rename (cosmetic, no security payoff), `SMTP_USERNAME` in three files, and `OPS_DB_PASSWORD` (a separate credential, not rotated here). |
| ⬜ **Purge decision: secrets in public git history — scanned, and the recommendation is NOT to purge** | `git history`, 3 branches on origin | ✅ **Both live exposures are closed**: `POSTGRES_PASSWORD` (2026-08-21) and `REDIS_PASSWORD` (2026-08-23, found in 9 deleted files). ⭐ **`DB_ENCRYPTION_KEY` was never committed** — the one secret that cannot be rotated. ⚠ `SMTP_USERNAME` is in history and **unremovable**: it is the git author email on 865 of 1,018 commits. With rotation done, nothing left in history opens a live door, and the repo has been public since 2025-01-21, so a purge cannot un-publish anything. The one genuine argument for a rewrite is non-secret: an **entire agent worktree snapshot** (`.claude/worktrees/pedantic-kapitsa-c79380/`) is committed on `feature/chatbot`, `feature/grm-ticketing`, `features/chatbot`. ⬜ Also unread: GitHub's own secret-scanning alerts (API 403 — needs the scope). Method + full table: [`sprints/followups/secrets-in-public-git-history.md`](sprints/followups/secrets-in-public-git-history.md) |
| 🟠 **Dependency vulnerabilities: 34 on GitHub are against a 2-month-stale `main`; the real count on this branch is 6 Python + 4 npm-high** | `requirements*.txt`, `channels/ticketing-ui/package.json` | Measured 2026-08-23 in-container ([`dpg/dependency-licenses.md`](dpg/dependency-licenses.md) §Known vulnerabilities). ⚠ **Prioritise by reachability, not count.** `sanic-cors` + `wheel` arrive via `rasa-sdk` and **neither is reachable** — `sanic` is imported by no module here and `wheel` is build tooling. ⚠ **And removing `rasa-sdk` is a refactor, not a deletion**: 49 modules import it, `BaseFormValidationAction` **inherits** `FormValidationAction`, and the orchestrator executes `action.run(...)`. Do it for ownership of the form loop if ever, never for these two CVEs, and put a characterization net over intake first. ⚠ **`ecdsa` (via `python-jose`, Keycloak JWT) has NO published fix** — the only one a bump cannot close, and the only one on a reachable path. `sharp`/libvips (4 high) needs a Next.js bump outside the stated range. `python-dotenv`/`setuptools` are trivial. ⚠ **`ops` is not deployed**, so the nightly CVE + licence scans run only in a dev stack. |
| ⬜ **Secrets migrated to SOPS + age locally; staging + DOR prod not started, and no credential rotated** | `secrets.enc.env`, `.env.shared`, `scripts/ops/gen_env_local.sh` | 2026-08-21, steps 0–5 of [`deployment/18_sops_migration_handover.md`](deployment/18_sops_migration_handover.md). Outstanding, in priority order: (1) ⚠ **back up the age private key to Proton Pass + paper — until then a disk failure makes `secrets.enc.env` permanently unreadable**; (2) fill the `Owner` `TBC` cells in [`deployment/13_security.md`](deployment/13_security.md) §5.3.1; (3) the rotation pass — every credential reads *"unknown — treat as never"* in [`deployment/14_key_and_secret_lifecycle.md`](deployment/14_key_and_secret_lifecycle.md) §1; (4) migrate both hosts with a **per-server keypair** added via `sops updatekeys` — ⚠ **six secrets (§5.3.1 rows 2, 4, 7–10) exist only on those hosts and are not in `secrets.enc.env` yet**, so a green local round-trip proves nothing about them |

---

*Updated by Claude Code. Stage this file with every relevant commit.*
