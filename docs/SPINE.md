# The register — what is next

**Status:** authoritative (2026-09-04). **The single answer to "what is next."** If another document
appears to answer it too, that document is wrong — say so and fix it.
**Last updated:** 2026-09-14 — ✅ **STAGING DEPLOYED at `5984ccd`, digest-verified** (`GRM-100`): all 7 services healthy, migrations at head, externally serving. ⛔ **But the deploy exposed `GRM-099`: a "pulling" deploy still builds on the host** — the migration step never gets `IMAGE_TAG` — and **both success signals lied**. Swap (added an hour earlier) absorbed 486 MB of it. ⚠ `GRM-065` only half true (`:5001` fixed, `:8000` not — orchestrator not redeployed); ⛔ `GRM-014` **not in effect** (nginx never reloaded). Earlier, ⛔ **`GRM-098`: QA-01 was never built and the staging host still has 0 swap** — measured while preparing a deploy, and it is `GRM-008`'s exact condition. ⚠ **`GRM-096` corrected**: `sops` is not on that host, so `make env-local` cannot run there and the blanking risk is latent rather than imminent — the first version of the row over-claimed. Earlier, ✅ **`GRM-096` closed and `GRM-095`'s file half done**, so `make env-local` is safe on the host and the staging deploy is unblocked on secrets. ⛔ **`GRM-095` is NOT finished: the AWS access key is still live in IAM** — dropping it from a file does not disable it. Earlier, ⛔ **`GRM-096` CONFIRMED on the host**: staging's `TICKETING_SECRET_KEY` is a live 43-char secret and `secrets.enc.env`'s is empty, so `make env-local` there is an outage. `GRM-097` records what else the session measured — **staging is `aarch64`** and **ghcr.io is reachable from it**, both previously assumed. Earlier, ⛔ **`GRM-096` opened and it blocks the staging deploy outright:** `A-11`'s step 3 (`make env-local` on the host) would blank `TICKETING_SECRET_KEY` and the ticketing API refuses to boot without it — verified in `main.py`, not inferred. ✅ `A-11`'s credential half is **done and proven**: a classic PAT is in `secrets.enc.env`, `docker login` succeeds and a private image pulls (tag is **7** chars). `GRM-094` fixed, `GRM-095` done by the owner. Earlier, ⚠ **`make env-local` is broken two ways, both found by running it rather than reading it.** `GRM-094` fixed (the script it invokes was never executable, so the target failed on every fresh clone); `GRM-095` **open and owner-only** — two unused AWS credentials whose removal `.env.shared:128` has instructed since 2026-08-24 in `secrets.enc.env` with no marker, which fail the halves-disagree check and should be deleted and revoked rather than marked. ⚠ **`GRM-095` blocks the staging deploy**, because step 3 of `A-11` is `make env-local` on the host. Earlier, `GRM-093` opened and fixed, and it corrected `A-11` on the way: **GHCR does not accept fine-grained tokens**, so the recommended credential was unbuildable and the `403` already recorded as evidence was that limit, misread. Earlier, `GRM-093` opened and fixed: the pulling deploy `03_operations` §6a documents **could not work** — private packages since D-010, and no `docker login` anywhere in the Makefile. Closes the code half of `A-11`; the token and two env steps remain the owner's. Earlier, ⭐ **the `settings-ui` lane is `tested`, not merely `implemented`:** Docker came up, the UI image was rebuilt and all six items were driven in a browser. **69 e2e specs green.** The browser earned its keep three times over — it caught a placeholder-keyed spec `GRM-087` had broken (tsc, eslint and 143 unit tests were all green on it), a **real race** in `GRM-090`'s disclosure that showed up only as a flaky spec, and two wrong selectors in specs written blind, one of which would have created a project per run. Earlier the same day, **the `settings-ui` lane is complete: all six items merged** (`GRM-085`–`GRM-090`), plus `GRM-064` closed and `GRM-091`/`GRM-092` opened on the way through. ⚠ **Every one is `implemented`, not `tested`** — Docker was unreachable throughout, so nothing in this lane has been driven in a browser and two e2e specs have never been run. Earlier the same day, `GRM-086` built (wireframe first), which **unblocks `GRM-088`** — the last open item in the `settings-ui` lane, and the one Q-01 deliberately made droppable. Five of six merged. Earlier the same day, `GRM-090` built: staffing levels collapse (closed unless a required slot is unstaffed, count in words) and the picker can invite. Four of the lane's six items are now merged; `GRM-086` and `GRM-088` remain. Earlier the same day, `GRM-089` built, and it was a **restoration**: the inline create-an-organization flow was lost as collateral when `21176f5c` dropped the deprecated actor-role table, days before D-005 reinstated the catalog. `GRM-092` opened (the e2e stops short of creating a real row). Earlier the same day, `GRM-085` and `GRM-087` built and merged (both `implemented`, **not** `tested`: no Docker in this environment, so nothing was driven in a browser); `GRM-091` opened, a spec-vs-screen fork found only because `GRM-087` made someone read the section. Earlier the same day, `GRM-085`–`GRM-090` opened as the `settings-ui` lane ([design](sprints/2026-09_settings_findability/DESIGN-settings-findability.md)), from client feedback on the Settings screens; intake retired `G-CONTRACT` from `GRM-086` after reading the endpoint, and `GRM-064` closed on the same lane's Q-04 (the shipped screens replace the stale `ui/04` mockup). Earlier the same day, `GRM-084` opened by QA-04c tier 2 (the GRC chair cannot convene a GRC hearing — the control is gated on a role key the cast model no longer issues); `GRM-083` opened by QA-05 (the e2e gate's report-only period expires 2026-09-21 and somebody has to remove the flag); `GRM-080`, `GRM-081` and `GRM-082` opened by QA-03, all three found by standing a **freshly seeded** stack beside the drifted dev one; `GRM-078` and `GRM-079` opened by QA-02 (production still builds its own images; CI and the Makefile disagree on migration order); `GRM-076` opened by QA-04d (the complainant webchat hard-depends on three foreign CDNs at runtime, on a firewalled production host) plus `GRM-077` (four sweep items need a filed case); `GRM-073`, `GRM-074` and `GRM-075` opened by QA-04c (CLAUDE.md contradicts the escalate/resolve photo gate; every seeded position type is unusable; officer provisioning is not driven, with a follow-up); `GRM-072` opened (the officer QR page sends every package's intake token to a third-party image service); `GRM-071` opened **and fixed** (sharing a report was a 500 on any deployment where nobody had shared one before — found by QA-04b probing the route it needed); `GRM-070` opened (the demo seed's scenario state decays under the SLA watchdog, found by QA-04a); `GRM-066` fixed (one line: a generator taught to emit a header, never re-run) and `GRM-068` closed: the header rule is now checked *before* the commit exists, and the check is green again for the first time since 2026-09-04. OM-09 closed at `implemented` (a release gate cannot be proven without a release); OM-07 deliberately re-blocked behind QA-04 (owner sequencing decision); OM-06 closed (`07` §11 rows 5 and 6 with it); OM-02, OM-03 and OM-04 closed (the register test now has ten checks, and a `ready` row without a profile is one of them), OM-07 unblocked; and the port measurement landed: `GRM-013` closed, `GRM-065` fixed pending
deploy, `GRM-014` reframed and downgraded, `GRM-067` + `GRM-068` opened. Created 2026-09-04 (OM-02), seeded from
`sprints/README.md`, the four live sprint trackers, and the open rows of `TODO.md`, which is now
retired behind a forwarding note.
**Audience:** internal — excluded from the public repository (lifecycle §10.4).
**Governed by:** [`engineering/07_work_items.md`](engineering/07_work_items.md) — kinds, the four-question
triage test, the nine gates, the six profiles, definition of ready, and the two state fields.

---

## How to read this

**Four sections, and they mean different things.**

| Section | Holds | Rule |
|---|---|---|
| **Current** | The one item in flight per lane | [07](engineering/07_work_items.md) §7.3 — one `current` **per lane**, not per project |
| **Register** | Items that are **ready**: someone could start today | §6.1 — every row is actionable, or it does not belong here |
| **Backlog** | Proposed items. Not ready, not forgotten | The interim inbox until a tracker is wired (OM-07) |
| **Done** | Closed this quarter, then archived | Q-06 |

**Two state fields, never one** (§7.1). `state` says where it is in the queue
(`proposed → ready → current → merged → done`, plus `blocked` / `dropped`). `verification` says how
true it is (`planned → implemented → tested → applied locally → deployed → verified in production`).
An item is `done` only when its verification level meets what its profile requires (§7.2).

**IDs.** New items take the next `GRM-###` — **the file is the allocator** (Q-07): take the next
number, and let git surface a double-claim as a merge conflict, which is the behaviour we want.
**Historical prefixes are never renumbered** (§8.2): `OM-`, `QA-`, `HR-`, `DPG-`, `T3-`, `H2-`, `SH-`,
`TP-`, `CL-` keep their identity and are cited across the tree.

⚠ **`GRM-102` is the next free id.** (`GRM-067`–`GRM-069` were opened 2026-09-06 — the port measurement, the full-suite run it triggered, and the CI half of `GRM-068`'s fix; `GRM-070`–`GRM-072` by QA-04a and QA-04b; `GRM-073`–`GRM-075` by QA-04c, all three found by driving flows a reader of the code would have believed worked; `GRM-076` and `GRM-077` by QA-04d; `GRM-085`–`GRM-090` by the `settings-ui` lane, from client feedback; `GRM-091` while building `GRM-087`; `GRM-092` while building `GRM-089`; `GRM-093` while preparing the staging deploy; `GRM-094` and `GRM-095` when a human first ran `make env-local` on that path; `GRM-096` one command before it would have taken staging down.)

---

## Open security findings

⭐ **This section exists because a `+SENSITIVE` row buried in a backlog reads as less urgent than it
is.** Severity is a field here, not a marker inside prose, so *"how many open high-severity findings"*
is answerable without a grep — a question ADB and a DPG assessor both ask.
Full list, including the low-severity ones: § *Backlog → Carrying `+SENSITIVE`*.

| Id | Severity | Item | State | Note |
|---|---|---|---|---|
| `GRM-065` | 🔴 **high** | **The orchestrator's port was published on every deployed host — and on staging it was reachable** | `merged` · `tested` | ✅ **MEASURED AND FIXED 2026-09-06.** Measured 2026-09-06 from outside both hosts (egress `146.70.252.25`, read-only: no `POST` sent): DOR prod filtered on 8000/5001/5002/5433/3001/18080; **AWS staging ANSWERED on `:8000`** — the orchestrator's uvicorn direct, `/docs` rendering the interactive Swagger UI and `/openapi.json` returning `securitySchemes: NONE`. Not an unconfigured firewall: 5001/5002 were filtered on the same host, so the security group was configured and 8000 explicitly allowed. **Two fixes:** the staging SG rule was removed by the owner (re-probed closed ×3, `/message` still routing 200/405 through nginx), and [`docker-compose.grm.yml`](../docker-compose.grm.yml) now binds `127.0.0.1:` on 8000 and 5001. Nothing needed the published port — nginx reaches both by Docker service name. ⚠ **`implemented`, not `deployed`:** the compose change is true on neither host until the next deploy |
| `GRM-014` | 🟠 **med** *(was 🔴 high)* | **The public complainant endpoint's only possible controls are edge-side — and staging had none** | `ready` · `implemented` | ⭐ **REFRAMED 2026-09-06, and the old title asked for something that would break the chatbot.** `POST /message` is publicly reachable on **both** hosts through nginx on 443 (confirmed: `GET /message` → 405, routed, no session created) and **must be** — the complainant webchat calls it unauthenticated, by design. So "add auth to the orchestrator" is not implementable; the controls are rate limiting, CORS and session-id unguessability. Prod carried `limit_req`/`limit_conn` on `/message`; **staging carried none, plus `Access-Control-Allow-Origin: *`**, so any page could drive a complainant session from a visitor's browser at unlimited rate. Ported prod's zones + limits into [`webchat_rest_compose_aws.conf`](../deployment/nginx/webchat_rest_compose_aws.conf) and restricted CORS to the staging origin (`nginx -t` clean). **Left open:** session-id unguessability is unmeasured, and `backend/orchestrator/main.py:102` still declares no security scheme |
| `GRM-013` | ✅ **closed** | DOR prod firewall for `:5001` is **unverified** | `done` · `verified` | ✅ **ANSWERED 2026-09-06 — the firewall holds.** Measured 2026-09-06 from outside both hosts (egress `146.70.252.25`, read-only: no `POST` sent): 5001 filtered on DOR prod, as were 8000/5002/5433/3001/18080, with 443/80 answering from the same probe so the path was live. ⚠ **One vantage only** — a source-IP allowlist would look identical from here; this observes reachability, not the ruleset. See § *Done* |
| `GRM-001` | 🔴 **high** | `next@16.2.6` ships nine advisories in the officer portal | `ready` | Middleware/proxy bypass in App Router · SSRF in Server Actions · SSRF in rewrites · unauthenticated disclosure of internal Server Function endpoints. **Ships.** ⭐ Chore-shaped, `+SENSITIVE` by blast radius (§4.2) |
| `GRM-067` | 🟠 **med** | **Staging's nginx has drifted from prod's hardened conf, and nobody diffed them** | `ready` | ⭐ **Found 2026-09-06 while fixing `GRM-014` — by diffing the two confs, which is the check that was missing.** Beyond the rate limiting now ported: staging carries a **server-wide `add_header Access-Control-Allow-Origin * always`** ([`webchat_rest_compose_aws.conf:370`](../deployment/nginx/webchat_rest_compose_aws.conf)) that prod does not, and lacks prod's security-header block entirely — `server_tokens off`, HSTS, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`. **Deliberately not fixed in the same commit:** removing a server-wide CORS header can break an embed silently, and the approved change was the rate limiting. Needs a look at what actually embeds the staging webchat first |
| `GRM-076` | 🟠 **med** | **The complainant webchat cannot function without three foreign CDNs, and production is firewalled** | `ready` | ⭐ **Measured 2026-09-06 by QA-04d, by blocking egress in a browser and reading what the page had left.** `channels/REST_webchat/index.html` loads `socket.io` (cdn.socket.io), `leaflet` (unpkg.com) and `exifr` (cdn.jsdelivr.net) at runtime. With them unreachable the shell still renders — so it *looks* fine — but `window.io`, `window.L` and `window.exifr` are all `undefined`: **no real-time file/task status, no map pin, no EXIF handling**, on the surface a complainant in rural Nepal uses to file a grievance. ⚠ **DOR production is firewalled** (`GRM-013`'s probe found 8000/5001/5002/5433/3001/18080 filtered), and nobody has checked outbound to these three hosts. **Also a privacy leak and a DPG-independence problem**: every complainant's browser — including one filing a SEAH report — announces its IP, UA and referrer to three foreign CDNs before a word is typed. ⚠ **HR-07 pinned these scripts with SRI, which fixes tampering and not availability**; the two were never the same question. Fix is vendoring the three libraries, which must re-derive the integrity hashes HR-07 added |
| `GRM-072` | 🟡 **low** | **The officer QR page sends every package's intake token to a third-party image service** | `ready` | ⭐ **Found 2026-09-06 by QA-04b, by blocking egress in a browser and watching what the page still asked for.** `app/qr-codes/page.tsx` renders each QR as `<img src="https://api.qrserver.com/v1/create-qr-code/?…&data=<the full scan URL>">` — *"no npm needed"*, per the code's own comment. So every officer who opens the page hands `api.qrserver.com` the production hostname and the complete set of live intake tokens, one request per package (7 measured on this stack), plus their IP and the time they looked. **Severity is low because the token is designed to be public** — it is printed on a signboard at a work site. **What is not low is the dependency:** the page's images come from a service nobody here operates, and DOR production is firewalled (see `GRM-013`'s probe), so this may already render as broken images there and nobody would know. ⚠ **Fix is a local QR encoder**, not an allowlist entry |
| `GRM-008` | 🔴 high | A routine `make aws-deploy` took staging off the network for ~40 min | `ready` | **Owned by [QA-01](sprints/2026-09_qa_automation/01-QA-01-deploy-safety.md)** — not loose |
| `GRM-006` `GRM-007` `GRM-009` `GRM-010` | 🟠 med ×4 | ops selfcheck on deploy · `AWS_DEPLOY_SERVICES` omits the orchestrator · `ops` password fallback should be fatal · Keycloak event storage on DOR prod | `proposed` | Backlog |
| `GRM-002` `GRM-003` `GRM-004` `GRM-011` | 🟡🔵 low ×4 | See the backlog table | `proposed` | Backlog |

⚠ **The measurement was run 2026-09-06, and it moved every one of the three.** `GRM-013` closed
(prod's firewall holds), `GRM-065` was confirmed *on staging only* and is fixed pending deploy, and
`GRM-014` turned out to be **mis-specified** — it asked for auth on an endpoint that must stay public,
so it was reframed and dropped to 🟠 med. **One 🔴 high remains `ready` and unassigned: `GRM-001`**
(the `next@16.2.6` bump — recommended paired with `HR-07`, whose browser sweep is what verifies it).

⭐ **The lesson worth keeping.** Two of the three rows were wrong in *opposite* directions, and no
amount of reading the compose file would have told you which: prod was safer than the register
claimed, staging was worse, and the endpoint the register wanted to authenticate is public by
design. **The five minutes were the work.**

---

## Current

| Lane | Item | Kind | Profile | State | Verification |
|---|---|---|---|---|---|
| *(operating-model)* | — | | | | *OM-09 closed 2026-09-06. **OM-05 is the last one that can start** — and it needs owner input; OM-07 is behind QA-04, OM-08 behind OM-05* |
| *(all others)* | — | | | | *no lane has a second `current`* |

---

## Register — ready

Items someone could pick up today. Sprint tickets keep their own IDs and link to their specs.

| Item | Kind | Profile | State | Size | Lane | Notes |
|---|---|---|---|---|---|---|
| [OM-05](sprints/2026-09_operating_model/05-OM-05-product-and-roadmap.md) — `PRODUCT.md` + `ROADMAP.md` | feature | chore+SPEC | `ready` | M | operating-model | Mostly assembly |
| [OM-07](sprints/2026-09_operating_model/07-OM-07-intake-and-tracker.md) — unify intake, wire the tracker | feature | backend-feature −DATA | `blocked` | M | operating-model | **Blocker: QA-04** (owner decision 2026-09-06 — sequencing, not dependency). OM-02 cleared its original blocker; it was then deliberately put behind the browser harness because **OM-07 is plumbing with no external risk, while QA-04 is what makes the verification ladder ([`06`](engineering/06_documentation_lifecycle.md) §4a) reachable without 75 minutes of human clicking**. ⚠ **Unblock condition:** QA-04a + QA-04b land — which needs the QA sprint approved first |
| [OM-08](sprints/2026-09_operating_model/08-OM-08-starter-kit-extraction.md) — starter-kit extraction | feature | chore+SPEC | `blocked` | S | operating-model | **Blocker: OM-05 only** — OM-01, OM-02, OM-03, OM-04 and OM-06 all closed 2026-09-06. Last by design |
| [QA-01](sprints/2026-09_qa_automation/01-QA-01-deploy-safety.md) — stop the next deploy being an outage | feature | backend-feature | `ready` | S | qa *(sprint not approved)* | ½ d · from the 2026-09-04 deploy outage |
| [QA-02](sprints/2026-09_qa_automation/02-QA-02-ci-built-images.md) — CI-built images | feature | backend-feature+RELEASE | `ready` | L | qa *(not approved)* | ⚠ **Q-01 must be re-decided** — its GHCR premise inverted when the repo went private |
| [QA-03](sprints/2026-09_qa_automation/03-QA-03-stack-isolation.md) — `COMPOSE_PROJECT_NAME` + ports | feature | chore | `blocked` | S | qa *(not approved)* | **Blocker:** QA-02 |
| [QA-04](sprints/2026-09_qa_automation/04-QA-04-browser-harness.md) — Playwright harness + coverage | feature | chore+TEST | `ready` | L | qa *(not approved)* | ⚠ 04c needs a flow inventory that does not exist |
| [QA-05](sprints/2026-09_qa_automation/05-QA-05-ci-gate-and-coverage.md) — run it in CI | feature | chore+TEST | `blocked` | M | qa *(not approved)* | **Blockers:** QA-02, QA-03, QA-04; and its own Q-16 |
| `GRM-085` — [the Settings tab strip does not open on the tab it opens on](sprints/2026-09_settings_findability/01-GRM-085-settings-tab-order.md) | feature | UI feature | `merged` · `tested` | XS | settings-ui | ✅ **VERIFIED IN A BROWSER 2026-09-07** — the strip renders Projects & packages first and active. ✅ **BUILT 2026-09-07.** ⚠ **`implemented`, not `tested`: Docker is unreachable in this WSL distro, so the new Playwright order assertion has never run against a browser.** tsc + eslint + vitest + `tests/repo` all clean. Reorder to Projects & packages · Organizations & officers · Workflows · Settings. ⭐ **It fixes an inconsistency nobody filed:** `/settings` already defaults to `projects` ([`page.tsx:134`](../channels/ticketing-ui/app/settings/page.tsx)) while the strip's *first* tab is Organizations & officers, so the page opens with its active underline in third position. ✅ Verified safe for the suite — [`settings-sections.spec.ts`](../channels/ticketing-ui/e2e/flows/settings-sections.spec.ts) selects tabs by accessible name, never by index |
| `GRM-086` — [the organization tree has no way to find anything in it](sprints/2026-09_settings_findability/02-GRM-086-organizations-search-and-filters.md) | feature | UI feature −CONTRACT | `merged` · `tested` | M | settings-ui | ✅ **VERIFIED IN A BROWSER 2026-09-07** — searching prunes the government line away and the count line reads honestly; clearing restores the tree. ✅ **BUILT 2026-09-07**, wireframe first ([`ui/07`](ticketing_system/ui/07_org_directory_filters.html)) — the one surface in this lane rule 9a.4 still demanded one for. Search keeps a match's **ancestors as context**: present, de-emphasised, and **not counted**, so five rows read honestly as *2 match, 3 more shown to place them*. ⭐ **Type is `unit_type`, not `org_category`** — category is root-only and inherited, so filtering on it would silently drop every child whose column is null, and the two groups already express it. Area options are codes present on real organisations, so the control stays correct outside Nepal. ⚠ **A bug found in my own change before commit:** clearing the filter left the forest half-collapsed, because forcing a filtered tree open had no else-branch. Fixed; it would have read as *the filter broke the tree*. 12 unit tests on [`orgFilter.ts`](../channels/ticketing-ui/components/settings/org/orgFilter.ts). ⚠ **`implemented`, not `tested`:** no Docker, nothing driven in a browser. Search keeping ancestors visible, plus Type and Area filters; **no parent filter** — the tree *is* the parent relationship. ⭐ **`G-CONTRACT` was assumed at intake and retired on reading the endpoint:** [`locations.py:362`](../ticketing/api/routers/locations.py)'s existing `q` returns a **flat** set, so a match whose parent does not match comes back without its parent and the forest cannot be rebuilt — server-side search is structurally incapable of the ancestor rule. `OrgTree` already fetches the whole forest with every filter field on it, so this is client-side and touches no API. ⚠ **Owes a committed `.html` wireframe** — the one surface in the lane that exists on no shipped screen |
| `GRM-087` — [officer search ignores the two columns admins scan by](sprints/2026-09_settings_findability/03-GRM-087-officer-search-fields.md) | feature | UI feature −DESIGN | `merged` · `tested` | XS | settings-ui | ✅ **VERIFIED IN A BROWSER 2026-09-07.** ⚠ **It also broke an existing spec, which only the browser caught:** `settings-officers.spec.ts` selected the box by its PLACEHOLDER, and this item reworded it. tsc, eslint and 143 unit tests were all green on that break. The input now carries `aria-label="Search officers"` and the spec keys on that — a control's test handle must not be its copy. ✅ **BUILT 2026-09-07** — matching is now [`lib/officerSearch.ts`](../channels/ticketing-ui/lib/officerSearch.ts), a pure function with 11 unit tests, covering name · email · position · office (en + ne) · project name and code · area, plus raw location codes. Split the empty state in two (*no officers yet* vs *nothing matched*, with **Clear search**) — they looked identical and meant opposite things. ⚠ **`implemented`, not `tested`:** no browser was available; the unit tests ran, the e2e did not. ⭐ Opened `GRM-091` on the way past. The haystack is `display_name`, `email`, `positions` ([`OfficersDirectory.tsx:156`](../channels/ticketing-ui/components/settings/officers-v2/OfficersDirectory.tsx)) — so Office and Project / area, the two columns an admin actually scans, are unsearchable. ✅ **The data is already in hand**: the same component computes org name, project codes and location scopes client-side for `projectAreaCell`. A haystack edit, not a fetch change. ⚠ Key specs on the email, not the display name (`GRM-081`) |
| `GRM-088` — [the officer directory has no structured filters](sprints/2026-09_settings_findability/04-GRM-088-officer-filters.md) | feature | UI feature | `merged` · `tested` | M | settings-ui | ✅ **VERIFIED IN A BROWSER 2026-09-07.** ✅ **BUILT 2026-09-07**, straight after `GRM-086` unblocked it. Office · Project · Area, the three axes the table's own columns render, **ANDed** with the search term and with the track filter — adding a control narrows, never widens, and there is a test for that because the opposite is what a wrong implementation does. ⭐ **Options come from the roster, not from the org and project lists:** a filter offering an office that employs nobody is a control whose only possible outcome is an empty table. ⚠ **The Standard / SEAH track filter is deliberately left outside** — it partitions by sensitivity and is permission-gated, so folding it in would put a permissions question in the same bag as a convenience one. 18 unit tests on the shared module. ⚠ **`implemented`, not `tested`.** 
| `GRM-089` — [the project's Organizations pane can only pick an organization that already exists](sprints/2026-09_settings_findability/05-GRM-089-create-organization-from-project.md) | feature | UI feature | `merged` · `tested` | S | settings-ui | ✅ **VERIFIED IN A BROWSER 2026-09-07** — the picker offers the create option, the modal opens, cancel returns to the picker. ✅ **BUILT 2026-09-07 — and it turned out to be a RESTORATION, not a new feature.** `21176f5c` (2026-08-04) dropped the deprecated actor-role table and took `ProjectActorAddRow` out of the editor with it — **that component carried the create-an-organization-inline flow.** [D-005](DECISIONS.md) reinstated the catalog days later, the pane was rebuilt, and the flow was never carried across: lost as collateral, not removed on purpose. ⭐ **The plumbing survived the whole time** — `ProjectsSection` has always passed `onOrganizationCreated` down and `ProjectEditor` has been declaring it and not using it ever since. Both pickers now end with *+ Create a new organization…*; on save the org is created, lifted into the picker's list, then linked into the slot that asked — in that order, so a failed link still leaves a selectable org. ⚠ **`implemented`, not `tested`:** no Docker, so the new spec has **never been run**. Opened `GRM-092`. Opens the existing [`OrgCreateModal`](../channels/ticketing-ui/components/settings/OrgCreateModal.tsx) from the picker, and **selects and links the new org on save** — a modal that creates a record and returns you to an empty picker has moved the work, not removed it, and that picker is a 20+ entry unsearchable `<select>` carrying full joint-venture legal names. ⚠ Makes [`OfficerJurisdictionForm.tsx:573`](../channels/ticketing-ui/components/settings/OfficerJurisdictionForm.tsx)'s "link organizations first, then invite officers" copy stale — check it in the same pass |
| `GRM-090` — [staffing shows every level expanded, so the two blockers are below the fold](sprints/2026-09_settings_findability/06-GRM-090-staffing-add-officer-and-collapse.md) | feature | UI feature | `merged` · `tested` | S | settings-ui | ✅ **VERIFIED IN A BROWSER 2026-09-07 — and the browser found a real bug the unit tests could not.** The disclosure seeded itself from an effect that waited on three async loads, so a click landing first made the seed skip itself and the blocking level stayed shut — the one outcome the feature exists to prevent. Surfaced as a FLAKY e2e spec, never as a red unit test. Replaced with a derived default plus an override captured on any interaction, so there is no window where the answer is not yet known; re-run 5× clean. ✅ **BUILT 2026-09-07.** Levels collapse on the packages disclosure pattern, **closed unless a required slot is unstaffed**, with the count in words on the header (*Needs an officer*) — kept visible when open, because the go-live rail counts the same thing and the two must not disagree. The predicate is [`castDisclosure.ts`](../channels/ticketing-ui/components/settings/projects/castDisclosure.ts), pure, 9 unit tests. ⚠ **A package scope never counts as blocking** — it inherits the project-wide officer, so an empty package slot means *same as project*; counting those would open every level on every project and undo the item. There is a test for exactly that. Open/closed is seeded once then owned by the admin, so staffing the last slot does not collapse the level under the cursor. **Second half:** the officer picker's dead end (*invite one under Organizations & officers*) is now *+ Invite a new officer*, opening `ProjectOfficerModal` **pre-set to the slot's own role** — a catalog here would invite someone for a job other than the unstaffed one. It re-mounts a component orphaned by `21176f5c`, the same commit that orphaned `GRM-089`'s. ⚠ **`implemented`, not `tested`:** no Docker; the e2e is typechecked and unrun. Inline officer assignment via the existing [`ProjectOfficerModal`](../channels/ticketing-ui/components/settings/ProjectOfficerModal.tsx), plus collapsible levels using the packages disclosure pattern. ⭐ **The default state is the whole design decision:** collapsed *except* a level with an unmet required slot, with the count in **words** on the collapsed header ([`ui/05`](ticketing_system/ui/05_ui_copy_style.md) rule 6) — collapse-everything would hide exactly what the pane exists to surface. ⚠ `G-SENSITIVE` was checked and answered no (the pane has a SEAH tab): it becomes yes the moment a collapsed summary names officers rather than counting slots |
| `GRM-071` — sharing a report was HTTP 500 on any deployment where nobody had shared one before | bug | backend-feature+TEST | `merged` · `tested` | XS | standing | ✅ **FOUND AND FIXED 2026-09-06 by QA-04b**, which needed a real share token to cover `/reports/view/[token]` and `/reports/public/[token]` and could not get one. `ticketing/services/report_shares.py` passed `updated_by=` where the column is `updated_by_user_id`, **in both branches, failing differently in each**: the INSERT raised `TypeError: 'updated_by' is an invalid keyword argument for Settings` → 500, and the UPDATE silently set an unmapped Python attribute and discarded the value. ⭐ **The asymmetry is why it survived:** the settings row does not exist until the first share, so every developer's stack failed once, was worked around, and then looked healthy forever — while a fresh DOR prod, staging after a reset, or a CI stack fails for the first officer who clicks Share. **The route was in [`route_snapshot.txt`](../tests/ticketing/route_snapshot.txt) and had no test at all** — a snapshot proves a route exists, not that it works. Fixed, plus 5 tests and 2 mutation records; the other four services writing that table were checked and are correct |
| `GRM-099` — **a "pulling" deploy still builds on the host: the migration step never receives `IMAGE_TAG`** | bug | backend-feature+RELEASE | `ready` | S | standing | ⛔ **Found on the FIRST real pulling deploy, 2026-09-14 — and both success signals lied.** `make` exited 0 and `aws-deploy OK` printed, yet the log shows `app:local … not found` and then **`app:local Building`** with `apt-get install build-essential libvips-dev…` — a full backend image build, **on the host**, created 07:24:50, 350 MB. **Cause, read in [`REMOTE_DEPLOY_CORE`](../Makefile):** `$(REMOTE_IMAGE_ENV)` prefixes `up -d` but **not** the three `compose run --rm --no-deps backend … alembic upgrade head` lines, so they resolve `IMAGE_TAG=local`, fail to pull it, and compose falls back to `build:`. ⚠ **Impact 1 — it is `GRM-008`'s trigger again.** QA-02's whole promise is *no build on the host*. **Swap absorbed 486 MB during this deploy**, added ten minutes earlier (`GRM-098`) — without it this is very plausibly the second OOM outage. ⛔ **Impact 2 — a correctness bug, and the worse one.** The migrations run the **host checkout's** code, not the image's. Harmless today only because `7e8a181`'s tree is byte-identical to `5984ccd`. **A rollback — `make aws-deploy IMAGE_TAG=<older>` — would run `integration/stage` HEAD's migrations against containers running older code.** **Fix:** prefix all three `run` lines with `$(REMOTE_IMAGE_ENV)`, and add `--no-build` / `pull_policy: never` so a missing tag **fails loudly** instead of building. Pin it the way `test_registry_login.py` pins the login: every compose invocation on the pull path carries `IMAGE_TAG` |
| `GRM-101` — **the keycloak UI image build can hang until the 60-minute timeout, so `integration/stage` HEAD has no deployable image** | bug | chore+RELEASE | `ready` | S | standing | ⛔ **Measured 2026-09-14 on `7e8a181`, the merge that promoted PR #10:** `build (ui … NEXT_PUBLIC_AUTH_MODE=keycloak)` ran **06:46:48 → 07:47:03 and was cancelled** — exactly `timeout-minutes: 60` — while its **bypass twin, same Dockerfile and source, one build-arg different, finished in 6.9 min** and `app` in 13.3. `e2e` was therefore **skipped**. A hang, not slowness. **Consequence right now:** `integration/stage` HEAD has **no `ui:7e8a181` in the registry and no e2e run**, so `make aws-deploy IMAGE_TAG=7e8a181` would fail at the pull. Staging runs `5984ccd` (byte-identical tree, `GRM-100`), which is why this did not block today. ⚠ **Probable contributors, unconfirmed:** the arm64 half builds under QEMU, and Actions caches are **branch-scoped** — `integration/stage` cannot read `dev/qa-automation`'s cache, where the same job took ~45 s — so every promotion is a cold build. That explains slow, **not a hang**; the bypass variant was equally cold and finished. **Cheapest next step:** re-run that one job and see whether it hangs again. If it does, the logs of a *completed* hang are the evidence the cancelled run cannot give. ⚠ `gh run cancel` and re-run both need an Actions-write token, which the owner's PAT lacks |
| `GRM-100` — **staging deployed at `5984ccd`, 2026-09-14** *(record)* | chore | chore | `done` · `verified` | S | standing | ✅ **Digest-verified, not tag-trusted:** running `backend` = `sha256:200a5685777783ea` and `grm_ui` = `sha256:b2704e2ad35e8387`, **identical** to the registry manifests for `app:5984ccd` / `ui:5984ccd` — the images CI built **and** ran e2e against. Host checks out `7e8a181`; **the tag differs from the commit deliberately**: `7e8a181`'s image build hung (the keycloak UI job ran 31+ min where its bypass twin took 6.9), and `git diff 7e8a181 5984ccd` is empty. **All 7 deployed services healthy**, `ticketing_api` included — which proves it booted past the fail-closed guard with the host's secret. External: officer UI **302** (to login), webchat **200**, `/message` **405**. Migrations at head: ticketing `2b4d6f8`, public `pub000_public_core_baseline`, ops `ops003_reportgrants`. Swap used **486 MB**, RAM available after **1764 MB**. ⚠ **Mixed stack, by existing design:** `orchestrator`, `celery_llm`, `celery_file` stay on old on-box builds (`GRM-007`). ⚠ **Rollback is NOT `IMAGE_TAG=<older>`** — nothing in the registry matches what ran before, and `GRM-099` would migrate with the wrong code anyway. The previous 7 images are tagged **`nepal_chatbot-<svc>:rollback-66e91f8`** on the host. ⚠ Images carry **no `org.opencontainers.image.revision` label** — digests are the only proof of provenance |
| `GRM-098` — **QA-01 was never built, so the staging host still has 0 swap — the exact condition of the 40-minute outage** | bug | backend-feature+RELEASE | `merged` · `verified` | S | standing | ✅ **MITIGATED ON STAGING 2026-09-14, and the mitigation was exercised within the hour.** 4 GB `/swapfile`, persisted in `/etc/fstab` (backup kept), perms 600, all 14 containers undisturbed. **The deploy that followed used 486 MB of it** — during `GRM-099`'s unplanned on-box build. ⚠ **QA-01 itself is still unbuilt:** this was done by hand on one host, so a fresh staging box or DOR production has none. ⛔ **Measured on the host 2026-09-14, while preparing a deploy: `swap: 0MB`, `RAM available: 848MB` of 3824, 14 containers running.** That is `GRM-008`'s condition, unchanged. ⭐ **[QA-01](sprints/2026-09_qa_automation/01-QA-01-deploy-safety.md) is still `ready`** — the sprint shipped QA-02…QA-05 and **left the deploy-safety ticket unbuilt**. Its own §1 deliverable, `scripts/ops/add_swap.sh` (*"a 4 GB swapfile… persisted in `/etc/fstab`… delivered as an idempotent script"*), **does not exist in the tree**. The ticket describes it in the past tense, which is how it read as done. ⚠ **Severity is lower than it looks for a PULLING deploy** — the 2026-09-04 outage was `npm ci` twice during an on-box build, and QA-02 removed the build from staging. What remains is `compose up -d` on 7 services plus three `compose run --rm` migrations, on 848 MB with **no swap to absorb an overshoot**: without swap an overrun is an OOM kill, possibly of postgres, rather than a slowdown. **Cheapest mitigation is minutes and is what QA-01 specified:** create the swapfile and persist it, before the next deploy |
| `GRM-097` — **staging host facts, measured 2026-09-14** *(record, not work)* | chore | chore | `done` · `verified` | XS | standing | Measured read-only over SSH while preparing the deploy, because three of these had been assumed for weeks. **`uname -m` → `aarch64`** — staging is ARM, so the multi-arch build (`linux/amd64,linux/arm64`, A-10's *stay multi-arch*) is load-bearing here and not insurance. **`curl -sI https://ghcr.io/v2/` → `HTTP/2 405`** — ghcr.io **is** reachable from staging, which was unmeasured and gated the pulling deploy. ⚠ **This says nothing about the DOR production host** (`GRM-078` still needs its own session; it is VPN-only). **Repo at `/home/ubuntu/nepal_chatbot`, on `integration/stage` at `66e91f8`, 4 commits behind origin — a clean fast-forward**, so `REMOTE_DEPLOY_CORE`'s `git pull --ff-only` will not fight. **`GHCR_READ_TOKEN` absent** from the host's `env.local`. ⚠ **`APP_ENV` and `AUTH_MODE` are not in staging's `env.local` at all** — they fall back to the settings defaults, which is worth knowing before anyone reasons about the fail-closed guard from those two names |
| `GRM-096` — **⛔ running `make env-local` on staging will blank `TICKETING_SECRET_KEY` and the API will refuse to boot** | bug | chore+SENSITIVE+RELEASE | `merged` · `verified` | S | standing | ✅ **CLOSED 2026-09-14.** The host's real 43-char value was copied into `secrets.enc.env` — **copied, not regenerated**: it is shared across chatbot ↔ ticketing ↔ backend webhooks and a fresh one breaks the other side. `make env-local` now regenerates cleanly here (54 vars, 16 encrypted). ⚠ **`implemented`, not `verified`** — the proof is the host regenerating without the API refusing to boot, and that has not happened yet. ⚠ **CORRECTED 2026-09-14 — the risk is REAL but LATENT, and the first version of this row over-claimed it.** `sops` and the age key are **not installed on the staging host**, so `gen_env_local.sh` dies at its first line (`command -v sops || die`) and never reaches the splice. **`make env-local` cannot run there at all**, so nothing was ever about to be blanked. The trap fires the day someone installs `sops` on that host — which [`18_sops_migration_handover.md`](deployment/18_sops_migration_handover.md) intends. Fixing it was still right; calling it imminent was not. **Measured, not assumed:** Measured read-only over SSH: staging's `env.local` holds `TICKETING_SECRET_KEY` **SET (43 chars)** while `secrets.enc.env` holds it **empty**. Running `make env-local` there replaces a working 43-character secret with nothing, and the API then refuses to boot. ⭐ **Found one command before it would have happened** — step 3 of `A-11` is *run `make env-local` on the host*. `TICKETING_SECRET_KEY` is **empty** in `secrets.enc.env`, and `gen_env_local.sh` splices that empty value into `env.local`, which *"Never hand-edit… the next regeneration discards the edit"* is explicit about overwriting. ⚠ **The API then refuses to start, by design:** [`ticketing/api/main.py:59`](../ticketing/api/main.py) `_assert_auth_configured` raises `RuntimeError` unless `bypass_enabled` (`APP_ENV=dev` **and** `AUTH_MODE=bypass`) — staging is `keycloak`. **Read in the code, not inferred from prose.** ⭐ **The handover already named the trap and did not close it:** [`18_sops_migration_handover.md`](deployment/18_sops_migration_handover.md) §2 — *"empty locally, but secret-class everywhere else — leaving it in the committed plaintext half is a trap for whoever sets it on a host."* Locally it is harmless because the dev bypass is on; that is the whole reason nobody has hit it. ⚠ **Do not regenerate on the host until this is resolved.** Sequence: read staging's current `env.local` value **first**; if it holds one and `secrets.enc.env` does not, regenerating destroys a working secret. ⚠ It is a **shared** integration secret (chatbot ↔ ticketing ↔ backend webhooks), so inventing a new one on one side breaks the other — [`14_key_and_secret_lifecycle.md`](deployment/14_key_and_secret_lifecycle.md) rates it *Integration outage · forged webhooks*. **Fix is a decision, not code:** either put the real value in `secrets.enc.env` (one secret, both hosts), or teach `gen_env_local.sh` to preserve a host-set value it has no replacement for. The first is simpler; the second is what the handover's wording implies was intended |
| `GRM-095` — **revoke the two dropped AWS access keys in IAM** *(was: they sit in `secrets.enc.env` and break `make env-local`)* | bug | chore+SENSITIVE | `ready` | XS | standing | ✅ **FILE HALF DONE 2026-09-14** — both keys dropped from `secrets.enc.env`, and `.env.shared`'s three-week-old instruction to do so is replaced rather than left to mislead. ⛔ **What remains is the half that actually matters, and it is not in this repository: the IAM access key is still live.** Deleting a credential from a file does not disable it. **AWS console → IAM → Users → Security credentials → delete the access key.** Until then these are working credentials that nothing needs — and `A-8` lists both among the 9 secrets baked into an image layer, so they have a second reason to go. ⚠ Owner-only |
| `GRM-094` — **`make env-local` could never run: the script it invokes is not executable** | bug | chore+TEST | `merged` · `tested` | XS | standing | ✅ **FOUND AND FIXED 2026-09-14, by a human following the deploy runbook and getting `Permission denied`.** `scripts/ops/gen_env_local.sh` was mode **100644** in git while the recipe is a bare `scripts/ops/gen_env_local.sh` — so the target died on every fresh clone, including the deploy host. Two more were the same: `security-preflight.sh`, and `aws_to_prod_db_sync.sh`. ⭐ **The tell was one target away the whole time.** `aws_to_prod_db_sync` runs `@chmod +x` on its own script before invoking it — somebody hit this, fixed the one in front of them, and left the cause. That workaround is why the other two went unnoticed. All three set to `100755` in the index, pinned by [`test_make_scripts_executable.py`](../tests/repo/test_make_scripts_executable.py), which reads **git's mode, not the working tree's** — a local `chmod` cures the symptom on one machine and nothing for the next clone |
| `GRM-093` — [**a pulling deploy never authenticates, so it cannot pull a private package**](items/GRM-093-registry-login-for-pulling-deploy.md) | deviation | deviation+SENSITIVE+RELEASE | `merged` · `tested` | S | standing | ⭐ **Found 2026-09-14 while preparing the staging deploy, by reading the Makefile instead of trusting the runbook.** [`03_operations`](deployment/03_operations.md) §6a says *"Staging pulls images that CI already built"* and `aws-deploy` sets `DEPLOY_BUILD=0` to do it — but [D-010](DECISIONS.md) made the packages private on 2026-09-04 and **`grep -n "docker login" Makefile` returned nothing**. The documented capability answers `denied`, at deploy time, on a remote host. **Reclassified from `bug` to `deviation` under [07 §2.3](engineering/07_work_items.md)**: fixing it meant editing §6a to add the precondition, and that edit is the fork. ⚠ **`G-SENSITIVE` fires on two triggers, not on size** — it introduces a credential into the deploy path and makes the host authenticate to an external service. Boundary tests: the token is never in argv (`--password-stdin`), never echoed, and **an absent token skips rather than fails** so `wsl-up` and `DEPLOY_BUILD=1` are provably untouched. 5 checks in [`test_registry_login.py`](../tests/repo/test_registry_login.py), three red before the fix. ⚠ **This closes only the CODE half of `A-11`.** Three manual steps remain and none is code: create the token — ⚠ **a CLASSIC PAT, `read:packages` only; `A-11`'s "fine-grained" recommendation is wrong and was corrected 2026-09-14, because GHCR does not accept fine-grained tokens at all** — add value **and** `#@secret` marker together (`gen_env_local.sh` fails if the halves disagree), and run `make env-local` **on the host**. Plus one unmeasured question: `curl -sI https://ghcr.io/v2/` from staging |
| `GRM-084` — **the GRC chair cannot convene a GRC hearing; only the super admin can** | bug | UI feature | `ready` | S | standing | ⭐ **Found 2026-09-07 by QA-04c tier 2, by walking a ticket L1 → L2 → L3 and then trying to convene as the officer it landed with.** [`app/tickets/[id]/page.tsx:787`](../channels/ticketing-ui/app/tickets/%5Bid%5D/page.tsx) gates the control on `roleKeys.includes("grc_chair") \|\| roleKeys.includes("super_admin")` — a **legacy** role key. The cast model issues `wf:KL_ROAD_STANDARD:LEVEL_3_GRC:actor`, and measured against the seeded roster **no officer holds `grc_chair` at all**. So the one action named after the GRC chair is invisible to them, and demo scenario 1's headline step works only for `admin@grm.local`. ⚠ **The escalation chain itself is correct** — the case *does* land with the GRC chair, which the spec pins — so this is the gate, not the routing. Fix is to derive it from the workflow step's actor tier the way the rest of the action panel does. The spec drives the convene as the admin meanwhile, with a note to change one line when this is fixed |
| `GRM-083` — **the e2e gate is report-only until 2026-09-21; take the flag off** | debt | chore+TEST | `ready` | XS | standing | ⭐ **This row IS the control.** `images.yml`'s `e2e` job carries `continue-on-error: true` so a two-week bedding-in period cannot fail anyone's build — and `continue-on-error` with no expiry is precisely how a gate becomes decoration, which this repository has been bitten by twice (nine `dpg/**` commits ungated, 27 test files never collected). **On or after 2026-09-21:** delete the flag, then add the `e2e` context to the `Main` ruleset's required checks (the other five are already there). ⚠ **Check the flake rate first** — if it is not clean over the period, the answer is to fix the flake, never to extend the date quietly |
| `GRM-080` — a bypass build renders as `super_admin` for ~10 s on a cold stack | bug | UI feature+SENSITIVE | `ready` | S | standing | ⭐ **Found 2026-09-07 by QA-03, by running the e2e suite against a freshly seeded stack instead of the warm dev one.** `AuthProvider` seeds its state with `fallbackBypassToken()`, whose `custom:grm_roles` is **`super_admin`** ([`AuthProvider.tsx:191`](../channels/ticketing-ui/app/providers/AuthProvider.tsx)), and replaces it only when `listOfficerRoster()` resolves. **That call takes ~10 s on a fresh database and 0.2 s on a warm one**, so the window is seconds wide exactly where it matters. Measured: `l1-officer@grm.local` — a Level 1 officer with no admin role — was served the **full Settings page** (Organizations & officers, Workflows, Projects & packages) on the fresh stack, and the correct "only accessible to administrators" panel on the warm one. ⚠ **Not a production vulnerability** — the bypass branch is dev/test only and HR-01 makes production fail closed — but it is a real UI defect, and it is the kind that becomes one the day the branch is reachable. **Two fixes, both wanted:** render nothing role-dependent until the identity resolves (the fallback should be *no* roles, not the highest), and find out why the roster costs 10 s on a cold database |
| `GRM-081` — the seed never applies officers' real names, so CI and a dev box disagree | bug | chore+DATA | `ready` | XS | standing | `DemoOfficerSpec` carries `first_name`/`last_name`, and `mock_tickets` uses only `email`, `role_key`, `organization_id` and `user_role_location` — so a freshly seeded roster derives display names from the email: `l1-officer@grm.local` → **`L1-Officer`**, where the dev box shows `Site Officer L1`. Fresh roster **17** officers, dev box **67**. ⚠ **This is what makes a spec keyed on a display name pass locally and fail in CI**, and it bit QA-04c's settings specs — now keyed on the email, which is the officer's identity everywhere. Fix is a few lines in the seed, and it removes a whole class of environment-dependent test |
| `GRM-082` — `make check_grm_ports` could only ever fail | bug | chore | `merged` · `tested` | XS | standing | ✅ **FOUND AND FIXED 2026-09-07 by QA-03.** It invoked `docker compose` **without `--env-file env.local`** — alone among the Makefile's compose calls — so interpolation died on `POSTGRES_USER is missing a value`, `port` returned empty, and it reported *"grm_ui not on :3001 (actual: )"* on any host that keeps its config in `env.local`, which is all of them. Verified pre-existing by running the Makefile at `HEAD`: identical output. ⭐ **A check that cannot pass is the mirror of a check that cannot fail** — both are decoration, and this one had been reporting a port problem that was never there. Now passes against the default stack *and* a moved one |
| `GRM-078` — production still builds its own images, and the switch is ready | debt | chore+RELEASE | `blocked` | S | standing | QA-02 converted staging to pulling and left every `prod-*` target building, verified byte-for-byte by diffing `make -n prod-deploy`. ⚠ **Blocked on one SSH session to the DOR host**, not on any code: `curl -sI https://ghcr.io/v2/` (can it reach the registry) and `uname -m` (`A-1` — one architecture or two). ⚠ **Plus a credential** — the repo is private so its packages are, and no registry token exists (`A-11`). ⭐ **The asymmetry is deliberate:** the on-box build OOM has happened on staging and never on production, so guessing wrong about a VPN-only host in a maintenance window costs more than waiting. [`followups/prod-still-builds-on-the-box.md`](sprints/2026-09_qa_automation/followups/prod-still-builds-on-the-box.md) |
| `GRM-079` — CI and the Makefile run the migration streams in different orders | debt | chore+DATA | `ready` | S | standing | `ci.yml` runs public → ticketing → ops; `REMOTE_DEPLOY_CORE` and `migrate_all` run **ticketing → public → ops**; `07_migrations_policy.md` documents a third (ticketing → public, ops unmentioned). ⚠ **Nothing is known to be broken** — both orders work every run — which is exactly why it is worth resolving before something depends on the accident. QA-02 and QA-05 each preserved the order their own path already used rather than inventing a third, because reordering a deploy's migrations as a side effect of an image change is how a schema surprise ships. Decisive experiment (migrate empty in both orders, diff the dumps) and the definition of done: [`followups/migration-order-disagreement.md`](sprints/2026-09_qa_automation/followups/migration-order-disagreement.md) |
| `GRM-076` — the complainant webchat hard-depends on three foreign CDNs | debt | chore+SENSITIVE | `ready` | M | standing | 🟠 socket.io · leaflet · exifr, all loaded at runtime from cdn.socket.io / unpkg.com / cdn.jsdelivr.net. Blocked ⇒ no real-time status, no map pin, no EXIF — and the page still *looks* fine. ⚠ **Unverified on the firewalled DOR prod host**, where it may already be degraded. Detail in § *Open security findings*. Vendoring is the fix and it must re-derive HR-07's SRI hashes |
| `GRM-077` — four of HR-07's sweep items need an intake driven to a filed case | debt | chore+TEST | `ready` | M | standing | The upload itself, the voice note (also needs an ASR stub), the map pin and the filed banner. **Six of ten items are automated and green** ([`e2e/webchat/`](../channels/ticketing-ui/e2e/webchat)); these four need a case, and reaching one runs the classifier — a paid call per run, on a pipeline that excludes `@live_llm` because it costs money. ⭐ **Cheapest partial: a seeded, already-filed grievance** the status-check flow can select, which turns two of the four green with no new machinery. Reasoning and definition of done: [`followups/webchat-items-needing-a-filed-case.md`](sprints/2026-09_qa_automation/followups/webchat-items-needing-a-filed-case.md) |
| `GRM-073` — `CLAUDE.md` says the escalate/resolve photo rule is *"not blocked"*; the code hard-blocks, on both sides | bug | chore+SPEC | `ready` | XS | standing | ⭐ **Found 2026-09-06 by QA-04c, which could not escalate a ticket until it uploaded a photo.** [`CLAUDE.md`](../CLAUDE.md) § *Officer case view*: *"File attachment on escalate/resolve: warning encouraged but not blocked (B)"*. The reality: [`ticketing/engine/ticket_actions.py`](../ticketing/engine/ticket_actions.py) raises *"At least one image attachment is required before escalating"* (and the same before resolving), and `lib/useTicketThread.ts` refuses to open the form. **No live spec records the rule at all** — the only written trace is an archived June5 sprint doc. ⚠ **This is the exact failure `CLAUDE.md`'s own preamble warns about** (the `GET /api/grievance/{id}` decryption line that was false for months): an agent reading the binding file builds against the wrong contract. A superseded-table row was added 2026-09-06 as the minimum repair; **the real fix is a line in a live ticketing spec**, and a decision on which behaviour is wanted |
| `GRM-074` — every seeded position type is unusable, so officer onboarding dead-ends on a fresh database | bug | backend-feature | `ready` | S | standing | ⭐ **Found 2026-09-06 by QA-04c while driving Settings → Officers → Invite.** The seed creates **two** organisations — DOR (`unit_type: department`) and ADB (`development_partner`) — and **five** position types, every one restricted to `directorate`, `provincial_office` or `division_office`. **No seeded organisation has any of those unit types**, so the invite form's "Pick a position…" is empty under either office and the flow cannot be completed. Measured on this stack: the only positions offered under DOR were three unowned `test*` rows a developer had made by hand, which no fresh environment has. ⚠ **This is a fresh-environment defect** — it is invisible on any box where somebody once created a position, and it is exactly what a new DOR deployment would hit while following the onboarding runbook. Fix is either an org unit of a usable type in the seed, or `allowed_unit_types` that match the tree the seed actually builds |
| `GRM-075` — the e2e suite drives officer admin up to account provisioning and stops | debt | chore+TEST | `ready` | S | standing | Deliberate: submitting an invite calls `keycloak_create_user` whenever `KEYCLOAK_ADMIN_URL` is set (it is, on the dev stack), so every run would create a real realm account and send a set-password email. Directory, search, the invite cascade and the admin gate **are** driven. Reasoning, the two ways to close it, and a definition of done: [`followups/officer-provisioning-not-driven-by-e2e.md`](sprints/2026-09_qa_automation/followups/officer-provisioning-not-driven-by-e2e.md). ⚠ Blocked in practice by `GRM-074` |
| `GRM-072` — the officer QR page renders through a third-party image service | debt | chore+SENSITIVE | `ready` | S | standing | 🟡 Every `/qr-codes` view sends each package's full scan URL — intake token included — to `api.qrserver.com`, and the page's images depend on that service being reachable. ⚠ **Unverified on DOR prod**, which is firewalled; if it is blocked there the page has been showing broken images. Detail in § *Open security findings*. Stubbed in the e2e smoke suite so CI neither depends on it nor feeds it |
| `GRM-070` — the demo seed's two scenarios decay into a different state as the stack ages | bug | backend-feature | `ready` | S | standing | ⭐ **Found 2026-09-06 by QA-04a, and it is a demo problem before it is a test problem.** `mock_tickets.py` seeds scenario 1 (`GRV-2025-001`) at `IN_PROGRESS`, ready for the GRC `CONVENE` its demo script calls for. The dev database holds it `ESCALATED`, and the event log names the cause: `system` / *"Auto-escalated: SLA exceeded at previous step"*, 2026-08-13 — the SLA watchdog acting on a ticket dated `created_days_ago=14` on a long-running stack. **So the carefully-built demo state is a function of how long the box has been up.** The tests are already protected (`e2e/fixtures/seed.ts` and [`engineering/04_testing.md`](engineering/04_testing.md) §6a rule 6a.4 forbid asserting on seeded status); what is unprotected is somebody opening the demo. ⚠ **Not fixed in passing:** the plausible fixes — exempting seeded tickets from the watchdog, or re-dating them on a schedule — each change either production behaviour or the seed's contract, and choosing needs the owner. Adjacent precedent: `ensure_officer_coverage` repairs seed drift **additively** rather than by `--reset` |
| `GRM-065` — the orchestrator port was published on every deployed host | deviation | chore+SENSITIVE | `merged` · `deployed` | S | standing | ✅ **HALF VERIFIED ON STAGING 2026-09-14.** `backend` now binds **`127.0.0.1:5001`** (was `0.0.0.0`), and `:5001` is closed from outside. ⛔ **`:8000` is NOT fixed at the compose level** — `orchestrator` is not in `AWS_DEPLOY_SERVICES` (`GRM-007`), so it still binds `0.0.0.0:8000` on its old build. It is closed from outside **only because of the security-group rule the owner removed**, not because of this change. Stays open until `orchestrator` is redeployed. ✅ Measured + fixed 2026-09-06 — staging `:8000` was open, SG rule pulled, `127.0.0.1:` bound in compose. ⚠ **Stays open until a deploy makes the compose change true on both hosts.** Detail in § *Open security findings* |
| `GRM-014` — the public complainant endpoint has only edge-side controls | deviation | backend-feature+SENSITIVE | `ready` | S *(was M)* | standing | ⛔ **NOT IN EFFECT ON STAGING, measured 2026-09-14 after the deploy.** The conf is on disk (`limit_req_zone` × 2) but the **running** nginx has **0** (`nginx -T`) — `nginx` is not in `AWS_DEPLOY_SERVICES`, has been up since 2026-09-04, and a bind-mounted conf changing on disk does not reload it. So staging **still has no rate limit on `/message` and still sends `Access-Control-Allow-Origin: *`**. Applying it is `nginx -t && nginx -s reload` — ⚠ **gated on `GRM-067`**: check what embeds the staging webchat before the CORS header changes. ⭐ **Reframed 2026-09-06** — the old title ("add auth to the orchestrator") is not implementable: the webchat calls `/message` unauthenticated by design. Rate limiting + CORS ported to staging; **what remains is session-id unguessability**, which is unmeasured |
| `GRM-067` — staging nginx has drifted from prod's hardened conf | debt | chore+SENSITIVE | `ready` | S | standing | 🟠 Server-wide `Access-Control-Allow-Origin *` that prod does not carry, and none of prod's security headers (HSTS · `nosniff` · `X-Frame-Options` · `Referrer-Policy` · `server_tokens off`). ⚠ **Check what embeds the staging webchat before pulling the CORS header** |
| `GRM-069` — the pre-commit hook has no teeth against `--no-verify` or an un-run `make hooks` | feature | chore+TEST | `ready` | S | standing | The CI half of `GRM-068`'s fix, deliberately split out. A job over each pull request's **commit range**, running the same `--check-staged` logic per commit, cannot be bypassed — and with `strict_required_status_checks_policy` on since 2026-09-06 a red check genuinely blocks the merge. ⚠ **Kept separate on purpose:** bundling it would have delayed the hook, which is the half that makes the fix free; and its failure mode is different — the fix at CI time is an amend or rebase, not an edit. Named as `⚠ not built` in [`06`](engineering/06_documentation_lifecycle.md) §6.1a so the standard does not claim it |
| `GRM-066` — `dpg/02_questions.md` had drifted from the source it is generated from | bug | chore+TEST | `merged` | XS | standing | ✅ **FIXED 2026-09-06 — and the drift was exactly one line.** `75f65b94` (2026-09-04) taught the generator to emit a `**Status:**` header *and never re-ran it*, so the committed file lacked the line its own generator produces. ⭐ **A half-applied fix, which is the general shape worth remembering:** changing a generator without regenerating leaves the artifact wrong in a way only the pinning test can see. Was: found 2026-09-04 while running the full `tests/repo` suite for OM-03. `test_dpg_questions_generated` is **red at the session's starting commit** with `docs/dpg/` untouched — so it is pre-existing, not caused by this sprint. ⭐ **It is a bug, not debt:** the generator's own pinning test says the two must agree, and they do not. Fix is `python scripts/ops/gen_dpg_questions.py`, then read the diff before committing — the drift is the finding |
| `GRM-001` — bump `next` off 16.2.6 | chore | chore+SENSITIVE | `ready` | S | standing | 🔴 Nine advisories, ships. Chore-shaped, Opus-and-boundary-tests by blast radius |
| **HR-05** — prove a red build blocks | bug | chore+TEST | `ready` | XS | hardening | ⭐ **Five minutes.** The ruleset is active; nobody has watched it refuse a merge ([17](deployment/17_manual_browser_sweep.md)) |
| **HR-07** — manual browser sweep | chore | chore+VERIFY | `ready` | M | hardening | [`deployment/17_manual_browser_sweep.md`](deployment/17_manual_browser_sweep.md) · 60–75 min · ⚠ read its warning first |
| **DPG sprint** — `2026-08-llm` | feature | mixed | `current` *(own lane)* | XL | dpg | 27 tickets, in progress — [tracker](sprints/2026-08-llm/PROGRESS.md). ⚠ **XL means split**, and it is already split into four sub-sprints |
| **Review feedback loop** | feature | UI feature | `proposed` | L | — | [DESIGN](sprints/2026-09_review_feedback_loop/DESIGN-review-feedback-loop.md) · its D1 is settled by [D-010](DECISIONS.md); OM-07 owns the vocabulary |
| **Public repo split** | feature | chore+RELEASE | `proposed` | M | — | Prune list · the 17-file infrastructure-identifier scrub · history decision. Gated on [D-009](DECISIONS.md) |

---

## Backlog — proposed

**Not ready, not forgotten.** Absorbed from `TODO.md` on 2026-09-04 (Q-03). ⚠ **These rows were given
an id and a home, not a classification** — every title is preserved **verbatim**, including its own
status marker. Per [07 §11.9](engineering/07_work_items.md) and Q-05, an item's kind, profile and
state are confirmed **when it is scheduled**, not in a big-bang pass. Several rows below are marked
done in their own text; those resolve as they are picked up.

### Found while building, not yet classified by the owner

| Id | Kind | Profile | Item | Where |
|---|---|---|---|---|
| `GRM-092` | debt | — | **the e2e suite drives organization creation up to the modal and stops** | ⚠ **Deliberate, and the same shape as `GRM-075`:** submitting the create form writes a **real organization row**, and `DELETE /organizations/{id}` refuses once anything references it — so a run either leaves `E2E Test Org 17` behind or deletes non-deterministically. Driven: the affordance in both pickers, the modal opening, cancel returning to the picker. Not driven: the create itself, and therefore **the create→lift→link ordering that is the whole point of `GRM-089`**. ⭐ **Recommended fix closes `GRM-075` too** — decide that e2e always runs against a disposable stack (`QA-03` already builds one in CI), which needs no new code. [`followups/org-creation-not-driven-by-e2e.md`](sprints/2026-09_settings_findability/followups/org-creation-not-driven-by-e2e.md) |
| `GRM-091` | deviation | — | **`07 §5.1` describes an officer roster the UI does not build** | ⭐ **Found 2026-09-07 while editing that section for `GRM-087`** — which is the only reason anyone read it. §5.1 names columns *Name · Email · Role · Area covered*; the screen renders *Officer · Position · Office · Project / area*, **one row per active position**, not per officer. The *role / project / package / location* filters it promises do not exist. The component's own header cites a **later** design than §5.1 does, so the spec is probably a superseded target rather than an unmet requirement — but that is a **fork**, and [07 §2.3](engineering/07_work_items.md) forbids deciding one silently inside another item. §5.1 carries a dated marker meanwhile. ⚠ **`GRM-088` is not the fix** — it builds the filters and leaves the columns and the row model untouched. [`followups/officer-roster-spec-drift.md`](sprints/2026-09_settings_findability/followups/officer-roster-spec-drift.md) |

### Carrying `+SENSITIVE` — PII · auth · SEAH · complainant channel · egress

⚠ §4.2: **the modifier is never waived by kind or by size.** A one-line fix on any of these is not a chore.

| Id | Severity | Kind | Profile | Item | Where |
|---|---|---|---|---|---|
| `GRM-002` | 🟡 low | debt | **+SENSITIVE** | 🟡 **The officer's category correction overwrites the AI's answer with no record of what it proposed** | `ticketing/api/routers/tickets/crud.py:523` · `backend/services/database_services/grievance_manager.py:83`  |
| `GRM-003` | 🟡 low | debt | **+SENSITIVE** | 🟡 **The SEAH detector's confidence is computed on every call and thrown away** | `backend/services/LLM_services.py:633` · `backend/actions/grievance_intake/sensitive.py:103` · `ticketing/models/ticket.py:94`  |
| `GRM-004` | ⏳ stale | debt | **+SENSITIVE** | ⏳ **The classification benchmark has not been re-run since the D-51 storage guard** | `scripts/ops/llm_benchmark.py` · [`docs/dpg/model-benchmarks.md`](dpg/model-benchmarks.md) §3.1  |
| `GRM-006` | 🟠 med | debt | **+SENSITIVE** | 🟠 **The deploy should run `ops.selfcheck`, not trust a runbook** | `Makefile` (`REMOTE_DEPLOY_CORE`)  |
| `GRM-007` | 🟠 med | debt | **+SENSITIVE** | 🟠 **`AWS_DEPLOY_SERVICES` omits the orchestrator, which is what executes `backend/actions/`** | `Makefile:88` (`AWS_DEPLOY_SERVICES`) · `REMOTE_VERIFY_GRM_PORTS`  |
| `GRM-008` | 🔴 high | debt | **+SENSITIVE** | 🔴 **A routine `make aws-deploy` took staging OFF THE NETWORK for ~40 minutes — the box has 4 GB and no swap, and builds the Next.js image on itself** | `Makefile` (`REMOTE_DEPLOY_CORE` → `build --pull`) · EC2 `i-07e144c2f6d9db18e` (`t4g.medium`, 2 vCPU / 3825 MB / **swap 0**)  |
| `GRM-009` | 🟠 med | debt | **+SENSITIVE** | 🟠 **`ops/config.py`'s password fallback should be fatal, not a warning** | `ops/config.py:104-118`  |
| `GRM-010` | — unset | debt | **+SENSITIVE** | **Keycloak event storage** — ✅ **local 2026-08-24, STAGING 2026-09-03**; ⛔ **DOR production still OFF** | `ticketing/auth/keycloak_setup.py` (`setup_realm_event_logging`)  |
| `GRM-011` | 🔵 info | debt | **+SENSITIVE** | 🔵 **`NEXT_PUBLIC_OIDC_CLIENT_ID` holds the CONFIDENTIAL client, and the name says the opposite** | `docker-compose.grm.yml:322` · `channels/ticketing-ui/lib/auth/runtime-config.ts:22` · `channels/ticketing-ui/Dockerfile:25`  |
| `GRM-012` | — unset | debt | **+SENSITIVE** | **Breach procedure: B1/B2/B3 have no DOR owner** | [`deployment/19_incident_response.md`](deployment/19_incident_response.md) §0  |

### Debt

| Id | Kind | Profile | Item | Where |
|---|---|---|---|---|
| `GRM-015` | debt | — | 🔵 **Encrypt the original narrative and work from the redacted one — reveal by case type, not by frequency** | [`docs/sprints/2026-08-llm/04-pii-redaction-spec.md`](sprints/2026-08-llm/04-pii-redaction-spec.md) · `backend/services/pii_ser… |
| `GRM-016` | debt | — | 🟡 **The grievance taxonomy is authored in two files — and the copy that seeds every database is not the one anybody edits.** ⭐ **Seen in the wild 2026-09-06:** a dev DB carried six `Road Hazard` subcategories the benchmark set has no examples for, failing `test_benchmark_set` locally while **CI stayed green** — because CI seeds from the authored CSV and the dev DB had drifted from it. The two-file split is what lets a local database and CI disagree about what the taxonomy *is* | [`ticketing/constants/grievance_categories_default.json`](../ticketing/constants/grievance_categories_default.json) · [`backend… |
| `GRM-017` | debt | — | 🔴 **A shortlisted open model refuses grievances about harm to children — and the filter may be the provider's, not the model's** | `scripts/ops/llm_candidates.json` · `docs/dpg/open-model-configuration.md` |
| `GRM-018` | debt | — | ⏸ **The end-to-end test against a live vLLM endpoint is deferred — it is the step that turns "documented" into "demonstrated"** | `docs/dpg/vllm-deployment.md` |
| `GRM-019` | debt | — | 🔵 **A Nepali ASR fine-tune on OpenSLR SLR54 would be a genuine DPG *contribution*, not just a dependency** | — (research task, no code) |
| `GRM-020` | debt | — | 🔴 **`.env.open`'s ASR endpoint does not exist — the committed open configuration cannot transcribe** | [`.env.open`](../.env.open) · `docs/dpg/open-model-configuration.md` |
| `GRM-021` | debt | — | 🟡 **The SEAH detector flags gendered complaints that are not harassment — ⭐ REFRAMED 2026-08-24: this is intentional, and the gap is the return path** | `backend/services/LLM_services.py:625` (the detection prompt) |
| `GRM-022` | debt | — | ⏸ **The Hugging Face account has no inference credit — the measurement half of Sprint 2 is blocked on a purchase, not on code** | `env.local` (`HG_TOKEN`) · `scripts/ops/llm_smoke.py` |
| `GRM-023` | debt | — | 🟠 **The stored grievance summary carries no names** ✅ **DECIDED by the owner 2026-08-27** | `ticketing/models/ticket.py:59` · `ticketing/api/routers/tickets/crud.py:341` · `ticketing/services/report_rows.py:459` · Sprin… |
| `GRM-024` | debt | — | 🔵 **The OTP-expired message says "invalid code" — accurate in effect, imprecise about why** | `backend/actions/utils/utterance_mapping_rasa.py` (`action_ask_otp_input` utterance 6) · `backend/actions/forms/form_otp.py:390` |
| `GRM-025` | debt | — | 🟡 **A credential is written to the application log — and the OTP has no expiry at all** | `backend/actions/forms/form_otp.py:312`, `:343`, `:121`, `:352-364` · `backend/actions/services/otp/verification.py` · `form_st… |
| `GRM-026` | debt | — | 🟠 **Nothing re-drives a classification that never ran — grievances can sit at `pending` forever** | `backend/task_queue/celery_app.py` (no `beat_schedule`) · `backend/config/database_tables.py:41` |
| `GRM-027` | debt | — | 🔵 **The live LLM spec never says which of the nine call sites actually run** | [`docs/services/06_llm_service.md`](services/06_llm_service.md) |
| `GRM-028` | debt | — | 🔵 **The two-minute back-fill is what makes the classification budget survivable, and the ticketing spec does not mention it** | [`docs/deployment/11_llm_pipeline_policy.md`](deployment/11_llm_pipeline_policy.md) |
| `GRM-029` | debt | — | 🟠 **Nobody has measured the one path the 30 s classification budget exists for** | `backend/actions/forms/form_otp.py:181-183` · [`deployment/17_manual_browser_sweep.md`](deployment/17_manual_browser_sweep.md) |
| `GRM-030` | debt | — | 🟠 **The benchmark set has no audio, so DPG-22 cannot measure word error rate** | `tests/data/benchmark/` |
| `GRM-031` | debt | — | ✅ **DONE 2026-08-19 — two degraded-mode terminal states that no code path reached** — `LLM_failed` after a model outage, and the closure document after an LLM failure | `backend/task_queue/registered_tasks.py` (`_persist_classification_failed_if_final`) · `ticketing/tasks/llm.py` (`generate_reso… |
| `GRM-032` | debt | — | 🟠 **Classification can take longer than the chatbot waits for it — the complainant sees "no classification" on a *successful* call** | `backend/actions/grievance_intake/classification.py` (`CLASSIFICATION_POLL_MAX_SECONDS = 20.0`) · `backend/services/LLM_service… |
| `GRM-033` | debt | — | 🟠 **The translation error path raises `UnboundLocalError`, and the one-line fix would leak the narrative into the logs** | `backend/services/LLM_services.py` · `translate_grievance_to_english_LLM` (⚠ a line number was here and rotted twice — the func… |
| `GRM-034` | debt | — | ✅ **DONE 2026-08-19 — CI was red on `integration/stage` for 10 consecutive runs (2026-08-08 → 08-17) and gated nothing** | `.github/workflows/ci.yml` · `channels/ticketing-ui/lib/useTicketThread.ts` · `tests/ticketing/test_donor_guardrail.py`, `test_… |
| `GRM-035` | debt | — | ✅ **DONE 2026-08-19 — a tracked file was rewritten on import** | `backend/task_queue/config.py` → `backend/scripts/task_queue/config.sh` |
| `GRM-036` | debt | — | 🟡 **112 backticked paths in `docs/` point at files that do not exist** | 39 distinct targets · worst: `ticketing/api/routers/tickets.py` ×22 (module → package, prose never chased) |
| `GRM-037` | debt | — | 🟠 **`ops` writes no findings at all when `OPS_DB_PASSWORD` is unset** | `ops/config.py:104-111` · `ops/migrations/versions/ops001_init.py:54-64` · `env.local` |
| `GRM-038` | debt | — | ✅ **DONE 2026-08-19 — three privacy defects in the storage layer** | `backend/services/database_services/base_manager.py:266-297`, `:559-573` · `scripts/ops/backup_db.sh:45-60` |
| `GRM-039` | debt | — | 🟠 **27 test files have never run in CI** — 21 sitting directly in `tests/` plus 6 in `tests/shared/`, including SEAH routing, complainant functions, sensitive-content detection and Q… | `.github/workflows/ci.yml:178` |
| `GRM-040` | debt | — | 🟠 **NEW FEATURE — a SEAH officer cannot explicitly return a cleared case to the standard queue** | `ticketing/engine/ticket_actions.py:561` (no action) · `ticketing/api/routers/tickets/crud.py:523` + `ticketing/services/ticket… |
| `GRM-041` | debt | — | 🟠 **Two AWS credentials remain in `secrets.enc.env` with no consumer, and the licence audit is stale** | `secrets.enc.env` (`AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`) · `docs/dpg/dependency-licenses.md` |
| `GRM-042` | debt | — | 🟡 **HALF CLOSED 2026-09-06 — the release/versioning policy exists** ([`deployment/20_release_and_versioning.md`](deployment/20_release_and_versioning.md), `CHANGELOG.md`, and a deploy gate that refuses an untagged production deploy). ⏸ **The governance model is still deferred** — that half is what DPG indicator 8 still wants | repo root |
| `GRM-043` | debt | — | 🟡 **Demo officer switcher one-way door — ROOT FIX LANDED 2026-07-16; C4 unblocked. Optional portal hardening remains.** | `ticketing/api/dependencies.py` (`require_admin_or_bypass`) · `ticketing/api/routers/users.py` (roster routes) |
| `GRM-044` | debt | — | Seed log message still says `PROVINCE_1` | `kl_road_standard.py:319` |
| `GRM-045` | debt | — | `_scope_candidates` calls `_location_and_ancestors` twice (branches B + C) | `workflow_engine.py` |
| `GRM-046` | debt | — | `OfficerScope` seed creates `UserRole` rows but no `OfficerScope` rows | `mock_tickets.py` |
| `GRM-047` | debt | — | Utterance `file_name` derived from module name (~200 sites) — move a class to another file and its copy silently breaks | `backend/actions/base_classes/base_mixins.py:63` |
| `GRM-048` | debt | — | `backend/actions/forms/form_story_main_route_step.py` is **entirely dead code** (79 lines, `ValidateMenuForm` + `ValidateFormStoryStep`, zero importers) | `backend/actions/forms/form_story_main_route_step.py` |
| `GRM-049` | debt | — | The grievance API has **no rate limiting** — `§6`'s "loggable, authorizable, **rate-limitable** at one place" chokepoint argument is now 2/3 true, not 3/3 | `backend/api/routers/grievance.py` |
| `GRM-050` | debt | — | ✅ **FIXED 2026-09-06 (with `GRM-065`, same one-line class of defect)** — `5001:5001` bound the backend API to **all host interfaces** on deployed hosts; now `127.0.0.1:5001:5001`. ⚠ 5001 was measured **filtered** on both hosts, so this was latent, not exposed. Pending deploy | `docker-compose.grm.yml:112-117` |
| `GRM-051` | debt | — | Voice-chunk upload sessions live in a **process-local dict** (+ local `.part` file) — the chunked protocol is stateful across requests, so every chunk must hit the same process | `backend/services/file_server_core.py:29-31`, `docker-compose.yml:48` |
| `GRM-052` | debt | — | Portal ESLint debt: 143 warnings, 0 errors (HR-05 downgraded 5 rule families to `warn` to reserve the CI error channel) | `channels/ticketing-ui/` |
| `GRM-053` | debt | — | Settings tabs have **no render smoke tests** — the portal has no DOM test harness at all | `channels/ticketing-ui/vitest.config.ts`, `package.json` |
| `GRM-054` | debt | — | `ProjectWorkflowSelect` is **dead code** (zero callers; superseded by `ProjectWorkflowsEditor`) | `channels/ticketing-ui/components/settings/workflows/ProjectWorkflowSelect.tsx` |
| `GRM-055` | debt | — | `StepCast` greys **wrong-track** roles only; the other two "why-excluded" reasons need a backend endpoint, and role labels are EN-only (`ticketing.roles` has no `_ne`) | `channels/ticketing-ui/components/settings/workflows/StepCast.tsx` · `ticketing/api/routers/users.py` · `ticketing/models/user.py` |
| `GRM-056` | debt | — | ⬜ **Purge decision: secrets in public git history — scanned, and the recommendation is NOT to purge** | `git history`, 3 branches on origin |
| `GRM-057` | debt | — | 🟠 **Dependency vulnerabilities: 34 on GitHub are against a 2-month-stale `main`; the real count on this branch is 6 Python + 4 npm-high** | `requirements*.txt`, `channels/ticketing-ui/package.json` |
| `GRM-058` | debt | — | ⬜ **Secrets migrated to SOPS + age locally; staging + DOR prod not started, and no credential rotated** | `secrets.enc.env`, `.env.shared`, `scripts/ops/gen_env_local.sh` |
| `GRM-059` | debt | — | Outstanding | Why |
| `GRM-060` | debt | — | **Scopes left where the key backed 2 slots in one workflow** | Which slot a row meant is unknowable; guessing could promote an observer to actor. They are inert (no slot uses those keys) — r… |
| `GRM-061` | debt | — | Outstanding | Why it is not done |
| `GRM-062` | debt | — | **Drop `ticketing.project_locations`** | Left in place, unread. Dropping a populated table is irreversible and belongs in its own migration — see [`followups/drop-proje… |
| `GRM-063` | debt | — | **Staffing screen wording for overlapping packages** | Packages may overlap (a bridge contract covers several road packages), so a district can have *plural* responsible officers. Th… |
| `GRM-064` | debt | — | ✅ **CLOSED 2026-09-07 by decision, not by work** — `ui/04` mockup is stale | The mockup still showed Locations and Packages as separate sections and organizations inside the package card. [Q-04](sprints/2026-09_settings_findability/QUESTIONS.md) settled it: **the shipped screens replace the mockup.** It is marked superseded where it is cited ([`05_frontend`](engineering/05_frontend.md) rule 9a.3) rather than hand-refreshed, and the live route becomes the wireframe baseline. ⭐ **Why not redraw it:** a mockup earns its cost when it decides a shape nobody has seen; redrawing a screen that renders in a browser today is transcription, and a transcribed mockup is precisely the artifact that goes stale again |

---

## Done — 2026 Q3

| Item | Kind | Closed | Verification |
|---|---|---|---|
| [OM-01](sprints/2026-09_operating_model/01-OM-01-work-item-standard.md) — the work-item standard | feature | 2026-09-04 | `implemented` — ⚠ the standard is adopted, **not in force**; its §11 is the gap list |
| **A-3 / HR-05 (settings half)** — branch protection | chore | 2026-09-04 | `verified` — ruleset active on `main` + `integration/*`, read back through the API. ⚠ The **proof** half is `GRM-`-less and sits in the register above |
| **D-010** — the working repository is private | chore | 2026-09-04 | `verified` — `gh repo view` → `PRIVATE` |
| `GRM-068` — the doc-header gate could only see a violation after the commit landed | bug | 2026-09-06 | `tested` — [`06`](engineering/06_documentation_lifecycle.md) §6.1a + [`.githooks/pre-commit`](../.githooks/pre-commit) + `doc_headers.py --check-staged`, reusing the one `SPEC_DIRS` rather than a second copy. Proven both directions (refuses a body edit with no bump; passes when the bump rides along) and the hook wiring is pinned by two tests, one shown to fail. **`CUTOFF` moved to 2026-09-06**, grandfathering the 11 — ⚠ they are *not fixed*, and that is deliberate: bumping their headers today would claim a review nobody performed (§4.1). The `--no-verify` half is [`GRM-069`](#register--ready) |
| [OM-09](sprints/2026-09_operating_model/09-OM-09-release-and-versioning.md) — release + versioning | feature | 2026-09-06 | `implemented` — ⚠ **deliberately not `tested`.** [`deployment/20_release_and_versioning.md`](deployment/20_release_and_versioning.md) + `CHANGELOG.md` + [D-011](DECISIONS.md) (dated releases, semver rejected with its reason); every `prod-deploy*` target refuses without a tag, with one loud `HOTFIX=1` bypass. The gate, the bypass, the `.N` same-day increment and `git show <tag>:docs/…` were all **run** — the tag mechanics in a throwaway clone, so no release was invented here. What is unproven is a real production deploy under it, which is the one thing that cannot be faked |
| [OM-06](sprints/2026-09_operating_model/06-OM-06-standard-amendments.md) — four standard amendments | feature | 2026-09-06 | `tested` — each landed in the standard that owns the concern, none duplicated into `07`: reference packs + an 11th rule ([`00`](engineering/00_engineering_index.md)), the design gate ([`05`](engineering/05_frontend.md) §9a), the verification ladder ([`06`](engineering/06_documentation_lifecycle.md) §4a), session close + a verified tooling inventory (`AGENTS.md`). Closes `07` §11 rows 5 and 6 — the standard is now **mostly in force**, with only rows 2 and 8 open |
| [OM-04](sprints/2026-09_operating_model/04-OM-04-sensitive-path-at-intake.md) — sensitive-path question at intake | feature | 2026-09-06 | `tested` — the six derivation questions moved to [`items/TEMPLATE.md`](items/TEMPLATE.md); the PR template confirms a profile instead of deciding one; `+SENSITIVE` ⇒ Opus is mechanical in `AGENTS.md`. **Enforced:** a `ready`/`current` row with an empty profile now fails `test_spine.py` (check 10, proven to fail two ways). Closes `07` §11 row 4 |
| [OM-03](sprints/2026-09_operating_model/03-OM-03-register-test.md) — `tests/repo/test_spine.py` | feature | 2026-09-06 | `tested` — nine checks, each proven to fail by a mutation sweep, running in CI ([`ci.yml`](../.github/workflows/ci.yml)). §7.2: a chore-profile item is done at `tested` |
| [OM-02](sprints/2026-09_operating_model/02-OM-02-the-register.md) — the register | feature | 2026-09-06 | `tested` — OM-03 is its TEST gate, and its VERIFY gate was met by use: this session read the register, acted on three of its rows, and wrote five back. ⚠ Closed the day after it was written, by its own author-lineage — a second reader has not audited it |
| `GRM-013` — DOR prod firewall for `:5001` | chore | 2026-09-06 | `verified` — 5001 filtered from outside the host (egress `146.70.252.25`), with 443/80 answering on the same probe. ⚠ **One vantage**: this observes reachability, not the ruleset — a source-IP allowlist is indistinguishable from here |
| `GRM-005` — the ops monitor was blind and the report had never worked | debt | 2026-08-24 | `deployed` — ⚠ absorbed from `TODO.md` already marked ✅ FIXED; **not re-verified in this pass** |

*Closed items move to `sprints/archive/` at quarter end (Q-06).*

---

## What this replaced

| Source | What happened to it |
|---|---|
| `TODO.md` | **Retired 2026-09-04.** Open rows are the Backlog above; the full file is preserved at [`sprints/archive/TODO-2026-09-04.md`](sprints/archive/TODO-2026-09-04.md). ⚠ Roughly 60 `followups/` documents say *"logged in `TODO.md`"* — **those records are historically accurate and are not being rewritten** |
| `PROGRESS.md` § *In progress / next* | Deleted (Q-04). `PROGRESS.md` keeps the build narrative and the deviations record — it answers *how did we get here*, never *what is next* |
| `sprints/README.md` status column | Still the map of sprints. Ticket-level status lives here |

✅ **This file is validated.** [`tests/repo/test_spine.py`](../tests/repo/test_spine.py) (OM-03) landed
2026-09-04 and runs in CI: nine checks covering duplicate ids, a stale next-free-id, an unknown kind,
a `blocked` row with no named blocker, two `current` items in one lane, a `done` row with no
verification level, a security view citing an item the register does not hold, and dead `followups/`
links. ⚠ **What it deliberately does not check** is whether the plan is any good — that judgement
would be wrong often enough to get the gate muted, and a muted gate is decoration.

⚠ **This paragraph claimed the opposite until 2026-09-06** — it said the test "does not exist" for two
days after it shipped, in the file whose entire purpose is to be the one true answer. The commit that
landed the test edited this file and missed this line. **That is rule §3's failure mode reproducing
itself inside the fix for rule §3**, and it is why `GRM-068` (the doc-header gate cannot see a
violation until after the commit lands) is a design gap and not a clerical one.
