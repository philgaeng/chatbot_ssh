# Legacy Rasa runner config (archival)

These files were used when training or running the full **Rasa** CLI/server. The production REST stack uses **`backend.orchestrator`** and **`backend.actions`** only; it does **not** load these files.

Do **not** put production secrets here — use environment variables and your deployment’s secret store.
