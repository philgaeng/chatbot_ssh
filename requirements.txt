# requirements_rest.txt
# Python dependencies for the REST application stack only.
# Python: 3.10 recommended (compatible with rasa-sdk 3.6.x)
# No Rasa, TensorFlow, or NLU libraries.
# Use with: pip install -r requirements_rest.txt
#
# Stack: orchestrator (FastAPI) + backend (Flask) + rasa_chatbot.actions (via rasa-sdk)
# Channels: REST_webchat, accessible, shared (frontend - no Python deps)

# --- Orchestrator ---
fastapi>=0.100.0
uvicorn>=0.22.0
pyyaml>=6.0
pydantic>=2.0

# --- Rasa SDK (for action classes used by orchestrator) ---
rasa-sdk==3.6.2

# --- Backend (Flask, file uploads, database, messaging) ---
Flask==3.1.0
Flask-SocketIO==5.5.1
flask-cors>=6.0.0

# --- Database ---
psycopg2-binary==2.9.10
SQLAlchemy>=2.0.36
pytz

# --- Task Queue ---
redis==4.6.0
celery==5.5.2
flower==2.0.1

# --- Messaging (SES/SMS) ---
boto3==1.37.28

# --- LLM (classification, if LLM_CLASSIFICATION=True) ---
openai==1.70.0

# --- HTTP clients ---
requests>=2.32.3
httpx==0.28.1

# --- Data (backend shared_functions, gsheet_query_manager) ---
pandas==2.2.3
numpy==1.26.4

# --- Utilities ---
python-dotenv==1.1.1
rapidfuzz==3.13.0
icecream
